## LLxprt Code Added Memories

- Before checking in code changes from the main project directory, run: npm run test, npm run lint, npm run typecheck, npm run format, npm run build, and node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"; fix any errors, ensure code is formatted before pushing/creating PRs, and always use gh for PRs/issues/comments (never webfetch).
- When user requests to address GitHub issue #NUM: 1) Start from clean up-to-date main (with origin), 2) Create branch "issueNUM", 3) Use gh to pull issue and comments, 4) Research issue in codebase using description/comments as starting point, 5) Create test-first plan following dev-docs/RULES.md behavioral tests (no mock theater), 6) Delegate to typescriptexpert subagent with exquisite detail to implement (must run full verification: npm run test/lint/typecheck/format/build and node scripts/start.js --profile-load synthetic "write me a haiku"), 7) Have deepthinker review for compliance and issue intent (run verification cycle), 8) If fails: remediate with typescriptexpert (giving feedback, run verification), then deepthinker review again - loop until satisfied, 9) Add/commit/push and use gh to create PR with exquisite detail including "closes #NUM" or "fixes #NUM", 10) Use gh watch with 5m delay until workflows finish - address coderabbit comments/fix failures (using same subagents), run verification cycle, add/commit/push, watch again - loop until all pass, 11) Use proper gh command syntax (avoid unescaped backticks), 12) NEVER exit to prompt telling user workflows are still running - must watch, loop, fix, watch until complete.
- For smoke testing llxprt-code Command: node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
