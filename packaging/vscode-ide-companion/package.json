{
  "name": "llxprt-code-vscode-ide-companion-packaging",
  "version": "0.0.0",
  "private": true,
  "description": "Release-only packaging context that pins @vscode/vsce for VSIX packaging and publication. Deliberately NOT a root workspace, so an ordinary repository install never resolves VSCE (issue #2754). It also owns its own node_modules so vsce resolves its required mime@1 instead of the repo-hoisted mime@3.",
  "license": "Apache-2.0",
  "dependencies": {
    "@vscode/vsce": "3.9.2"
  }
}
