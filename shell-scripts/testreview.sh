#!/bin/bash

bun ./scripts/start.ts --profile-load zai -i "have the codeanalyzer and deepthinker review this branch to see if it is good quality, fixes the issue, eliminiates any new dead code, follows our api vs cli separation, and dev-docs/RULES.md. Run them as parallel synchronous subagents. Once done compare their findings and summarize, if there is a disagreement then do your own investigration." --yolo
