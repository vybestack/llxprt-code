{
  "name": "@vybestack/llxprt-code-test-utils",
  "version": "0.10.0",
  "private": true,
  "main": "src/index.ts",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun ../../scripts/build_package.ts",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "bun test --path-ignore-patterns dist",
    "typecheck": "tsc --noEmit",
    "test:ci": "bun test --path-ignore-patterns dist",
    "test:vitest": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "@vybestack/llxprt-code-storage": "file:../storage"
  },
  "devDependencies": {
    "@lydell/node-pty": "1.1.0",
    "strip-ansi": "^7.1.0",
    "typescript": "^5.3.3",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=24"
  }
}
