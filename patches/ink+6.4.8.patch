diff --git a/node_modules/ink/build/data-limited-lru-map.d.ts b/node_modules/ink/build/data-limited-lru-map.d.ts
index 9582c49..51d0bdc 100644
--- a/node_modules/ink/build/data-limited-lru-map.d.ts
+++ b/node_modules/ink/build/data-limited-lru-map.d.ts
@@ -1,19 +1,26 @@
 /**
  * A specialized LRU map that enforces limits on both the number of entries and
- * the total character length of the keys.
+ * the total size of keys and values.
  *
- * When adding a new item, if the new key's length would exceed `maxDataSize`,
- * it evicts the least recently used items until space is available.
+ * When adding a new item, if the entry would exceed `maxDataSize`, it evicts
+ * the least recently used items until space is available.
  *
  * @template V - The type of the values stored in the map.
  */
 export declare class DataLimitedLruMap<V> {
     private readonly map;
+    private readonly maxKeys;
     private readonly maxDataSize;
+    private readonly valueSize;
     private currentDataSize;
-    constructor(maxKeys: number, maxDataSize: number);
+    private currentValueSize;
+    constructor(maxKeys: number, maxDataSize: number, valueSize?: (value: V) => number);
     get(key: string): V | undefined;
     set(key: string, value: V): void;
+    delete(key: string): boolean;
+    clear(): void;
+    keys(): IterableIterator<string>;
     get size(): number;
     get currentDataSizeValue(): number;
+    get currentValueSizeValue(): number;
 }
diff --git a/node_modules/ink/build/data-limited-lru-map.js b/node_modules/ink/build/data-limited-lru-map.js
index 80485a6..07b06c9 100644
--- a/node_modules/ink/build/data-limited-lru-map.js
+++ b/node_modules/ink/build/data-limited-lru-map.js
@@ -1,55 +1,66 @@
-import { LRUMapWithDelete } from 'mnemonist';
 /**
  * A specialized LRU map that enforces limits on both the number of entries and
- * the total character length of the keys.
+ * the total size of keys and values.
  *
- * When adding a new item, if the new key's length would exceed `maxDataSize`,
- * it evicts the least recently used items until space is available.
+ * When adding a new item, if the entry would exceed `maxDataSize`, it evicts
+ * the least recently used items until space is available.
  *
  * @template V - The type of the values stored in the map.
  */
 export class DataLimitedLruMap {
-    map;
+    map = new Map();
+    maxKeys;
     maxDataSize;
+    valueSize;
     currentDataSize = 0;
-    constructor(maxKeys, maxDataSize) {
-        this.map = new LRUMapWithDelete(maxKeys);
+    currentValueSize = 0;
+    constructor(maxKeys, maxDataSize, valueSize = () => 0) {
+        this.maxKeys = maxKeys;
         this.maxDataSize = maxDataSize;
+        this.valueSize = valueSize;
     }
     get(key) {
-        return this.map.get(key);
+        const entry = this.map.get(key);
+        if (entry === undefined) {
+            return undefined;
+        }
+        this.map.delete(key);
+        this.map.set(key, entry);
+        return entry.value;
     }
     set(key, value) {
-        const size = key.length;
-        const hasKey = this.map.has(key);
-        while (this.currentDataSize + size > this.maxDataSize &&
-            this.map.size > 0) {
-            const map = this.map;
-            const lruKey = map.K[map.tail];
-            if (lruKey === undefined) {
-                break;
-            }
-            // If the key is not found in the map, we can't evict it.
-            // This can happen if the map is in an inconsistent state.
-            // To avoid an infinite loop, we break here.
-            // The LRUMap still has its own size limit so it is acceptable
-            // for us to make a best effort to stay within the data size limit.
-            // The alternative would be to continue searching for a valid key
-            // to delete but that would risk making calling set O(n) instead of
-            // O(1).
-            if (!this.map.delete(lruKey)) {
-                break;
-            }
-            this.currentDataSize -= lruKey.length;
+        this.delete(key);
+        const valueSize = this.valueSize(value);
+        const dataSize = key.length + valueSize;
+        if (dataSize > this.maxDataSize) {
+            return;
         }
-        const result = this.map.setpop(key, value);
-        if (result?.evicted) {
-            this.currentDataSize -= result.key.length;
+        while (this.map.size >= this.maxKeys ||
+            this.currentDataSize + dataSize > this.maxDataSize) {
+            const lruKey = this.map.keys().next().value;
+            this.delete(lruKey);
         }
-        if (hasKey) {
-            this.currentDataSize -= size;
+        this.map.set(key, { value, dataSize, valueSize });
+        this.currentDataSize += dataSize;
+        this.currentValueSize += valueSize;
+    }
+    delete(key) {
+        const entry = this.map.get(key);
+        if (entry === undefined) {
+            return false;
         }
-        this.currentDataSize += size;
+        this.map.delete(key);
+        this.currentDataSize -= entry.dataSize;
+        this.currentValueSize -= entry.valueSize;
+        return true;
+    }
+    clear() {
+        this.map.clear();
+        this.currentDataSize = 0;
+        this.currentValueSize = 0;
+    }
+    keys() {
+        return this.map.keys();
     }
     get size() {
         return this.map.size;
@@ -57,5 +68,8 @@ export class DataLimitedLruMap {
     get currentDataSizeValue() {
         return this.currentDataSize;
     }
+    get currentValueSizeValue() {
+        return this.currentValueSize;
+    }
 }
 //# sourceMappingURL=data-limited-lru-map.js.map
diff --git a/node_modules/ink/build/ink.d.ts b/node_modules/ink/build/ink.d.ts
index 1ff7c4c..5825b42 100644
--- a/node_modules/ink/build/ink.d.ts
+++ b/node_modules/ink/build/ink.d.ts
@@ -42,7 +42,11 @@ export default class Ink {
     private lastCursorPosition?;
     private readonly container;
     private readonly rootNode;
-    private fullStaticOutput;
+    private fullStaticOutputChunks;
+    private fullStaticOutputLength;
+    private get fullStaticOutput();
+    private appendStaticOutput;
+    private staticOutputForRender;
     private exitPromise?;
     private restoreConsole?;
     private readonly unsubscribeResize?;
diff --git a/node_modules/ink/build/ink.js b/node_modules/ink/build/ink.js
index 0dc50a2..d7f1739 100644
--- a/node_modules/ink/build/ink.js
+++ b/node_modules/ink/build/ink.js
@@ -21,6 +21,8 @@ import { calculateScroll } from './scroll.js';
 import { ResizeObserverEntry } from './resize-observer.js';
 import { Selection } from './selection.js';
 const noop = () => { };
+const MAX_RETAINED_STATIC_OUTPUT_CODE_UNITS = 4 * 1024 * 1024;
+const MAX_RETAINED_STATIC_OUTPUT_CHUNKS = 1024;
 const rainbowColors = [
     'red',
     'green',
@@ -51,9 +53,8 @@ export default class Ink {
     lastCursorPosition;
     container;
     rootNode;
-    // This variable is used only in debug mode to store full static output
-    // so that it's rerendered every time, not just new static parts, like in non-debug mode
-    fullStaticOutput;
+    fullStaticOutputChunks;
+    fullStaticOutputLength;
     exitPromise;
     restoreConsole;
     unsubscribeResize;
@@ -112,9 +113,8 @@ export default class Ink {
         // Store last output to only rerender when needed
         this.lastOutput = '';
         this.lastOutputHeight = 0;
-        // This variable is used only in debug mode to store full static output
-        // so that it's rerendered every time, not just new static parts, like in non-debug mode
-        this.fullStaticOutput = '';
+        this.fullStaticOutputChunks = [];
+        this.fullStaticOutputLength = 0;
         // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
         this.container = reconciler.createContainer(this.rootNode, LegacyRoot, null, false, null, 'id', () => { }, () => { }, () => { }, () => { }, null);
         // Unmount when process exits
@@ -138,6 +138,28 @@ export default class Ink {
             };
         }
     }
+    get fullStaticOutput() {
+        return this.fullStaticOutputChunks.join('');
+    }
+    appendStaticOutput(output) {
+        if (output.length > MAX_RETAINED_STATIC_OUTPUT_CODE_UNITS) {
+            return false;
+        }
+        while (this.fullStaticOutputChunks.length > 0 &&
+            (this.fullStaticOutputChunks.length >= MAX_RETAINED_STATIC_OUTPUT_CHUNKS ||
+                this.fullStaticOutputLength + output.length > MAX_RETAINED_STATIC_OUTPUT_CODE_UNITS)) {
+            const removed = this.fullStaticOutputChunks.shift();
+            this.fullStaticOutputLength -= removed.length;
+        }
+        this.fullStaticOutputChunks.push(output);
+        this.fullStaticOutputLength += output.length;
+        return true;
+    }
+    staticOutputForRender(output) {
+        return this.appendStaticOutput(output)
+            ? this.fullStaticOutput
+            : this.fullStaticOutput + output;
+    }
     resized = () => {
         this.calculateLayout();
         this.onRender();
@@ -211,10 +233,10 @@ export default class Ink {
         // If <Static> output isn't empty, it means new children have been added to it
         const hasStaticOutput = staticOutput && staticOutput !== '\n';
         if (this.options.debug) {
-            if (hasStaticOutput) {
-                this.fullStaticOutput += staticOutput;
-            }
-            this.options.stdout.write(this.fullStaticOutput + output);
+            const staticOutputForRender = hasStaticOutput
+                ? this.staticOutputForRender(staticOutput)
+                : this.fullStaticOutput;
+            this.options.stdout.write(staticOutputForRender + output);
             return;
         }
         if (isInCi) {
@@ -226,10 +248,10 @@ export default class Ink {
             return;
         }
         if (this.options.alternateBuffer) {
-            if (hasStaticOutput) {
-                this.fullStaticOutput += staticOutput;
-            }
-            this.log(this.fullStaticOutput + output, styledOutput, debugRainbowColor, cursorPosition);
+            const staticOutputForRender = hasStaticOutput
+                ? this.staticOutputForRender(staticOutput)
+                : this.fullStaticOutput;
+            this.log(staticOutputForRender + output, styledOutput, debugRainbowColor, cursorPosition);
             this.lastOutput = output;
             return;
         }
@@ -266,13 +288,16 @@ export default class Ink {
                 wrappedOutput === '' ? 0 : wrappedOutput.split('\n').length;
             return;
         }
-        if (hasStaticOutput) {
-            this.fullStaticOutput += staticOutput;
-        }
+        const staticOutputRetained = hasStaticOutput
+            ? this.appendStaticOutput(staticOutput)
+            : true;
         if (this.lastOutputHeight >= this.options.stdout.rows) {
             // Build a single buffer for all operations
             let buffer = '';
-            buffer += ansiEscapes.clearTerminal + this.fullStaticOutput + output;
+            const staticOutputForRender = staticOutputRetained
+                ? this.fullStaticOutput
+                : this.fullStaticOutput + staticOutput;
+            buffer += ansiEscapes.clearTerminal + staticOutputForRender + output;
             // Position cursor after screen clear if requested by a component
             if (cursorPosition) {
                 const lineCount = output.split('\n').length;
diff --git a/node_modules/ink/build/measure-text.d.ts b/node_modules/ink/build/measure-text.d.ts
index 5619c54..e6b6254 100644
--- a/node_modules/ink/build/measure-text.d.ts
+++ b/node_modules/ink/build/measure-text.d.ts
@@ -1,6 +1,19 @@
 import { type StyledChar } from '@alcalzone/ansi-tokenize';
 import { type DOMNode } from './dom.js';
 export type StringWidth = (text: string) => number;
+export declare const internal_memoryRetentionPatchVersion = 2;
+export type StyledCharactersCacheStats = {
+    readonly entryCount: number;
+    readonly dataSize: number;
+    readonly styledCharacterCells: number;
+};
+export type WidthCacheStats = {
+    readonly entryCount: number;
+    readonly dataSize: number;
+    readonly maxKeyLength: number;
+    readonly oldestKey: string | undefined;
+    readonly newestKey: string | undefined;
+};
 /**
  * Character offset range within a text sequence.
  * Used for mapping DOM nodes to their character positions.
@@ -17,6 +30,9 @@ export type CharOffsetRange = {
 export type CharOffsetMap = Map<DOMNode, CharOffsetRange>;
 export declare function setStringWidthFunction(fn: StringWidth): void;
 export declare function clearStringWidthCache(): void;
+export declare function internal_getWidthCacheStats(): Readonly<WidthCacheStats>;
+export declare function internal_resetToStyledCharactersCache(): void;
+export declare function internal_getToStyledCharactersCacheStats(): Readonly<StyledCharactersCacheStats>;
 export declare function toStyledCharacters(text: string): StyledChar[];
 export declare function styledCharsWidth(styledChars: StyledChar[]): number;
 export declare function inkCharacterWidth(text: string): number;
diff --git a/node_modules/ink/build/measure-text.js b/node_modules/ink/build/measure-text.js
index 94955b7..0b15987 100644
--- a/node_modules/ink/build/measure-text.js
+++ b/node_modules/ink/build/measure-text.js
@@ -1,18 +1,18 @@
 import stringWidth from 'string-width';
 import { tokenize, styledCharsFromTokens, } from '@alcalzone/ansi-tokenize';
 import { DataLimitedLruMap } from './data-limited-lru-map.js';
+export const internal_memoryRetentionPatchVersion = 2;
 const defaultStringWidth = stringWidth;
 let currentStringWidth = defaultStringWidth;
 // This cache must be cleared each time the string width function is changed.
-// The strings passed as input are single characters so there is no need to
-// limit the size of the cache as there are only a limited number of valid
-// characters.
-const widthCache = new Map();
+// Styled-character values can contain arbitrary multi-code-unit graphemes, so
+// keep both the entry count and retained key data bounded.
+const widthCache = new DataLimitedLruMap(10_000, 65_536);
 // This cache can persist for the lifetime of the application.
-// The keys for this cache can be very large so we need to limit the size
-// of the data cached as well as the number of keys cached to prevent
-// memory issues.
-const toStyledCharactersCache = new DataLimitedLruMap(10_000, 1_000_000);
+// Both the source strings and styled-character arrays can be very large, so
+// include both in the data limit to prevent the retained value graph from
+// growing without bound.
+const toStyledCharactersCache = new DataLimitedLruMap(10_000, 65_536, characters => characters.length);
 export function setStringWidthFunction(fn) {
     currentStringWidth = fn;
     // Clear the width cache to avoid stale values.
@@ -21,6 +21,26 @@ export function setStringWidthFunction(fn) {
 export function clearStringWidthCache() {
     widthCache.clear();
 }
+export function internal_getWidthCacheStats() {
+    const keys = [...widthCache.keys()];
+    return Object.freeze({
+        entryCount: widthCache.size,
+        dataSize: widthCache.currentDataSizeValue,
+        maxKeyLength: keys.reduce((maximum, key) => Math.max(maximum, key.length), 0),
+        oldestKey: keys[0],
+        newestKey: keys.at(-1),
+    });
+}
+export function internal_resetToStyledCharactersCache() {
+    toStyledCharactersCache.clear();
+}
+export function internal_getToStyledCharactersCacheStats() {
+    return Object.freeze({
+        entryCount: toStyledCharactersCache.size,
+        dataSize: toStyledCharactersCache.currentDataSizeValue,
+        styledCharacterCells: toStyledCharactersCache.currentValueSizeValue,
+    });
+}
 export function toStyledCharacters(text) {
     const cached = toStyledCharactersCache.get(text);
     if (cached !== undefined) {
