# Phase 15: Session Cleanup Stub — Completed

## Phase ID
`PLAN-20260211-SESSIONRECORDING.P15`

## What Was Done

### File Created
- **`packages/core/src/recording/sessionCleanupUtils.ts`** — Stub utility functions for .jsonl session cleanup

### File Modified
- **`packages/core/src/recording/index.ts`** — Added barrel export for `JsonlSessionFileEntry`, `getAllJsonlSessionFiles`, `shouldDeleteSession`, `cleanupStaleLocks`

### Interfaces Added
- **`JsonlSessionFileEntry`** — Represents a .jsonl session file entry for cleanup evaluation, with `fileName`, `filePath`, `stat` (mtime/size), and `sessionInfo` (id/lastUpdated/isCurrentSession or null)

### Stub Functions Added
1. **`getAllJsonlSessionFiles(chatsDir, currentSessionId?)`** → `Promise<JsonlSessionFileEntry[]>` — Returns empty array (stub). Matches pseudocode lines 13-38. Tagged with `@requirement REQ-CLN-001`.
2. **`shouldDeleteSession(entry)`** → `Promise<'delete' | 'skip' | 'stale-lock-only'>` — Returns `'delete'` (stub). Matches pseudocode lines 50-74. Tagged with `@requirement REQ-CLN-002`.
3. **`cleanupStaleLocks(chatsDir)`** → `Promise<number>` — Returns 0 (stub). Matches pseudocode lines 85-130. Tagged with `@requirement REQ-CLN-004`.

### Markers
All functions and the file-level JSDoc include `@plan PLAN-20260211-SESSIONRECORDING.P15`, `@requirement`, and `@pseudocode` markers.

## Verification
- `npm run typecheck` — [OK] All workspaces pass
- `npm run test` — [OK] All 10,435+ tests pass (0 failures)
- Plan markers grep — [OK] 4 markers in source file
- Function signatures grep — [OK] All 3 functions found
- No TODO markers — [OK] Clean

## Notes
- Stubs return safe default values (empty array, `'delete'`, 0) — no `throw new Error('NotYetImplemented')`
- Parameters are prefixed with `_` to suppress unused-parameter lint warnings
- No existing files (sessionCleanup.ts, sessionUtils.ts) were modified
- No test files were modified
