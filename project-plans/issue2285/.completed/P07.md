# P07 Completion Marker

## Phase
PLAN-20260629-ISSUE2285.P07

## Purpose

Removed `PUBLIC_AGENT_SYMBOLS` and the bare-root symbol-checking behavior from
`scripts/check-cli-import-boundary.mjs`, converting the boundary checker to
pure specifier/subpath-contract rules. Updated the fixture test suite with
new specifier-based tests (un-skipped from the start) and removed the old
symbol-level tests. Reclassified the P06 characterization proof into a live
regression proof for the NEW behavior.

## Prerequisite Verification

```
$ test -f project-plans/issue2285/.completed/P06a.md && echo "P06a EXISTS"
P06a EXISTS
```

Exit status: 0.

## Files Changed (P07-owned)

Modified (tracked, in `git diff`):
- `scripts/check-cli-import-boundary.mjs` — removed `PUBLIC_AGENT_SYMBOLS`,
  `AGENTS_PACKAGE_ROOT`, `importedSymbolsOf`, the bare-root symbol-check
  block, the `agents-internal-symbol` / `agents-namespace-import`
  classifications, and the `v.symbol` reporting branch. Preserved
  `scanGetConfigEscapeHatch`, `isNonLiteralViMock`,
  `PUBLIC_SUBPATHS_BY_PACKAGE`, the thin-entry guard, the self-pruning
  allowlist, and `isDisallowedDeepImport`.
- `scripts/tests/cli-import-boundary.test.js` — removed the P06 annotation
  comment block, removed 9 old symbol-level tests, added 3 new
  specifier-based tests (bare root allowed, internals subpath forbidden,
  deep source path forbidden), folded the old "allows importing a PUBLIC
  agents symbol" test into the new "allows a bare agents root import" test.

Modified (untracked, new from prior phases):
- `project-plans/issue2285/analysis/boundary-checker-characterization-proof.mjs`
  — CONVERTED from a P06 current-behavior characterization proof (asserting
  old symbol-level behavior) to a live regression proof for the NEW
  specifier-based behavior (bare root allowed, internals subpath forbidden,
  deep source path forbidden, old classifications gone). Choice: CONVERT
  (per plan finding 7 — conversion is preferred over deletion).
- `project-plans/issue2285/plan/07-boundary-checker-impl.md` — updated stale
  `node --test` verification command to the established Vitest runner.
- `project-plans/issue2285/plan/07a-boundary-checker-impl-verification.md` —
  updated stale `node --test` verification command to the established
  Vitest runner.

Not modified by P07:
- `packages/agents/src/index.ts` — already depolluted in P05.
- `packages/agents/package.json` — the `./internals.js` export entry stays.
- `PUBLIC_SUBPATHS_BY_PACKAGE` — agents NOT added (internals subpath stays
  forbidden by the deep-import rule).
- `.llxprt/`

## Diff Evidence

```
$ git diff --stat -- scripts/check-cli-import-boundary.mjs scripts/tests/cli-import-boundary.test.js
 scripts/check-cli-import-boundary.mjs     | 182 +-----------------------------
 scripts/tests/cli-import-boundary.test.js | 178 ++++++-----------------------
 2 files changed, 37 insertions(+), 323 deletions(-)
```

## Command Outputs (exit status + key output)

### 1. PUBLIC_AGENT_SYMBOLS fully removed (not renamed)

```
$ grep -c 'PUBLIC_AGENT_SYMBOLS' scripts/check-cli-import-boundary.mjs
0
```

Exit status: 0.

### 2. AGENTS_PACKAGE_ROOT removed

```
$ grep -c 'AGENTS_PACKAGE_ROOT' scripts/check-cli-import-boundary.mjs
0
```

Exit status: 0.

### 3. importedSymbolsOf removed

```
$ grep -c 'importedSymbolsOf' scripts/check-cli-import-boundary.mjs
0
```

Exit status: 0.

### 4. agents-internal-symbol / agents-namespace-import classifications removed

```
$ grep -c 'agents-internal-symbol\|agents-namespace-import' scripts/check-cli-import-boundary.mjs
0
```

Exit status: 0.

### 5. Preserved behaviors present (all four)

```
$ grep -q 'scanGetConfigEscapeHatch' scripts/check-cli-import-boundary.mjs && echo "scanGetConfigEscapeHatch: PRESENT"
scanGetConfigEscapeHatch: PRESENT
$ grep -q 'isNonLiteralViMock' scripts/check-cli-import-boundary.mjs && echo "isNonLiteralViMock: PRESENT"
isNonLiteralViMock: PRESENT
$ grep -q 'PUBLIC_SUBPATHS_BY_PACKAGE' scripts/check-cli-import-boundary.mjs && echo "PUBLIC_SUBPATHS_BY_PACKAGE: PRESENT"
PUBLIC_SUBPATHS_BY_PACKAGE: PRESENT
$ grep -q 'THIN_ENTRY_MAX_LINES' scripts/check-cli-import-boundary.mjs && echo "THIN_ENTRY_MAX_LINES: PRESENT"
THIN_ENTRY_MAX_LINES: PRESENT
```

Exit status: 0.

### 6. Agents NOT added to PUBLIC_SUBPATHS_BY_PACKAGE

```
$ grep -A20 'PUBLIC_SUBPATHS_BY_PACKAGE = {' scripts/check-cli-import-boundary.mjs | grep -c 'agents'
0
```

Exit status: 0.

### 7. npm run lint:cli-boundary passes

```
$ npm run lint:cli-boundary
Scanning 630 production source files...
PASS: no disallowed deep runtime imports in CLI source.
PASS: no getConfig() escape-hatch usage in CLI source.
PASS: allowlist is fresh (no stale entries).
PASS: packages/cli/index.ts is 61 lines (<= 200).
PASS: packages/cli/src/cli.tsx does not directly import runtime-construction deep paths.
CLI import boundary check PASSED.
```

Exit status: 0.

### 8. Vitest fixture tests pass (established runner)

```
$ npx vitest run --config ./scripts/tests/vitest.config.ts scripts/tests/cli-import-boundary.test.js
 RUN  v3.2.4
 ✓ scripts/tests/cli-import-boundary.test.js (21 tests) 3922ms
 Test Files  1 passed (1)
      Tests  21 passed (21)
```

Exit status: 0.

### 9. Characterization proof converted to GREEN regression proof

```
$ node project-plans/issue2285/analysis/boundary-checker-characterization-proof.mjs
  [GREEN] bare-root internal-symbol import (AgentClient) is ALLOWED at the specifier level
  [GREEN] internals subpath (@vybestack/llxprt-code-agents/internals.js) is forbidden as a deep import
  [GREEN] deep agents source-path import (/core/client.js) is forbidden as a deep import
  [GREEN] bare-root public-symbol import (createAgent) is allowed at the specifier level
  [GREEN] namespace import (import * as ns) from bare agents root is allowed at the specifier level
Regression proof PASSED: 5 assertions confirmed the new specifier-based boundary checker behavior.
```

Exit status: 0.

Reclassification choice: CONVERT (per plan finding 7 — conversion preferred
over deletion). The proof now asserts the NEW behavior and would FAIL if the
old symbol-allowlist behavior were reintroduced.

### 10. No deferred language (scoped to phase-owned files)

```
$ grep -rn -E "(TODO|FIXME|HACK|STUB|TEMPORARY|placeholder|for now)" scripts/check-cli-import-boundary.mjs scripts/tests/cli-import-boundary.test.js project-plans/issue2285/analysis/boundary-checker-characterization-proof.mjs
(no output)
```

Exit status: 0.

### 11. eslint-guard passes

```
$ npm run lint:eslint-guard
ESLint policy guard passed.
```

Exit status: 0.

### 12. No skipped/guarded tests in the boundary test diff

```
$ git diff HEAD -- scripts/tests/cli-import-boundary.test.js | grep -E "^\+.*\.(skip|todo)|BOUNDARY_V2"
(no output)
```

Exit status: 0.

### 13. No .llxprt changes

```
$ git status --short .llxprt
(no output)
```

Exit status: 0.

## Tracker / Gate Evidence

Updated `project-plans/issue2285/execution-tracker.md`:

- P07 row marked DONE with verification date `2026-07-01`.
- Gate 2 (Manual Symbol Allowlist Gate) items satisfied:
  - `PUBLIC_AGENT_SYMBOLS` removed — grep count 0.
  - Bare agents root imports allowed — fixture test + regression proof GREEN.
  - `@vybestack/llxprt-code-agents/internals.js` forbidden — fixture test +
    proof GREEN (deep-import classification `static-import`).
  - Deep runtime package imports remain forbidden — `lint:cli-boundary` PASS
    against real repo.
  - Stale allowlist self-pruning preserved — `lint:cli-boundary` PASS
    ("allowlist is fresh").
  - getConfig escape-hatch scan continues to work — `scanGetConfigEscapeHatch`
    present; `lint:cli-boundary` PASS ("no getConfig() escape-hatch usage").
  - Non-literal vi.mock detection continues to work — `isNonLiteralViMock`
    present; fixture test GREEN.
  - Thin-entry checks continue to work — `THIN_ENTRY_MAX_LINES` present;
    `lint:cli-boundary` PASS.
  - Scoped public subpath logic continues to work —
    `PUBLIC_SUBPATHS_BY_PACKAGE` present; providers public-subpath fixture
    tests GREEN.
  - `CLI_BOUNDARY_ROOT` fixture tests updated — old symbol-allowlist tests
    removed; new specifier-based tests added un-skipped; 21 tests GREEN.

## Verdict

PASS — `PUBLIC_AGENT_SYMBOLS`, `AGENTS_PACKAGE_ROOT`, `importedSymbolsOf`,
and the bare-root symbol-check block fully removed. The `agents-internal-symbol`
/ `agents-namespace-import` classifications are gone. New specifier-based
fixture tests are committed un-skipped and GREEN. Old symbol-level tests
removed/converted. Preserved behaviors intact (getConfig scan, vi.mock
detection, thin-entry guard, scoped subpath logic, self-pruning allowlist).
The P06 characterization proof was CONVERTED into a live regression proof for
the NEW behavior (GREEN). `npm run lint:cli-boundary` passes against the real
repo. The Vitest fixture suite passes (21/21). No marker comment blocks added
to the checker script. No deferred language, no lint loosening, no suppression
directives, no `.llxprt` changes.
