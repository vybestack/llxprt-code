# P05a Completion Marker

## Phase
PLAN-20260629-ISSUE2285.P05a

## Verdict
PASS — P05 agents root depollution and API-surface guard deny-mode verification completed. No P06 or later work was executed.

## Files Changed (P05a verifier-owned)

- `project-plans/issue2285/.completed/P05a.md` — created this verifier completion marker with structured evidence.
- `project-plans/issue2285/execution-tracker.md` — updated P05a execution row and Gate 1 verifier evidence.

No production source files were modified by P05a verification. No `.llxprt` files were modified.

## Diff Stats (P05a verifier-owned)

Expected P05a-owned plan artifact changes:

```text
project-plans/issue2285/.completed/P05a.md        | created
project-plans/issue2285/execution-tracker.md     | P05a row + Gate 1 verifier evidence updated
```

## Command Outputs (exit status + key output)

All commands were run from `/Users/acoliver/projects/llxprt/branch-7/llxprt-code`.

### Root clean

```text
$ test "$(grep -c "export \\* from './internals.js'" packages/agents/src/index.ts)" -eq 0
PASS
```

Exit 0. `packages/agents/src/index.ts` exports `./api/index.js` and `createTaskToolRegistration`; it does not export-star `./internals.js`.

### API-surface guard GREEN in deny mode

```text
$ npm run lint:agents-api-surface
Parsed 156 exported names (recursive export-star resolution).
PASS: agents API-surface report matches expected snapshot.
```

Exit 0. The declaration-aware report matched the depolluted 156-name snapshot.

### Guard test GREEN in deny mode

```text
$ npm run test --workspace @vybestack/llxprt-code-agents -- publicSurface.guard
✓ src/api/__tests__/publicSurface.guard.test.ts (8 tests) 7ms
Test Files  1 passed (1)
Tests       8 passed (8)
```

Exit 0. `DENY_MODE = true` and denied internal names are asserted absent.

### Guard test build constraints

```text
$ grep -E "execSync|spawnSync|child_process|npm run build|globalSetup" packages/agents/src/api/__tests__/publicSurface.guard.test.ts
# no matches
OK: no in-lifecycle build
```

Exit 0 for the fail-closed check. The guard test reads the standalone report and does not shell out to build or use globalSetup.

### Fresh agents build + semantic declaration export-graph proof

```text
$ npm run build --workspace @vybestack/llxprt-code-agents
Successfully copied files.

$ node --input-type=module -e "parseExportedNames('./packages/agents/dist/index.d.ts') ..."
OK: semantic export-graph check — denied absent, curated present (156 names resolved)
```

Exit 0. This used `apiSurfaceParser.mjs` over the freshly built root declaration export-star graph, not flat grep or JSDoc/comment text. Denied names absent from the resolved set: `AgentClient`, `CoreToolScheduler`, concrete `AgenticLoop`. Curated names present: `createAgentClient`, `createToolScheduler`, `createTaskToolRegistration`, `createAgenticLoop`, `AgenticLoopRunner`, `AgenticLoopEvent`, `AgenticLoopMessage`.

### Parser export-star support and snapshot path proof

```text
$ grep -c 'export \\*\\|exportStar\\|resolveExport\\|ts\\.\\|createProgram' packages/agents/src/api/__tests__/apiSurfaceParser.mjs
>= 1
$ grep -c 'existsSync\\|readFileSync\\|import.meta.url\\|toMatchSnapshot\\|skip' packages/agents/src/api/__tests__/publicSurface.guard.test.ts
>= 1
```

Exit 0. Parser contains TypeScript AST/export traversal support, and guard test has explicit snapshot/report path resolution proof.

### `createTaskToolRegistration` retained semantically

```text
$ node --input-type=module -e "parseExportedNames('./packages/agents/src/index.ts') ..."
OK: createTaskToolRegistration present in root source export graph
```

Exit 0. The curated compatibility export remains at the root.

### Identity assertions removed and root DENY assertions present

```text
$ grep -rc 'root.AgentClient === internals.AgentClient' packages/agents/src/api/__tests__/ | awk ...
0
$ grep -R "toBeUndefined" packages/agents/src/api/__tests__/publicSurface.nonbreaking.test.ts packages/agents/src/api/__tests__/nonBreaking.exports.test.ts
publicSurface.nonbreaking.test.ts: ... root.AgentClient must be undefined after depollution ... toBeUndefined()
nonBreaking.exports.test.ts: expect(root.AgentClient).toBeUndefined();
```

Exit 0. Identity assertions were removed and replaced by root deny assertions.

### Full repo typecheck

```text
$ npm run typecheck
... all workspace tsc --noEmit commands completed ...
```

Exit 0. No cross-package breakage from depollution.

### Agents workspace typecheck and tests

```text
$ npm run typecheck --workspace @vybestack/llxprt-code-agents
> tsc --noEmit
```

Exit 0.

```text
$ npm run test --workspace @vybestack/llxprt-code-agents
Test Files  233 passed (233)
Tests       2444 passed (2444)
EXIT:0
```

Exit 0. A prior combined verification shell returned nonzero after tests despite Vitest reporting 233/233 files and 2444/2444 tests passed; the agents test command was immediately rerun standalone and exited 0 with the same pass counts.

### API guard CI wiring and report path constraints

```text
OK: lint:agents-api-surface wired in package.json
OK: lint:agents-api-surface is in the GitHub CI workflow
OK: report path is gitignored (node_modules/.cache)
OK: script marker-free
$ grep -c 'writeFileSync\\|fs.write' packages/agents/src/api/__tests__/publicSurface.guard.test.ts
0
```

Exit 0 for all fail-closed checks. The API guard is wired in package.json and CI, writes its report under gitignored cache, has no executable-script markers, and the guard test does not write tracked files.

### Disambiguation audit evidence

```text
AgenticLoopMessage OK three-evidence trail
ApprovalHandler OK three-evidence trail
CompressionResult OK three-evidence trail
```

Exit 0. Each disambiguation symbol has snapshot evidence, declaration evidence, and api-barrel evidence.

### Snapshot/report deny and curated semantic check

```text
snapshot denied AgentClient false
snapshot denied CoreToolScheduler false
snapshot denied AgenticLoop false
report denied AgentClient false
report denied CoreToolScheduler false
report denied AgenticLoop false
snapshot curated createAgentClient true
snapshot curated createToolScheduler true
snapshot curated createTaskToolRegistration true
snapshot curated createAgenticLoop true
snapshot curated AgenticLoopRunner true
snapshot curated AgenticLoopEvent true
snapshot curated AgenticLoopMessage true
report curated createAgentClient true
report curated createToolScheduler true
report curated createTaskToolRegistration true
report curated createAgenticLoop true
report curated AgenticLoopRunner true
report curated AgenticLoopEvent true
report curated AgenticLoopMessage true
OK snapshot/report deny+curated semantics
```

Exit 0. The deny/curated proof came from the JSON snapshot/report and declaration parser output, not comment text.

### Deferred-language diff scan

```text
OK: no newly introduced deferred language
```

Exit 0.

### ESLint guard

```text
$ npm run lint:eslint-guard
ESLint policy guard passed.
```

Exit 0. No lint/complexity loosening or suppression directives were introduced.

### Export-star synthetic re-proof

```text
OK: synthetic export-star re-proof resolved all denied names
```

Exit 0. The real parser resolves `.js` specifiers to `.d.ts` and would catch a reintroduced export-star leak without mutating production source.

### No issue2285 marker churn in production source/scripts; no `.llxprt` changes

```text
OK_MARKERS
OK_LLXPRT
```

Exit 0. Existing unrelated markers were not modified.

## Semantic Verification Summary

- Root depollution is real: `packages/agents/src/index.ts` no longer re-exports `./internals.js`.
- Denied internals (`AgentClient`, `CoreToolScheduler`, concrete `AgenticLoop`) are absent from the declaration-aware report, snapshot, and resolved fresh `dist/index.d.ts` export graph.
- Curated APIs remain present: `createAgentClient`, `createToolScheduler`, `createTaskToolRegistration`, `createAgenticLoop`, `AgenticLoopRunner`, `AgenticLoopEvent`, `AgenticLoopMessage`.
- `createTaskToolRegistration` is retained in the root source export graph.
- Semantic proof used `parseExportedNames` and JSON report/snapshot data. Names appearing only in comments/JSDoc were not treated as API proof.
- Disambiguation audit has snapshot + declaration + api-barrel evidence for `AgenticLoopMessage`, `ApprovalHandler`, and `CompressionResult`.
- Identity assertions were replaced by root deny assertions.
- API guard is CI-wired, marker-free as an executable script, writes reports under `node_modules/.cache`, and has no tracked-file writes from the guard test.
- Full repo typecheck, agents typecheck/tests, API guard, guard test, synthetic export-star re-proof, deferred-language scan, and eslint guard all pass.

## Tracker / Gate Evidence Updated

Updated `project-plans/issue2285/execution-tracker.md`:

- P05a row set to DONE with verification summary.
- Gate 1 verifier evidence filled for agents root depollution, declaration-aware guard, curated loop API preservation, type-only deny coverage, `createTaskToolRegistration`, disambiguation audit, identity assertion replacement, and generated-dist policy.

## Blockers

None.
