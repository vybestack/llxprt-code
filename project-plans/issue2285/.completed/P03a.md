# Phase P03a Completion Marker

Plan ID: PLAN-20260629-ISSUE2285
Phase: P03a (API-Surface Guard TDD Verification)
Status: DONE
Date: 2026-07-01

## Summary

Verified Phase P03/P03a artifacts only. P03 is complete and the current API/type-surface guard characterization contract satisfies the P03/P03a plan: the parser helper is marker-free `.mjs`, declaration-aware, recursively resolves export-star re-exports with `.js`→`.d.ts` normalization, records exported aliases for named exports, and exports `parseExportedNames`, `DENIED_INTERNAL_NAMES`, `loadExpectedSurface`, and `API_SURFACE_REPORT_PATH`. The standalone script uses the recorded B1a isolated temp tsconfig mechanism, writes the report only to `node_modules/.cache/agents-api-surface/report.json`, compares against `expected-root-surface.json`, trap-cleans temp output, and exits nonzero on snapshot drift. The guard test has P03 markers, reads the report, fail-closes when absent except the explicit local `LLXPRT_API_SURFACE_SKIP=1` escape hatch, and does not run builds in the test lifecycle.

No P04 or later work was executed.

## Files Changed By P03a

- `project-plans/issue2285/.completed/P03a.md` — created this verification completion marker.
- `project-plans/issue2285/execution-tracker.md` — updated the P03a row to DONE with verifier evidence.

No source/test/package/script/.github files were modified by P03a verification.

## Diff Evidence

Pre-marker diff evidence for phase-owned P03 artifacts:

```text
$ git diff --name-only HEAD -- .github/workflows/ci.yml package.json scripts/check-agents-api-surface.mjs packages/agents/src/api/__tests__/apiSurfaceParser.mjs packages/agents/src/api/__tests__/publicSurface.guard.test.ts packages/agents/src/api/__tests__/expected-root-surface.json project-plans/issue2285/.completed/P03.md project-plans/issue2285/execution-tracker.md
.github/workflows/ci.yml
package.json

$ git diff --stat HEAD -- .github/workflows/ci.yml package.json scripts/check-agents-api-surface.mjs packages/agents/src/api/__tests__/apiSurfaceParser.mjs packages/agents/src/api/__tests__/publicSurface.guard.test.ts packages/agents/src/api/__tests__/expected-root-surface.json project-plans/issue2285/.completed/P03.md project-plans/issue2285/execution-tracker.md
 .github/workflows/ci.yml | 8 ++++++++
 package.json             | 1 +
 2 files changed, 9 insertions(+)
```

Note: newly created P03 files are untracked at this phase boundary, so `git diff HEAD` reports tracked modifications (`.github/workflows/ci.yml`, `package.json`) and `git status --short` records the untracked phase-owned files under `packages/agents/src/api/__tests__/`, `scripts/check-agents-api-surface.mjs`, and `project-plans/issue2285/`.

## Verification Checklist Evidence

### P03 completion marker

Verified `project-plans/issue2285/.completed/P03.md` exists and contains structured evidence:

- Contract-first failing proof: section `Contract-First Sub-Step 1: Initial Failing/Characterization Proof` records the guard test failing before `apiSurfaceParser.mjs` existed.
- Green characterization proof: section `Characterization GREEN Sub-Step 3` records the guard test passing after implementation.
- Export-star leak proof: section `Export-Star Leak Proof (finding 2)` records `AgentClient`, `CoreToolScheduler`, and `AgenticLoop` resolved through export-star.
- Remediation evidence: section `Remediation: P03a Alias Finding (local named-export alias names)` records the alias export remediation and post-fix verification.

### Parser helper

Verified `packages/agents/src/api/__tests__/apiSurfaceParser.mjs`:

- Is a plain `.mjs` executable helper and is marker-free (`@plan`/`@requirement` absent).
- Exports `parseExportedNames`, `DENIED_INTERNAL_NAMES`, `loadExpectedSurface`, and `API_SURFACE_REPORT_PATH`.
- Uses the TypeScript compiler API (`typescript` via `createRequire`) to parse declarations.
- Normalizes specifiers to declaration files in `normalizeSpecifierToDecl`, including `.js`/`.mjs`/`.cjs`/`.ts`/`.tsx`/`.d.ts` normalization.
- Recursively resolves `export *` re-exports in `parseExportedNames` and follows named re-export declarations.
- Covers value and type exports (`class`, `function`, `enum`, `const`/`let`/`var`, `interface`, `type`, `export type { ... }`).
- Records exported aliases for local named exports and re-exports via `el.name.text` in `collectDirectExports`.
- `DENIED_INTERNAL_NAMES` includes `AgentClient`, `CoreToolScheduler`, and `AgenticLoop`.

### Guard test

Verified `packages/agents/src/api/__tests__/publicSurface.guard.test.ts`:

- Contains P03 markers: `@plan PLAN-20260629-ISSUE2285.P03`, `@requirement REQ-002`, and pseudocode reference.
- Reads the JSON report through `API_SURFACE_REPORT_PATH`, which resolves to `node_modules/.cache/agents-api-surface/report.json`.
- Fails closed when the report is absent, except for explicit local `LLXPRT_API_SURFACE_SKIP=1` when not in CI.
- Does not shell out to builds or use `globalSetup`/`child_process` in the test lifecycle.
- Includes type-only export coverage (`AgenticLoopMessage`) and alias regression coverage for `export { X as Y }`.
- Contains no mock-theater assertions (`toHaveBeenCalled` absent) and no reverse-test marker (`NotYetImplemented` absent).

### Standalone script

Verified `scripts/check-agents-api-surface.mjs`:

- Is marker-free.
- Implements recorded B1a isolated temp tsconfig mechanism: extends `packages/agents/tsconfig.json`, sets `rootDir` to repo root, writes `outDir` and `tsBuildInfoFile` under a temp directory, and invokes `tsc -p` directly.
- Trap-cleans temp output with `process.on('exit', ...)` and `rmSync(tempDir, { recursive: true, force: true })`.
- Writes the report only to `API_SURFACE_REPORT_PATH` under `node_modules/.cache/agents-api-surface/report.json`.
- Compares report output against `packages/agents/src/api/__tests__/expected-root-surface.json` and exits nonzero on added/removed snapshot drift.

### Package and CI wiring

Verified:

- `package.json` contains script `lint:agents-api-surface`: `node scripts/check-agents-api-surface.mjs`.
- `.github/workflows/ci.yml` has B1a job-scoped wiring:
  - `lint_javascript` job: `Run agents API-surface guard` after `Run CLI import-boundary guard`.
  - `test` job: `Generate agents API-surface report` after build/bundle steps and before tests.

## Command Evidence

```text
$ npm run lint:agents-api-surface
Building agents declarations via isolated temp tsconfig (B1a)...
Declaration emission complete (tsc exit 2; nonzero is tolerated — preflight confirms it is caused by test-file type errors and one WASM import, not root-barrel emission).
Parsing declaration surface from /var/folders/.../agents-api-surface-.../packages/agents/src/index.d.ts...
Parsed 284 exported names (recursive export-star resolution).
Writing surface report to /Users/acoliver/projects/llxprt/branch-7/llxprt-code/node_modules/.cache/agents-api-surface/report.json...
Comparing against snapshot /Users/acoliver/projects/llxprt/branch-7/llxprt-code/packages/agents/src/api/__tests__/expected-root-surface.json...
PASS: agents API-surface report matches expected snapshot.
Exit: 0

$ npm run test --workspace @vybestack/llxprt-code-agents -- publicSurface.guard
✓ src/api/__tests__/publicSurface.guard.test.ts (8 tests) 26ms
Test Files  1 passed (1)
Tests       8 passed (8)
Exit: 0

$ npm run lint:eslint-guard
ESLint policy guard passed.
Exit: 0

$ node -e "... verify report contains denied names ..."
OK: export-star leak proof passed; report names=284
Exit: 0
```

The required P03a ordering was followed: `npm run lint:agents-api-surface` ran immediately before `npm run test --workspace @vybestack/llxprt-code-agents -- publicSurface.guard`.

## Scope / Git Status Evidence

Final pre-marker status evidence:

```text
$ git status --short
 M .github/workflows/ci.yml
 M package.json
?? packages/agents/src/api/__tests__/apiSurfaceParser.mjs
?? packages/agents/src/api/__tests__/expected-root-surface.json
?? packages/agents/src/api/__tests__/publicSurface.guard.test.ts
?? project-plans/issue2285/
?? scripts/check-agents-api-surface.mjs
```

No `.llxprt` changes are present in git status. No `.llxprt` files were modified or deleted. No `git clean` was run.

## Verdict

PASS — Phase P03/P03a verification succeeded. The P03 API-surface guard characterization is complete and green, and the current report includes `AgentClient`, `CoreToolScheduler`, and `AgenticLoop` in characterization mode.
