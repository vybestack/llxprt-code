# P08a Completion Marker

## Phase
`PLAN-20260629-ISSUE2285.P08a` — Runtime Factory Contract Type-Proof Verification.

## Verdict

PASS — P08 is semantically verified on the recorded `single-source` branch only.
The executable proof uses a disposable CURRENT-WORKING-TREE copy, not a clean
HEAD-only worktree, applies the exact P09 migration to the copy only, runs
`npm install`, `npm run build`, and `npm run typecheck` in the copy, and cleans
up. The real working-tree production runtime factory declarations were not
modified.

## Prerequisite Verification

```bash
$ test -f project-plans/issue2285/.completed/P07a.md && echo P07a_MARKER_PRESENT
P07a_MARKER_PRESENT
$ test -f project-plans/issue2285/.completed/P08.md && echo P08_MARKER_PRESENT
P08_MARKER_PRESENT
```

Exit status: 0.

## Decision Branch

```bash
$ grep -E '^decision:' project-plans/issue2285/analysis/runtime-factory-contract-decision.md | head -1
decision: single-source
```

The verifier ran only the `single-source` branch. No drift guard is required
or expected because P09 will eliminate the duplicate declarations by moving
the contract to core as the single source of truth.

## Files Read / Semantically Verified

- `project-plans/issue2285/plan/08-runtime-factory-typeproof.md`
- `project-plans/issue2285/plan/08a-runtime-factory-typeproof-verification.md`
- `project-plans/issue2285/.completed/P08.md`
- `project-plans/issue2285/analysis/runtime-factory-contract-decision.md`
- `project-plans/issue2285/analysis/runtime-factory-typeproof.md`
- `project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs`
- `packages/core/src/core/clientContract.ts`
- `packages/agents/src/api/runtimeFactories.ts`
- `packages/providers/src/runtime/runtimeContextFactory.ts`
- `project-plans/issue2285/execution-tracker.md`

## Files Changed by P08a

- `project-plans/issue2285/.completed/P08a.md` — created this structured verifier marker.
- `project-plans/issue2285/execution-tracker.md` — marked P08a DONE and added Gate 5 verifier evidence.

No production source files were modified by P08a.

## Diff Evidence

Before the P08a marker/tracker writes, P08 phase artifacts were untracked under
`project-plans/issue2285/` and production-target diffs were empty:

```bash
$ git diff --name-only -- project-plans/issue2285/analysis/runtime-factory-typeproof.md project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs project-plans/issue2285/.completed/P08.md project-plans/issue2285/execution-tracker.md
(empty; phase artifacts are untracked)
$ git status --short project-plans/issue2285/analysis/runtime-factory-typeproof.md project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs project-plans/issue2285/.completed/P08.md project-plans/issue2285/execution-tracker.md
?? project-plans/issue2285/.completed/P08.md
?? project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs
?? project-plans/issue2285/analysis/runtime-factory-typeproof.md
?? project-plans/issue2285/execution-tracker.md
```

Production-target diff check:

```bash
$ git diff --stat -- packages/core/src packages/agents/src/api/runtimeFactories.ts packages/providers/src/runtime/runtimeContextFactory.ts
(empty)
$ git diff --name-only -- packages/core/src packages/agents/src/api/runtimeFactories.ts packages/providers/src/runtime/runtimeContextFactory.ts
(empty)
```

Exit status: 0.

## Semantic Evidence

### Type-proof document

Verified `runtime-factory-typeproof.md` records:

- exact core target: `packages/core/src/core/clientContract.ts`
- exact root re-export path: `@vybestack/llxprt-code-core` via `packages/core/src/index.ts` export-star of `./core/clientContract.js`
- no-cycle proof: agents and providers already depend on core; providers does not depend on agents
- exact P09 imports/re-exports at agents and providers sites
- no-drift-guard rationale for `single-source`
- successful proof output using the current-working-tree disposable copy

### Executable proof script

Verified `runtime-factory-single-source-proof.mjs`:

- enumerates tracked current working-tree files with `git ls-files -z`
- enumerates untracked, non-ignored files with `git ls-files --others --exclude-standard -z`
- copies current file bytes into a temp full-repo copy, excluding ignored artifacts through git's ignore semantics
- runs `npm install` in the copy to create real workspace links
- applies the exact P09 changes to the copy only:
  - add `AgentRuntimeFactoryBindings` to `packages/core/src/core/clientContract.ts`
  - import/re-export from the real core root `@vybestack/llxprt-code-core`
  - remove the agents/providers local declarations in the copy
- runs `npm run build` and `npm run typecheck` in the copy
- removes the copy via `rmSync(copyPath, { recursive: true, force: true })` on exit/signals
- does not execute `git worktree`
- is marker-free and deferred-language-free

## Command Outputs (exit status + key output)

### 1. Prerequisites, decision, and doc anchors

```bash
$ test -f project-plans/issue2285/.completed/P07a.md && echo P07a_MARKER_PRESENT && test -f project-plans/issue2285/.completed/P08.md && echo P08_MARKER_PRESENT && test -f project-plans/issue2285/analysis/runtime-factory-typeproof.md && echo TYPEPROOF_DOC_PRESENT && test -f project-plans/issue2285/analysis/runtime-factory-contract-decision.md && echo DECISION_RECORD_PRESENT && DECISION="$(grep -E '^decision:' project-plans/issue2285/analysis/runtime-factory-contract-decision.md | head -1 | sed 's/^decision:[[:space:]]*//')" && echo "Recorded decision: $DECISION" && test "$DECISION" = "single-source" && test -f project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && echo SINGLE_SOURCE_PROOF_PRESENT && grep -q "packages/core/src/core/clientContract.ts" project-plans/issue2285/analysis/runtime-factory-typeproof.md && grep -q "@vybestack/llxprt-code-core" project-plans/issue2285/analysis/runtime-factory-typeproof.md && grep -q "No drift guard required" project-plans/issue2285/analysis/runtime-factory-typeproof.md && grep -q "git ls-files -z" project-plans/issue2285/analysis/runtime-factory-typeproof.md && grep -q "git ls-files --others --exclude-standard -z" project-plans/issue2285/analysis/runtime-factory-typeproof.md && echo DOC_SEMANTIC_ANCHORS_PRESENT
P07a_MARKER_PRESENT
P08_MARKER_PRESENT
TYPEPROOF_DOC_PRESENT
DECISION_RECORD_PRESENT
Recorded decision: single-source
SINGLE_SOURCE_PROOF_PRESENT
DOC_SEMANTIC_ANCHORS_PRESENT
```

Exit status: 0.

### 2. Single-source executable proof

```bash
$ node project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs
[proof] creating disposable working-tree copy: /var/folders/.../T/llxprt-rf-proof-87315
[proof] copied 9185 working-tree files (skipped 0 deleted) into copy
[proof] running npm install in copy (fresh workspace links)...
[proof] npm install done
[proof] added AgentRuntimeFactoryBindings to core (copy)
[proof] migrated agents import site to core (copy)
[proof] migrated providers import site to core (copy)
[proof] running npm run build in copy...
[proof] build PASSED in copy
[proof] running npm run typecheck in copy...
... all 14 workspace typechecks passed ...
[proof] typecheck PASSED in copy
[proof] real working tree production source unchanged

PASS: single-source migration typechecks under the REAL workspace resolution.
  - Disposable copy built from CURRENT working tree (tracked + untracked, non-ignored)
  - AgentRuntimeFactoryBindings added to packages/core/src/core/clientContract.ts (copy)
  - re-exported via the real core root barrel (@vybestack/llxprt-code-core)
  - agents/providers import sites migrated to import from core (copy)
  - npm install + npm run build + npm run typecheck PASSED in the copy
  - production source in the real working tree is unchanged
```

Exit status: 0.

### 3. Real working-tree typecheck

```bash
$ npm run typecheck
> @vybestack/llxprt-code@0.10.0 typecheck
> npm run typecheck --workspaces --if-present
... all 14 workspace typechecks passed ...
```

Exit status: 0.

### 4. Proof-script marker-free/deferred-language/current-copy scan

```bash
$ grep -q "git ls-files -z" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "git ls-files --others --exclude-standard -z" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "spawnSync('npm', \['install'\]" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "spawnSync('npm', \['run', 'build'\]" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "spawnSync('npm', \['run', 'typecheck'\]" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "rmSync(copyPath, { recursive: true, force: true })" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "@vybestack/llxprt-code-core" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && grep -q "packages/core/src/core/clientContract.ts" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && ! grep -q "runInRepo('git', \['worktree'" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && ! grep -q "spawnSync('git', \['worktree'" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && ! grep -E "@plan:|@requirement:" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && ! grep -rn -E "(TODO|FIXME|HACK|STUB|TEMPORARY|placeholder|for now)" project-plans/issue2285/analysis/runtime-factory-single-source-proof.mjs && echo "OK: proof script semantic anchors, marker-free, deferred-language-free"
OK: proof script semantic anchors, marker-free, deferred-language-free
```

Exit status: 0.

### 5. No production diff / exactly two pre-migration declarations

```bash
$ DECL_COUNT="$(node -e "const {execSync}=require('child_process'); const fs=require('fs'); const out=execSync('grep -rln \\\"interface AgentRuntimeFactoryBindings\\\" packages/ --include=*.ts || true',{encoding:'utf8'}).split('\\n').filter(Boolean); let n=0; for (const f of out) { if (f.includes('node_modules') || f.includes('dist')) continue; const s=fs.readFileSync(f,'utf8'); if (/interface\\s+AgentRuntimeFactoryBindings/.test(s)) n++; } process.stdout.write(String(n));")" && echo "DECL_COUNT=$DECL_COUNT" && test "$DECL_COUNT" -eq 2 && git diff --stat -- packages/core/src packages/agents/src/api/runtimeFactories.ts packages/providers/src/runtime/runtimeContextFactory.ts && test -z "$(git diff --name-only -- packages/core/src packages/agents/src/api/runtimeFactories.ts packages/providers/src/runtime/runtimeContextFactory.ts)" && echo "OK: no production diff in core target/agents/providers runtime factory files"
DECL_COUNT=2
OK: no production diff in core target/agents/providers runtime factory files
```

Exit status: 0.

### 6. ESLint policy guard

```bash
$ npm run lint:eslint-guard
> @vybestack/llxprt-code@0.10.0 lint:eslint-guard
> node scripts/check-eslint-guard.js

ESLint policy guard passed.
```

Exit status: 0.

### 7. `.llxprt` status check

```bash
$ git status --short .llxprt && test -z "$(git status --short .llxprt)" && echo "OK: no .llxprt changes"
OK: no .llxprt changes
```

Exit status: 0.

## Tracker / Gate Evidence

Updated `project-plans/issue2285/execution-tracker.md`:

- P08a row marked DONE with verification date `2026-07-01`.
- Gate 5 ownership/dependency evidence updated from P08 to P08/P08a.
- Gate 5 single-source proof evidence updated with P08a verifier results, including current-working-tree copy proof run, production no-diff check, marker/deferred-language scan, real-tree typecheck, eslint guard, and `.llxprt` status.

## Blockers

None.
