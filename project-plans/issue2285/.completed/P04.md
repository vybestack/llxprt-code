# P04 Completion Marker

## Phase
PLAN-20260629-ISSUE2285.P04

## Deferred Baseline (architect review finding 6)

Captured BEFORE any edits. The four P04-owned A2A production files were
scanned for deferred language (`TODO|FIXME|HACK|STUB|TEMPORARY|placeholder|for now`).

**Baseline result**: ZERO hits across all four files.
```
grep -rn -E "(TODO|FIXME|HACK|STUB|TEMPORARY|placeholder|for now)" \
  packages/a2a-server/src/config/config.ts \
  packages/a2a-server/src/agent/task.ts \
  packages/a2a-server/src/agent/task-runtime-helpers.ts \
  packages/a2a-server/src/utils/testing_utils.ts
# Output: (empty — NO_HITS)
```

Post-phase comparison: also ZERO hits. No newly introduced deferred language.

## Files Changed (git diff --name-only of phase-owned files)

Modified (production + test consumers):
- `packages/a2a-server/src/config/config.ts` — migrated to `createAgentClient`/`createToolScheduler` public factories
- `packages/a2a-server/src/agent/task.ts` — migrated to `createAgentClient` + `AgentClientContract` type
- `packages/a2a-server/src/agent/task-runtime-helpers.ts` — migrated type import to `AgentClientContract` from core root
- `packages/a2a-server/src/utils/testing_utils.ts` — migrated type import to `ToolSchedulerContract` from core root
- `packages/a2a-server/src/http/app.test.ts` — updated mock to intercept `createAgentClient` (test-side fix for factory migration)
- `packages/cli/src/integration-tests/test-utils.ts` — internals subpath for `AgentClient`/`CoreToolScheduler`; `createTaskToolRegistration` kept from root
- `packages/cli/src/integration-tests/todo-continuation.integration.test.ts` — internals subpath for `AgentClient`/`Turn`
- `packages/cli/src/ui/hooks/useTodoContinuation.spec.ts` — internals subpath + mock target update
- `packages/cli/src/ui/hooks/useToolScheduler.test.ts` — `ToolSchedulerContract` from core root
- `packages/cli/src/ui/hooks/useToolScheduler.part2.test.ts` — same
- `packages/cli/src/ui/hooks/useToolScheduler.part3.test.ts` — same
- `packages/cli/src/ui/hooks/useToolScheduler.part4.test.ts` — same
- `packages/cli/src/ui/hooks/useToolScheduler.part5.test.ts` — same
- `packages/cli/src/ui/hooks/geminiStream/__tests__/useAgenticLoop.test.tsx` — internals subpath for `CoreToolScheduler` (value)

Created:
- `packages/a2a-server/src/config/config.factory-migration.test.ts` (NEW — colocated, @plan/@requirement markers, real factory assertions)
- `packages/a2a-server/src/agent/task.factory-migration.integration.test.ts` (NEW — colocated, @plan/@requirement markers, real API assertions)
- `project-plans/issue2285/analysis/a2a-exception-records.md` (NEW — no exceptions; all migrated to public factories/contracts)

## Diff Stats (git diff --stat of phase-owned files)
```
 packages/a2a-server/src/agent/task-runtime-helpers.ts        |  4 ++--
 packages/a2a-server/src/agent/task.ts                        |  7 ++++---
 packages/a2a-server/src/config/config.ts                     |  8 ++++----
 packages/a2a-server/src/http/app.test.ts                     | 12 +++++++-----
 packages/a2a-server/src/utils/testing_utils.ts               |  4 ++--
 packages/cli/src/integration-tests/test-utils.ts             |  4 ++--
 .../integration-tests/todo-continuation.integration.test.ts  |  2 +-
 .../ui/hooks/geminiStream/__tests__/useAgenticLoop.test.tsx  |  2 +-
 packages/cli/src/ui/hooks/useTodoContinuation.spec.ts        |  4 ++--
 packages/cli/src/ui/hooks/useToolScheduler.part2.test.ts     |  6 +++---
 packages/cli/src/ui/hooks/useToolScheduler.part3.test.ts     |  6 +++---
 packages/cli/src/ui/hooks/useToolScheduler.part4.test.ts     |  6 +++---
 packages/cli/src/ui/hooks/useToolScheduler.part5.test.ts     |  6 +++---
 packages/cli/src/ui/hooks/useToolScheduler.test.ts           |  6 +++---
 14 files changed, 40 insertions(+), 37 deletions(-)
```
(Plus 3 new untracked files not reflected in diff --stat.)

## Command Outputs (key verification commands)

- **No A2A production internals-only imports from agents root**: PASS
  ```
  grep -rnE "\bAgentClient\b|\bCoreToolScheduler\b" packages/a2a-server/src --include="*.ts" | grep "llxprt-code-agents'" | grep -v "createAgentClient\|createToolScheduler"
  # Output: (empty — CLEAN)
  ```
  (Note: `createAgentClient` import in task.ts is the sanctioned public factory; the plan's literal grep pattern matches `AgentClient` as a substring of `createAgentClient`, but the intent — no internals-only class imports — is satisfied.)

- **A2A typecheck** (`npm run typecheck --workspace @vybestack/llxprt-code-a2a-server`): PASS (exit 0, no errors)

- **A2A tests** (`npm run test --workspace @vybestack/llxprt-code-a2a-server`): PASS — 13 test files, 106 tests passed

- **A2A behavior tests** (`npm run test --workspace @vybestack/llxprt-code-a2a-server -- config.factory-migration task.factory-migration`): PASS — 2 test files, 6 tests passed

- **Affected CLI tests** (`npm run test --workspace @vybestack/llxprt-code -- useToolScheduler useTodoContinuation useAgenticLoop`): PASS — 7 test files, 39 tests passed

- **Full typecheck** (`npm run typecheck`): PASS — all workspaces exit 0

- **Exception records exist** (`test -f project-plans/issue2285/analysis/a2a-exception-records.md`): PASS

- **Root STILL exports internals** (`grep -c "export \* from './internals.js'" packages/agents/src/index.ts`): PASS — count = 1

- **config.factory-migration.test.ts checks**: PASS — file exists, colocated, @plan/@requirement markers present, `createAgentClient`/`createToolScheduler` assertions present, `sendMessageStream` referenced, no mock theater (no `toHaveBeenCalledWith`)

- **task.factory-migration.integration.test.ts checks**: PASS — file exists, colocated, @plan/@requirement markers present, real API assertions (`Task.create`/`sendMessageStream`/`schedule`/`publish`), no nonexistent APIs (`.sendMessage`/`new Task(`), no mock theater

- **Deferred-language post-baseline comparison**: PASS — zero hits pre-phase, zero hits post-phase; no newly introduced deferred language

- **Marker discipline**: PASS — no NEW issue2285 `@plan`/`@requirement` markers in production source files; pre-existing `@plan PLAN-20260610-ISSUE1592` markers in config.ts preserved unchanged

- **No suppression directives**: PASS — no new eslint-disable/ts-ignore/ts-expect-error/ts-nocheck added

- **Forbidden files not modified**: PASS — `packages/agents/src/index.ts` unchanged, `scripts/check-cli-import-boundary.mjs` unchanged, `.llxprt/` unchanged

## Tracker Evidence

- Gate 3 (Production Consumer Internals Gate — REQ-004):
  - "A2A server internals-only imports migrated (public factory first)": SATISFIED — all four A2A production consumers migrated to `createAgentClient`/`createToolScheduler`/`AgentClientContract`/`ToolSchedulerContract`
  - "Per-use exception records for any retained A2A internals subpath": SATISFIED — `a2a-exception-records.md` documents NO exceptions (all public factory/contract paths)
  - "CLI test compile-breakers migrated to internals subpath (not root)": SATISFIED — all 9 CLI test files migrated
  - "A2A factory-migration behavior verified by exact named test files with real observable assertions (anti-mock-theater)": SATISFIED — `config.factory-migration.test.ts` and `task.factory-migration.integration.test.ts` GREEN with real API assertions, colocated, markers present

## Verifier Sign-off
- Verified by: (pending P04a verification)
- Verification phase: P04a

## P04a Remediation Evidence (blocker fixes applied)

The P04a verification pass identified two blockers; both have been remediated
in P04-owned files only (no P04a marker created — that is the verifier's
responsibility).

### Blocker 1: fail-closed A2A root import gate (NONPUBLIC_HITS)

The P04a `NONPUBLIC_HITS` grep is line-based and flagged the closing
`} from '@vybestack/llxprt-code-agents';` lines of multiline imports in
`config.ts` and `config.factory-migration.test.ts` (those lines contain no
allowed factory name, so the `-v` filter did not exclude them).

Fix: reformatted the multiline imports into single-line import statements so
each `from '@vybestack/llxprt-code-agents'` line itself carries an allowed
name (`createAgentClient` / `createToolScheduler` /
`createTaskToolRegistration`). The gate is NOT weakened — it still fails-closed;
the imports were merely reformatted to avoid multiline false positives while
using only allowed root symbols.

Files changed:
- `packages/a2a-server/src/config/config.ts` (import split)
- `packages/a2a-server/src/config/config.factory-migration.test.ts` (import split)

Evidence:
```
NONPUBLIC_HITS="$(grep -rn "from '@vybestack/llxprt-code-agents'" packages/a2a-server/src --include="*.ts" | grep -v "createTaskToolRegistration\|createAgentClient\|createToolScheduler\|createTaskRegistration" || true)"
# Output: (empty) — PASS
```

### Blocker 2: A2A behavior tests strengthened to P04 semantic bar

#### config.factory-migration.test.ts

The prior test only asserted the async iterable shape and used
`new Config({ ... } as unknown as ConfigParameters)` fixture fraud. Remediated:

- `createRealConfig()` now builds a REAL `ConfigParameters` value (no cast)
  with the required fields (`sessionId`, `targetDir`, `cwd`, `debugMode`,
  `model`, `embeddingModel`, `approvalMode`) and passes it to `new Config(...)`
  — the exact builder recorded by preflight (P01 section 3).
- Added test: "factory-produced client sendMessageStream drives the real
  dispatch pipeline and emits content matching the stub model reply". This
  injects a stub at the MODEL-PROVIDER seam (the ChatSession — where the
  provider response enters the runtime), NOT at `sendMessageStream`. The REAL
  `client.sendMessageStream` → orchestrator → Turn pipeline executes
  end-to-end. The test collects emitted `ServerGeminiStreamEvent`s and asserts
  a `Content` event whose value equals the stub reply (`'OK'`).
- No `vi.spyOn`/`vi.fn`/`toHaveBeenCalledWith`/`toHaveBeenCalledTimes`.

#### task.factory-migration.integration.test.ts

The prior test stubbed `task.agentClient.sendMessageStream` with `vi.spyOn`,
replaced `task.scheduler` with a mocked scheduler, and asserted
`scheduleMock` call count. Remediated:

- Removed ALL `vi.spyOn`/`vi.fn`/`Mock` usage and `toHaveBeenCalledWith`/
  `toHaveBeenCalledTimes` assertions.
- The model-response stub is injected at the ChatSession seam (model-provider
  boundary), NOT at `agentClient.sendMessageStream`. The real
  `acceptUserMessage` → `agentClient.sendMessageStream` → orchestrator → Turn
  pipeline executes; the test observes emitted `Content` events matching the
  stub reply AND the published `working` status event on the event bus AND
  `task.taskState === 'working'`.
- The scheduler is the REAL scheduler produced by
  `config.getOrCreateScheduler` inside `Task.create` — NOT replaced. The test
  drives the real `scheduleToolCalls` → `scheduler.schedule` path and observes
  published tool-lifecycle events (artifact-update/status-update) on the event
  bus.
- Uses `Task.create` (not `new Task`), real `agentClient.sendMessageStream`,
  real `scheduler.schedule`, observes real published events/task state.

### Remediation Verification (all PASS)

1. NONPUBLIC_HITS (exact P04a command): PASS (empty).
2. `npm run test --workspace @vybestack/llxprt-code-a2a-server -- config.factory-migration task.factory-migration`: PASS — 2 files, 6 tests.
3. `npm run test --workspace @vybestack/llxprt-code-a2a-server`: PASS — 13 files, 106 tests.
4. `npm run test --workspace @vybestack/llxprt-code -- useToolScheduler useTodoContinuation useAgenticLoop`: PASS — 7 files, 39 tests.
5. `npm run typecheck`: PASS (all workspaces exit 0).
6. `npm run lint:eslint-guard`: PASS.
7. No `.sendMessage`/`new Task(`/`toHaveBeenCalledWith`/mock-theater in the two behavior test files: confirmed (grep clean).
8. No new `@plan:PLAN-20260629-ISSUE2285` markers in production source files: confirmed.
9. Root still exports internals (`export * from './internals.js'` count = 1); `.llxprt/` unchanged: confirmed.

## P04a Remediation Evidence — Round 2 (blocker fixes applied)

The second P04a verification pass found two remaining blockers; both
remediated in P04-owned files only (no P04a marker created — that is the
verifier's responsibility).

### Blocker 1 (semantic): task behavior test used createMockConfig

`task.factory-migration.integration.test.ts` still imported `createMockConfig()`
from `testing_utils.ts`, which installs `getOrCreateScheduler` through
`createSchedulerFactory()` with `vi.fn().mockImplementation(...)` and returns a
cast `ToolSchedulerContract`. This conflicted with P04/P04a's requirement that
A2A behavior tests use real Config/Task/factory/scheduler paths and avoid
scheduler mock theater.

Fix: rewrote the test so it does NOT import/use `createMockConfig` and does NOT
rely on the mocked scheduler factory from testing_utils. The test now:

- Builds a REAL `Config` via `new Config(...)` with a REAL `ConfigParameters`
  value (no cast) and the SAME public-factory wiring A2A's production config.ts
  uses (`agentClientFactory: createAgentClient`, `toolSchedulerFactory:
  createToolScheduler`).
- Uses real `Task.create`, real `agentClient.sendMessageStream` (stub injected
  only at the model-provider ChatSession seam), real `scheduler.schedule`
  (produced by `config.getOrCreateScheduler` inside `Task.create` — NOT
  replaced), and observes real published events/task state on a recording
  event bus.
- No `vi.fn`/`vi.spyOn`/`toHaveBeenCalledWith`/`toHaveBeenCalledTimes` or
  `.sendMessage` non-S / `new Task(` in the file.

File changed:
- `packages/a2a-server/src/agent/task.factory-migration.integration.test.ts`

### Blocker 2 (extractor): deferred-baseline heading misaligned

The P04a deferred-language extractor (`sed -n '/^## Deferred Baseline/,/^## /p'`)
could not find the baseline section because the P04 marker heading was
`## Deferred-Language Pre-Phase Baseline`, not `## Deferred Baseline`. The
manual scan was clean, but the scripted command warned.

Fix: renamed the heading to `## Deferred Baseline` (preserving all existing
evidence verbatim). The P04a extraction command now finds the section and the
diff comparison passes (zero newly introduced deferred language).

File changed:
- `project-plans/issue2285/.completed/P04.md` (heading rename only; evidence
  preserved)

### Remediation Verification — Round 2 (all PASS)

1. `createMockConfig` not imported/used in
   `task.factory-migration.integration.test.ts`: confirmed (grep clean).
2. No `vi.fn`/`vi.spyOn`/`toHaveBeenCalledWith`/`toHaveBeenCalledTimes`/
   `.sendMessage` non-S/`new Task(` in either P04 behavior test file: confirmed
   (grep clean).
3. `npm run test --workspace @vybestack/llxprt-code-a2a-server -- config.factory-migration task.factory-migration`: PASS — 2 files, 6 tests.
4. `npm run test --workspace @vybestack/llxprt-code-a2a-server`: PASS — 13 files, 106 tests.
5. `npm run test --workspace @vybestack/llxprt-code -- useToolScheduler useTodoContinuation useAgenticLoop`: PASS — 7 files, 39 tests.
6. `npm run typecheck`: PASS (all workspaces exit 0).
7. `npm run lint:eslint-guard`: PASS.
8. P04a deferred-baseline command (`sed -n '/^## Deferred Baseline/,/^## /p'`):
   finds the section (no warning); diff comparison passes — zero newly
   introduced deferred language.
9. Root still exports internals (`export * from './internals.js'` count = 1);
   `.llxprt/` unchanged: confirmed.

## P04a Remediation Evidence — Round 3 (blocker fix applied)

The third P04a verification pass found one remaining blocker; remediated in
the P04-owned test file only (no P04a marker created — that is the verifier's
responsibility).

### Blocker: NONPUBLIC_HITS flagged task test multiline import closing line

The P04a `NONPUBLIC_HITS` grep is line-based. The
`task.factory-migration.integration.test.ts` file had a multiline import from
`@vybestack/llxprt-code-agents`:

```
import {
  createAgentClient,
  createToolScheduler,
} from '@vybestack/llxprt-code-agents';
```

The closing line `} from '@vybestack/llxprt-code-agents';` contains no
allowlisted public symbol, so the line-based fail-closed grep flagged it.

Fix: split the multiline import into two single-line import statements so
every line containing `from '@vybestack/llxprt-code-agents'` also carries an
allowlisted public symbol (`createAgentClient` / `createToolScheduler`). The
gate is NOT weakened — it still fails-closed; the imports were merely
reformatted to avoid multiline false positives while using only allowed root
symbols. Behavior and test semantics are unchanged (identical symbols imported).

File changed:
- `packages/a2a-server/src/agent/task.factory-migration.integration.test.ts`
  (import split only)

### Remediation Verification — Round 3 (all PASS)

1. NONPUBLIC_HITS (exact P04a command): PASS (empty).
2. No `createMockConfig`/`vi.fn`/`vi.spyOn`/`toHaveBeenCalledWith`/
   `toHaveBeenCalledTimes`/`.sendMessage` non-S/`new Task(` in either P04
   behavior test file: confirmed (grep clean).
3. `npm run test --workspace @vybestack/llxprt-code-a2a-server -- config.factory-migration task.factory-migration`: PASS — 2 files, 6 tests.
4. `npm run typecheck`: PASS (all workspaces exit 0).
5. `npm run lint:eslint-guard`: PASS.
6. Root still exports internals (`export * from './internals.js'` count = 1);
   `.llxprt/` unchanged: confirmed.
