# P04a Completion Marker

## Phase
PLAN-20260629-ISSUE2285.P04a

## Verdict
PASS — Phase P04/P04a verification only. P05 depollution was not executed; `packages/agents/src/index.ts` still exports internals.

## Files Changed (P04a verifier-owned artifacts only)

- `project-plans/issue2285/.completed/P04a.md` — created this verifier completion marker.
- `project-plans/issue2285/execution-tracker.md` — updated P04a row and Gate 3 verifier evidence for P04a-owned checks.

No source/test/package/script/.github files were modified by P04a verification.

## Diff Stats (P04a verifier-owned artifacts)

```text
project-plans/issue2285/.completed/P04a.md | created
project-plans/issue2285/execution-tracker.md | updated P04a row and Gate 3 verifier evidence
```

## P04 Marker Verification

`project-plans/issue2285/.completed/P04.md` exists and contains structured evidence:

- `## Deferred Baseline` heading is present and readable by the P04a extractor.
- Deferred baseline evidence records ZERO pre-phase hits across the four P04-owned A2A production files.
- Post-baseline comparison records ZERO hits / no newly introduced deferred language.
- Files changed, diff stats, command outputs, tracker evidence, and remediation evidence are present.

## Semantic Verification Evidence

- A2A production migration: `packages/a2a-server/src/config/config.ts` imports and uses `createAgentClient` / `createToolScheduler`; `packages/a2a-server/src/agent/task.ts` imports and uses `createAgentClient`.
- A2A type imports: `AgentClientContract` / `ToolSchedulerContract` are imported from `@vybestack/llxprt-code-core` root in A2A source. Deep core contract path grep returned empty.
- A2A exception records: `project-plans/issue2285/analysis/a2a-exception-records.md` exists and documents `Status: NO EXCEPTIONS`; grep found no retained A2A production imports of `@vybestack/llxprt-code-agents/internals.js`.
- CLI test migration: CLI affected test consumers import internals-only values from `@vybestack/llxprt-code-agents/internals.js` or use core-root contracts; the P04a CLI root internals grep returned empty.
- A2A behavior tests: required colocated files exist with P04 markers:
  - `packages/a2a-server/src/config/config.factory-migration.test.ts`
  - `packages/a2a-server/src/agent/task.factory-migration.integration.test.ts`
- Behavior test semantic checks confirmed use of real APIs (`Task.create`, `sendMessageStream`, `scheduler.schedule`, event-bus `publish` observations as applicable), no `createMockConfig`, no mocked scheduler factories in the P04 behavior tests, no nonexistent `.sendMessage`, no direct `new Task(`, and no `vi.fn`/`vi.spyOn`/`toHaveBeenCalledWith`/`toHaveBeenCalledTimes` mock-theater patterns in those two files.
- Production marker discipline: grep found no new `@plan:PLAN-20260629-ISSUE2285` / `@requirement:REQ-004` markers in A2A production source files; markers are only in the P04 behavior test files.
- Root internals still exported: `grep -c "export \* from './internals.js'" packages/agents/src/index.ts` returned `1`.
- `.llxprt` status: `git status --short .llxprt` returned empty.

## Command Outputs (exit status + key output)

### Exact P04a NONPUBLIC_HITS command

```bash
NONPUBLIC_HITS="$(grep -rn "from '@vybestack/llxprt-code-agents'" packages/a2a-server/src --include="*.ts" | grep -v "createTaskToolRegistration\|createAgentClient\|createToolScheduler\|createTaskRegistration" || true)"
test -z "$NONPUBLIC_HITS" || { echo "FAIL: non-public agents root import in A2A:"; echo "$NONPUBLIC_HITS"; exit 1; }
```

Result: PASS (exit 0), output `PASS: NONPUBLIC_HITS empty`.

### Static verification bundle

Result: PASS for all mandatory static checks:

- Public factory usage confirmed in A2A source/tests.
- CLI root internals test grep clean.
- Exception records file exists.
- Required behavior test files exist and have markers.
- Behavior tests contain required real API references and no forbidden mock-theater patterns.
- Deep core contract paths grep clean.
- Core-root contract type import present in A2A.
- Root internals still exported check returned count `1`.
- Deferred-language baseline comparison passed via `## Deferred Baseline` extraction.
- Production issue2285 marker grep clean.
- `.llxprt` git status clean.

### Full typecheck

Command: `npm run typecheck`

Result: PASS (exit 0). All workspaces ran `tsc --noEmit` successfully, including `@vybestack/llxprt-code-a2a-server`, `@vybestack/llxprt-code`, `@vybestack/llxprt-code-agents`, and dependencies.

### A2A workspace tests

Command: `npm run test --workspace @vybestack/llxprt-code-a2a-server`

Result: PASS (exit 0): 13 test files, 106 tests passed.

### Affected CLI tests

Command: `npm run test --workspace @vybestack/llxprt-code -- useToolScheduler useTodoContinuation useAgenticLoop`

Result: PASS (exit 0): 7 test files, 39 tests passed.

### A2A behavior tests

Command: `npm run test --workspace @vybestack/llxprt-code-a2a-server -- config.factory-migration task.factory-migration`

Result: PASS (exit 0): 2 test files, 6 tests passed.

### Deferred-language baseline comparison

The P04a extractor found `## Deferred Baseline` in `P04.md`; current A2A phase-file scan compared against the recorded baseline with no new deferred-language hits.

Result: PASS (exit 0), output `OK: no newly introduced deferred language (pre-existing baseline tolerated)`.

### ESLint guard

Command: `npm run lint:eslint-guard`

Result: PASS (exit 0), output `ESLint policy guard passed.`

## Git Status Scope

Final `git status --short` shows accumulated P03/P04/plan artifacts, including expected source/test changes from completed phases and untracked `project-plans/issue2285/`. No `.llxprt` changes were present.

P04a modified only:

- `project-plans/issue2285/.completed/P04a.md`
- `project-plans/issue2285/execution-tracker.md`

## Tracker Evidence

Updated `project-plans/issue2285/execution-tracker.md`:

- P04a row marked DONE with verifier evidence summary.
- Gate 3 P04a-relevant items marked checked with verifier evidence:
  - Production CLI source does not import agents internals via root leaked symbols.
  - A2A server internals-only imports migrated public-factory-first.
  - Per-use exception records for retained A2A internals subpath.
  - CLI test compile-breakers migrated to internals subpath, not root.
  - Legitimate internals subpath consumers resolve under typecheck and Vitest.
  - A2A factory-migration behavior verified by exact named colocated real-behavior tests.

## Verifier Sign-off

Verified by: LLxprt Code
Verification phase: P04a
Verdict: PASS
