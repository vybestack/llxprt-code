# P02a Completion: Path and File Service Stubs

## Status: PASS

## Files Created

1. `packages/storage/src/config/storage.ts`
   - Exports: `LLXPRT_DIR` (const = `''`), `PROVIDER_ACCOUNTS_FILENAME` (const = `''`), `OAUTH_FILE` (const = `''`), `Storage` (class, constructor throws "not implemented")
   - All static and instance methods throw "not implemented"
   - Constants typed as `string` but wrong values (stub behavior)

2. `packages/storage/src/services/fileSystemService.ts`
   - Exports: `FileSystemService` (abstract class), `StandardFileSystemService` (class extending FileSystemService, methods throw "not implemented")
   - Correct method signatures: `readTextFile(filePath: string): Promise<string>`, `writeTextFile(filePath: string, content: string): Promise<void>`
   - **REMEDIATED**: Verifier flagged `filesystem_class_missing` — `FileSystemService` was exported as `interface` but plan requires `abstract class` with methods throwing "not implemented". Fixed: changed `interface` → `abstract class` with `abstract` methods; changed `StandardFileSystemService` from `implements FileSystemService` → `extends FileSystemService`.

3. `packages/storage/src/services/fileDiscoveryService.ts`
   - Exports: `FileDiscoveryService` (class, constructor throws "not implemented"), `FilterFilesOptions` (interface), `FilterReport` (interface)
   - All methods throw "not implemented"

4. `packages/storage/src/utils/gitIgnoreParser.ts`
   - Exports: `GitIgnoreFilter` (interface), `GitIgnoreParser` (class, constructor throws "not implemented")
   - Methods: `isIgnored()`, `getPatterns()` — all throw "not implemented"

5. `packages/storage/src/utils/gitUtils.ts`
   - Exports: `isGitRepository(directory: string): boolean`, `findGitRoot(directory: string): string | null`
   - Both throw "not implemented"

## Barrel (index.ts) — NOT modified
- Per plan, barrel remains exporting only logger types from P01.
- Stub modules are imported directly by test files, not through the barrel.

## Verification Commands Run

### 1. Typecheck
```
$ npm run typecheck --workspace @vybestack/llxprt-code-storage
> tsc --noEmit
Exit Code: 0 (PASS)
```

### 2. Stub File Existence
```
All stub files exist. (PASS)
```

### 3. Export Surface Checks
```
OK: storage.ts exports constants (LLXPRT_DIR, PROVIDER_ACCOUNTS_FILENAME, OAUTH_FILE)
OK: storage.ts exports Storage class
OK: fileSystemService.ts exports FileSystemService (abstract class)
OK: fileSystemService.ts exports StandardFileSystemService
OK: fileDiscoveryService.ts exports FileDiscoveryService
OK: fileDiscoveryService.ts exports FilterFilesOptions
OK: fileDiscoveryService.ts exports FilterReport
OK: gitUtils.ts exports isGitRepository
OK: gitUtils.ts exports findGitRoot
OK: gitIgnoreParser.ts exports GitIgnoreParser
OK: gitIgnoreParser.ts exports GitIgnoreFilter
```

### 4. Barrel Check
```
OK: barrel does not export stubs (PASS)
```

## Complete Public API Surface (P02a Stubs)

| Symbol | Type | File | Stub Behavior |
|--------|------|------|---------------|
| `LLXPRT_DIR` | const string | config/storage.ts | returns `''` |
| `PROVIDER_ACCOUNTS_FILENAME` | const string | config/storage.ts | returns `''` |
| `OAUTH_FILE` | const string | config/storage.ts | returns `''` |
| `Storage` | class | config/storage.ts | constructor throws "not implemented" |
| `FileSystemService` | abstract class | services/fileSystemService.ts | abstract methods (no runtime behavior) |
| `StandardFileSystemService` | class | services/fileSystemService.ts | methods throw "not implemented" |
| `FileDiscoveryService` | class | services/fileDiscoveryService.ts | constructor throws "not implemented" |
| `FilterFilesOptions` | interface | services/fileDiscoveryService.ts | — |
| `FilterReport` | interface | services/fileDiscoveryService.ts | — |
| `GitIgnoreFilter` | interface | utils/gitIgnoreParser.ts | — |
| `GitIgnoreParser` | class | utils/gitIgnoreParser.ts | constructor throws "not implemented" |
| `isGitRepository` | function | utils/gitUtils.ts | throws "not implemented" |
| `findGitRoot` | function | utils/gitUtils.ts | throws "not implemented" |

## Findings
- No issues. All stubs typecheck cleanly and export complete API surface.
- Private method `getFilePathHash` removed from stub to avoid TS6133 (noUnusedLocals); will be restored in P02c with real implementation.

## Remediation (filesystem_class_missing)

**Failure**: Verifier grep `filesystem_class_missing` — `FileSystemService` was exported as `interface` but P02a plan requires `class` or `abstract class` with methods throwing "not implemented".

**Fix applied**: `packages/storage/src/services/fileSystemService.ts`
- Changed `export interface FileSystemService { ... }` → `export abstract class FileSystemService { abstract readTextFile(...); abstract writeTextFile(...); }`
- Changed `export class StandardFileSystemService implements FileSystemService` → `export class StandardFileSystemService extends FileSystemService`

**Post-fix typecheck**:
```
$ npm run typecheck --workspace @vybestack/llxprt-code-storage
> @vybestack/llxprt-code-storage@0.10.0 typecheck
> tsc --noEmit
Exit Code: 0 (PASS)
```

**Post-fix grep checks**:
```
OK: FileSystemService exported as class (abstract)
OK: StandardFileSystemService exported (extends FileSystemService)
```
