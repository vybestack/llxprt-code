# P07 Completion: Full Verification and Smoke

## Phase ID

`PLAN-20260609-ISSUE1590.P07`

## Status: PASS

## Prerequisite Check

- `project-plans/issue1590/.completed/P06.md` present — OK.
- `project-plans/issue1590/.completed/P06-V.md` present (PASS) — OK.

## Full Repository Verification

| Command | Result |
|---|---|
| `npm install` | EXIT 0 — workspace links refreshed; lockfile reconciled by postinstall (expected). |
| `npm run build` (full monorepo) | EXIT 0 — all packages built in order (storage → core → mcp → providers → a2a-server → test-utils → cli → vscode-ide-companion → lsp). |
| `npm run typecheck` (full) | EXIT 0 — core, storage, providers, cli, a2a-server, test-utils, lsp all clean. |
| `npm run lint` (full) | EXIT 0 — zero errors (warnings only, pre-existing). |
| `npm run format` ×2 then `npm run format:check` | EXIT 0/0/0 — deterministic, "All matched files use Prettier code style!" |
| `npm run build` (rebuild after format) | EXIT 0 — dist reconciled with formatted sources. |

## Per-Package Test Results (Dependency Order)

| Package | Tests | Result |
|---|---|---|
| `@vybestack/llxprt-code-storage` | 191 passed (10 files) | EXIT 0 |
| `@vybestack/llxprt-code-core` | 7609 passed, 30 skipped (412 files, 1 skipped) | EXIT 0 |
| `@vybestack/llxprt-code-mcp` | 329 passed (15 files) | EXIT 0 |
| `@vybestack/llxprt-code-providers` | 1918 passed, 14 skipped (151 files, 1 skipped) — incl. LoggingProviderWrapper behavioral JSONL test | EXIT 0 |
| `@vybestack/llxprt-code-a2a-server` | 100 passed (11 files) | EXIT 0 |
| `@vybestack/llxprt-code-test-utils` | No `test` script (build/typecheck only — both pass via full build/typecheck) | N/A |
| `@vybestack/llxprt-code` (cli) | 5746 passed, 7 skipped (506 files) | EXIT 0 |

All counts match the known-good baselines.

## Lint Errors Fixed During P07

The full lint surfaced 10 errors in files authored during this extraction. All fixed (no behavior change):

1. `packages/storage/src/conversation/ConversationFileWriter.test.ts` — `Record<string, unknown>[]` → `Array<Record<string, unknown>>` (`@typescript-eslint/array-type`).
2. `scripts/check-storage-import-boundary.mjs` — removed unused `existsSync` import; removed unused `movedSymbols` parameter from `collectNamespaceAccesses` (and its call site).
3. `scripts/preflight-import-inventory.mjs` — removed unused `statSync` and `pathToFileURL` imports.
4. `packages/providers/src/LoggingProviderWrapper.test.ts` — prefixed unused `contentOrOptions` param with `_`; moved top-level `afterEach` inside the `describe` block (`vitest/require-top-level-describe`); removed three unnecessary type/non-null assertions.

## Integrity Checks

| Check | Result |
|---|---|
| Storage leaf (no core/`packages/core` refs in `src` or `package.json`) | LEAF_OK |
| Core moved files contain no class definitions (shims only) | SHIM_OK |
| Stale deep-path imports (rg) | Only the intentional shim-identity references remain: `packages/core/src/storage/storage-compat.test.ts` (29 identity tests) and `packages/providers/src/LoggingProviderWrapper.test.ts` (scenario-2 singleton identity). Documented P06 deviations. |
| Root-constant stale imports (LLXPRT_DIR/PROVIDER_ACCOUNTS_FILENAME/OAUTH_FILE) | ROOTCONST_OK |
| Parser boundary — deep-paths (`--detect-kinds` static/namespace/import-equals/dynamic/vi.mock) | PASS — 0 violations across 7460 files |
| Parser boundary — root-imports (26 moved symbols, with documented exclusions for `storage-compat.test.ts` and `LoggingProviderWrapper.test.ts`) | PASS — 0 violations across 2227 files |
| `check-storage-package-cycle.mjs --production` | PASS — storage is a leaf, no cycles |
| `check-storage-package-cycle.mjs --all-dependencies` | PASS — storage is a leaf, no cycles |
| `resetConversationFileWriterForTesting` confined to `@vybestack/llxprt-code-storage/testing` (NOT in barrel or core shims) | LEAK_OK |

## Documented Deviations (carried from P06, re-confirmed)

1. `packages/providers/src/LoggingProviderWrapper.test.ts` intentionally imports `getConversationFileWriter` from `@vybestack/llxprt-code-core/storage/ConversationFileWriter.js` to assert the core shim and storage resolve to the SAME singleton instance (scenario-2). This file is excluded from the root-imports boundary scan via `--exclude-glob`.
2. `packages/core/src/storage/storage-compat.test.ts` deliberately imports all 7 core deep-path shims to assert re-export identity; excluded via `--exclude-core-compat-tests` / `--exclude-glob`.

## Smoke Test

```
node scripts/start.js --profile-load ollamakimi "write me a haiku and nothing else"
```

EXIT 0. Output:

```
[ollamakimi]
Winter moonlight falls
Silent snowflakes drift softly
Peace fills the cold night
```

(The "Source file ... modified since last build" warnings appeared because `npm run format` touched sources after the build; a subsequent `npm run build` reconciled dist. Runtime resolution of the new storage package succeeded.)

## Diff Summary

`git diff --stat HEAD`: 88 files changed, 245 insertions(+), 5152 deletions(-) — large deletions are the moved implementations now living in `packages/storage` (untracked) and replaced in core by thin re-export shims. Untracked roots: `packages/storage/`, `project-plans/issue1590/`, new `scripts/`. Stray root `.completed/` artifact (subagent CWD bug; intermediate RED-gate copy of P02b) was removed — the canonical marker remains under `project-plans/issue1590/.completed/`.

## Pseudocode Verification (lines 28-34)

- 28 `npm install` run — done.
- 29-30 Per-package test/typecheck/build pass — done (table above).
- 31 Storage leaf check passes — LEAF_OK + cycle leaf confirmation.
- 32 No dependency cycle — both cycle modes PASS.
- 33 Full repo verification + smoke passes — done.
- 34 Failure recovery loop — exercised for the 10 lint errors; all resolved, downstream re-verified.

## Final Assessment

All required commands pass. Full per-package and monorepo test/lint/typecheck/format/build are green, formatting is deterministic, import boundaries and dependency cycles are clean (storage is a verified leaf), the test-only export is confined to the `/testing` entry point, and the runtime smoke command produced the requested haiku. Ready for P07-V verification and commit.

## Remaining Risks

- None functional. Pre-existing lint warnings (sonarjs, etc.) are unchanged and out of scope.
- CI may re-run the full `npm run test` (which exceeds local single-invocation time); per-package runs here cover the same suites.
