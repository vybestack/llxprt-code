# P04a Completion: Session/Conversation Storage Stubs

## Files Created

1. `packages/storage/src/session/sessionTypes.ts`
   - Exports: `SESSION_FILE_PREFIX` (stub: `''`, correct type `string`)
   - Exports: `ConversationRecord` interface (exact shape from core)
   - Exports: `BaseMessageRecord` interface (exact shape from core)
   - Exports: `ToolCallRecord` interface (exact shape from core)

2. `packages/storage/src/conversation/ConversationFileWriter.ts`
   - Exports: `class ConversationFileWriter` — constructor and all methods throw `'not implemented'`
   - Exports: `function getConversationFileWriter` — throws `'not implemented'`
   - Exports: `function resetConversationFileWriterForTesting` — empty body (no-op)

## Verification Output

```
=== P04a Verification (final) ===

--- Typecheck ---
npm run typecheck --workspace @vybestack/llxprt-code-storage
PASS: typecheck (exit 0, no errors)

--- API surface: sessionTypes.ts ---
PASS: SESSION_FILE_PREFIX found
PASS: ConversationRecord found
PASS: BaseMessageRecord found
PASS: ToolCallRecord found

--- API surface: ConversationFileWriter.ts ---
PASS: ConversationFileWriter class found
PASS: getConversationFileWriter found
PASS: resetConversationFileWriterForTesting found

--- Storage leaf boundary check (no core imports) ---
PASS: no core dependencies

--- Runtime behavior: constructor/getter throw ---
6 throw statements (constructor, writeEntry, writeRequest, writeResponse, writeToolCall, getConversationFileWriter)

--- SESSION_FILE_PREFIX stub value ---
export const SESSION_FILE_PREFIX = '';  (wrong value, correct type)

--- Barrel NOT modified ---
PASS: barrel unchanged

--- testing.ts NOT modified ---
Content: "Populated in P04d" comment only

=== P04a Verification Complete ===
```

## Findings

- All 7 public API symbols exported correctly across both stub files.
- Stubs typecheck cleanly with `noUnusedLocals: true` in root tsconfig.
- No core imports (`debugLogger`, `@vybestack/llxprt-code-core`, etc.) in stub files.
- Root barrel (`src/index.ts`) and testing exports (`src/testing.ts`) NOT modified (P04d scope).
- `SESSION_FILE_PREFIX` stub returns `''` instead of `'session-'` — will cause runtime test failures in P04b RED gate as intended.
- `ConversationFileWriter` constructor and `getConversationFileWriter` throw `'not implemented'` — will cause behavioral failures in P04b RED gate as intended.
- `resetConversationFileWriterForTesting` is an empty no-op — safe for test import.

## Notes

- Initial attempt had TS6133 unused variable errors due to private fields and module-level `let` that were never read. Resolved by removing unnecessary private fields from the stub class (only needed for real implementation in P04d) and eliminating the module-level singleton variable.
