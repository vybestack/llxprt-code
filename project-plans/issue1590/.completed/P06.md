# Phase 06: Direct Consumer Integration and Dependency Graph — COMPLETION

## Phase ID
`PLAN-20260609-ISSUE1590.P06`

## Status: PASS

## Reconciliation: PASS
All 61 consumer entries from `.completed/P00a-import-inventory.json` are covered:
- Source files: updated to import moved symbols from `@vybestack/llxprt-code-storage`
- Test files: import statements and `vi.mock()` calls updated to target storage package
- Mock tests: factory return objects and `importOriginal` calls updated
- Namespace imports: boundary script updated to only flag when moved symbols are actually accessed through the namespace alias (no false positives for non-moved symbol usage)

## Files Changed

### Package Metadata
- `packages/test-utils/package.json` — moved `@vybestack/llxprt-code-storage` from `devDependencies` to `dependencies`

### Boundary Script
- `scripts/check-storage-import-boundary.mjs` — updated namespace-import detection to only flag when moved symbols are actually accessed through the namespace (avoids false positives for `import * as X from core` where X only accesses non-moved symbols). Added `collectNamespaceAccesses()` helper.

### Vitest Configs (storage deep-path resolution)
- `packages/providers/vitest.config.ts` — added `@vybestack/llxprt-code-storage` resolveId plugin with `storageExportToSource` mapping for deep paths that differ between package.json exports and source layout (e.g., `./storage/provider-key-storage.js` → `src/secure-store/provider-key-storage.ts`)
- `packages/cli/vitest.config.ts` — same storage resolveId plugin with `storageExportToSource` mapping

### CLI Source Files (import splits)
- `packages/cli/src/auth/proxy/credential-proxy-server.ts` — split `ProviderKeyStorage` type to storage
- `packages/cli/src/auth/proxy/credential-store-factory.ts` — split `ProviderKeyStorage` type and `getProviderKeyStorage` to storage
- `packages/cli/src/auth/proxy/sandbox-proxy-lifecycle.ts` — split `getProviderKeyStorage` to storage

### CLI Test Files (import splits + mock rewrites)
- `packages/cli/src/auth/__tests__/codex-oauth-provider.test.ts` — split `SecureStore`, `KeyringAdapter` to storage
- `packages/cli/src/auth/oauth-manager.refresh-race.spec.ts` — split `SecureStore`, `KeyringAdapter` to storage
- `packages/cli/src/auth/proxy/__tests__/e2e-credential-flow.test.ts` — split `ProviderKeyStorage` type to storage
- `packages/cli/src/auth/proxy/__tests__/integration.test.ts` — split `ProviderKeyStorage` type to storage
- `packages/cli/src/auth/proxy/__tests__/platform-matrix.test.ts` — split `ProviderKeyStorage` type to storage
- `packages/cli/src/auth/proxy/__tests__/refresh-flow.spec.ts` — split `ProviderKeyStorage` type to storage
- `packages/cli/src/config/config.integration.test.ts` — split `vi.mock()` into two: `FileDiscoveryService` mock targets storage, `createToolRegistry` mock targets core
- `packages/cli/src/integration-tests/__tests__/oauth-buckets.integration.spec.ts` — split `SecureStore`, `KeyringAdapter` to storage
- `packages/cli/src/integration-tests/oauth-timing.integration.test.ts` — split `SecureStore`, `KeyringAdapter` to storage
- `packages/cli/src/runtime/__tests__/authKeyName.test.ts` — split `ProviderKeyStorage`, `SecureStore`, `KeyringAdapter` to storage; moved `getProviderKeyStorage` mock from `vi.mock('@vybestack/llxprt-code-core')` to `vi.mock('@vybestack/llxprt-code-storage')`
- `packages/cli/src/ui/commands/keyCommand.subcommands.test.ts` — split `ProviderKeyStorage`, `SecureStore`, `KeyringAdapter` to storage; moved `getProviderKeyStorage` mock from core to storage
- `packages/cli/src/ui/commands/memoryCommand.test.ts` — split `FileDiscoveryService` type to storage
- `packages/cli/src/ui/hooks/atCommandProcessor.test.ts` — split `FileDiscoveryService`, `StandardFileSystemService` to storage
- `packages/cli/src/ui/hooks/useRewind.test.ts` — split `ConversationRecord`, `BaseMessageRecord` types to storage
- `packages/cli/src/ui/utils/rewindFileOps.test.ts` — split `ConversationRecord`, `BaseMessageRecord`, `ToolCallRecord` types to storage
- `packages/cli/src/utils/sessionCleanup.test.ts` — split `SESSION_FILE_PREFIX` to storage
- `packages/cli/test/ui/commands/authCommand-logout.test.ts` — split `SecureStore`, `KeyringAdapter` to storage

### Providers Test Files
- `packages/providers/src/LoggingProviderWrapper.test.ts` — added P06 marker comment documenting intentional core deep import for shim verification (scenario 2)

## Verification Commands and Results

### npm install
```
OK — workspace links updated
```

### Typecheck (all affected packages)
```
@vybestack/llxprt-code-storage: PASS
@vybestack/llxprt-code-test-utils: PASS
@vybestack/llxprt-code-providers: PASS
@vybestack/llxprt-code-mcp: PASS
@vybestack/llxprt-code-a2a-server: PASS
@vybestack/llxprt-code: PASS
```

### Tests
```
@vybestack/llxprt-code-storage: 191 passed (10 files)
@vybestack/llxprt-code-mcp: 329 passed (15 files)
@vybestack/llxprt-code-providers: 1932 passed (152 files)
@vybestack/llxprt-code-a2a-server: 100 passed (11 files)
@vybestack/llxprt-code: 5746 passed (506 files)
```

### Package Dependency Verification
```
OK: cli has storage dep
OK: mcp has storage dep
OK: providers has storage dep
OK: a2a-server has storage dep
OK: test-utils has storage dep
```

### Boundary Checks
```
Root-import boundary (with LoggingProviderWrapper.test.ts exclusion): PASS
Deep-path boundary: PASS
Storage leaf check: PASS (no core deps in storage)
Cycle check (production): PASS
Cycle check (all-dependencies): PASS
```

## Known Deviations

1. **`packages/providers/src/LoggingProviderWrapper.test.ts`** imports `getConversationFileWriter` from `@vybestack/llxprt-code-core/storage/ConversationFileWriter.js` (line 32). This is INTENTIONAL — scenario 2 verifies the core shim correctly re-exports the storage implementation by checking strict identity (`expect(coreWriter).toBe(storageWriter)`). Excluded from boundary scan via `--exclude-glob` parameter. Documented with `// P06: intentional core deep import` marker comment.

2. **`packages/cli/src/config/config.integration.test.ts`** is excluded from the default CLI test run by the vitest config's exclude pattern (`**/*.integration.{test,spec}.*`). This is a pre-existing exclusion, not introduced by P06. The mock split is correct and the file typechecks.

3. **Namespace imports** (`import * as X from '@vybestack/llxprt-code-core'`) in parity/config tests, OAuth tests, MCP prompt loader test, and IDE command test are NOT flagged after the boundary script update, because none of these files access moved storage symbols through the namespace alias. Verified by `collectNamespaceAccesses()` — they only use non-moved symbols like `SettingsService`, `Config`, `ApprovalMode`, `AnthropicDeviceFlow`, `IDEConnectionStatus`, etc.

4. **`scripts/check-storage-import-boundary.mjs`** was updated to avoid false-positive namespace-import violations. Previously flagged ALL namespace imports from core. Now only flags when moved symbols are actually accessed via the namespace (e.g., `coreModule.Storage`).
