# P03b Completion: Tests/RED

## Status: PASS (RED gate satisfied)

## Files Created

1. `packages/storage/src/secure-store/secure-store.test.ts` — copied from core with import rewrites (1509 lines)
2. `packages/storage/src/secure-store/secure-store.spec.ts` — copied from core with import rewrites (372 lines)
3. `packages/storage/src/secure-store/secure-store-integration.test.ts` — copied from core with import rewrites and inline `maskKeyForDisplay` helper (607 lines)
4. `packages/storage/src/secure-store/provider-key-storage.test.ts` — copied from core with import rewrites (477 lines)

## Import Rewrites Applied

- `secure-store.test.ts`: No changes needed — already imports from `./secure-store.js`
- `secure-store.spec.ts`: No changes needed — already imports from `./secure-store.js`
- `secure-store-integration.test.ts`: Replaced `import { maskKeyForDisplay } from '../tools/tool-key-storage.js'` with inline `maskKeyForDisplay` helper function (matching original implementation: first2 + stars + last2)
- `provider-key-storage.test.ts`: No changes needed — already imports from `./secure-store.js` and `./provider-key-storage.js`
- Zero imports from `@vybestack/llxprt-code-core` or any `packages/core` relative path remain.

## Typecheck

```
$ npm run typecheck --workspace @vybestack/llxprt-code-storage
> @vybestack/llxprt-code-storage@0.10.0 typecheck
> tsc --noEmit
(exit 0)
```

## RED Output

> **Command note (P03b remediation)**: The original plan used root-relative paths
> (`packages/storage/src/secure-store/...`) after `npm run test --workspace
> @vybestack/llxprt-code-storage -- ...`, but workspace vitest runs from
> `packages/storage`, so those paths resolve to
> `packages/storage/packages/storage/src/...` — yielding "No test files found".
> The corrected command uses workspace-relative paths (`src/secure-store/...`).
> The plan file has been updated accordingly.

```
$ npm run test --workspace @vybestack/llxprt-code-storage -- src/secure-store/secure-store.test.ts src/secure-store/secure-store.spec.ts src/secure-store/secure-store-integration.test.ts src/secure-store/provider-key-storage.test.ts

> @vybestack/llxprt-code-storage@0.10.0 test
> vitest run src/secure-store/secure-store.test.ts src/secure-store/secure-store.spec.ts src/secure-store/secure-store-integration.test.ts src/secure-store/provider-key-storage.test.ts

 RUN  v3.2.4 /Users/acoliver/projects/llxprt/branch-2/llxprt-code/packages/storage

  src/secure-store/secure-store.spec.ts (10 tests | 10 failed) 8ms
  src/secure-store/secure-store-integration.test.ts (17 tests | 17 failed) 10ms
  src/secure-store/provider-key-storage.test.ts (27 tests | 27 failed) 13ms
  src/secure-store/secure-store.test.ts (47 tests | 47 failed) 18ms

 Test Files  4 failed (4)
      Tests  101 failed (101)
   Start at  22:57:28
   Duration  239ms (transform 82ms, setup 0ms, collect 126ms, tests 47ms, environment 0ms, prepare 121ms)
```

### Failure Breakdown by File

**secure-store.test.ts** (47 tests, 47 failed):
- Keyring Access (4 tests): `new SecureStore()` throws "not implemented"
- Availability Probe (4 tests): `new SecureStore()` throws "not implemented"
- CRUD Operations (12 tests): `new SecureStore()` throws "not implemented"
- Encrypted File Fallback (6 tests): `new SecureStore()` throws "not implemented"
- No Backward Compatibility (2 tests): `new SecureStore()` throws "not implemented"
- Error Taxonomy (3 tests): `new SecureStore()` throws "not implemented"
- Resilience (2 tests): `new SecureStore()` throws "not implemented"
- Key Validation (4 tests): `new SecureStore()` throws "not implemented"
- Probe Cache Invalidation (3 tests): `new SecureStore()` throws "not implemented"
- Fault Injection (3 tests): `new SecureStore()` throws "not implemented"
- Fallback Policy (2 tests): `new SecureStore()` throws "not implemented"
- Cross-Instance Consistency (2 tests): `new SecureStore()` throws "not implemented"
- Default Path (1 test): `new SecureStore()` throws "not implemented"

**secure-store.spec.ts** (10 tests, 10 failed):
- Linux Keyring Fallback Reliability (8 tests): `new SecureStore()` throws "not implemented"
- fallbackPolicy semantics preservation (2 tests): `new SecureStore()` throws "not implemented"

**secure-store-integration.test.ts** (17 tests, 17 failed):
- ToolKeyStorage Pattern (5 tests): `new SecureStore()` throws "not implemented"
- KeychainTokenStorage Pattern (4 tests): `new SecureStore()` throws "not implemented"
- ExtensionSettingsStorage Pattern (2 tests): `new SecureStore()` throws "not implemented"
- Cross-Wrapper Isolation (2 tests): `new SecureStore()` throws "not implemented"
- Legacy Format Detection (4 tests): `new SecureStore()` throws "not implemented"

**provider-key-storage.test.ts** (27 tests, 27 failed):
- Key Name Validation (8 tests):
  - `validateKeyName` throws "not implemented" (7 tests)
  - `KEY_NAME_REGEX.source` is `^$` instead of expected `^[a-zA-Z0-9._-]{1,64}$` (1 test — wrong value behavioral failure)
- CRUD Operations (13 tests): `new SecureStore()` or `new ProviderKeyStorage()` throws "not implemented"
- Case Sensitivity (1 test): `new SecureStore()` throws "not implemented"
- Encrypted Fallback (2 tests): `new SecureStore()` throws "not implemented"
- Singleton (2 tests): `getProviderKeyStorage()` throws "not implemented"
  - `resetProviderKeyStorage()` is an intentional no-op stub — test for instance identity fails because `getProviderKeyStorage()` always throws, never returning a new instance

### Unrelated Tests (PASS)

Full storage suite shows 4 passed files (73 tests) — existing P02c logger/path/file service tests unaffected.

## Semantic RED Assessment

### Criteria (a): Tests ran, not just import/module resolution errors
[OK] CONFIRMED — All 101 tests executed. Zero `Cannot find module` or `has no exported member` errors. All failures are runtime throws or assertion mismatches.

### Criteria (b): Multiple specific behavioral failures
[OK] CONFIRMED — Failure categories include:
- `new SecureStore()` throws "not implemented" (constructor failure)
- `new ProviderKeyStorage()` throws "not implemented" (constructor failure)
- `getProviderKeyStorage()` throws "not implemented" (singleton failure)
- `validateKeyName()` throws "not implemented" (validation failure)
- `KEY_NAME_REGEX.source` is `^$` not `^[a-zA-Z0-9._-]{1,64}$` (wrong value)

### Criteria (c): Every test asserting against P03a-stubbed symbols shows behavioral failure
[OK] CONFIRMED — Every single test that touches `SecureStore`, `SecureStoreError`, `ProviderKeyStorage`, `KEY_NAME_REGEX`, `validateKeyName`, `getProviderKeyStorage`, or `resetProviderKeyStorage` fails with a behavioral assertion mismatch or "not implemented" throw. Zero P03-targeted tests pass.

### Criteria (d): Each test file targeting P03a-stubbed behavior has at least one targeted behavioral failure
[OK] CONFIRMED:
- `secure-store.test.ts`: 47 failures — all behavioral "not implemented"
- `secure-store.spec.ts`: 10 failures — all behavioral "not implemented"
- `secure-store-integration.test.ts`: 17 failures — all behavioral "not implemented"
- `provider-key-storage.test.ts`: 27 failures — includes both "not implemented" throws and wrong-value assertion (`KEY_NAME_REGEX.source` mismatch)

## Notes

- Tests use infrastructure adapters (in-memory `KeyringAdapter` via `createMockKeyring()`, temp filesystem directories via `mkdtemp`) — NOT mocking SecureStore or ProviderKeyStorage themselves.
- No test asserts against "not implemented" or placeholder behavior — all tests assert against the expected real contract.
- The `SecureStoreError` class in the stub has a real constructor (not a throw), so error type assertions would work if the constructor path were reached — but the `SecureStore` constructor throws before any method calls can trigger errors.
