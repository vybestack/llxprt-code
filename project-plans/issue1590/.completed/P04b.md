# Phase P04b: Tests/RED

Phase: P04b
Status: RED (all targeted behavioral tests fail as expected)
Date: 2025-06-08

## Files Created

- `packages/storage/src/session/sessionTypes.test.ts` — Session types behavioral tests (5 tests: 1 runtime assertion + 4 type-shape assertions)
- `packages/storage/src/conversation/ConversationFileWriter.test.ts` — ConversationFileWriter behavioral tests (10 tests across 8 scenarios)

## Test Coverage

### Session Types Tests (5 tests)
1. `SESSION_FILE_PREFIX is "session-"` — Runtime assertion (FAILS: stub returns `''`)
2. `ConversationRecord satisfies the expected interface shape` — Type assertion (passes, erased at runtime)
3. `BaseMessageRecord satisfies the expected interface shape` — Type assertion (passes)
4. `ToolCallRecord satisfies the expected interface shape` — Type assertion (passes)
5. `ConversationRecord with nested messages satisfies the interface` — Type assertion (passes)

### ConversationFileWriter Tests (10 tests)
1. **Scenario 1 (Request Write)**: Writes request, reads JSONL, asserts type/provider/messages/context/timestamp
2. **Scenario 2 (Response Write)**: Writes response, reads JSONL, asserts type/provider/response/metadata/timestamp
3. **Scenario 3 (Tool Call Write)**: Writes tool call, reads JSONL, asserts type/provider/tool/context-spread/timestamp
4. **Scenario 4a (Singleton Reuse)**: `getConversationFileWriter` returns same instance
5. **Scenario 4b (Singleton Reset)**: `resetConversationFileWriterForTesting` clears singleton
6. **Scenario 5 (Zero-Arg Backward Compat)**: `new ConversationFileWriter()` constructs, logPath includes `.llxprt` and `conversations`
7. **Scenario 6 (One-Arg Backward Compat)**: `new ConversationFileWriter(tmpPath)` writes valid JSONL
8. **Scenario 7 (writeEntry Error Path)**: Deterministic mkdir failure via file-as-parent, logger error recorded
9. **Scenario 8 (Logger Injection)**: Injected StorageLogger records errors to observable array
10. **Multiple Writes**: Appends multiple entries as separate JSONL lines

## Safety

- No test writes to real `~/.llxprt` — all use `os.tmpdir()` via `fsp.mkdtemp()`
- Error logging tests use real `StorageLogger` objects with observable arrays, not `vi.fn()` mock theater
- Zero-arg backward-compat test only inspects resolved path string via reflection, does not write

## RED Output

```
RUN  v3.2.4 /Users/acoliver/projects/llxprt/branch-2/llxprt-code/packages/storage

 × src/session/sessionTypes.test.ts > Session Types — Constants > SESSION_FILE_PREFIX is "session-" 3ms
   → expected '' to be 'session-' // Object.is equality
 [OK] src/session/sessionTypes.test.ts > Session Types — Type Shape Assertions > ConversationRecord satisfies the expected interface shape 0ms
 [OK] src/session/sessionTypes.test.ts > Session Types — Type Shape Assertions > BaseMessageRecord satisfies the expected interface shape 0ms
 [OK] src/session/sessionTypes.test.ts > Session Types — Type Shape Assertions > ToolCallRecord satisfies the expected interface shape 0ms
 [OK] src/session/sessionTypes.test.ts > Session Types — Type Shape Assertions > ConversationRecord with nested messages satisfies the interface 0ms
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Request Write > writes a request entry with type, provider, messages, context, and timestamp 3ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Response Write > writes a response entry with type, provider, response payload, metadata, and timestamp 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Tool Call Write > writes a tool_call entry with context spread at top level, not nested under context 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Singleton Reuse > getConversationFileWriter returns the same instance on repeated calls 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Singleton Reuse > resetConversationFileWriterForTesting clears the singleton 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Zero-Arg Backward Compat > constructs without error and resolves logPath including .llxprt and conversations 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — One-Arg Backward Compat > constructs with a custom path and writes a valid JSONL entry 0ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — writeEntry Error Path > logs an error when directory creation fails deterministically 1ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Logger Injection > injected logger.error is called when write fails 1ms
   → not implemented
 × src/conversation/ConversationFileWriter.test.ts > ConversationFileWriter — Multiple Writes > appends multiple entries as separate JSONL lines 0ms
   → not implemented

 Test Files  2 failed (2)
      Tests  11 failed | 4 passed (15)
   Duration  219ms
```

## Semantic Assessment

### RED Gate Criteria — All Met

1. **Tests ran** — All 15 tests executed; no import/module resolution errors, no type errors.
2. **Session types runtime assertion failed** — `expect(SESSION_FILE_PREFIX).toBe('session-')` correctly rejects stub value `''`.
3. **Session types compile-time assertions passed** — Type-shape tests for `ConversationRecord`, `BaseMessageRecord`, `ToolCallRecord` pass (as expected: erased at runtime).
4. **ConversationFileWriter tests failed with behavioral failures**:
   - Construction tests (Scenarios 1-3, 5-8, Multiple Writes): `new ConversationFileWriter(...)` throws `Error: not implemented` from stub constructor.
   - Singleton access tests (Scenario 4): `getConversationFileWriter(...)` throws `Error: not implemented` from stub function.
5. **Every test asserting against P04a-stubbed symbols shows a behavioral failure** — confirmed for all 11 failing tests.
6. **Failures are behavioral** — `Error: not implemented` throws and assertion failures (`expected '' to be 'session-'`), not structural (no missing imports or type errors).

### Plan Compliance Notes

- **Barrel import (Scenario 3)**: Deferred to P04d as specified in plan. P04b does not test root barrel imports because barrel has not been updated yet.
- **resetConversationFileWriterForTesting import**: Imported directly from `./ConversationFileWriter.js` (not from `@vybestack/llxprt-code-storage/testing`) because `testing.ts` is not yet populated. P04d will update `testing.ts` and tests should switch.
- **No vi.mock used**: Zero-arg backward-compat test uses Approach B (reflection-based path inspection, no writing). Avoids the mock-leakage issue with vitest hoisted `vi.mock`.
