# P03a Completion: Secure Store & Provider Key Storage Stubs

## Status: PASS

## Files Created

1. `packages/storage/src/secure-store/secure-store.ts` — Stub exports:
   - `SecureStoreErrorCode` (type alias)
   - `SecureStoreError` (class — full constructor, throws not needed since constructor body is valid)
   - `KeyringAdapter` (interface)
   - `SecureStoreOptions` (interface)
   - `SecureStore` (class — constructor throws "not implemented")
   - `createDefaultKeyringAdapter` (async function — throws "not implemented")

2. `packages/storage/src/secure-store/provider-key-storage.ts` — Stub exports:
   - `KEY_NAME_REGEX` (set to `/^$/` — valid regex, wrong value)
   - `validateKeyName` (function — throws "not implemented")
   - `ProviderKeyStorage` (class — constructor throws "not implemented")
   - `getProviderKeyStorage` (function — throws "not implemented")
   - `resetProviderKeyStorage` (function — empty no-op)

## Verification Output

### File Existence
```
PASS: secure-store.ts exists
PASS: provider-key-storage.ts exists
```

### Typecheck
```
> @vybestack/llxprt-code-storage@0.10.0 typecheck
> tsc --noEmit
Exit Code: 0 (PASS)
```

### API Surface — secure-store.ts
```
19:export type SecureStoreErrorCode = ...
27:export class SecureStoreError extends Error { ... }
45:export interface KeyringAdapter { ... }
60:export interface SecureStoreOptions { ... }
68:export class SecureStore { ... }
76:export async function createDefaultKeyringAdapter(): Promise<KeyringAdapter | null> { ... }
```

### API Surface — provider-key-storage.ts
```
22:export const KEY_NAME_REGEX = /^$/;
26:export function validateKeyName(_name: string): void { ... }
32:export class ProviderKeyStorage { ... }
40:export function getProviderKeyStorage(): ProviderKeyStorage { ... }
44:export function resetProviderKeyStorage(): void { ... }
```

### Specific Checks
```
PASS: SecureStoreErrorCode found
PASS: KeyringAdapter found
PASS: SecureStoreOptions found
```

### Leaf Boundary
```
PASS: no core imports in storage secure-store
```

## Notes

- `SecureStoreError` has a real constructor (no throw) because tests may need to catch/classify thrown `SecureStoreError` instances. The class body is valid; only `SecureStore` constructor throws "not implemented".
- `provider-key-storage.ts` imports `SecureStore` as type-only (`import type`) since it's only used in a constructor parameter type.
- `resetProviderKeyStorage` is an empty no-op (not a throw) since it's a void function and tests may call it for cleanup.
- Barrel (`packages/storage/src/index.ts`) was NOT modified — plan specifies barrel update in P03c, not P03a.
