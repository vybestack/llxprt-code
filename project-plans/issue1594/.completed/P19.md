# Phase 19 Complete — Impl Profiles CRUD + apply

Phase ID: PLAN-20260617-COREAPI.P19
Worker: typescriptexpert
Prerequisite: P18a (PASS)
Requirement: REQ-009

## Scope delivered

Implemented the public profiles CRUD surface `AgentProfileControl` in
`packages/agents/src/api/control/profilesControl.ts`. The four methods that were
previously `throw new Error('NotYetImplemented')` are now genuinely implemented
against the per-agent in-memory `savedProfiles` Map (hermetic — no host durable
profile store, no new dependency):

- `create(name, detail)` — stores a faithful `ProfileDetail` (`provider`/`model`
  preserved, `modelParams` deep-copied, undefined optional keys omitted,
  `isDefault:false`).
- `delete(name)` — idempotent map removal; clears `defaultName` if it matches.
- `setDefault(name)` — validates resolvability (saved OR dir-scan) then tracks
  `defaultName`; throws `Profile '<name>' not found` on unknown.
- `getDefault()` — resolves the tracked default name via `get()`, returns
  `undefined` when unset/unresolvable.
- `apply(name)` — extended to resolve `savedProfiles` FIRST, then fall back to the
  working-dir public-shape dir-scan; both branches feed ONE downstream
  context-preserving switch path (applySwitch → applyParams → setKeyName →
  setLoadBalancer). No chat reset → history preserved across apply.
- `withSurfacedDefault(detail)` helper — recomputes `isDefault` from `defaultName`
  on a spread copy (stored object never mutated; `modelParams` preserved).

## Tests turned GREEN

- T18d (profiles CRUD + apply with context preservation) in
  `auth-profiles.spec.ts` → auth-profiles now 8/8.

## Keep-green confirmed (no regression)

- switch-context 11/11 (T4b standard + load-balancer apply intact).
- Full api-spec regression: 31 failed / 89 passed — only delta vs prior baseline
  is T18d flipping fail→pass. All 31 remaining failures belong to later phases
  (disposal/T13, mcp-discovery, ide, hooks, core-history, scheduler-factory,
  core-conversation T10, app-service-boundary).

## Verification (coordinator-independent)

- `auth-profiles.spec.ts`: 8 passed (8).
- `switch-context.spec.ts`: 11 passed (11).
- Full `src/api/__tests__`: 31 failed | 89 passed (120) — only T18d flipped green.
- `npm run typecheck --workspace @vybestack/llxprt-code-agents`: 0 errors.
- ESLint clean; Prettier clean on `profilesControl.ts`.
- Deferred-impl scan on `profilesControl.ts`: empty (NotYetImplemented gone).
- 24 P19/REQ-009 marker lines present in the file.

## Markers

- @plan:PLAN-20260617-COREAPI.P19
- @requirement:REQ-009
