# Phase 20 Complete — Impl history/session/compression

Phase ID: PLAN-20260617-COREAPI.P20
Worker: typescriptexpert
Prerequisite: P19a (PASS)
Requirements: REQ-010 (history/session), REQ-011 (compression)

## Scope delivered

Implemented 8 previously-`NotYetImplemented` methods on `AgentImpl`
(`packages/agents/src/api/agentImpl.ts`), plus 3 private projection helpers:

- `addHistory(message)` → faithful delegation to `client.addHistory`.
- `restoreHistory(items)` → delegation to `client.restoreHistory` (spread-cast to mutable IContent[] mirroring existing setHistory pattern).
- `resetChat()` → delegation to `client.resetChat`.
- `updateSystemInstruction()` → delegation to `client.updateSystemInstruction`.
- `addDirectoryContext()` → delegation to `client.addDirectoryContext`.
- `compress(opts?)` → `client.getChat().performCompression(promptId)`; maps
  `PerformCompressionResult` enum → public `CompressionResult.status`
  (COMPRESSED→'compressed', SKIPPED_*→'skipped', FAILED→'failed'); captures
  before/after token counts from the HistoryService and guarantees monotonicity
  (`Math.max`) only on the 'compressed' path.
- `getStats()` → `projectStats()` snapshot.
- `onStats(cb)` → subscribes `uiTelemetryService.on('update', …)`, returns an
  unsubscribe calling `.off`; delivers one immediate projection at subscription
  so callers are guaranteed ≥1 stats frame.
- Helpers: `projectStats()` (maps uiTelemetryService metrics → SessionStats, all
  numeric, `?? 0`-defaulted), `readCompressionTokenCount()`, `readTurnCount()`.

### Sourcing decisions (empirically grounded)
- Stats source = the in-process `uiTelemetryService` singleton (legal core
  subpath `@vybestack/llxprt-code-core/telemetry/uiTelemetry.js`). Empirically
  the fake-seam stream emits ZERO 'usage' events but uiTelemetryService fires
  exactly one 'update' per stream turn.
- `PerformCompressionResult` imported as a value (enum) from
  `@vybestack/llxprt-code-core/core/turn.js`.
- No new package dependency added; no host durable store touched.

## Tests turned GREEN (core-history.spec.ts)
T7 (resetChat), T8 (compress), T8b (onStats/getStats), T14b (addHistory/
updateSystemInstruction/addDirectoryContext). Stable 7/7 across 5 consecutive runs (T8b non-flaky).

## Independent verification (coordinator, from filesystem ground truth)
- core-history.spec.ts: 7 passed ×5 runs (no flake).
- Full api-spec suite: 27 failed | 93 passed (120) — delta from post-P19 baseline
  (31 failed | 89 passed) is exactly +4 green = {T7,T8,T8b,T14b}; zero regressions.
- Keep-green authoritative set intact: switch-context 11/11, core-tools 8/8,
  event-characterization 38/38, provider-bootstrap 4/4, boundary 3/3,
  core-conversation T1/T9/T14 (T10 stays RED → P21).
- Remaining 27 failures all map to future phases: app-service-boundary 1 (P21/P27),
  mcp-discovery 6 (P22/P25), ide 5 (P22), scheduler-factory 1 (P23), disposal 9
  (P24), hooks 4 (P23), core-conversation T10 1 (P21).
- typecheck 0 errors; eslint 0/0 and prettier clean on agentImpl.ts.
- Deferred-impl scan CLEAN; remaining `NotYetImplemented` only on out-of-scope
  methods (generate/generateJson/generateEmbedding → P21; SafeHookControl → P23).
- 11 `@plan:PLAN-20260617-COREAPI.P20` markers present.

## Markers
@plan:PLAN-20260617-COREAPI.P20
@requirement:REQ-010
@requirement:REQ-011
