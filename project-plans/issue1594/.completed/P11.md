Phase: P11
Completed: 2026-06-17 19:21
Files Created:
- packages/agents/src/api/__tests__/fixtures/thinking-text.jsonl
- packages/agents/src/api/__tests__/fixtures/tool-call-then-answer.jsonl
- packages/agents/src/api/__tests__/fixtures/multi-tool-then-answer.jsonl
- packages/agents/src/api/__tests__/fixtures/plain-text.jsonl
- packages/agents/src/api/__tests__/helpers/agentHarness.ts
- packages/agents/src/api/__tests__/core-conversation.spec.ts
- packages/agents/src/api/__tests__/core-tools.spec.ts
- packages/agents/src/api/__tests__/core-history.spec.ts
Files Modified: none

Verification:
1. typecheck (`npm run typecheck --workspace @vybestack/llxprt-code-agents`):
   PASS — 0 errors. Output: `> tsc --noEmit` with no diagnostics.
2. lint (`npm run lint --workspace @vybestack/llxprt-code-agents`):
   PASS — 0 errors workspace-wide (138 warnings, all pre-existing in files
   outside P11). `npx eslint` on the 4 P11 files reports 0 errors and 0
   warnings. The P11 files introduced zero new errors and zero new warnings.
3. prettier (`npx prettier --check` on the 4 P11 files):
   PASS — "All matched files use Prettier code style!" (ran `--write` once to
   normalize, then re-checked clean).
4. P11 tests (`npm test --workspace @vybestack/llxprt-code-agents -- --run
   --testNamePattern "@plan:.*P11"`):
   20 tests RUN and ALL FAIL NATURALLY. 3 test files collected, 0 collection
   errors. Every failure is `Error: NotYetImplemented` thrown by the stub
   AgentImpl / ToolControl methods (e.g. `AgentImpl.stream`,
   `AgentImpl.chat`, `AgentImpl.getHistory`, `AgentImpl.compress`,
   `ToolControl.onConfirmationRequest`, `ToolControl.setEditorCallbacks`,
   `AgentImpl.onStats`, `AgentImpl.generate`). The two property tests fail
   with "Property failed after 1 tests — Caused by: Error: NotYetImplemented"
   (natural RED from the async property body hitting the stub). Summary:
   `Test Files 3 failed | 94 skipped (97)` / `Tests 20 failed | 1650
   skipped (1670)`.
5. boundary.spec.ts (`npx vitest run
   packages/agents/src/api/__tests__/boundary.spec.ts`):
   PASS — 3/3 tests pass ("consumer-facing specs import only public entry",
   "helper scaffolding excluded", "no deep core/providers/tools/auth path").
   The P11 core-*.spec.ts import ONLY: vitest, fast-check,
   @vybestack/llxprt-code-agents (public root), and ./helpers/agentHarness.js;
   agentHarness.ts is NOT a .spec.ts so it is excluded from the consumer scan.
   (Note: app-service-boundary.spec.ts T23 fails with "Cannot find module
   .../src/app-service.js" — this is a PRE-EXISTING failure owned by a later
   phase, NOT caused by P11; the entire src/api/ dir is untracked branch work
   and app-service.js does not yet exist. boundary.spec.ts itself is 3/3
   green.)
6. Reverse-test guard (`grep -rn "toHaveBeenCalled\|not\.toThrow\|
   toThrow('NotYetImplemented')" packages/agents/src/api/__tests__/core-*.spec.ts`):
   OK — empty (printed "OK", no forbidden patterns).
7. Marker / property / T-row greps:
   - `grep -rc "@plan:PLAN-20260617-COREAPI.P11"`: core-conversation.spec.ts=7,
     core-history.spec.ts=9, core-tools.spec.ts=10, helpers/agentHarness.ts=1
     (all non-zero).
   - Property tests present: core-history.spec.ts uses
     `fc.assert(fc.asyncProperty(fc.array(fc.record(...))))` (T6p history
     round-trip); core-tools.spec.ts uses
     `fc.assert(fc.asyncProperty(fc.dictionary(fc.string(...), fc.jsonValue())))`
     (T2p tool-arg projection stability). 2 genuine property tests contributed
     toward the global ≥30% gate (enforced in P29).
   - All 17 T-row tokens present (T1 T2 T2b T3 T3b T3c T6 T7 T8 T8b T9 T10 T11
     T14 T14b T21 T22): the token-loop printed no MISSING lines.
8. T-row token loop: no MISSING output (all 17 rows greppable in core-*.spec.ts).

Decisions on FakeProvider capability (honesty notes):
- T8 (compression): asserted ONLY the EXPLICIT compress() contract (status in
  compressed/skipped/failed + numeric token fields when compressed). The
  AUTO-compression event path was NOT asserted because FakeProvider replays
  fixed JSONL turns and cannot reliably exceed the context window at runtime
  to trigger the auto path. This matches the phase contract's allowance.
- T3c (editor callback): FakeProvider cannot drive a real ModifyWithEditor
  edit. The test asserts the PUBLIC contract behaviorally — registers editor
  callbacks via agent.tools.setEditorCallbacks, asserts the registered getter
  returns its value, and asserts a confirmation surfaces with a structured
  (non-null) details payload. No mock-theater; assertions are on VALUES.
- Tool scenarios (T2/T2b/T3/T3b/T11/T21): fixtures encode IContent tool_call
  blocks (the provider-agnostic model-request representation); the public
  Agent must surface them as tool-call/tool-confirmation/tool-result events
  via the real scheduler continuation. Confirmation is driven through the
  public agent.tools.onConfirmationRequest + respondToConfirmation API (and
  the config onApproval seam for T11 headless).

Verdict: PASS
