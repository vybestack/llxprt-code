# Phase 18a — Auth/keys verification: PASS

Phase ID: `PLAN-20260617-COREAPI.P18a`
Reviewer: architect (clean first-time review) | Coordinator-verified independently
Prerequisite: P18 implementation present

## Verdict: PASS

REQ-008 (auth/keys) correctly and completely delivered. All seven behavioral contracts that should pass (T18 ×3, T18b, T18c ×3) pass by genuine LOGIC, not incidentally.

## Evidence per correctness criterion
1. **Precedence/winner model** — `computeAuthWinner` (authState.ts) orders `raw > keyName > inline > keyfile > oauth > none` exactly per REQ-008. `getProviderStatus` surfaces `keyName` only when winner==='keyName', `keyFile` only when winner==='keyfile'. All three T18 cases satisfied by logic.
2. **Secret hygiene** — secrets live only in `authState.keyStore` (Map). `AgentProviderState` has no secret field; `getProviderStatus` builds from provider/model/winner/keyName(reference)/keyFile(path)/baseUrl only. `use()` passes secret to runtime mutator, never to state. T18b `JSON.stringify` exclusion holds by construction.
3. **Hermeticity** — only in-memory Map used; the single host-storage grep hit is a doc-comment (authState.ts:19). No `llxprt-code-storage` dependency in package.json.
4. **OAuth seam** — `login()` requires `onOAuthPrompt`; absent ⇒ clear throw; present+truthy ⇒ authenticated + seeded bucket; falsy ⇒ 'declined'. `status()`/`listBuckets()` synchronous from in-memory state. No blocking OAuthManager flow invoked.
5. **Cross-control coupling (minimal)** — `mcpLogin` adds to `mcpAuth`; `mcp.auth()` reads it. `saveCurrent` stores `authKeyName` reference in-memory. `create/delete/setDefault/getDefault` remain `NotYetImplemented`. T18d still fails by design.
6. **Public-type fidelity** — implements existing `agent.ts` interfaces; harness + fixtures untouched.
7. **Standards** — strict typecheck passes, eslint clean, no suppressions, no deferred-impl language, markers present in control files.

## Command evidence
- Targeted: `Tests 1 failed | 7 passed (8)` — T18×3, T18b, T18c×3 PASS; T18d `NotYetImplemented` (by design).
- Regression: `Tests 32 failed | 88 passed (120)` — keep-green intact (switch-context 11/11, core-tools 8/8, event-characterization 38/38, provider-bootstrap 4/4, boundary 3/3, core-conversation T1/T9). All failures belong to later phases.
- Typecheck: `tsc --noEmit` exit 0.
- Deferred-impl scan: no matches.
- ESLint: clean. No forbidden host-storage import/call.

## Minor (non-blocking) observations
- `mcpControl.ts` carries `@requirement:REQ-013` (MCP domain) rather than REQ-008 — defensible; `mcp.auth` is genuinely MCP-domain, the cross-control read is incidental.
- `createAgent.ts finalizeAgent` threads `initialAuth` without a P18 marker — trivial one-line plumbing in pre-existing P15/P16 function, not new logic.
- `profilesControl.ts:233` double-cast is in P16-scoped `apply()`, not P18 code; P18-added `saveCurrent`/`get`/`list` are cast-free.

None rise to a behavioral defect or REQ-008/test-contract violation.

## Markers
@plan:PLAN-20260617-COREAPI.P18a
@requirement:REQ-008
