Phase: P01a (Domain Analysis Verification)
Phase ID: PLAN-20260617-COREAPI.P01a
Role: typescriptreviewer (standing in)
Completed: 2026-06-17
Artifact under review: project-plans/issue1594/analysis/domain-model.md
Files Created: project-plans/issue1594/.completed/P01a.md (this marker)
Files Modified: none

================================================================================
VERIFICATION COMMAND OUTPUTS
================================================================================

--- Phase-file "Verification Commands" bash block (run from repo root) ---

$ missing=0
$ grep -q "@plan:PLAN-20260617-COREAPI.P01" project-plans/issue1594/analysis/domain-model.md || { echo "MISSING plan marker"; missing=1; }
$ for n in $(seq -w 1 21); do grep -q "REQ-0$n" project-plans/issue1594/analysis/domain-model.md || { echo "MISSING REQ-0$n"; missing=1; }; done
$ grep -nE "function .*\{|=> \{.*return" project-plans/issue1594/analysis/domain-model.md && { echo "FAIL: impl code present"; missing=1; }
$ echo "EXIT=$missing"
EXIT=0

  Result: PASS. Plan marker present. All REQ-001..REQ-021 present. The impl-code
  regex matched NOTHING (no `function ... {` and no `=> { ... return` arrow-body
  with a return), so no FAIL line was printed. Final exit status 0.

--- Additional rigorous checks (reviewer-added, beyond the phase minimum) ---

$ # T-row coverage T1..T25 (main rows)
  T1..T25: ALL present (25/25). No MISSING line emitted.

$ # T-row sub-variants
  T2b,T3b,T3c,T4b,T4c,T4d,T4e,T4f,T6b,T8b,T12b,T14b,T15b,T15c,T18b,T18c,T18d,T18e:
  ALL 18 present. No MISSING line emitted.

$ # P00a-correction keyword occurrences in domain model
  JSONL: 2
  FakeProvider: 5
  uiTelemetry: 4
  telemetry/uiTelemetry.js: 1
  createIsolatedRuntimeContext: 4
  createHeadlessProviderManager: 4   (each used as a "DO NOT use bare ..." warning)
  initializeContentGeneratorConfig: 5
  internals.js: 4
  handle.activate: 4
  createAgentRuntimeState: 3
  RUNTIME_ID_MISSING: 1

$ # Broader impl-code scan (return/await/const-let-var/async function/new/if/for/switch/.then)
  - "return " : only line 223 — inside an ASCII transition arrow
    `[ready] --compress()--> reduce history --> return CompressionResult{...} --> [ready]`
    (diagram label, not a TS function body).
  - "await ..." : lines 107,108,109,361 — all inside ASCII state-transition diagrams
    (`await handle.activate()`, `await config.initialize(...)`, `await config.refreshAuth(...)`)
    used as state-node labels, plus a prose sentence at 361. Not executable bodies.
  - const/let/var assignments: NONE.
  - "async function": NONE.
  - "new X(" / "if (" / "for (" / "switch (" / ".then(": the only matches are
    diagram arrows (`new AgenticLoop(config.getAgentClient())`, `new Config(params,...)`)
    and a contract signature reference `new FakeProvider(filePath, cwd?)`. No
    control-flow constructs (`if (`, `for (`, `switch (`, `.then(`) appear at all.

$ # Code-fence audit
  grep -c '```' => 18 fences (9 balanced pairs). grep -nE '^```' shows EVERY fence is
  UNLABELED (no ```typescript / ```ts language tag). All fenced blocks are ASCII
  relationship/state-transition/ownership diagrams or tables — design sketches, not
  compilable TypeScript.

$ # Ownership / REQ-002 escape-hatch consistency
  Lines 85, 250, 368, 446 confirm: caller-supplied callbacks/factory NOT disposed;
  Agent-created scheduler instances ARE disposed (matches dispose.md table + T19).
  Lines 32-33, 411 confirm REQ-002 typed-field classification + UNSTABLE `settings`
  escape hatch (matches config-adapter.md lines 60-87).

================================================================================
MANDATORY SEMANTIC VERIFICATION CHECKLIST (all 4 questions)
================================================================================

--------------------------------------------------------------------------------
Q1. Does the model cover every REQ (REQ-001..REQ-021) and every T-row (T1..T25)?
--------------------------------------------------------------------------------
ANSWER: YES — full coverage, no gaps.

REQ coverage: §7 "Requirement Coverage (REQ-001 through REQ-021)" maps EVERY one of
the 21 formal requirements to its entities, transition(s), invariant(s), and harness
rows. The automated grep confirmed all 21 REQ ids are present (EXIT=0). Spot-checks
against specification.md:
  - REQ-001 → §3.1 + §7 (shared runtime context, B4/B5/B6 corrections folded in).
  - REQ-002 → §1.2/§1.3 + §7 (typed | sub-surface | settings classification; sandbox
    as agent-instance config, T18e).
  - REQ-003 → §1.4/§3.3 + §7 (19 public variants projecting 21 GeminiEventType members,
    exactly-one-done, synthesized done). The "19 vs 21" reconciliation is explicitly
    documented (MaxSessionTurns folds to done; ContextWindowWillOverflow terminal folds
    to context-warning+done) and matches the spec's 21-variant table.
  - REQ-004/005 → §3.2 + §7 (client rebind, same HistoryService).
  - REQ-006/007 → §3.4/§3.5 + §7 (correlationId, dual consumer paths, one-active-run).
  - REQ-008..REQ-021 → all present in §7 and §10 sub-surface table, including
    REQ-020 (docs — correctly flagged "no harness row; documentation deliverable") and
    REQ-014 IDE (T15).

T-row coverage: §9 "Harness Row Cross-Reference (T1 through T25)" is a full table
mapping every T-row to REQ(s), the documenting section, and the harness layer.
Automated checks confirm all 25 main rows AND all 18 sub-variants (T2b..T18e) are
present and each is cited next to the behavior it exercises (e.g. T4d/T4e/T4f under
§3.2; T16 under §3.3; T13/T19 under §3.7). Layer numbers (1–5) in §9 match overview.md
§"Harness layering". No T-row is orphaned.

GAPS: none.

--------------------------------------------------------------------------------
Q2. Are invariants testable and consistent with P00a corrections?
--------------------------------------------------------------------------------
ANSWER: YES.

Testability: §4 defines 12 named invariants (R-CTX, R-CLIENT, R-DONE, R-TERMINAL,
R-CORR, R-PROJECT, R-NOHANDLER, R-AUTHPREC, R-OWN, R-BOUNDARY, R-NODEEP, R-SIDE) and
EACH lists the explicit harness rows that test it ("Testable via: T..."). None are
vague assertions; each maps to an executable harness row, satisfying the
"invariants must be testable" success criterion.

Consistency with the four named P00a corrections:
  (a) File-based FakeProvider JSONL fixtures — §8.1 and §5.4 state every fixture
      supplies JSONL files (`new FakeProvider(filePath, cwd?)`, one FakeResponseTurn
      per line); variants unreachable by provider scripting are driven by direct
      emission/injection at the real emission site. Matches P00a (FakeProvider.ts
      file-based constructor) exactly. CORRECT.
  (b) Telemetry stats source via the core re-export subpath
      `@vybestack/llxprt-code-core/telemetry/uiTelemetry.js` — §1.7 and §8.2 pin the
      canonical token/usage source to uiTelemetryService via that LEGAL core subpath
      and explicitly forbid a direct `@vybestack/llxprt-code-telemetry` import (not in
      agents/package.json). Matches P00a + specification N1. CORRECT.
  (c) The agents `./internals.js` subpath must be CREATED — §8.3 and REQ-018 (§7)
      state `./internals.js` does NOT exist yet in agents/package.json and MUST be
      created (SET1, export phase P07), keeping current top-level exports until #1595
      trims. Matches P00a. CORRECT.
  (d) Rebuild-hook pinned to initializeContentGeneratorConfig — §8.5 and §3.2 pin the
      post-switch rebuild to `config.initializeContentGeneratorConfig()` (config.ts:329):
      extractExistingState → transferHistoryToNewClient (SAME HistoryService by
      reference) → new client → dispose previous; switchActiveProvider/applyProfileSnapshot
      rebuild internally, model-only setActiveModel does NOT (facade calls it
      explicitly). Matches P00a + switch-rebind.md. CORRECT.

Bonus P00a items also folded in: §8.4 shared runtime context via
createIsolatedRuntimeContext + handle.activate() (NOT bare createHeadlessProviderManager,
B6); §8.6 no-handler safe-denial (B7); §8.7 Stryker absent (added in P08); §8.8
providers subpaths already exist. All consistent with P00a.md and the spec.

--------------------------------------------------------------------------------
Q3. Is there ANY implementation code? (if yes → FAIL)
--------------------------------------------------------------------------------
ANSWER: NO implementation code. Only type sketches/contracts and ASCII diagrams.

Evidence:
  - The phase-file impl regex (`function .*\{` and `=> \{.*return`) matched nothing.
  - Broader manual scan found NO const/let/var assignments, NO `async function`, NO
    control-flow (`if (`, `for (`, `switch (`, `.then(`), NO function bodies.
  - All 18 code fences are UNLABELED (no ```typescript / ```ts) and contain ASCII
    relationship graphs, state-transition diagrams, ownership graphs, and tables.
  - The few "TS-looking" tokens (`new Config(...)`, `new AgenticLoop(...)`,
    `await handle.activate()`, `return CompressionResult{...}`) are all diagram NODE
    LABELS inside transition arrows or a single contract-signature reference
    (`new FakeProvider(filePath, cwd?)`), which is an allowed type/contract sketch —
    not executable logic.
The document is analysis-only, honoring the "NO implementation code" scope on line 4.

--------------------------------------------------------------------------------
Q4. Are the 6 state transitions internally consistent with the 6 pseudocode files?
--------------------------------------------------------------------------------
ANSWER: YES — all consistent. (The domain model defines 7 transitions §3.1–§3.7;
the 6 pseudocode files map cleanly onto them, with §3.4/§3.6 being facade-rule/control
methods not among the 6 pinned pseudocode components.)

1. §3.1 Bootstrap ↔ createAgent.md — CONSISTENT. Identical ordering:
   Config-construct → createIsolatedRuntimeContext (adopt OUR Config) → await
   handle.activate() → config.initialize({messageBus}) → refreshAuth →
   createAgentRuntimeState (runtimeId REQUIRED, must match context id) → bind post-auth
   config.getAgentClient() → initial rebuildLoop. Both forbid bare
   createHeadlessProviderManager (B6) and both require providerManager set before
   refreshAuth (createContentGenerator throws otherwise). Both fire SessionStart.

2. §3.2 Client rebinding ↔ switch-rebind.md — CONSISTENT. Both: reuse the SAME
   HistoryService by reference (existingHistoryService === newHistoryService);
   route through config.initializeContentGeneratorConfig() (switchActiveProvider /
   applyProfileSnapshot rebuild internally, model-only setActiveModel needs explicit
   rebuild); reconstruct the AgenticLoop via rebuildLoop() because AgenticLoop caches
   its readonly agentClient (B1); normalize via stripThoughts; treat manual switch ≡
   LB failover. Model-param mutators are lazy (no rebuild) in both.

3. §3.3 Stream lifecycle ↔ event-adapter.md — CONSISTENT. Both enforce exactly one
   done, synthesized at the AgenticLoop.run boundary (never an inner stream end);
   idle-timeout terminal; AgentExecutionStopped terminal (done{hook-stopped});
   AgentExecutionBlocked NON-terminal (hook-blocked, turn continues); MaxSessionTurns /
   ContextWindowWillOverflow(terminal) / LoopDetected / Error(BeforeAgent) synthesize
   done. The §3.3 decision table matches the event-adapter 21-variant terminal table
   row-for-row.

4. §3.5 Confirmation flow ↔ tool-confirmation-merge.md — CONSISTENT. Both key
   responses on correlationId/confirmationId (never tool name); expose BOTH
   confirmationId and toolCallId; dedup to one response per logical confirmation;
   editor-modify yields a NEW correlationId and retires the old; no-handler/handler-
   rejection is delegated to AgenticLoop safe-denial (B7) and the public path never
   throws (raw coordinator throw is scoped to ./internals.js only).

5. §3.7 Dispose ↔ dispose.md — CONSISTENT. Both tear down exactly the ownership-table
   rows (config-owned client, MCP transports, scheduler/coordinator, bus subscriptions,
   LSP, extensions, session locks, OAuth); idempotent (disposed flag); all awaited;
   SessionEnd fires; caller-supplied callbacks/factory NOT disposed while Agent-created
   scheduler instances ARE disposed (T19). The §6/§7/§10 references align with the
   dispose.md teardown table.

6. §3.4 Tool-loop sequencing and §3.6 Compression — CONSISTENT (no contradiction).
   §3.4 states only facade rules (one active run, defer completed tools, single
   continuation) and explicitly delegates scheduling/continuation to AgenticLoop —
   matching createAgent.md/switch-rebind.md/tool-confirmation-merge.md "DO NOT
   re-implement scheduling/continuation". §3.6 Compression is a historyControl method
   with no dedicated pseudocode among the six pinned files; nothing in §3.6 contradicts
   any pseudocode file (explicit compress() returns CompressionResult; automatic emits
   a `compression` stream event — consistent with the event-adapter ChatCompressed row).

No internal inconsistencies found between the seven transitions and the six pseudocode
files; the config-adapter.md (REQ-002) is reflected in §1.2/§1.3/§7-REQ-002 and is
consistent (typed fields, sub-surface concerns, settings escape hatch, callbacks not
ConfigParameters).

================================================================================
HOLISTIC FUNCTIONALITY ASSESSMENT
================================================================================

WHAT THE DOMAIN MODEL DESCRIBES
The model describes a curated, typed, STABLE public Agent API
(`createAgent(config) → Agent` + the `Agent` control plane + the `AgentEvent` stream)
published from `@vybestack/llxprt-code-agents`, built as a FACADE/COMPOSITION over
already-shipped engine primitives — NOT new orchestration. It specifies:
  - Entities (§1): Agent aggregate root + sub-surfaces (providers/tools/mcp/auth/ide/
    session/hooks/profiles/history/generate/discovery/dispose); AgentConfig (value
    object) → ConfigParameters (translation target); AgentEvent discriminated union
    (19 public variants projecting 21 GeminiEventType members); DoneReason/AgentStopInfo;
    projected public tool shapes; normalized SessionStats.
  - Relationships & ownership graph (§2) driving the dispose table.
  - Seven state transitions (§3): bootstrap, client rebinding, stream lifecycle,
    tool-loop sequencing, confirmation flow, compression, dispose — each with explicit
    pre/post-conditions.
  - 12 named, testable invariants (§4); edge cases (§5); error scenarios (§6);
    full REQ coverage (§7); P00a corrections (§8); the T1..T25 cross-reference (§9);
    and a sub-surface responsibility summary (§10).

DOES IT SATISFY THE REQUIREMENTS? (with section citations)
YES. Every requirement REQ-001..REQ-021 in specification.md §"Formal Requirements" is
mapped in domain-model.md §7 to entities, transition(s), invariant(s), and harness
rows, and every harness row T1..T25 (plus sub-variants) is back-mapped in §9 to the
section documenting its behavior. Key correctness anchors:
  - REQ-001 (bootstrap ordering, shared settings identity, runtimeId, post-auth bind):
    domain §3.1 + §7-REQ-001, mirroring createAgent.md and folding P00a §8.4.
  - REQ-003 (typed stream, 21-variant mapping, exactly-one-done, synthesized done):
    domain §1.4 + §3.3 + §7-REQ-003, mirroring event-adapter.md; the 19-public-variant
    reconciliation to the 21 GeminiEventType members is explicit and correct.
  - REQ-004/005 (switch + context preservation, same HistoryService): domain §3.2 +
    §7, mirroring switch-rebind.md.
  - REQ-006/007 (correlationId confirmation, dual consumer paths, safe-denial,
    one-active-run, scheduler-factory ownership): domain §3.4/§3.5 + §7, mirroring
    tool-confirmation-merge.md + dispose.md (T19).
  - REQ-008..REQ-021 (auth precedence, profiles, history/session/recording,
    compression, generate, MCP gating, IDE, hooks, dispose, discovery, export strategy,
    no-deep-import, docs, runtime-vs-app boundary): all in §7 + §10.

DATA FLOW THE BOOTSTRAP / REBIND / STREAM DESCRIBE
  - Bootstrap: AgentConfig --(adapter)--> ConfigParameters --(+injected
    agentClientFactory)--> new Config --(createIsolatedRuntimeContext adopts Config
    under one shared SettingsService + runtimeId, binds context OAuthManager to the ONE
    shared MessageBus)--> handle.activate() (registers runtime so the switch pipeline
    resolves THESE instances) --> config.initialize({messageBus}) (transient pre-auth
    client; NOT bound) --> config.refreshAuth (NEW post-auth client; history
    transferred; prev disposed) --> createAgentRuntimeState(runtimeId) --> bind
    config.getAgentClient() (post-auth) --> initial rebuildLoop builds AgenticLoop
    bound to that client --> SessionStart hook --> Agent.
  - Rebind: setProvider/setModel/profiles.apply/auth.* --> (switchActiveProvider /
    setActiveModel+explicit initializeContentGeneratorConfig / applyProfileSnapshot)
    via getCliRuntimeServices() on the activated context -->
    extractExistingState → transferHistoryToNewClient (SAME HistoryService by
    reference; stripThoughts as needed) → new client, prev disposed --> rebuildLoop()
    tears down the old loop+subscriptions and constructs a fresh AgenticLoop bound to
    the new client. Chat is NOT reset; the next turn uses the new provider/model.
  - Stream: stream() subscribes to merge(AgenticLoop.run() → AgentEvent via the
    21-variant mapping, scheduler callbacks → tool-status); yields projected
    AgentEvents; the single terminal `done` is decided at the AgenticLoop.run boundary
    (synthesized when no Finished); then all subscriptions unsubscribe.

WHAT COULD GO WRONG (gaps / risks to watch in later phases — none blocking)
These are downstream IMPLEMENTATION risks the model already flags, not analysis gaps:
  1. The B2 single-MessageBus seam requires a small PRODUCTION change to
     createIsolatedRuntimeContext (add optional `messageBus?`), planned for P15. The
     domain model correctly threads ONE bus; if P15 omits the seam, OAuth prompts and
     tool/hook events would split across two buses (REQ-006/008/015). Flagged in
     createAgent.md; the domain model assumes the seam. Watch in P15/P15a.
  2. The 19→21 event-variant fold (MaxSessionTurns, ContextWindowWillOverflow terminal)
     is correct but subtle; the exactly-one-`done` invariant and terminal table must be
     exhaustively exercised by T16 — the top correctness risk per the spec.
  3. tool-status is described as a scheduler-origin update merged into the stream
     ("not a stream event"); the merge ordering (tool-call precedes its
     tool-status/tool-result) is a spec requirement that the harness (T2/T3) must pin —
     the model documents it but ordering correctness is an impl concern.
  4. §3.6 Compression has no dedicated pseudocode among the six pinned files; that is
     acceptable for analysis (it is a control method, and config-adapter/createAgent/
     event-adapter/switch-rebind/tool-confirmation-merge/dispose are the six pinned
     components), but P20 will need its own implementation rigor.
None of these are gaps in the DOMAIN MODEL itself; they are correctly-flagged
implementation-phase risks.

================================================================================
Verdict: PASS
================================================================================
The domain model (project-plans/issue1594/analysis/domain-model.md) satisfies every
Phase-01a success criterion:
  - It covers every REQ (REQ-001..REQ-021) and every harness row (T1..T25 + all
    sub-variants), with explicit §7 and §9 cross-reference tables — no gaps.
  - It contains NO implementation code: the phase-file impl regex matched nothing, and
    a broader manual scan found only ASCII diagrams, contract/type sketches, and tables
    (all 18 code fences are unlabeled, non-compilable).
  - Its 12 named invariants are each tied to concrete harness rows (testable) and are
    consistent with all four P00a corrections (file-based FakeProvider JSONL fixtures;
    telemetry stats via @vybestack/llxprt-code-core/telemetry/uiTelemetry.js; the agents
    ./internals.js subpath must be created; rebuild-hook pinned to
    config.initializeContentGeneratorConfig) — plus the B5/B6 shared-runtime-context and
    B7 safe-denial corrections.
  - Its seven state transitions (§3.1–§3.7) are internally consistent with the six
    pseudocode files (createAgent, config-adapter, event-adapter, switch-rebind,
    tool-confirmation-merge, dispose) — no contradictions found.
Phase 01 is verified complete and correct.
