# P27a — Verify App-service Subpaths + command→API map (REQ-021) — PASS

Plan: PLAN-20260617-COREAPI · Phase 27a · Requirement REQ-021
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**
Coordinator independent re-verification: **PASS** (corroborated)

## Per-axis results
1. **Boundary contract real, not weakened** — PASS. `app-service-boundary.spec.ts`
   (T23/T24) retains FULL-STRENGTH independent anchors: its own hardcoded
   `VALID_KINDS`, `REQUIRED_DURABLE_COMMANDS` (7: /mcp add, /mcp remove,
   /extensions, /skills, /memory edit, settings mutation, diagnostics),
   `APP_SERVICE_SPECIFIER` literal, T23 dynamic `await import(target)` +
   `typeof===function`, T24 completion classification, orphan-free + unique-command
   checks. The harness `command-api-map.ts` re-exporting the PRODUCTION
   `COMMAND_API_MAP` is NOT tautological — an empty/missing/wrong-target/missing-export
   production map would still FAIL. The test keeps its teeth.
2. **T17 not circumvented** — PASS. `boundary.spec.ts` itself defines and asserts the
   `helpers/` exclusion (its own header documents it; `isHelper` flag + the
   "helper scaffolding under helpers/ is excluded" test). Placing deep-import
   fixtures in `__tests__/helpers/app-service-fixtures.ts` is SANCTIONED by the
   locked guard's own design, not a smuggled loophole. Production app-service
   modules import NO `packages/cli` (only a comment reference in mcp-config.ts) and
   NO `__tests__`.
3. **Behavior-real, no mock theater** — PASS. `app-service.spec.ts` (9 round-trips)
   constructs REAL services (SettingsService, ProfileManager+temp dir, SkillManager
   discovering a seeded SKILL.md, ExtensionLoader, temp memory file) and asserts
   PERSISTED state (file contents, get-after-set, list membership before/after).
   All 11 exports wrap genuine backings: editMemory→MemoryTool.performAddMemoryEntry;
   addMcpServer/removeMcpServer→SettingsService.get/set('mcpServers');
   profiles→ProfileManager save/list/delete; mutateSettings/getDiagnostics/getAbout→
   SettingsService.updateSettings/getDiagnosticsData; manageSkills→SkillManager;
   manageExtensions→ExtensionLoader.getExtensions + disabledExtensions persistence.
4. **No deferred/unsupported** — PASS. Grep of app-service.ts + app-services/ for
   TODO/FIXME/HACK/STUB/XXX/WIP/NotYetImplemented/unsupported/deferred and prose
   forms returned nothing. No required durable command returns unsupported/deferred.
5. **Durable ops OFF Agent surface** — PASS. Grep `mcpAdd|extensionEnable|memoryWrite|
   sandboxProfileSave` over api/agent.ts found nothing; api `types.ts` does not exist;
   durable ops live ONLY on the public `./app-service.js` subpath.
6. **Standards & markers** — PASS. `@plan:…P27` + `@requirement:REQ-021` on every new
   production file; strict TS (no any/as any/!-abuse; explicit return types); package
   export `./app-service.js` → built `./dist/src/app-service.{d.ts,js}` (mirrors
   ./internals.js); built module exposes all 11 functions + COMMAND_API_MAP (37 entries).
7. **No regressions / full suite green** — PASS. Rebuilt chain (core/mcp/ide/agents all
   exit 0). Full agents api suite: **17 files / 129 tests passed, 0 failed**
   (previously-RED app-service-boundary now GREEN). typecheck 0; eslint 0 on changed files.

## Escalation gate
PASS — all 7 REQUIRED_DURABLE_COMMANDS map to a real, genuinely-persisting backing
service. None faked. No STOP/escalation required.

## Specifier reconciliation note
The LOCKED P09 harness pins the SINGULAR `@vybestack/llxprt-code-agents/app-service.js`
(resolved via vitest workspaceAliasPlugin → `packages/agents/src/app-service.ts`),
overriding the plan prose's plural `app-services.js`. Production module created at the
singular filename with a singular `./app-service.js` package export; internal helper
submodules live under `src/app-services/` and re-export through the singular barrel.

Status: **PASS** — P27 complete. Proceed to P28 (Docs/agent-api.md).
