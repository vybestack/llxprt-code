# P24a — Verify Full Dispose / Teardown (REQ-016, + REQ-015 SessionEnd) — PASS

Plan: PLAN-20260617-COREAPI · Phase 24a · Requirement REQ-016 (+REQ-015)
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**
Implementer: architect · Pseudocode-compliance gate (dispose.md)

## Outcome
Full ordered `Agent.dispose()` teardown implemented EXACTLY per
`analysis/pseudocode/dispose.md`. All 9 RED T13 `disposal.spec.ts` tests GREEN.
One remediation cycle applied (step-80 extensions teardown), re-verified PASS.

## Per-axis results
1. **Tests GREEN / no mock theater** — PASS. Rebuilt chain (core/mcp/ide/agents).
   `disposal.spec.ts` 9/9. Full api suite 119 passed / 1 failed — sole failure is
   `app-service-boundary.spec.ts` (P09-authored RED for future P27; missing
   `src/app-service.js`). All other specs GREEN (event-characterization 38, ide 5,
   scheduler-factory 1, provider-bootstrap 4, auth-profiles 8, sandbox-boundary 2,
   hooks 4, core-conversation 5, core-history 7, switch-context 11, mcp-discovery 7,
   core-tools 8). No regressions.
2. **Pseudocode compliance (ordered teardown)** — PASS. agentImpl.ts dispose()
   follows dispose.md order: idempotency guard (11-13); SessionEnd hook (20,
   REQ-015); activeRunController.abort (30); CONDITIONAL injected-factory
   scheduler/coordinator dispose (40-47); bus unsubscribe + hooks.detach → emitter
   0 listeners (50-52); runtimeHandle.cleanup (55); config.dispose (60);
   config.shutdownLspService + lspShutDown marker after await (70); extensions
   teardown (80); session-lock release loop + sessionLocksReleased marker (81-83);
   defensive oauthManager dispose (90-92); throw AggregateDisposeError if errors
   (100-102). safe()/safeSync() collect without short-circuiting (110-113).
3. **Step 80 genuine (remediated)** — PASS. dispose.md's `extensionsManager.dispose()`
   does not exist; impl wires the REAL Config-owned `ExtensionLoader` via
   `ownership.config.getExtensionLoader()` (configBaseCore.ts:588), iterating active
   extensions (`getExtensions()`) and awaiting `unloadExtension()` (extensionLoader.ts:275)
   per extension inside `safe(errors, …)` so a failing unload is COLLECTED into
   AggregateDisposeError; marker set AFTER the awaited loop. Helpers
   `collectActiveExtensions`/`unloadExtensionSafely` use runtime-guarded structural
   narrowing (`ExtensionTeardownSurface`) mirroring `disposeOAuthManager` — no any/as any.
   Vacuous in headless (zero active extensions) but a genuine awaited call-path —
   NOT a bare flag-set. (Initial submission flagged a bare flag-set here; remediated.)
4. **9 T13 observables genuine** — PASS. agentClient `_unsubscribe` function→undefined
   via config.dispose()→agentClient.dispose(); injected scheduler/coordinator real
   `disposed` boolean on the SAME retained recording handle; messageBus emitter total
   listenerCount→0; lsp/extensions/sessionLock completion markers set after the real
   awaited teardown; idempotent second dispose short-circuits on guard; AggregateDisposeError
   induced by failing injected handle, collected, thrown at END with all steps run.
5. **Scope + invariants** — PASS. Per-turn loop schedulers NOT disposed by dispose()
   (only ownership.injectedSchedulerHandles iterated; no config.disposeScheduler/
   getOrCreateScheduler in agentImpl.ts). Caller-owned factory FUNCTION not disposed.
   Single shared MessageBus (one constructed in createAgent, threaded everywhere;
   AgentImpl exposes deps.messageBus). No caller resources disposed. No locked
   spec/helper assertions modified.
6. **Standards** — PASS. @plan:P24 + @requirement:REQ-016/REQ-015 + @pseudocode
   dispose.md step refs on changed prod files. Strict TS (no any/as any/!-abuse;
   explicit return types). typecheck 0; eslint 0 on changed files; deferred-impl
   scan OK/OK; no production import from __tests__. AggregateDisposeError shape
   (name==='AggregateDisposeError', errors array) matches isAggregateDisposeError.

## Files (P24)
- `packages/agents/src/api/agentImpl.ts` — AggregateDisposeError class; agentClient/
  messageBus fields; injectedFactoryScheduler/injectedFactoryCoordinator getters;
  full ordered dispose() with safe()/safeSync(); disposeOAuthManager +
  collectActiveExtensions/unloadExtensionSafely helpers.
- `packages/agents/src/api/agentBootstrap.ts` — SessionLock interface; OwnershipRecord
  additions (subscriptions, sessionLocks, lspShutDown/extensionsDisposed/
  sessionLocksReleased markers); recordOwnership threading.
- `packages/agents/src/api/createAgent.ts` — threading.

Status: **PASS** — P24 complete. Proceed to P25 (discovery helpers).
