# Phase P12a — CLI-parity Harness Verification (independent gate)

**Phase:** P12a (verification of P12)
**Plan:** PLAN-20260617-COREAPI.P12
**Branch:** issue1594
**Verdict authority:** independent re-run; no pasted output trusted — every command below was executed.

---

## 1. Commands run + REAL outputs

### 1.1 Full P12 suite — M (passed) MUST be 0

```
npm test --workspace @vybestack/llxprt-code-agents -- --run --testNamePattern "@plan:.*P12"

 Test Files  7 failed | 97 skipped (104)
      Tests  40 failed | 1670 skipped (1710)
```

Per-file failure breakdown (sums to 40, zero passed):
- auth-profiles.spec.ts        8/8 failed
- mcp-discovery.spec.ts        7/7 failed
- switch-context.spec.ts      10/10 failed (incl. 2 property tests)
- ide.spec.ts                  5/5 failed
- hooks.spec.ts                4/4 failed
- provider-bootstrap.spec.ts   4/4 failed
- sandbox-boundary.spec.ts     2/2 failed

**M passed == 0.** No collection errors (all files collected and executed their tests; failures are runtime, not import/collection).

### 1.2 Failure-reason aggregation (all trace to real stub bodies)

```
npm test ... --testNamePattern "@plan:.*P12" 2>&1 | grep -E "NotYetImplemented|agentImpl.ts:|Caused by" | sort | uniq -c | sort -rn

  38 Error: NotYetImplemented
  38     → NotYetImplemented
   7  ❯ AgentImpl.stream src/api/agentImpl.ts:52:11
   4  ❯ AgentImpl.chat  src/api/agentImpl.ts:44:11
   2 Error: Property failed after 1 tests
   2 Caused by: Error: NotYetImplemented
   2  ❯ AgentImpl.listProviders src/api/agentImpl.ts:157:11
   2  ❯ AgentImpl.getProvider   src/api/agentImpl.ts:56:11
   1  ❯ AgentImpl.setModel / setHistory / listTools / getProviderStatus / getModelParams ...
   (plus sub-surface control stubs at lines 17/18/22/25/26/29/32/36/37/39)
```

All 40 failures originate in real `AgentImpl.*` / sub-surface control stub bodies throwing `new Error('NotYetImplemented')`, reached through the public API/helper. The 2 "Property failed after 1 tests" rows are the fast-check wrappers (T4dp via `setHistory`, T5p via `getModelParams`) whose root cause is `Caused by: Error: NotYetImplemented`. No assertion-level false failure; no `toThrow('NotYetImplemented')` matcher anywhere.

### 1.3 Both typechecks — 0 errors

```
npm run typecheck --workspace @vybestack/llxprt-code-agents     → tsc --noEmit, exit 0
npm run typecheck --workspace @vybestack/llxprt-code-providers  → tsc --noEmit, exit 0
```

Trap A confirmed: the providers messageBus options are typed as an intersection variable, so providers typecheck stays GREEN while the test fails at RUNTIME.

### 1.4 Boundary scanner (T17) — 3 passed

```
npx vitest run --root packages/agents src/api/__tests__/boundary.spec.ts
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

### 1.5 Cast grep (real type-casts) — EMPTY

```
grep -rn " as " <7 specs> | grep -vE "as const|// |^\s*\*|@plan|import "
(no output)
```
Only `as const` use is the allowed `fc.constant('user' as const)` at switch-context.spec.ts:338.

### 1.6 Forbidden-token grep — EMPTY

```
grep -rnE "toHaveBeenCalled|not\.toThrow|NotYetImplemented|as unknown as" <7 specs>
(no output)
```
`NotYetImplemented` literal absent from ALL `*.spec.ts` (checked across the whole dir).

### 1.7 Reverse/tautology grep — EMPTY

```
grep -rnE "not\.toBe\('function'\)|expect\(true\)\.toBe\(true\)" packages/agents/src/api/__tests__/*.spec.ts
(no output)
```

### 1.8 All 19 T-rows present

```
for t in T4 T4b T4c T4d T4e T4f T5 T12 T12b T15 T15b T15c T18 T18b T18c T18d T18e T20 T25; do ...; done
OK for every row (19/19). No MISSING.
```

### 1.9 Marker discipline

Every `it()` carries `@plan:PLAN-20260617-COREAPI.P12` AND `@requirement:` (both "without-marker" greps returned empty). Each spec file header carries `@plan` + `@requirement`.

### 1.10 Import boundary (specs)

```
imports across 7 specs:
  './helpers/agentHarness.js', './helpers/fakeHook.js', './helpers/fakeIde.js',
  './helpers/fakeMcpServer.js', '@vybestack/llxprt-code-agents', 'fast-check', 'vitest'
```
Only the public root + `./helpers/*.js` + `vitest`/`fast-check`. No deep imports.

### 1.11 Property-based tests genuine

```
grep -rnE "fc\.(assert|asyncProperty|property)" <7 specs>
switch-context.spec.ts:334 fc.assert( + :335 fc.asyncProperty(   (T4dp)
switch-context.spec.ts:385 fc.assert( + :386 fc.asyncProperty(   (T5p)
```
Real `fc.assert(fc.asyncProperty(...))`; both fail naturally with `Caused by: NotYetImplemented`.

### 1.12 Providers messageBus RED contract

```
npx vitest run --root packages/providers src/runtime/runtimeContextFactory.messageBus.test.ts
 ✓ DEFAULT — no messageBus option → private bus built + activates; handle matches services
 × PROVIDED BUS — caller-provided MessageBus used (identity)   @145 expect(runtimeBus).toBe(providedBus)
   AssertionError: expected MessageBus{…} to be MessageBus{…} // Object.is equality
 Test Files  1 failed (1)
      Tests  1 failed | 1 passed (2)
```
DEFAULT private-bus behavior preserved (passes); PROVIDED-BUS identity fails NATURALLY because the `messageBus?` seam does not exist until P15. Compiles under providers typecheck (1.3). This is a `.test.ts`, not subject to the agents no-cast-in-spec rule.

### 1.13 Fixtures present

`provider-switch-two-turn.jsonl`, `profile-standard.json`, `profile-load-balancer.json`, `plain-text.jsonl`, `thinking-text.jsonl`, `tool-call-then-answer.jsonl` — all present (+ pre-existing basic-text/multi-tool fixtures).

---

## 2. Semantic checklist results (one line each)

- **T4d/T4e HistoryService IDENTITY** — PASS. Both use `expect(newHistoryService).toBe(existingHistoryService)` (Object.is identity, not equal contents); the switch (`setProvider` @ T4d / `profiles.apply` @ T4e) runs BEFORE the identity compare (lines 222→226 and ~256→262).
- **T18 REQ-008 precedence chain** — PASS. Three tests assert raw>key-name, key-name(CLI)>auth-key-name(profile), auth-key(inline)>keyfile; winner read from `getProviderStatus()` — matches `raw > key-name > auth-key-name > auth-key(inline) > keyfile > env`.
- **T20 error surface** — PASS. Reads `result.error?.code === 'mcp_discovery_failed'` from the BUFFERED `agent.chat()` `AgentResult` (typed `AgentErrorCode`, no cast); stream `done:error` invariant checked separately via cast-free predicates on a fresh agent; comment documents that the stream `error` payload has no `code` field.
- **T18e sandbox** — PASS. createAgent-time config; behavioral via `agent.stream`; recreate path expressed positively (a NEW agent with a different sandbox); GREEN-reachable (`AgentConfig.sandbox?: SandboxConfig` exists in config-types.ts); no reverse/absence assertion.
- **T25 RED before P15** — PASS. provider-bootstrap.spec.ts (4 tests) asserts one-call bootstrap, distinct-instance independence, post-auth client binding, static discovery; all fail at NYI. (Test-2 distinctness uses `.not.toBe(second.agent)` — a legitimate value assertion, not a forbidden absence/reverse pattern; it fails earlier at `getProvider()` NYI regardless.)
- **providers messageBus** — PASS. DEFAULT private-bus passes; PROVIDED-BUS identity fails naturally; compiles under providers typecheck.
- **Property tests genuine** — PASS. T4dp + T5p are real `fc.assert(fc.asyncProperty(...))` failing via NotYetImplemented (contribute to the GLOBAL ≥30% gate computed later in P29 — informational here).
- **Infra fakes only** — PASS. Specs import only `@vybestack/llxprt-code-agents` + `./helpers/*` (fake MCP/IDE/hook + agentHarness which builds the REAL Agent via the `LLXPRT_FAKE_RESPONSES` seq); the component under test is never faked.
- **GREEN-reachability** — PASS. Every asserted property/method (setProvider, profiles.apply/create/get/saveCurrent/delete, auth.keys.*, auth.login/enableOAuth/listBuckets/mcpLogin/status, mcp.listServers/status/toolsByServer/auth/discoveryState, ide.current/detected/status/trust/openEditor/closeEditor, hooks.*, getProviderStatus.keyName/keyFile/authStatus, AgentResult.error.code, getHistory) is declared on the public `Agent`/sub-control interfaces in `packages/agents/src/api/agent.ts` and `config-types.ts`; agents typecheck (1.3) is GREEN, confirming all spec config fields conform.

---

## 3. Defects

None blocking. Two non-blocking notes (NOT defects against the P12 RED contract):

1. **P15 implementation note (not a RED blocker):** the providers messageBus test imports `createIsolatedRuntimeContext` via the re-export chain; the carryover note references `runtimeContextFactory.ts ~185-208`. P15 must reconcile the exact seam insertion point when adding `messageBus?: MessageBus` to `IsolatedRuntimeContextOptions`. This does not affect RED correctness (the test fails naturally today and compiles).
2. **T18c no-handler test ordering (intentional, verified safe):** the `expect(rejected).toBe(true)` line would pass in isolation, but the test continues to `agent.auth.status(...)` which throws NYI, so the test as a whole FAILS at RED (confirmed: auth-profiles 8/8 failed, M==0). GREEN-reachable. Acceptable.

---

## 4. Hard-standards roll-up

| Standard | Result |
|---|---|
| Natural RED, no reverse tests, M passed == 0 | PASS (40 failed / 0 passed; all NotYetImplemented) |
| No mock-theater (real Agent; real values/sequences) | PASS |
| No casts in specs | PASS (cast grep empty; only allowed `as const`) |
| T17 boundary scanner | PASS (3 passed) |
| Marker discipline (@plan + @requirement, file + it()) | PASS |
| GREEN-reachability (assertions hit real public surface) | PASS |
| All 19 T-rows present | PASS |
| Both typechecks 0 errors | PASS |
| providers messageBus DEFAULT pass / PROVIDED-BUS natural fail | PASS |

---

## Verdict: PASS
