# Phase 18 Complete — Impl auth/keys

Phase ID: `PLAN-20260617-COREAPI.P18`
Worker: typescriptexpert | Coordinator-verified (independent ground-truth re-verification)
Prerequisite: P17a PASS (`.completed/P17a.md` present)

## Scope delivered
Public Agent auth surface (`agent.auth.*` + `agent.auth.keys.*`) + minimal cross-control support (REQ-008).
GREEN: T18 (×3 — raw-wins / cli-key-name-wins-over-profile / inline-wins-over-keyfile), T18b (secure-store reference + profile-save round-trip), T18c (×3 — OAuth-with-handler / no-handler-rejects / mcpLogin).
T18d intentionally stays RED (profiles CRUD = REQ-009/P19; `ProfilesControl.create` still throws `NotYetImplemented`).

- `control/authState.ts` (NEW): `AgentAuthState` holder + `createAgentAuthState()` + `computeAuthWinner()`. Per-agent in-memory state: `rawKeyPresent`, `inlineKeyPresent`, `keyFile`, `baseUrl`, `oauthEnabled`/`oauthAuthenticated` sets, `keyStore: Map<string,string>` (in-memory named secrets — NEVER surfaced), `mcpAuth` set, `buckets` map. Winner ordering `raw > keyName > inline > keyfile > oauth > none` (REQ-008 precedence).
- `control/authKeysControl.ts`: `AuthKeysControlDeps` bundle; `save/use/delete/setRaw/setKeyFile/list` against the in-memory `keyStore`. `save` stores secret in-memory only; `use` sets `providerState.keyName` (the reference, the winner when no raw key) and pushes secret to runtime mutator (try/catch, no-op under fake seam); `setRaw(value|null)` toggles `rawKeyPresent`. Secret never copied onto providerState/status.
- `control/authControl.ts`: expanded `AuthControlDeps`; implemented `enableOAuth/disableOAuth/login/logout/listBuckets/switchBucket/mcpLogin/setBaseUrl/status`. `login` requires `onOAuthPrompt` handler (absent ⇒ clear reject; truthy ⇒ authenticated + seeded bucket; falsy ⇒ 'declined'). `status()`/`listBuckets()` SYNC from in-memory state. Constructs `AuthKeysControl` internally.
- `control/mcpControl.ts`: added `McpControlDeps` constructor; implemented `auth(server)` reading `authState.mcpAuth`. Other methods remain NYI (P22).
- `control/profilesControl.ts`: added in-memory `savedProfiles` Map; `saveCurrent` stores key-NAME reference (`authKeyName`) only; `get`/`list` read in-memory store first. `create/delete/setDefault/getDefault` remain NYI (P19).
- `agentImpl.ts`: added `initialAuth` to `AgentDeps`; `authState` field seeded in constructor from config.auth; winner-based `computeAuthStatus` + `getProviderStatus` surfacing (`keyName` only when keyName-winner, `keyFile` only when keyfile-winner).
- `createAgent.ts`: `finalizeAgent` widened to thread `initialAuth: parsed.auth` into `buildAgent` deps (seeds initial auth state).

## Hermeticity
Real named-key storage (`ProviderKeyStorage` / `createProviderKeyStorage` / `getProviderKeyStorage`) persists to the HOST keychain / `~/.llxprt/provider-keys`. The agents auth-profiles spec does NOT mock it. To avoid polluting the host machine, P18 uses a per-agent IN-MEMORY `keyStore` Map. No host key-storage import/call (grep hits are doc-comments only); no new `@vybestack/llxprt-code-storage` dependency added to `packages/agents/package.json`.

## Independent verification (coordinator, from filesystem ground truth)
- Targeted `auth-profiles.spec.ts`: `1 failed | 7 passed (8)` — 7 GREEN (T18×3,T18b,T18c×3); T18d fails at `profilesControl.ts:166` (`NotYetImplemented`) by design.
- Full agents api specs: `32 failed | 88 passed (120)` — all 32 failures belong to later phases (disposal/P24, mcp-discovery/P22-25, ide/P22, hooks/P23, core-history/P20, scheduler-factory/P23, core-conversation T10/P21, app-service/P27, T18d/P19). Keep-green intact: switch-context 11/11, core-tools 8/8, event-characterization 38/38, provider-bootstrap 4/4, boundary 3/3, core-conversation T1/T9.
- Full agents package: `32 failed | 1689 passed (1721)` — no broader regression.
- `npm run typecheck --workspace @vybestack/llxprt-code-agents`: 0 errors.
- ESLint (7 touched files): clean. Prettier (7 touched files): clean.
- Deferred-impl scan over touched files: empty.
- Markers `@plan:PLAN-20260617-COREAPI.P18` + `@requirement:REQ-008` present in control files.

## Markers
@plan:PLAN-20260617-COREAPI.P18
@requirement:REQ-008
