# Phase P10a — Event Characterization Harness Verification

- **Phase ID:** PLAN-20260617-COREAPI.P10a
- **Completed:** 2026-06-17T19:01:30-0300
- **Verifier:** typescriptreviewer (independent, source-first)
- **Verdict:** **PASS**

---

## Executive Summary

- P10 suite RUNS and fails NATURALLY: **38 executed, 38 failed, 1612 skipped**. Every one of the 38 rows
  failed for the SAME, RIGHT reason — the future adapter module is genuinely absent:
  `Cannot find module '../../eventAdapter.js'` → `Failed to load url ../../eventAdapter.js (resolved id: ../../eventAdapter.js). Does the file exist?`
  Confirmed `packages/agents/src/api/eventAdapter.ts` does NOT exist (created in P14). The suite reports
  executed tests (not a collection error, not a reverse test, not a broken import of an existing module).
- P09 boundary NOT regressed: `boundary.spec.ts` passes **3/3** in isolation; the only red row under the
  P09 tag is the unrelated, expected `app-service-boundary.spec.ts` T23 row (`app-service.js` absent).
- All 21 variants asserted at a real source category by VALUE; impossible-FakeProvider trap clean; spec is
  public-only; helper holds all deep imports; typecheck 0 errors; lint 0 errors (138 pre-existing warnings,
  NONE in P10 files); prettier clean.

---

## Pasted Verification Outputs (a)–(i)

### (a) P10 suite RUNS and fails naturally (adapter absent)

```
 FAIL  src/api/__tests__/event-characterization.spec.ts > ... (38 rows) ...
Error: Cannot find module '../../eventAdapter.js' imported from
  '/Users/.../packages/agents/src/api/__tests__/helpers/eventHarness.ts'
 ❯ runAdapter src/api/__tests__/helpers/eventHarness.ts:79:40
     77|   loopEvents: readonly AgenticLoopEvent[],
     78| ): Promise<AgentEvent[]> {
     79|   const mod: Record<string, unknown> = await import(ADAPTER_MODULE);
       |                                        ^
 ❯ src/api/__tests__/event-characterization.spec.ts:93:20

Caused by: Error: Failed to load url ../../eventAdapter.js (resolved id: ../../eventAdapter.js). Does the file exist?
 ❯ loadAndTransform ../../node_modules/vite/dist/node/chunks/config.js:23280:33

 Test Files  1 failed | 93 skipped (94)
      Tests  38 failed | 1612 skipped (1650)
```
Row count cross-check: `grep -c "  it(" event-characterization.spec.ts` → **38**, so all 38 rows
executed-and-failed (no silent skips). `ls packages/agents/src/api/eventAdapter.ts` → **No such file**.

### (b) P09 boundary not regressed

```
# isolated boundary.spec.ts:
 ✓ src/api/__tests__/boundary.spec.ts (3 tests) 2ms
 Test Files  1 passed (1)
      Tests  3 passed (3)

# --testNamePattern "@plan:.*P09" across the dir:
 FAIL  src/api/__tests__/app-service-boundary.spec.ts > ... durable app-service subpaths ... (T23)
Caused by: Error: Failed to load url /Users/.../packages/agents/src/app-service.js ... Does the file exist?
 Test Files  1 failed | 1 passed | 92 skipped (94)
      Tests  1 failed | 7 passed | 1642 skipped (1650)
```
Only `app-service-boundary.spec.ts` (T23, expected red until app-service.js ships) is failing; the
T17 `boundary.spec.ts` is fully green.

### (c) 21 variants present
```
variants-done
```
(no `MISSING` lines)

### (d) 6 source categories present
```
cats-done
```
(no `MISSING category` lines)

### (e) impossible-FakeProvider guard
```
trap-done
```
(no `FAIL impossible-fake` lines — no impossible raw variant is routed through fake-provider)

### (f) reverse/mock guard
```
OK guard
```
(`toThrow('NotYetImplemented')`, `not.toThrow`, `toHaveBeenCalled` all absent from spec AND harness)

### (g) spec public-only
```
OK public-only
```
(no deep `@vybestack/llxprt-code-{core,providers,tools,auth,settings,policy}/` import in the spec)

### (h) boundary.spec.ts tests #1/#3 unchanged in intent
Read in full (192 lines). Scan machinery intact: `FORBIDDEN_DEEP_PREFIXES`, `collectSpecFiles`/
`collectFromEntries` (collects ONLY `*.spec.ts`, marks `isHelper` when path contains `helpers`),
`extractSpecifiers`, `isForbiddenDeep`, `isAllowed`, `scanForViolations`, `scanDeepImports`.
- Test #1: `expect(VIOLATION_SUMMARY).toBe(''); expect(CONSUMER_VIOLATIONS).toHaveLength(0);` — teeth intact.
- Test #2 (modified): `expect(HELPERS_PRESENT).toBe(true); const helperSpecs = ALL_FILES.filter(f=>f.isHelper);
  expect(helperSpecs).toHaveLength(0); expect(CONSUMER_VIOLATIONS).toHaveLength(0);`
- Test #3: `expect(DEEP_IMPORT_HITS).toHaveLength(0);` — teeth intact.

### (i) typecheck + lint + prettier
```
# typecheck:
> tsc --noEmit          (exit 0, no diagnostics)

# lint:
✖ 138 problems (0 errors, 138 warnings)
# P10-file-scoped grep (api/__tests__|event-characterization|eventHarness):
NO P10 FILE WARNINGS

# prettier:
Checking formatting...
All matched files use Prettier code style!
```

---

## Point-by-Point Checklist Ruling (file:line evidence)

### 1. All 21 variants at a REAL source category; FakeProvider only for Content/text; values asserted — **PASS**
- **FakeProvider used only for Content/text:** `fakeProviderContentLoopEvents` (harness:579-595) instantiates a
  REAL `FakeProvider`, reads `basic-text.jsonl`, consumes `generateChatCompletion`, and maps `block.type==='text'`
  to `Content`. Spec row asserts the VALUE: `textEvents[0].text` toBe `'hello from fake provider'`
  (spec:90-96). The 13 impossible variants are NOT routed through fake-provider (guard (e) clean).
- **Source-category routing:** adapter-characterization for Thought/Citation/Usage/ModelInfo/Notice/
  Compressed/Retry/Invalid/IdleTimeout/Error/LoopDetected/Finished (spec:104-237); scheduler for
  ToolCall*/loop-native tool_update|tool_output|tools_complete and the REAL `runRealLoopExecuteTool`
  awaiting_approval row (spec:245-316); abort for UserCancelled + REAL `runRealLoopAbort` (spec:320-336);
  hook for Stopped/Blocked (spec:340-365); config/orchestrator for ContextOverflow/MaxTurns (spec:369-388).
- **Values asserted, not just `type`:**
  - thinking deep-equals ThoughtSummary: `thought.subject`/`thought.description` (spec:118-119).
  - context-warning carries BOTH token numbers: `estimatedRequestTokenCount` toBe 50000 AND
    `remainingTokenCount` toBe 1000 (spec:374-377).
  - done.reason equals mapped DoneReason: stop/aborted/max-turns/context-overflow/loop-detected/error/
    hook-stopped all asserted (spec:225,331,386,378,217,200,209,348).
  - hook-blocked.info.reason equals the FLAT reason: `blockedEvents[0].info.reason` toBe `'rate_limit'`,
    `info.systemMessage` toBe `'slow down'` (spec:360-362); harness `streamBlocked` emits FLAT fields with
    NO `.value` wrapper (harness:255-271), matching turn.ts:281-286.
  - hook-stopped FLAT stop payload: `done[0].stop?.reason`='policy_block', `systemMessage`='blocked by hook',
    `contextCleared`=true (spec:346-349); `streamStopped` FLAT (harness:235-251), matching turn.ts:274-279.
  Note: harness builders use the REAL `ServerGeminiStreamEvent` `.value` shapes (Content `value:string`,
  StreamIdleTimeout/Error `value:{error}`, Finished `value:{reason}`, ContextWindowWillOverflow
  `value:{estimatedRequestTokenCount,remainingTokenCount}`) verified against turn.ts:131-300.

### 2. Exactly-one-`done` genuinely tested (would catch a double-done) — **PASS**
`doneEvents()` (spec:78-80) filters ALL done events; assertions are `toHaveLength(1)` (e.g. spec:198,216,
331,348,377,385) and `toHaveLength(0)` for the non-terminal blocked case (spec:363). A second `done` would
make length 2 and fail — this catches double-done, not merely "a done exists."

### 3. Synthesized-`done` terminal paths (no Finished) with correct DoneReason — **PASS**
- MaxSessionTurns → done{max-turns}, no Finished (spec:382-388, 422-426).
- ContextWindowWillOverflow → done{context-overflow} (spec:428-434).
- LoopDetected → loop-detected BEFORE done{loop-detected}, ordering asserted via index compare
  (spec:436-446).
- StreamIdleTimeout → idle-timeout THEN done{error} with ordering (spec:448-459).
- Error → error THEN done{error} with ordering (spec:461-471).
- Dedicated "synthesized done cases end with done despite NO Finished input" row drives MaxTurns +
  ContextOverflow + Error and asserts each yields exactly one done (spec:486-498).

### 4. AgentExecutionBlocked NON-terminal; Stopped TERMINAL — **PASS**
- Blocked NON-terminal at hook category: yields hook-blocked, `doneEvents` length 0 (spec:357-364).
- Blocked NON-terminal continuation (decision-table): `[blocked, content('after block'), finished]` →
  hook-blocked present, a FOLLOWING Content still maps to text ('after block'), exactly ONE done at loop
  end with reason `stop`, ordering blocked < text < done (spec:473-484). This is the precise teeth for the
  "turn continues" rule.
- Stopped TERMINAL → done{hook-stopped} (spec:341-349 and invariant spec:473).

### 5. Assertions on VALUES/sequences, not mock-theater — **PASS**
No `toHaveBeenCalled`/`emitted` assertions (guard (f) clean). Assertions read payload fields (text, thought,
usage counts, token counts, reason, stop.systemMessage, call.id/name/args.query, result.id/name) and
ordering via `events.indexOf(...)` comparisons.

### 6. Tests fail for the RIGHT reason (adapter genuinely absent) — **PASS**
Single root cause across all 38 rows: dynamic `import('../../eventAdapter.js')` rejects because the module
does not exist (output (a)). Variable-specifier dynamic import (harness:71, `ADAPTER_MODULE` const at
harness:69) prevents static TS resolution, so RED is natural. Not a reverse test, not a collection error
(suite executed 38 tests), not a broken import of an existing module (every OTHER import in the harness
resolves — typecheck is 0 errors and the helper's deep imports are all valid).

### 7. boundary.spec.ts test #2 legitimate strengthening; #1/#3 + scan machinery intact — **PASS**
- The scan only ever collects `*.spec.ts` (collectFromEntries pushes when `name.endsWith('.spec.ts')`) and
  excludes helpers from the consumer set via `CONSUMER_FILES = ALL_FILES.filter(f => !f.isHelper)`. Thus
  `eventHarness.ts` (a non-spec helper) is NEVER scanned for deep imports — that is WHY the spec's deep
  imports are legal only inside the helper.
- Test #2 now proves: (i) `helpers/` actually exists (`HELPERS_PRESENT === true`); (ii) NO `*.spec.ts` was
  smuggled under `helpers/` (`helperSpecs.toHaveLength(0)`); (iii) consumer violations remain zero. This is a
  STRENGTHENING of the transitional `HELPERS_PRESENT === false` placeholder, not a weakening or vacuous
  check — it would fail if a spec were placed under helpers/ or if a consumer deep-import appeared.
- Tests #1/#3 and all machinery (`FORBIDDEN_DEEP_PREFIXES`, `collectSpecFiles`, `scanForViolations`,
  `scanDeepImports`) are byte-consistent with the P09 design (output (h)); boundary.spec.ts passes 3/3.

### 8. typecheck 0 / lint 0 errors (warnings not in P10 files) / prettier clean — **PASS**
typecheck exit 0; lint 0 errors + 138 warnings with NONE in `api/__tests__`/`event-characterization`/
`eventHarness` (output (i)); prettier "All matched files use Prettier code style!".

---

## Holistic Functionality Assessment

This is a **complete characterization of the public event contract**. All 21 `GeminiEventType` variants plus
the four loop-native `AgenticLoopEvent` kinds (tool_update/tool_output/tools_complete/awaiting_approval) are
pinned by VALUE to their `AgentEvent` projection, and every entry of the P02 terminal-vs-intermediate
decision table is asserted: intermediate variants (Content/Thought/Retry/Citation/Usage/ModelInfo/Notice/
Compression/InvalidStream/ToolCall*) emit no `done`; terminal variants synthesize exactly one `done` with the
mapped `DoneReason`; the synthesized-done-without-Finished paths (max-turns/context-overflow/loop-detected/
error/idle) are covered; AgentExecutionBlocked is proven non-terminal with stream continuation; Finished/
UserCancelled/Stopped are proven terminal. The exactly-one-`done` invariant is enforced with `toHaveLength(1)`
(catches double-done).

**Genuinely-unreachable variants and how they are injected:** The 13 variants the real FakeProvider/IContent
path cannot produce (Retry, InvalidStream, StreamIdleTimeout, Error, ContextWindowWillOverflow,
ToolCallResponse, ToolCallConfirmation, MaxSessionTurns, UserCancelled, LoopDetected, ChatCompressed,
AgentExecutionStopped, AgentExecutionBlocked) are injected NOT through fake-provider but through honest
`ServerGeminiStreamEvent` builders using the REAL `GeminiEventType` enum and REAL payload shapes
(`.value` wrappers where they exist; FLAT fields for Stopped/Blocked), wrapped as `{kind:'stream', event}`
loop events and fed to the adapter iterable (`adapter-characterization`/`hook`/`abort`/`config/orchestrator`
categories). Where a higher executable seam exists, the harness ALSO drives the REAL machinery: a REAL
`AgenticLoop` + REAL `CoreToolScheduler`/`MessageBus`/`MockTool`/`PolicyEngine` exercise the genuine
awaiting_approval continuation (`runRealLoopExecuteTool`) and the genuine AbortSignal path
(`runRealLoopAbort`). Content/text is the only variant sourced from a real FakeProvider JSONL fixture, which
is correct because it is the only variant the IContent conversion path can naturally emit.

**Minor observation (non-blocking, NOT a defect):** The `ToolCallResponse → tool-result [scheduler]` row
(spec:265-273) injects a raw `ServerGeminiStreamEvent` rather than the real tools_complete continuation; the
real continuation is nonetheless covered by the dedicated `loop-native tools_complete → tool-result`
(spec:296-304) and `runRealLoopExecuteTool` rows, and the projection value (`result.id`) is asserted. The
P02 pseudocode (line 215) explicitly maps the inner-stream `ToolCallResponse` case, so this row faithfully
characterizes a real adapter branch and does not use a forbidden FakeProvider fixture. No remediation needed.

---

## Verdict: PASS

Rationale: All 8 checklist items pass with file:line evidence. The 38-row suite executes and fails for the
single correct reason (the P14 `eventAdapter.ts` is genuinely absent); the failure is natural-RED via a
variable-specifier dynamic import, not a reverse test, broken import, or collection error. P09 boundary is
not regressed (boundary.spec.ts 3/3; only the expected app-service T23 row is red). The boundary.spec.ts
test #2 modification is a legitimate strengthening with machinery intact. Typecheck/lint/prettier are clean
for the P10 files.
