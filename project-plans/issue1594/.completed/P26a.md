# P26a — Verify Non-interactive Mode Parity (REQ-021) — PASS

Plan: PLAN-20260617-COREAPI · Phase 26a · Requirement REQ-021
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**

## Per-axis results

1. **AgentResult completeness** — PASS. Public shape `{text,toolCalls,finishReason,error?,usage?}`
   (agent.ts:84-90). `chat()` gates MCP discovery then returns
   `buildAgentResult(drainToResult(this.stream(...)))` (agentImpl.ts:545-563); gate-error
   path returns text/toolCalls/finishReason/error (551-558). Sufficient for text/json render,
   stderr/error mapping, exit status — NO CLI/internal deep imports.

2. **error + usage independence (critical fix)** — PASS. `buildAgentResult` now independently
   spreads error and usage (agentBootstrap.ts:380-391) — replacing the prior mutually-exclusive
   early-returns that DROPPED usage on an errored-but-token-consuming turn. Built-dist probe:
   BOTH→error✓usage✓; err-only→error✓usage✗; usage-only→error✗usage✓; neither→both omitted
   (keys: text,toolCalls,finishReason). Independently reproduced by coordinator AND deepthinker.

3. **finishReason derivation** — PASS. DoneReason union (event-types.ts:20-27);
   `drainToResult` takes `done.reason` (agentBootstrap.ts:342-346). eventAdapter emits
   `aborted` for UserCancelled (eventAdapter.ts:347-351) and `error` for error/idle-timeout
   terminal paths (272-286) — aborted vs error DISTINGUISHABLE for exit-code mapping.

4. **yolo/onApproval headless auto-answer** — PASS. createAgent wraps onApproval
   (createAgent.ts:258-259), threads wrapped handler + SAME messageBus into rebuildLoop
   (261-269 → rebuildLoop.ts:92-98); AgenticLoop subscribes TOOL_CONFIRMATION_REQUEST and
   responds via same bus (AgenticLoop.ts:207-230, wired 270-280; scheduler gets this.messageBus
   475-478). Genuinely reachable through the loop `chat()` drains; no second bus; T11 green.

5. **Markers & standards** — PASS. `@plan:...P26` + `@requirement:REQ-021` on chat
   (agentImpl.ts:541,543), drainToResult (agentBootstrap.ts:322,324), buildAgentResult
   (376,378) — alongside existing P15/REQ-003 (none removed). typecheck 0; eslint 0;
   deferred-impl OK; no `__tests__`/CLI imports.

6. **No mock theater / scope / weakened tests** — PASS. Production logic (real stream drain +
   generic optional-property assembly); T22 (core-history.spec.ts:200-233) intact & green;
   no unrelated phases touched.

7. **Holistic feasibility** — PASS. Headless run → AgentResult → output-format mapping feasible
   for a thin CLI wrapper (text→--output-format text, toolCalls→json, finishReason→exit code,
   error→non-zero exit+stderr, usage→stats).

## Test results
Build (core/mcp/ide-integration/agents) exit 0. Full api suite **119 passed / 1 failed** —
sole failure the expected future-P27 RED `app-service-boundary.spec.ts` (missing
`packages/agents/src/app-service.js`). typecheck 0; eslint 0 on changed files.

Status: **PASS** — P26 complete. Proceed to P27 (app-service subpaths + command→API map).
