Phase: P03 — AgentConfig Types + Zod Schema (config-schema)
Plan ID: PLAN-20260617-COREAPI.P03
Completed: 2026-06-17 13:55
Implementer role: typescriptexpert (TYPES + SCHEMA ONLY — no production logic)

================================================================================
## PREREQUISITE
================================================================================
project-plans/issue1594/.completed/P02a.md — CONFIRMED present (pseudocode
compliance gate PASS). Phase 02a verified all six pseudocode files and grounded
every cited API/event-payload against real source.

================================================================================
## GROUNDING (real ConfigParameters)
================================================================================
Grounded against: packages/core/src/config/configTypes.ts (line 347:
`export interface ConfigParameters`), the authoritative type consumed by
`new Config(...)`. Re-exported via packages/core/src/config/config.ts (lines
60-77) and reachable from agents via `@vybestack/llxprt-code-core/config/config.js`
(tsconfig `paths` mapping `@vybestack/llxprt-code-core/*` → `../core/src/*`).

Real handler/projection types grounded and imported (NOT re-invented):
- ApprovalMode (enum) — packages/core/src/config/configTypes.ts:59
- MCPServerConfig (class) — packages/core/src/config/configTypes.ts:178
- SandboxConfig (interface) — packages/core/src/config/configTypes.ts:169
- SkillDefinition — re-exported from configTypes.ts (imported from
  core/src/skills/skillManager.ts)
- PolicyEngineConfig — packages/core/src/policy/types.ts:18
- HookDefinition / HookEventName — packages/core/src/hooks/types.ts:65 / :34
- GeminiCLIExtension — packages/core/src/config/configTypes.ts:140
- ToolConfirmationOutcome (enum) — packages/tools/src/types/tool-confirmation-types.ts:21
  (reachable from `@vybestack/llxprt-code-tools` root)

NEW public projection types defined here (no existing public type):
ProviderAuth, ApprovalHandler, OAuthPromptHandler, EditorCallbacks,
AgentSchedulerFactory, AgentSchedulerHandle.

================================================================================
## FILES CREATED (exactly three + this marker)
================================================================================
1. packages/agents/src/api/config-types.ts
   - Marker: @plan:PLAN-20260617-COREAPI.P03 / @requirement:REQ-002, REQ-006, REQ-017
   - AgentConfig interface (all 55 field names present), ProviderAuth, AgentAuth,
     AgentHooks/AgentProjectHooks, AgentFileFiltering, AgentTelemetry,
     AgentCompression, AgentRecording, AgentIde, AgentShell,
     AgentToolOutputLimits, AgentExtensions.
   - Config-scoped handler/projection types: ApprovalHandler, OAuthPromptHandler,
     EditorCallbacks, AgentSchedulerFactory, AgentSchedulerFactoryOptions,
     AgentSchedulerHandle.
   - AgentConfig includes `toolSchedulerFactory?: AgentSchedulerFactory` with
     ownership doc: caller-owned factory / Agent-owned scheduler instances disposed
     by Agent.dispose().
   - `settings` escape hatch documented UNSTABLE.

2. packages/agents/src/api/config-schema.ts
   - Marker: @plan:PLAN-20260617-COREAPI.P03 / @requirement:REQ-002, REQ-017
   - Schema-first: AgentConfigSchema (.strict() — rejects unknown/shadowed keys),
     ProviderAuthSchema (.strict()), AgentAuthSchema, AgentFileFilteringSchema,
     AgentTelemetrySchema, AgentCompressionSchema, AgentRecordingSchema,
     AgentIdeSchema, AgentShellSchema, AgentToolOutputLimitsSchema,
     BlockedMcpServerEntrySchema, AgentSchedulerFactoryOptionsSchema.
   - ApprovalMode via z.nativeEnum; MCPServerConfig via z.instanceof.
   - AgentConfig type kept consistent with schema: ParsedAgentConfig = z.infer;
     AgentConfigWithCallbacks overlays the function-typed callback fields
     (onApproval/onOAuthPrompt/editorCallbacks/toolSchedulerFactory) that Zod
     cannot express. The hand-written AgentConfig interface (config-types.ts)
     is rigorously in sync with the schema shape.

3. packages/agents/src/api/config-classification.ts
   - Marker: @plan:PLAN-20260617-COREAPI.P03 / @requirement:REQ-002, REQ-017
   - Exhaustive CONFIG_FIELD_CLASSIFICATION table: 54 entries covering every
     AgentConfig field from spec §4.2 and current ConfigParameters.
   - FieldClassification union: typed | agent-sub-surface | app-service |
     settings-escape-hatch (literal string present).
   - classifyConfigField(field) + classificationCountsByCategory() helpers.
   - Every non-typed field carries a rationale.

================================================================================
## CLASSIFICATION SUMMARY (counts per category)
================================================================================
typed:                   46
agent-sub-surface:        7  (auth, extensions, recording, onApproval,
                              onOAuthPrompt, editorCallbacks, toolSchedulerFactory)
app-service:              0  (durable app-service concerns are NOT AgentConfig
                              fields — they are public app-service subpaths per
                              R-BOUNDARY; no AgentConfig field is classified
                              app-service because AgentConfig is the runtime
                              input value object, not the durable store)
settings-escape-hatch:    1  (settings — UNSTABLE)
TOTAL:                   54

Every CLI-needed field (folderTrust, embeddingModel, debugMode,
continueOnFailedApiCall, allowedTools, coreTools, toolDiscoveryCommand,
toolCallCommand, mcpServerCommand, allowedMcpServers, blockedMcpServers,
mcpEnabled, extensionsEnabled, compressionThreshold, projectHooks,
disabledHooks, ...) is classified `typed` — none left only in `settings`.

================================================================================
## VERIFICATION (full phase-spec block — exit 0)
================================================================================
```
$ set -e
$ missing=0
$ grep -rqc "@plan:PLAN-20260617-COREAPI.P03" packages/agents/src/api/ || { echo "MISSING plan marker"; missing=1; }
$ for f in provider model modelParams auth tools excludeTools mcpServers approvalMode systemPrompt workingDir sessionId includeDirectories fileFiltering telemetry proxy maxSessionTurns compression checkpointing recording policy extensions ide hooks memory streamIdleTimeoutMs toolOutputLimits outputFormat shell contextLimit compressionThreshold skills useWriteTodos sandbox folderTrust embeddingModel debugMode continueOnFailedApiCall allowedTools coreTools toolDiscoveryCommand toolCallCommand mcpServerCommand allowedMcpServers blockedMcpServers mcpEnabled extensionsEnabled projectHooks disabledHooks interactive onApproval onOAuthPrompt editorCallbacks toolSchedulerFactory settings; do
    grep -q "$f" packages/agents/src/api/config-types.ts || { echo "MISSING AgentConfig field $f"; missing=1; }
  done
$ for s in AgentSchedulerFactory AgentSchedulerHandle toolSchedulerFactory; do
    grep -q "$s" packages/agents/src/api/config-types.ts || { echo "MISSING scheduler factory contract $s"; missing=1; }
  done
$ for f in folderTrust embeddingModel debugMode continueOnFailedApiCall allowedTools coreTools toolDiscoveryCommand toolCallCommand mcpServerCommand allowedMcpServers blockedMcpServers mcpEnabled extensionsEnabled compressionThreshold projectHooks disabledHooks; do
    grep -q "$f" packages/agents/src/api/config-classification.ts || { echo "MISSING classification for $f"; missing=1; }
  done
$ grep -q "settings-escape-hatch" packages/agents/src/api/config-classification.ts || { echo "MISSING settings classification"; missing=1; }
$ npm run typecheck
==> running npm run typecheck...
   (all 13 packages: settings, telemetry, ide-integration, policy, mcp, core,
    lsp, providers, agents, root, a2a-server, test-utils)
   > @vybestack/llxprt-code-agents@0.10.0 typecheck
   > tsc --noEmit
   (clean — no errors)
==> typecheck exit: 0
$ grep -rnE ": any\b|as [A-Z]" packages/agents/src/api/config-types.ts packages/agents/src/api/config-schema.ts && { echo "FAIL: any/assertion"; missing=1; } || true
   (no matches)
$ echo "==> FINAL missing=$missing"
==> FINAL missing=0
$ exit $missing
Exit Code: 0
```

### any/assertion grep (phase spec)
```
PASS: no any/assertion in config-types.ts / config-schema.ts
```

### Deferred Implementation Detection (expect none)
```
grep -rnE "(TODO|FIXME|HACK|STUB|XXX)" \
  packages/agents/src/api/config-types.ts \
  packages/agents/src/api/config-schema.ts \
  packages/agents/src/api/config-classification.ts | grep -v ".spec.ts"
(no matches)
PASS: no deferred-implementation markers
```

================================================================================
## SEMANTIC CHECKLIST
================================================================================
[x] AgentConfig typed fields match §4.2 classification — every CLI-needed field
    is typed; none left only in `settings`.
[x] `toolSchedulerFactory?: AgentSchedulerFactory` and `AgentSchedulerHandle`
    present; ownership documented (factory caller-owned / created scheduler
    instances Agent-owned, disposed by Agent.dispose()).
[x] `settings` escape hatch documented UNSTABLE.
[x] Zod schema `.strict()` rejects unknown/shadowed shapes.
[x] `npm run typecheck` clean.
[x] NO `any`, NO type assertions (`as X`), explicit return types on
    classifyConfigField / classificationCountsByCategory.

================================================================================
## CONSTRAINTS (RULES.md) — met
================================================================================
- Strict TypeScript: NO `any`, NO type assertions, explicit return types.
- Zod schema-first (z.infer used; hand-written interface rigorously in sync).
- Immutable: readonly arrays/fields throughout AgentConfig + projection types.
- NO production logic — type/schema/classification definitions only.
- ONE marker block per file; sparse comments.
- No barrel re-export pulling production code; no index wiring added.
- Files compile standalone.

================================================================================
## VERDICT: PASS
================================================================================
