# P25a — Verify Discovery Helpers (REQ-017) — PASS

Plan: PLAN-20260617-COREAPI · Phase 25a · Requirement REQ-017
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**
Implementer: architect · Spec-driven phase (no pseudocode, no RED test for static module)

## Outcome
Static discovery module `packages/agents/src/api/discovery.ts` implemented
(replacing the `NotYetImplemented` stub). Instance discovery (P15) untouched.
No remediation cycle required — PASS on first verification.

## Per-axis results
1. **Static helpers genuinely callable with NO runtime** — PASS. `listProviders()`
   (discovery.ts:134-143) builds a runtime-free ProviderManager via
   `createProviderManager(context,{})` from `@vybestack/llxprt-code-providers/composition.js`
   fed a fresh `SettingsService`, then re-projects `manager.listProviders()` to the
   public `ProviderInfo` shape. `listTools()` (discovery.ts:158-163) projects the
   built-in tool classes' `static readonly Name` to the public `ToolInfo` shape.
   Independent throwaway dist import (no agent/runtime): providers=20, tools=25,
   correct shapes, NO throw.
2. **Spec-vs-reality constraint honored** — PASS. The phase-spec hint to use
   `@vybestack/llxprt-code-providers/runtime.js` `listProviders()` is a TRAP: that
   global accessor delegates to `getCliRuntimeServices()` (runtimeAccessors.ts:142)
   which THROWS without a registered Config runtime — breaking pre-agent discovery.
   Impl deliberately AVOIDS it (documented in the module header) and reaches the
   SAME `ProviderManager.listProviders()` accessor via the runtime-free composition
   seam. Confirmed no import/call of the throwing accessor.
3. **No mock theater / honest projection** — PASS. Tool list DERIVED from real
   tool-class references (BUILTIN_TOOL_CLASSES, discovery.ts:79-105) matching core's
   `registerStandardTools` (toolRegistryFactory.ts:333-413) — not invented literals.
   `source:'builtin'`/`enabled:true`/`configured:false` are honest structural
   pre-agent projections. No locked test imports the static module → no over-fit risk.
4. **Instance vs static difference real** — PASS. Instance `agentImpl.ts:880/891`
   still read the config-dependent runtime set (providerManager + tool registry incl.
   MCP/extensions); P15 markers intact; NOT modified by P25. Locked T25
   (provider-bootstrap.spec.ts:83) + T12 (mcp-discovery.spec.ts:50,88) GREEN (11/11).
5. **Boundary / standards** — PASS. No CLI deep import. New `@vybestack/llxprt-code-tools`
   import is a declared dep (agents/package.json) and a ROOT entry (not deep /src) —
   passes no-deep-import/package-boundary eslint. `@plan:P25` + `@requirement:REQ-017`
   on the module (stale P06 replaced). Strict TS (no any/as any/!-abuse; explicit
   readonly return types). No deferred-impl; no NotYetImplemented; no __tests__ import.
6. **No regressions / gates** — PASS. Full api suite 119 passed / 1 failed — sole
   failure `app-service-boundary.spec.ts` (documented future-P27 RED, missing
   `src/app-service.js`). typecheck 0; eslint 0 on discovery.ts.

## Files (P25)
- `packages/agents/src/api/discovery.ts` — static listProviders/listTools
  (runtime-free composition seam + built-in tool-class projection).

Status: **PASS** — P25 complete. Proceed to P26 (non-interactive parity).
