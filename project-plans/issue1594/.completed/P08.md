# Phase P08: Quality Gate Setup — Mutation + Property Infrastructure

**Phase ID:** `PLAN-20260617-COREAPI.P08`
**Timestamp:** 2026-06-17T17:23:34 (completion)
**Verdict:** ✅ PASS

---

## Files Created

1. `packages/agents/stryker.conf.json` — Stryker config scoped to `src/api/**/*.ts`, excludes specs/tests, `thresholds.break = 80`, JSON report at `reports/mutation/mutation.json`.
2. `packages/agents/src/api/quality-gate-smoke.ts` — Tiny pure production function `classifyString(input: string): 'empty' | 'non-empty'` with a real branch Stryker can mutate. Carries `@plan:PLAN-20260617-COREAPI.P08` + `@requirement:REQ-019`.
3. `packages/agents/src/api/__tests__/quality-gate-smoke.spec.ts` — Real behavioral test (empty/non-empty assertions + fast-check property). Carries `@plan:PLAN-20260617-COREAPI.P08` + `@requirement:REQ-019`.
4. `packages/agents/scripts/verify-api-property-ratio.js` — Property-ratio checker script (pure Node ESM, no new deps). Carries `// @plan:PLAN-20260617-COREAPI.P08  @requirement:REQ-019`.

## Files Modified

1. `packages/agents/package.json` — Added `@stryker-mutator/core` and `@stryker-mutator/vitest-runner` to devDependencies; added `"test:mutation:api": "stryker run stryker.conf.json"` script.
2. `eslint.config.js` — Added node-globals override block for `packages/agents/scripts/**/*.js` (mirrors existing `packages/vscode-ide-companion/scripts/**/*.js` pattern) so the new `.js` script lints cleanly.

---

## Stryker Dependency Versions

| Dependency | Version |
|---|---|
| `@stryker-mutator/core` | `^9.6.1` (installed: 9.6.1) |
| `@stryker-mutator/vitest-runner` | `^9.6.1` (installed: 9.6.1) |

### Vitest Compatibility Reasoning

The latest `@stryker-mutator/vitest-runner@9.6.1` declares peer dependency `vitest >= 2.0.0`, which is fully satisfied by the installed `vitest@3.2.4`. No peer-dependency conflict occurred. No vitest downgrade was needed. Stryker v9 is the current major and supports vitest 3.x natively. The `npm install -D --workspace @vybestack/llxprt-code-agents` completed cleanly with no peer warnings.

---

## Coverage Analysis Mode

**Mode used:** `"perTest"`

**Why:** Stryker v9 vitest-runner fully supports `perTest` coverage analysis with vitest 3.x. The viability run confirmed it: the initial dry run succeeded, per-test mutant attribution worked correctly (test 1 killed 5 mutants, test 2 killed 2, test 3 covered 5). No fallback to `"off"` was needed.

---

## Verification Output

### V1 — Stryker deps in package.json
```
stryker deps OK ^9.6.1 ^9.6.1
```

### V2 — stryker.conf.json valid JSON
```
stryker.conf.json valid JSON
```

### V3 — Smoke file exists
```
OK-smoke
```

### V4 — Ratio script exists
```
OK-ratio-script
```

### V5 — Code markers (grep)
```
packages/agents/src/api/quality-gate-smoke.ts:8: * @plan:PLAN-20260617-COREAPI.P08
packages/agents/src/api/__tests__/quality-gate-smoke.spec.ts:8: * @plan:PLAN-20260617-COREAPI.P08
```

### V6 — Typecheck (exit 0)
```
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit
(no errors)
```

### V7 — Lint (0 errors)
```
✖ 138 problems (0 errors, 138 warnings)
```
(All 138 warnings are pre-existing; 0 errors.)

### V8 — Prettier
```
Checking formatting...
All matched files use Prettier code style!
```

### V9 — Smoke test (vitest)
```
 RUN  v3.2.4
 ✓ src/api/__tests__/quality-gate-smoke.spec.ts (3 tests) 5ms

 Test Files  1 passed (1)
      Tests  3 passed (3)
```

### V10 — Property-ratio script (exit 0)
```
property-ratio: numerator=1 denominator=1 ratio=1.0000 (threshold=0.3)
property-ratio: PASS
EXIT CODE: 0
```

### V11 — Stryker Viability Run (the key gate)
Command: `npx stryker run stryker.conf.json --mutate "src/api/quality-gate-smoke.ts"` (from `packages/agents`)

```
17:23:30 (74389) INFO ProjectReader Found 1 of 630 file(s) to be mutated.
17:23:31 (74389) INFO Instrumenter Instrumented 1 source file(s) with 7 mutant(s)
17:23:32 (74389) INFO ConcurrencyTokenProvider Creating 15 test runner process(es).
17:23:33 (74389) INFO DryRunExecutor Starting initial test run (vitest test runner with "perTest" coverage analysis).
17:23:34 (74389) INFO DryRunExecutor Initial test run succeeded. Ran 3 tests in 0 seconds.

All tests/quality-gate-smoke.spec.ts
  ✓ classifyString returns "empty" for the empty string (killed 5)
  ✓ classifyString returns "non-empty" for a non-empty string (killed 2)
  ~ classifyString returns "non-empty" for any arbitrary non-empty string (property) (covered 5)

-----------------------|------------------|----------|-----------|------------|----------|----------|
                       | % Mutation score |          |           |            |          |          |
File                   |  total | covered | # killed | # timeout | # survived | # no cov | # errors |
-----------------------|--------|---------|----------|-----------|------------|----------|----------|
All files              | 100.00 |  100.00 |        7 |         0 |          0 |        0 |        0 |
 quality-gate-smoke.ts | 100.00 |  100.00 |        7 |         0 |          0 |        0 |        0 |
-----------------------|--------|---------|----------|-----------|------------|----------|----------|
17:23:34 (74389) INFO MutationTestReportHelper Final mutation score of 100.00 is greater than or equal to break threshold 80
17:23:34 (74389) INFO JsonReporter Your report can be found at: file:///.../packages/agents/reports/mutation/mutation.json
17:23:34 (74389) INFO MutationTestExecutor Done in 3 seconds.
```

**Mutation score: 100.00%** (7/7 mutants killed, break threshold 80 met). Exit code: 0.

---

## Summary

| Check | Result |
|---|---|
| Stryker deps installed | ✅ `@stryker-mutator/core@^9.6.1`, `@stryker-mutator/vitest-runner@^9.6.1` |
| Stryker config valid | ✅ |
| Smoke fn + spec created with markers | ✅ |
| Property-ratio script created | ✅ |
| Typecheck | ✅ exit 0 |
| Lint | ✅ 0 errors |
| Prettier | ✅ all files formatted |
| Smoke test (vitest) | ✅ 3/3 passed |
| Property-ratio script | ✅ exit 0, ratio=1.0 |
| Stryker viability | ✅ 100.00% mutation score, completed in 3s |
| coverageAnalysis | `perTest` (worked, no fallback needed) |
| api/index.ts barrel clean | ✅ smoke fn NOT exported from barrel |

**Verdict: PASS**
