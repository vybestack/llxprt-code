# P28a — Verify Docs / agent-api.md (REQ-020) — PASS

Plan: PLAN-20260617-COREAPI · Phase 28a · Requirement REQ-020
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**

## Outcome
`docs/agent-api.md` is factually accurate against the shipped `@vybestack/llxprt-code-agents`
surface. Initial review found two accuracy defects (FAIL); both were remediated by architect
and re-verified PASS from first principles.

## Per-axis results (all PASS, source-cited)
1. **Entry package & no-cycle rationale** — doc:11-23 states public API exported from
   `@vybestack/llxprt-code-agents` (NOT -core) w/ core→agents cycle rationale (B11);
   package.json:2 name confirmed; exports root/internals.js/app-service.js at package.json:13-25.
2. **createAgent signature** — doc:103-107 == createAgent.ts:71
   `createAgent(rawConfig: AgentConfig): Promise<Agent>`.
3. **AgentConfig fields** — required provider/model (config-types.ts:141-143) + optional fields
   (144-196, settings 203-208) all real; no invented field.
4. **Control plane** — 7 readonly sub-surfaces (profiles/tools/mcp/auth/ide/session/hooks)
   == agent.ts:336-342; top-level methods agent.ts:321-369; agent.mcp documented runtime-only.
5. **AgentEvent union** — all 19 variants exact vs event-types.ts:179-198; sole terminal = done.
6. **DoneReason** — exactly 7 (stop|aborted|max-turns|context-overflow|loop-detected|error|
   hook-stopped) == event-types.ts:20-27; exactly-one-done invariant + aborted≠error documented.
7. **Auth precedence** — doc raw→keyName→inline→keyfile→oauth→none == computeAuthWinner
   short-circuit order authState.ts:101-121 (AuthWinner union 35-41).
8. **Confirmation / safe denial** — wired-handler-reject→safe Cancel denial (AgenticLoop.ts:232-248);
   no-handler→no-op (createAgent.ts:258-259 undefined → AgenticLoop.ts:207-210), NOT auto-Cancel;
   non-interactive throws structured error (confirmation-coordinator.ts:320-325). Doc:357-381 accurate.
9. **COMMAND_API_MAP** — every kind:'runtime' `agent.*` target resolves to a real Agent member;
   `/chat save`→`agent.session.createCheckpoint` (command-api-map.ts:135-138, createCheckpoint
   agent.ts:291); no `agent.session.save` anywhere.
10. **Other decisions** — idle-timeout terminal; stats source uiTelemetry.js (agentImpl.ts:23);
    settings unstable/not-semver (config-types.ts:203-208); subpath/runtime-vs-app-service boundary.
11. **Quick-start** — provider:'fake' + LLXPRT_FAKE_RESPONSES seam real
    (providerManagerInstance.ts:537-553); AgentInput {text} agent.ts:33-35; dispose() shown; compiles.

## Remediated defects (initial FAIL → fixed)
- **Defect A** — confirmation section previously claimed "no handler wired → safe Cancel denial"
  (false). Rewritten to: wired-reject→Cancel safe denial; no-handler→clear error (non-interactive
  throw), not silent. Matches source.
- **Defect B** — production `COMMAND_API_MAP` `/chat save` target was dangling `agent.session.save`
  (no such method; save() is on AgentAuthKeysControl). Fixed to `agent.session.createCheckpoint`
  in command-api-map.ts AND doc. Full runtime-target audit: this was the only dangler.
- (Dismissed non-defect: "git status shows many changed files" — cumulative 59-phase branch;
  P28 correctly added only docs/agent-api.md + marker.)

## Mechanics
- Markers `@plan:PLAN-20260617-COREAPI.P28` + `@requirement:REQ-020` present (doc:1).
- Greps: entryOK; symbol-count 26; plan-marker 1; `agent.session.save` → no matches.
- Full agents api suite: **17 files / 129 tests / 0 failed** (specs run against rebuilt dist).
- typecheck 0; eslint 0 on changed map file.

Status: **PASS** — P28 complete. Proceed to P29 (final plan-quality evaluation).
