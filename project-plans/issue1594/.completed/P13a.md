Phase: P13a
Completed: 2026-06-18T01:18:00Z

Files Reviewed:
- project-plans/issue1594/plan/13-harness-resource-leak.md (phase contract)
- project-plans/issue1594/plan/13a-harness-resource-leak-verification.md (this checklist)
- project-plans/issue1594/analysis/pseudocode/dispose.md (numbered dispose() + ownership/teardown table + T13 disposed-observable-per-row table + Interface Contracts)
- project-plans/issue1594/analysis/pseudocode/createAgent.md (ownership record creation/retention, lines 150-156)
- packages/agents/src/api/__tests__/disposal.spec.ts (T13, 9 tests)
- packages/agents/src/api/__tests__/scheduler-factory.spec.ts (T19, 1 test)
- packages/agents/src/api/__tests__/helpers/disposalProbe.ts (white-box probe)
- packages/agents/src/api/__tests__/helpers/fakeScheduler.ts (recording/failing factories)
- packages/agents/src/api/__tests__/helpers/agentHarness.ts (buildAgent seam)
- packages/agents/src/api/__tests__/boundary.spec.ts (T17 boundary scan)
- packages/agents/src/api/agentImpl.ts (dispose() + tools NYI stubs)
- packages/agents/src/api/control/toolControl.ts (onConfirmationRequest NYI stub)
- packages/agents/src/api/createAgent.ts (returns AgentImpl stub)
- packages/agents/src/api/index.ts + src/index.ts (public-root re-exports of scheduler types)
- packages/agents/src/api/config-types.ts (AgentSchedulerFactory/Handle/Options)
- GROUND TRUTH: packages/agents/src/core/client.ts (AgentClient ctor:146 + dispose:259-266)
- GROUND TRUTH: packages/core/src/config/config.ts (Config.dispose L882-888)
- GROUND TRUTH: packages/core/src/config/configBase.ts (shutdownLspService L159)
- GROUND TRUTH: packages/core/src/config/lspIntegration.ts (shutdownLsp L388-419)
- GROUND TRUTH: packages/core/src/recording/SessionLockManager.ts (release closure var L113-122)
- GROUND TRUTH: packages/policy/src/confirmation-bus/message-bus.ts (private emitter:EventEmitter)

Verification:

=== PER-ROW GREEN-REACHABILITY CROSS-REFERENCE (the central thing) ===
Each post-dispose observable was cross-referenced against the REAL teardown source and
confirmed to genuinely transition for the agent the test actually builds.

ROW agentClient (disposal.spec.ts:88-101, probe.agentClientDisposed):
  Asserted observable: `agentClient._unsubscribe` transitions function -> undefined.
  Real teardown: Config.dispose() (config.ts:882-888) calls `this.agentClient?.dispose()`;
  AgentClient.dispose() (client.ts:259-266) calls `this._unsubscribe()` then sets it to
  `undefined`. AgentClient ctor (client.ts:146) sets `_unsubscribe =
  subscribeToAgentRuntimeState(...)` (a function) — so pre-dispose state is genuinely a
  function. AgentClient has NO `disposed` boolean; `_unsubscribe` function->undefined is
  the ONLY genuine transition, and the probe reads exactly that. The fake client IS built
  by Config.refreshAuth (dispose.md table: "partial (yes)"). GREEN-REACHABLE: YES.

ROW facade-held injected-factory scheduler T19 (disposal.spec.ts:103-138, probe.schedulerDisposed):
  Asserted observable: recording handle `.disposed === true`. Test builds the agent WITH an
  injected recording toolSchedulerFactory AND drives a tool turn so the facade creates+retains
  the instance (matching dispose.md table note "Most agents have NO facade-held scheduler").
  Real teardown: dispose.md line 41 `injectedFactoryScheduler.dispose()`. The recording fake
  (fakeScheduler.ts RecordingHandleImpl) exposes a REAL `disposed` getter flipped true by its
  async dispose(). createAgent.md lines 24-26 inject the factory; dispose.md retains the
  facade-held instance on the ownership record. GREEN-REACHABLE: YES.

ROW confirmationCoordinator T19 (disposal.spec.ts:140-167, probe.confirmationCoordinatorDisposed):
  Asserted observable: recording coordinator `.disposed === true`. Same injected-fake path;
  coordinator backs the injected scheduler. Real teardown: dispose.md line 46
  `injectedFactoryCoordinator.dispose()` (CONDITIONAL T19, owned by the scheduler).
  GREEN-REACHABLE: YES (conditional on the injected-factory build the test performs).

ROW messageBus subscriptions (disposal.spec.ts:169-198, probe.messageBusSubscriptionCount):
  Asserted observable: `emitter.eventNames().reduce((n,name)=>n+listenerCount(name),0) === 0`.
  Real surface: MessageBus holds `private readonly emitter: EventEmitter`
  (message-bus.ts:29,40). The probe reads the real private emitter and sums listeners across
  all event names — both `eventNames` and `listenerCount` are genuine EventEmitter methods.
  Real teardown: dispose.md lines 50-52 unsubscribe every recorded sub, driving the tally to
  baseline 0. GREEN-REACHABLE: YES (once P15 wires the `messageBus`/`bus` field the probe reads).

ROW LSP (disposal.spec.ts:200-219, probe.lspDisposed):
  Asserted observable: `ownership.lspShutDown === true`. CONFIRMED net-new: shutdownLsp
  (lspIntegration.ts:388-419) only NULLS state slots (lspMcpClient/lspMcpTransport/
  lspServiceClient = undefined) — LspState has NO boolean flag, and Config.dispose() does NOT
  call shutdownLspService today (config.ts:882-888 only does agentClient + mcpClientManager).
  shutdownLspService exists on Config (configBase.ts:159) but is unwired. The headless harness
  never starts LSP, so no primitive transition is observable. dispose.md line 70 + the
  "T13 disposed-observable per row" table specify the completion marker `ownership.lspShutDown`
  set INSIDE safe() after the await. The probe reads exactly that marker on the SAME ownership
  record createAgent.md lines 150-156 retain. GREEN-REACHABLE: YES (the only faithful observable).

ROW Extensions (disposal.spec.ts:221-238, probe.extensionsDisposed):
  Asserted observable: `ownership.extensionsDisposed === true`. CONFIRMED net-new: Config has
  NO `extensionsManager` field (only getExtensions() via _extensionLoader, config.ts:153/173/487);
  AgentConfig.extensions is DATA, not a disposable; harness creates none. No primitive boolean
  exists. dispose.md line 80 + per-row table specify the completion marker. Probe reads that
  marker. GREEN-REACHABLE: YES.

ROW Session/recording locks (disposal.spec.ts:240-257, probe.sessionLocksReleased):
  Asserted observable: `ownership.sessionLocksReleased === true`. CONFIRMED net-new:
  SessionLockManager.release() (SessionLockManager.ts:110-122) flips a CLOSURE variable
  `released = true`, NOT a property; LockHandle is `{ lockPath; release(): Promise<void> }` —
  no `.released`. Harness acquires no lock. dispose.md lines 81-83 + per-row table specify the
  completion marker set after the release loop (vacuously true with zero locks). Probe reads
  that marker. GREEN-REACHABLE: YES.

ROW idempotent re-dispose (disposal.spec.ts:259-278):
  Asserts second dispose() resolves without throwing and flags remain true. dispose.md lines
  11-13 guard with `ownership.disposed` short-circuit. Structural/behavioral, no value that
  cannot hold. GREEN-REACHABLE: YES.

ROW AggregateDisposeError partial-failure (disposal.spec.ts:280-330):
  Induced via createFailingSchedulerFactory whose handle.dispose() THROWS (a REAL failing
  path). Asserts isAggregateDisposeError(e) (cast-free structural predicate: e instanceof
  Error && name==='AggregateDisposeError' && Array.isArray(errors)) and that the induced
  message appears in e.errors. Real teardown: dispose.md line 41 dispose inside safe()
  (lines 110-112 collect), line 101 throws AggregateDisposeError(errors). Class created at P24;
  asserted STRUCTURALLY only (NOT value-imported). GREEN-REACHABLE: YES.

CONCLUSION: No row asserts an observable the faithful GREEN teardown would fail to produce.
The three net-new rows (lsp/extensions/sessionLocks) correctly read OwnershipRecord completion
markers (not non-existent primitive flags) exactly as dispose.md mandates and createAgent.md
retains. No latent always-false-at-GREEN defect found.

=== NATURAL RED RUN ===
Command: npx vitest run --root packages/agents src/api/__tests__/disposal.spec.ts src/api/__tests__/scheduler-factory.spec.ts
Result: Test Files 2 failed (2); Tests 10 failed (10). PASSED COUNT = 0. CONFIRMED.
Sample failure reasons (all reach a genuine public-API NYI stub OR a genuine value
assertion that cannot yet hold — NO ReferenceError/TypeError/missing-import/tautology):
  - T13 agentClient (line 97): Error NotYetImplemented at AgentImpl.dispose (agentImpl.ts:165) — public dispose surface.
  - T13 scheduler (line 117): Error NotYetImplemented at ToolControl.onConfirmationRequest (toolControl.ts:25) via respondToFirstConfirmation — public tools surface.
  - T13 confirmationCoordinator (line 157): NYI at ToolControl.onConfirmationRequest — public tools surface.
  - T13 messageBus (line 190): AssertionError "expected -1 to be greater than or equal to 0" — genuine value assertion; the bus field is not yet wired (AgentImpl ctor stores nothing), so the probe returns its honest sentinel -1; at GREEN (P15) the real emitter tally is >=0 and dispose drives it to 0. Green-reachable.
  - T13 lsp/extensions/sessionLock/idempotent (lines 212/231/250/269): NYI at AgentImpl.dispose — public dispose surface.
  - T13 AggregateDisposeError (line 291): NYI at ToolControl.onConfirmationRequest — public tools surface.
  - T19 scheduler-factory (line 46): NYI at ToolControl.onConfirmationRequest — public tools surface.
Note: 4 tests fail at onConfirmationRequest (the tool-turn setup) before reaching dispose();
this is still a valid natural RED because onConfirmationRequest is a real public Agent API
stub throwing NotYetImplemented, and at GREEN (P15+) those tests will drive the full tool turn
then exercise dispose() with no rewrite. Verified runtime threw NO ReferenceError (all spec
imports are used; cross-checked every imported symbol has >=2 occurrences).

=== TEST-SMELL GUARDS ===
grep for toHaveBeenCalled | not.toThrow | toThrow('NotYetImplemented') | expect(true).toBe(true)
 | expect(typeof : NO-SMELL (none found).
Literal "NotYetImplemented" in specs: NO-NYI-LITERAL (none).
AggregateDisposeError value-import: NONE — only the cast-free predicate isAggregateDisposeError
 (from helper) + comments/titles. Class not imported.
Casts in *.spec.ts (excl. as const): NO-CASTS (0). White-box casts live only in
 helpers/disposalProbe.ts (8, allowed).

=== BOUNDARY (T17) ===
npx vitest run --root packages/agents src/api/__tests__/boundary.spec.ts -> 3 passed.
Spec imports: ONLY vitest + ./helpers/agentHarness.js + ./helpers/disposalProbe.js +
 ./helpers/fakeScheduler.js. No deep @vybestack/* subpath imports in specs (NO-DEEP-IMPORTS-IN-SPECS).
Public-root scheduler types (AgentSchedulerFactory/Handle/Options) confirmed re-exported via
 api/config-types.ts -> api/index.ts (export *) -> src/index.ts; helpers import them from the
 public root (allowed for helpers).

=== HYGIENE ===
npm run typecheck --workspace @vybestack/llxprt-code-agents -> exit 0.
 (Note: tsconfig excludes **/*.spec.ts; helpers ARE typechecked and pass. Independently
  confirmed no spec calls an un-imported helper/probe symbol — runtime produced NO ReferenceError.)
npx eslint (2 specs + 2 helpers) -> ESLINT_EXIT=0 (0 errors / 0 warnings).
npx prettier --check (2 specs + 2 helpers) -> "All matched files use Prettier code style!" exit 0.

=== COVERAGE ===
All 7 ownership rows present in disposal.spec.ts (agentClient/scheduler/confirmationCoordinator/
 messageBus/lsp/extensions/sessionLock — verification grep all >=5 hits). Fixtures present:
 plain-text.jsonl + tool-call-then-answer.jsonl. T19/REQ-006 seam exercised in
 scheduler-factory.spec.ts (factory used -> tool-call event + createdHandles>=1; torn down ->
 handle.disposed true; caller factory not disposed).

=== MANDATORY SEMANTIC QUESTIONS (13a) ===
1. Does T13 assert EACH ownership-table row's disposed flag (cite the table)?
   YES. dispose.md "Resource-ownership / teardown table" + "T13 disposed-observable per row"
   table drive one test per row: agentClient(_unsubscribe), scheduler(.disposed),
   confirmationCoordinator(.disposed), messageBus(emitter listener tally=0), lsp(lspShutDown),
   extensions(extensionsDisposed), sessionLock(sessionLocksReleased). Net-new rows correctly use
   ownership completion markers, not generic handle checks.
2. Is idempotent re-dispose tested? YES (disposal.spec.ts:259-278): two dispose() calls; second
   must resolve without throwing; flags remain true (dispose.md lines 11-13 guard).
3. Is the AggregateDisposeError partial-failure path induced and asserted? YES
   (disposal.spec.ts:280-330): a failing scheduler handle whose dispose() throws is injected and
   driven; dispose() must reject with a structurally-asserted AggregateDisposeError carrying the
   induced message in .errors (no value-import of the class).
4. Does T19 prove the injected scheduler factory is used and torn down?
   YES (scheduler-factory.spec.ts:34-83): asserts a tool-call event occurred AND
   createdHandles.length>=1 (USED), each handle.disposed===true post-dispose (TORN DOWN), and
   the caller-owned factory function itself is NOT disposed (handle count unchanged).
5. Behavioral (observable teardown), not "dispose was called"? YES. Every assertion reads a real
   observable (emitter listener count, real disposed booleans on injected fakes, _unsubscribe
   transition, ownership completion markers, AggregateDisposeError shape). No toHaveBeenCalled,
   no call spies.
6. Fail naturally pending P24/P23? YES. All 10 tests fail now; 0 passed. Each reaches a genuine
   public-API NYI stub (dispose / onConfirmationRequest) or a genuine not-yet-holding value
   assertion (messageBus -1). No reverse tests, no tautologies. At GREEN (P15 wiring + P23
   factory + P24 dispose) the SAME assertions pass with no rewrite.

Holistic: The leak suite is table-driven (one row -> one test, all 7 rows + idempotency +
error-path + T19) and would catch a missed teardown because each assertion reads the genuine
observable a faithful dispose() must produce for the agent the test builds. Cross-referenced
against real teardown source: no assertion is GREEN-unreachable.

Verdict: PASS
