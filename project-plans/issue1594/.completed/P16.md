# Phase 16 Complete — Impl switch + context-preservation

Phase ID: `PLAN-20260617-COREAPI.P16`
Worker: typescriptexpert | Coordinator-verified

## Scope delivered
Live provider/model/param switching + conversation-context preservation on the public Agent API.

- `agentImpl.ts`: per-agent `AgentProviderState` holder (null-prototype modelParams); `setProvider`/`setModel`/`setModelParam`/`clearModelParam`/`getModelParams`/`getProvider`/`getModel`/`getProviderStatus`/`getCurrentSequenceModel`/`setHistory`; private `applyProviderSwitch` (fake-seam tolerant), `restoreChatVisibility`, `applyProfileParams`, `rebuild`, `computeAuthStatus`; `historyService` getter.
- `createAgent.ts`: `initialHistoryService` + `storeHistoryServiceForReuse`; threads approvalHandler/displayCallbacks; initial `rebuildLoop`.
- `control/profilesControl.ts`: `apply`/`list`/`get` (public-shape fixture resolution; same transfer path as manual switch ≡ LB failover). CRUD (create/saveCurrent/delete/setDefault/getDefault) intentionally deferred to P19.
- `control/authControl.ts`: synchronous `status()`.
- `loop/rebuildLoop.ts`: loop rebuild after every client-rebinding mutation.

## Pseudocode compliance
Faithful to `switch-rebind.md` steps 10-26 (rebuildLoop), 30-42 (setProvider), 50-60 (setModel), 70-78 (applyProfile), 90-111 (param mutators). REQ-005 identity preserved by delegating to the Config transfer path (`initializeContentGeneratorConfig` → `transferHistoryToNewClient` → `storeHistoryServiceForReuse`), NOT a manual history copy. `initializeContentGeneratorConfig` correctly NOT called after switchActiveProvider/applyProfile (they rebuild internally), and IS called after model-only setActiveModel.

## GREEN test rows (switch-context.spec.ts — 11/11, deterministic 10x)
T4, T4b(standard), T4b(load-balancer), T4c, T4d, T4e, T4f, T5, T5i, T4dp(property), T5p(property).

## Harness strengthening (REQ-005 made genuinely verifiable)
Architect review surfaced two under-powered tests; remediated (test file only, impl untouched, mutation-sanity proven):
- **T4f** was vacuous (asserted absence of `"type":"thought"`/`"type":"thinking"` substrings that never appear in genai Content output). Rewritten to seed a real `thoughtSignature`, assert present-before / absent-after `stripThoughts:true`, and assert conversation content survives. Mutation-sanity: neutralizing production `stripThoughts` makes T4f fail.
- **T5i** added: asserts HistoryService identity (`toBe`) across the REAL setModel client rebind (the setProvider path is a no-op under the fake seam). Mutation-sanity: asserting against a fresh object makes T5i fail.
- **T5p** key generator extended with prototype-hazard keys (`__proto__`/`constructor`/`prototype`/`toString`/`hasOwnProperty`) to regression-guard null-prototype storage.

## Coordinator-independent verification
- typecheck: clean
- switch-context.spec.ts: 11/11, 10/10 determinism runs green
- eslint: 0 errors; prettier: clean; deferred-language scan: clean
- regression: event-characterization 38/38 (T16), provider-bootstrap 4/4 (T25), core-conversation T1/T9 green; only T10 (→P21) and T14 (→P17) red (expected later-phase)
- `git diff HEAD packages/agents/src/core/client.ts`: empty (mutation-sanity fully reverted)
- markers present: `@plan:PLAN-20260617-COREAPI.P16`, `@requirement:REQ-004/REQ-005/REQ-009`, `@pseudocode switch-rebind.md`

## Markers/requirements
REQ-004 (provider/model/param switching), REQ-005 (context preservation across switch), REQ-009 (profile apply/list/get — live apply path).
