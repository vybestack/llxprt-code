# Phase 10: Harness Layer 2 — Event Characterization (T16, 21 variants) [RED]

**Phase:** PLAN-20260617-COREAPI.P10
**Completed:** 2026-06-17
**Subagent:** typescriptexpert (GLM/zai)

## Files Created

1. `packages/agents/src/api/__tests__/fixtures/basic-text.jsonl` — Real `FakeResponseTurn` fixture with one text chunk (`"hello from fake provider"`).
2. `packages/agents/src/api/__tests__/helpers/eventHarness.ts` — Event-characterization harness (T16). Exports:
   - `runAdapter(loopEvents)` — drives `mapLoopStream` via variable-specifier dynamic import → narrows → drains to `AgentEvent[]`.
   - Authentic `ServerGeminiStreamEvent` builders using REAL `GeminiEventType` + real value shapes: `streamContent`, `streamThought`, `streamCitation`, `streamUsage`, `streamModelInfo`, `streamNotice`, `streamCompressed`, `streamRetry`, `streamInvalid`, `streamIdleTimeout`, `streamError`, `streamLoopDetected`, `streamContextOverflow`, `streamMaxTurns`, `streamFinished`, `streamUserCancelled`, `streamToolCallRequest`, `streamToolCallResponse`, `streamToolCallConfirmation` (+ `buildExecuteConfirmationDetails`), `streamStopped` (FLAT), `streamBlocked` (FLAT).
   - `wrapStream(e)`, `loopStream(...events)`.
   - Loop-native event builders: `loopToolsComplete`, `loopAwaitingApproval`, `loopToolUpdate`, `loopToolOutput`.
   - Type guards for all 19 `AgentEvent` variants.
   - `fakeProviderContentLoopEvents(fixturePath, cwd)` — uses a REAL `FakeProvider` to read JSONL, consumes `IContent` stream, maps text blocks to Content stream events.
   - `runRealLoopExecuteTool()` — drives a REAL `AgenticLoop` with REAL `CoreToolScheduler`/`MessageBus`/`MockTool` (ASK_USER + ProceedOnce), collects emitted `AgenticLoopEvent`s.
   - `runRealLoopAbort()` — drives a REAL `AgenticLoop`, aborts via real `AbortSignal` after first event, collects emitted `AgenticLoopEvent`s.
3. `packages/agents/src/api/__tests__/event-characterization.spec.ts` — 38 test rows across 6 describe blocks. Public-only imports (`vitest`, `node:*`, `@vybestack/llxprt-code-agents` type, `./helpers/eventHarness.js`). NO deep imports.

## Files Modified

1. `packages/agents/src/api/__tests__/boundary.spec.ts` — STEP 0 surgical evolution of test #2: transitional placeholder `expect(HELPERS_PRESENT).toBe(false)` replaced with the REAL invariant:
   ```ts
   it('helper scaffolding under helpers/ is excluded from the consumer-facing deep-import scan @plan:PLAN-20260617-COREAPI.P09 @requirement:REQ-019', () => {
     expect(HELPERS_PRESENT).toBe(true);
     const helperSpecs = ALL_FILES.filter((f) => f.isHelper);
     expect(helperSpecs).toHaveLength(0);
     expect(CONSUMER_VIOLATIONS).toHaveLength(0);
   });
   ```
   Tests #1 and #3 left byte-identical. No helper functions modified.

## Per-Row Source-Category Table

| Variant | Source Category | Projection | Terminal? |
|---|---|---|---|
| Content | fake-provider | text | no |
| Thought | adapter-characterization | thinking | no |
| Citation | adapter-characterization | citation | no |
| UsageMetadata | adapter-characterization | usage | no |
| ModelInfo | adapter-characterization | model-info | no |
| SystemNotice | adapter-characterization | notice | no |
| ChatCompressed | adapter-characterization | compression | no |
| ChatCompressed(null) | adapter-characterization | compression(null) | no |
| Retry | adapter-characterization | retry | no |
| InvalidStream | adapter-characterization | invalid-stream | runtime-dependent |
| StreamIdleTimeout | adapter-characterization | idle-timeout THEN done{error} | YES |
| Error | adapter-characterization | error THEN done{error} | YES |
| LoopDetected | adapter-characterization | loop-detected THEN done{loop-detected} | YES |
| Finished | adapter-characterization | done{stop} | YES |
| ToolCallRequest | scheduler | tool-call | no |
| ToolCallResponse | scheduler | tool-result | no |
| ToolCallConfirmation | scheduler | tool-confirmation | no |
| tool_update (loop-native) | scheduler | tool-status | no |
| tool_output (loop-native) | scheduler | tool-status | no |
| tools_complete (loop-native) | scheduler | tool-result | no |
| awaiting_approval (real loop) | scheduler | tool-confirmation | no |
| UserCancelled | abort | done{aborted} | YES |
| real AbortSignal | abort | done (loop terminates) | YES |
| AgentExecutionStopped | hook | done{hook-stopped} | YES |
| AgentExecutionBlocked | hook | hook-blocked | NO (continues) |
| ContextWindowWillOverflow | config/orchestrator | context-warning THEN done{context-overflow} | YES |
| MaxSessionTurns | config/orchestrator | done{max-turns} | YES |

Invariant/decision-table: 11 additional rows asserting exactly-one-done for each terminal path, synthesized-done despite no Finished, NON-terminal AgentExecutionBlocked, loop-detected BEFORE done ordering, and intermediate events not emitting done.

## Verification

### P10 Tests (FAIL NATURALLY — adapter missing)
```
 Test Files  1 failed | 93 skipped (94)
      Tests  38 failed | 1612 skipped (1650)
```
Error: `Cannot find module '../../eventAdapter.js'` — the adapter is unimplemented until P14.

### P09 Boundary Tests (GREEN except pre-existing app-service T23)
```
 Test Files  1 failed | 1 passed | 92 skipped (94)
      Tests  1 failed | 7 passed | 1642 skipped (1650)
```
The 1 failure is the pre-existing app-service T23 row (`app-service.js` missing P27 subpath). Boundary tests #1/#2/#3 all PASS.

### Variant/Category/Trap Checks
```
=== variant check ===
(all 21 variants present — no MISSING)
=== category check ===
(all 6 categories present — no MISSING)
=== impossible-fake trap ===
trap done
(no impossible-fake-provider matches)
=== fixtures ===
packages/agents/src/api/__tests__/fixtures/basic-text.jsonl
=== guard ===
OK guard
=== deep-in-spec ===
OK public-only spec
```

### Typecheck
```
> tsc --noEmit
(0 errors)
```

### Lint
```
✖ 138 problems (0 errors, 138 warnings)
```
(All warnings pre-existing; 0 errors in P10 files.)

### Prettier
```
All matched files use Prettier code style!
```

## Verdict: PASS

All 21 GeminiEventType variants have characterization rows asserting the documented public projection by VALUE. The exactly-one-done invariant is asserted for every terminal path. AgentExecutionBlocked is correctly NON-terminal. FakeProvider JSONL is used only for the Content variant. Every row names its source category. 13 impossible variants never share a line with "fake-provider". Tests fail naturally pending P14. The P09 boundary suite is GREEN (tests #1/#2/#3 all pass) with only the pre-existing app-service T23 row remaining red.
