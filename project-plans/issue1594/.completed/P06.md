# Phase 06 — createAgent / Agent + Sub-surface Stubs

- **Phase:** P06
- **Plan:** PLAN-20260617-COREAPI.P06
- **Completed:** 2026-06-17T19:50:37Z

## Prerequisite

- `project-plans/issue1594/.completed/P05a.md` — present (PASS).

## Files Created

### Implementation classes + factory + discovery

1. `packages/agents/src/api/agentImpl.ts` — `class AgentImpl implements Agent`.
   Every method body is `throw new Error('NotYetImplemented')`. The 7
   sub-surface accessor properties (`profiles`, `tools`, `mcp`, `auth`, `ide`,
   `session`, `hooks`) return **constructed** stub control instances so the
   shape is navigable (they do NOT throw). The `stream` async generator carries
   the project-conventional `// eslint-disable-next-line require-yield,
   sonarjs/generator-without-yield` directive (established pattern in
   `core/src/utils/asyncIterator.test.ts` and provider test stubs) so it both
   throws `NotYetImplemented` on first iteration AND lints clean.
2. `packages/agents/src/api/createAgent.ts` —
   `export async function createAgent(config: AgentConfig): Promise<Agent>`
   that constructs and returns `new AgentImpl(config)`. Return type is the
   verified `Agent` interface (not `AgentImpl`). No composition logic (P14/P15).
3. `packages/agents/src/api/discovery.ts` — module-level
   `export function listProviders(): ProviderInfo[]` and
   `export function listTools(): ToolInfo[]`, both throw `NotYetImplemented`.

### Sub-surface control stubs (`packages/agents/src/api/control/`)

4. `toolControl.ts` — `class ToolControl implements AgentToolControl` (REQ-006).
5. `mcpControl.ts` — `class McpControl implements AgentMcpControl` (REQ-013).
6. `authKeysControl.ts` — `class AuthKeysControl implements AgentAuthKeysControl` (REQ-008).
7. `authControl.ts` — `class AuthControl implements AgentAuthControl` (REQ-008);
   exposes `readonly keys: AgentAuthKeysControl = new AuthKeysControl()`.
8. `ideControl.ts` — `class IdeControl implements AgentIdeControl` (REQ-014).
9. `sessionControl.ts` — `class SessionControl implements AgentSessionControl` (REQ-010).
10. `profilesControl.ts` — `class ProfilesControl implements AgentProfileControl` (REQ-009).
11. `hookControl.ts` — `class HookControl implements AgentHookControl` (REQ-015).

### Barrel update

12. `packages/agents/src/api/index.ts` — curated public API sub-barrel. Retains
    the existing `export * from './config-types.js' | './event-types.js' |
    './agent.js' | './config-schema.js' | './event-schema.js'` (so all public
    TYPE surface — `Agent`, `AgentConfig`, `AgentEvent`, `DoneReason`, event
    payload types, `ApprovalHandler`/`OAuthPromptHandler`/`EditorCallbacks`,
    `TurnOptions`/`GenerateOptions`/`AgentResult`/`AgentError`,
    `AgentConfigSchema`/`AgentEventSchema`, and all sub-surface interfaces —
    remains reachable) and ADDS
    `export { createAgent } from './createAgent.js'` and
    `export { listProviders, listTools } from './discovery.js'`.

## How the Verified Interface Was Preserved

- The `Agent` **interface** and all 7 sub-surface **interfaces**
  (`AgentToolControl`, `AgentMcpControl`, `AgentAuthControl`,
  `AgentAuthKeysControl`, `AgentIdeControl`, `AgentSessionControl`,
  `AgentProfileControl`, `AgentHookControl`) in
  `packages/agents/src/api/agent.ts` are **unchanged** — `git diff` on that
  file is empty. They were NOT replaced by classes and NOT renamed.
- New implementation classes `implements` those interfaces:
  - `AgentImpl implements Agent` lives in `packages/agents/src/api/agentImpl.ts`.
  - Each control class `implements` its matching interface.
- No second symbol named `Agent` exists anywhere.
- `packages/agents/src/index.ts` (package top-level barrel) was NOT modified
  (`git diff` empty) — P07 wires package exports additively.

Every new/edited file carries the marker block
`@plan:PLAN-20260617-COREAPI.P06` + a relevant `@requirement`. Marker format
(`@plan:` colon-style) matches the existing P03–P05 siblings in `api/` and the
phase spec's grep `grep -rc "@plan:PLAN-20260617-COREAPI.P06"`.

## Stub Count

- 81 `NotYetImplemented` throw sites across `packages/agents/src/api/`.
- Breakdown: agentImpl.ts=28, authControl.ts=9, profilesControl.ts=8,
  authKeysControl.ts=6, ideControl.ts=6, mcpControl.ts=6, sessionControl.ts=6,
  toolControl.ts=6, hookControl.ts=4, discovery.ts=2.
- 12 plan-marker blocks.

## Constraints Compliance

- Strict TypeScript: NO `any`, NO type assertions (`as X`) in any new file.
- `NotYetImplemented` appears ONLY as `throw new Error('NotYetImplemented')` —
  no exported sentinel, no property, no reverse-test hook.
- NO `*V2`/`*New`/`*Copy` parallel files (`find` prints OK).
- NO `TODO`/`FIXME`/`HACK`/`STUB`/`XXX` comments in `api/` (grep prints OK).
- Sparse comments: only the marker blocks + one eslint directive.

## Verification

### `npm run typecheck` (monorepo) — exit 0

```
> @vybestack/llxprt-code-ide-integration@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-policy@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-mcp@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-core@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-lsp@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-providers@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-a2a-server@0.10.0 typecheck
> tsc --noEmit
> @vybestack/llxprt-code-test-utils@0.10.0 typecheck
> tsc --noEmit
typecheck-monorepo exit: 0
```

### `npm run typecheck --workspace @vybestack/llxprt-code-agents` — exit 0

```
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit
typecheck-agents exit: 0
```

### `npm run lint --workspace @vybestack/llxprt-code-agents` — exit 0

```
✖ 138 problems (0 errors, 138 warnings)
npm lint exit code: 0
```

All 138 are **warnings** (pre-existing, in files outside `api/` such as
`core/`, `compression/`, `scheduler/`, `tools/`). The ONLY finding inside
`packages/agents/src/api/` is one pre-existing warning in the P03 file
`config-types.ts` (`sonarjs/redundant-type-aliases` on `AgentProjectHooks`,
not created or modified by this phase). **Zero errors. Zero warnings in any
file created by P06.**

### `npx prettier --check "packages/agents/src/api/**/*.ts"` — exit 0

```
Checking formatting...
All matched files use Prettier code style!
prettier exit: 0
```

### Phase-spec greps

```
=== 1. plan-marker count (must be > 0) ===
total markers: 12

=== 2. NotYetImplemented count (must be > 0) ===
81

=== 3. V2/duplicate find (must print OK) ===
OK

=== 4. TODO/FIXME grep (must print OK) ===
OK
```

### Barrel sanity

```
export { createAgent } from './createAgent.js';
export { listProviders, listTools } from './discovery.js';
export * from './config-types.js';   # AgentConfig + handlers
export * from './event-types.js';     # AgentEvent, DoneReason, payloads
export * from './agent.js';           # Agent + sub-surface interfaces
```

- `Agent` interface in `agent.ts`: `git diff` empty (unchanged).
- `packages/agents/src/index.ts`: `git diff` empty (untouched).

## Structural Verification Checklist

- [x] All Agent methods + sub-surfaces stubbed (28 + 7 controls).
- [x] Compiles (`npm run typecheck` — monorepo exit 0).
- [x] No reverse-test hooks; no parallel versions.
- [x] `packages/agents/src/api/index.ts` exports the new public API sub-barrel
      (createAgent, listProviders, listTools + full type re-exports).
- [x] Existing `packages/agents/src/index.ts` low-level exports untouched.

## Verdict: PASS
