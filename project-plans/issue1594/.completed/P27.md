# P27 — App-service Subpath Contracts + command→API map (COMPLETE)

**Phase ID:** `PLAN-20260617-COREAPI.P27`
**Requirement:** REQ-021 (runtime-vs-app-service boundary + command→API map)
**Status:** PASS — T23/T24 GREEN, full agents `api/__tests__` suite GREEN (129 passed / 0 failed), typecheck 0 errors, eslint clean, no deferred markers, durable ops absent from `Agent`.

---

## 1. Singular specifier reconciliation applied

The LOCKED P09 boundary harness pins the **SINGULAR** specifier and resolves it
through the vitest workspace alias:

- `packages/agents/src/api/__tests__/command-api-map.ts` declared
  `APP_SERVICE_SUBPATH = '@vybestack/llxprt-code-agents/app-service.js'`
  (singular).
- `app-service-boundary.spec.ts` (T23/T24) asserts every `subpath` entry's
  `target === '@vybestack/llxprt-code-agents/app-service.js'`, dynamically
  `await import(entry.target)`, and `typeof mod[entry.exportName] === 'function'`.
- The vitest alias (`packages/agents/vitest.config.ts` `workspaceAliasPlugin`)
  maps `@vybestack/llxprt-code-agents/<x>.js` → `packages/agents/src/<x>.ts`.

**Decision:** I created the PRODUCTION module at the SINGULAR filename
`packages/agents/src/app-service.ts` and added the SINGULAR `./app-service.js`
package export. I IGNORED the plan prose's plural `app-services.js` /
`src/app-services/index.ts` *public* name. The implementation submodules are
still internally organized under `packages/agents/src/app-services/` (a directory
of small helpers) and re-exported from the singular `app-service.ts` barrel —
this keeps files small AND satisfies the locked specifier. The PUBLIC subpath is
singular `./app-service.js`.

Built-file resolution mirrors `./internals.js`:

```jsonc
"./app-service.js": {
  "types": "./dist/src/app-service.d.ts",
  "import": "./dist/src/app-service.js"
}
```

Verified: `node -e require('./packages/agents/dist/src/app-service.js')` exposes
all 11 functions + `COMMAND_API_MAP` (37 entries) + `APP_SERVICE_SUBPATH`.

---

## 2. The 11 exported functions + their real backing service (file:line)

All are `function`, behavior-real, typed input → typed output, standalone (no
live `Agent`). NO `unsupported`/`deferred`/`NotYetImplemented` Result for any.

| # | Export | Backing service (file:line) | What it does |
|---|--------|------------------------------|--------------|
| 1 | `saveCurrentProfile` | `ProfileManager.saveProfile` — `packages/settings/src/profiles/ProfileManager.ts:92` | Persists a provided `Profile` under a name to the profiles dir. |
| 2 | `listProfiles` | `ProfileManager.listProfiles` — `packages/settings/src/profiles/ProfileManager.ts:267` | Reads durable profile names. |
| 3 | `deleteProfile` | `ProfileManager.deleteProfile` — `packages/settings/src/profiles/ProfileManager.ts:287` | Deletes a durable profile file. |
| 4 | `addMcpServer` | `SettingsService.get/set('mcpServers')` — `packages/settings/src/settings/SettingsService.ts:56/63` (mirrors CLI `packages/cli/src/commands/mcp/add.ts:177-186`) | Inserts `record[name]=config` into the durable `mcpServers` key. |
| 5 | `removeMcpServer` | `SettingsService.get/set('mcpServers')` — same (mirrors CLI `remove.ts:24-34`) | Deletes `record[name]`; reports `removed` boolean. |
| 6 | `editMemory` | `MemoryTool.performAddMemoryEntry(text, path, fsAdapter)` static — `packages/tools/src/tools/memoryTool.ts:500` | Durable append to a memory file via a real `node:fs` writeFile adapter. |
| 7 | `manageSkills` | `SkillManager.getAllSkills/getSkills/setDisabledSkills` — `packages/core/src/skills/skillManager.ts:327/320/341` | list/enable/disable; durable disabled set persisted via `SettingsService('disabledSkills')`. |
| 8 | `manageExtensions` | `ExtensionLoader.getExtensions()` — `packages/core/src/utils/extensionLoader.ts:258` | list active extensions; durable disabled set persisted via `SettingsService('disabledExtensions')`. |
| 9 | `getAbout` | `SettingsService.getDiagnosticsData()` — `packages/settings/src/settings/SettingsService.ts:396` | About metadata (provider/model/profile + sandbox preference). |
| 10 | `mutateSettings` | `SettingsService.updateSettings(changes)` + `get(key)` — `packages/settings/src/settings/SettingsService.ts:260/56` | Applies a typed change set, returns persisted values. |
| 11 | `getDiagnostics` | `SettingsService.getDiagnosticsData()` — `packages/settings/src/settings/SettingsService.ts:396` | Diagnostics snapshot (provider/model/profile/providerSettings/ephemeralSettings/modelParams + sandbox). |

**Sandbox handling (getAbout/getDiagnostics):** the configured sandbox
preference is surfaced from `ephemeralSettings['sandbox']` (string passthrough,
or boolean→`'on'`/`'off'`). If it is not durably reachable, the value is `null`
— never fabricated. (Sandbox is not durably owned by the settings package, so
the configured preference SOURCE is surfaced honestly per the phase spec.)

**Helper (NOT one of the 11):** `listCliLocalCompletions()` returns the explicit
typed CLI-local completion classifications drawn from the canonical map, so the
completion boundary is documented in production and no completion entry is an
orphan (T24).

---

## 3. Canonical command→API map location + harness re-export (structural-identity guarantee)

- **Production canonical map:**
  `packages/agents/src/app-services/command-api-map.ts` exports
  `COMMAND_API_MAP: readonly CommandApiMapping[]`, `APP_SERVICE_SUBPATH`, and
  the `CommandApiKind` / `CommandApiMapping` types. Marked
  `@plan:PLAN-20260617-COREAPI.P27 @requirement:REQ-021`.
- **Public re-export:** the singular `./app-service.js` subpath re-exports the
  map (via `app-services/index.ts` → `app-service.ts`), so consumers get it
  publicly.
- **Harness now re-exports (no duplication):**
  `packages/agents/src/api/__tests__/command-api-map.ts` now does
  `export { COMMAND_API_MAP, APP_SERVICE_SUBPATH } from
  '@vybestack/llxprt-code-agents/app-service.js'` and
  `export type { CommandApiKind, CommandApiMapping } from
  '@vybestack/llxprt-code-agents/app-service.js'` — preserving the EXACT exported
  names/shape that `app-service-boundary.spec.ts` imports.

**Structural-identity guarantee:** the production map is byte-for-structure
identical to the original harness map — same 37 commands, same kinds, the
SINGULAR `@vybestack/llxprt-code-agents/app-service.js` target for every
`subpath` entry, the same 11 `exportName`s, same completion + cli-local entries.
All existing P09 assertions stay green (orphan-free, unique commands, durable
set classified `subpath`, every subpath targets the pinned specifier + names an
export, completions classified). `app-service-boundary.spec.ts` was NOT modified.

---

## 4. Behavioral round-trip tests added (no mock theater)

New consumer-facing spec `packages/agents/src/api/__tests__/app-service.spec.ts`
(marked `@plan:PLAN-20260617-COREAPI.P27 @requirement:REQ-021`). It imports ONLY
the public subpath + a relative test-fixtures helper
(`./helpers/app-service-fixtures.ts`, which lives under `helpers/` and is
EXCLUDED from the T17 consumer-facing deep-import scan — so the real backing
services can be constructed from their canonical modules without violating the
boundary guard). All tests construct REAL services and assert PERSISTED state:

1. **mcp add → get → remove round-trip** — real `SettingsService`; asserts
   `get('mcpServers')` reflects insert then deletion.
2. **removeMcpServer reports false for a missing server** — honest negative path.
3. **mutateSettings persists and reads back** — `updateSettings` then `get(key)`.
4. **editMemory write → read** — real temp file; asserts both facts appended.
5. **profiles save → list → delete** — real `ProfileManager` + temp profiles dir;
   asserts the on-disk `demo.json` payload + list membership before/after delete.
6. **manageSkills lists real discovered skills + persists disable** — a real
   `SkillManager` discovered from a seeded `.llxprt/skills/tester/SKILL.md`;
   asserts disabled flag flips and `disabledSkills` is persisted then cleared.
7. **manageExtensions lists real extensions + persists disable** — real loader;
   asserts `disabledExtensions` persisted and the re-listed extension shows
   `disabled: true`.
8. **getDiagnostics & getAbout reflect real settings** — provider/model/sandbox
   set on a real `SettingsService` are reflected in both outputs.
9. **exposes the canonical COMMAND_API_MAP publicly** — every subpath entry
   targets the pinned singular specifier.

---

## 5. Verification results

| Check | Result |
|-------|--------|
| Build (core, mcp, ide-integration, agents) | PASS |
| `npx vitest run --root packages/agents src/api/__tests__` | **17 files / 129 tests passed, 0 failed** (incl. T23/T24 + T17 boundary + 9 new P27 round-trips) |
| `npm run typecheck` | 0 errors |
| eslint (app-service.ts, app-services/*.ts, new specs, harness map) | 0 errors |
| `./app-service.js` subpath in package.json | OK (`types`+`import` → `dist/src/app-service.{d.ts,js}`) |
| Deferred-impl grep (TODO/FIXME/HACK/STUB/XXX/WIP/NotYetImplemented/unsupported/deferred) | OK (none) |
| Prose grep (in a real / for now / placeholder / not yet / will be) | OK (none) |
| Boundary grep (`mcpAdd|extensionEnable|memoryWrite|sandboxProfileSave` on `agent.ts`/api types) | boundary OK (absent) |
| No `packages/cli` / deep `dist` / `__tests__` imports in production | OK |
| `@plan:P27` + `@requirement:REQ-021` markers on every new production file | OK |

---

## 6. Trace: one durable command end-to-end

`/mcp add <name> <config>` (CLI touchpoint)
→ canonical map entry `{ command: '/mcp add', kind: 'subpath', target:
'@vybestack/llxprt-code-agents/app-service.js', exportName: 'addMcpServer' }`
→ public subpath `@vybestack/llxprt-code-agents/app-service.js` (singular) →
`app-service.ts` barrel → `app-services/index.ts` → `app-services/mcp-config.ts`
`addMcpServer({ settingsService, name, config })`
→ `readServers()` calls `SettingsService.get('mcpServers')`, inserts
`record[name] = config`, then `SettingsService.set('mcpServers', record)`
(`packages/settings/src/settings/SettingsService.ts`) — the SAME durable
`mcpServers` key the CLI persists (`packages/cli/src/commands/mcp/add.ts:177-186`).
Round-trip proven by test #1: add → `get('mcpServers')` shows the server →
`removeMcpServer` → `get('mcpServers')` no longer has the key.

The durable op lives ONLY in `app-service.ts` (the public subpath); it is NOT on
the live `Agent` runtime surface (boundary grep confirms `mcpAdd` etc. absent
from `agent.ts`/api types). #1595 can be a thin UI importing the public subpath
without deep imports, while `Agent` stays runtime-only.

**VERDICT: PASS.**
