Phase: P02a (Pseudocode Verification — deep review gate)
Plan ID: PLAN-20260617-COREAPI.P02a
Completed: 2026-06-17 13:40
Reviewer role: deepthinker (pseudocode-compliance gate)
Files Created: project-plans/issue1594/.completed/P02a.md
Files Modified: none (review-only; no pseudocode was edited)

================================================================================
## VERIFICATION COMMAND OUTPUTS
================================================================================

### Numbered-line count (phase command)
```
for f in createAgent config-adapter event-adapter switch-rebind tool-confirmation-merge dispose; do
  echo "== $f =="; grep -cE "^[0-9]+:" project-plans/issue1594/analysis/pseudocode/$f.md;
done
```
Output:
```
== createAgent ==
120
== config-adapter ==
49
== event-adapter ==
72
== switch-rebind ==
70
== tool-confirmation-merge ==
68
== dispose ==
42
```
All six files contain numbered pseudocode lines (non-empty). PASS.

### Grounding greps against REAL source (every cited API/event-payload)

GeminiEventType — exactly 21 variants confirmed (turn.ts:48-70). Payload shapes
verified by reading turn.ts:40-310:
- Content.value:string; Thought.value:ThoughtSummary; ToolCallRequest.value:ToolCallRequestInfo;
  ToolCallResponse.value:ToolCallResponseInfo; ToolCallConfirmation.value:ServerToolCallConfirmationDetails;
  UsageMetadata.value:{...}; ModelInfo.value:ModelInfo; SystemNotice.value:string;
  ChatCompressed.value:ChatCompressionInfo|null; Citation.value:string;
  ContextWindowWillOverflow.value:{estimatedRequestTokenCount,remainingTokenCount};
  StreamIdleTimeout.value:GeminiErrorEventValue; Error.value:GeminiErrorEventValue;
  Finished.value:{reason:FinishReason,...}  — ALL value-bearing variants DO carry `.value`. ✓
- GeminiErrorEventValue = { error: StructuredError } (turn.ts:93-95) => Error/StreamIdleTimeout
  read via `.value.error`. ✓
- FLAT variants (NO `.value`): Retry{type}, InvalidStream{type}, UserCancelled{type},
  MaxSessionTurns{type}, LoopDetected{type}. ✓
- ServerGeminiAgentExecutionStoppedEvent (turn.ts:274-279) = {type, reason, systemMessage?,
  contextCleared?} — FLAT, NO `.value`. ✓
- ServerGeminiAgentExecutionBlockedEvent (turn.ts:281-286) = {type, reason, systemMessage?,
  contextCleared?} — FLAT, NO `.value`. ✓
- Full ServerGeminiStreamEvent union (turn.ts:288-310) enumerated; all 21 present. ✓

AgenticLoopEvent union (agents/src/core/agenticLoop/types.ts:36-41) confirmed EXACTLY:
- { kind:'stream'; event: ServerGeminiStreamEvent }  (field is `.event`, NOT `.value`) ✓
- { kind:'tool_update'; toolCalls: ToolCall[] } ✓
- { kind:'tool_output'; callId: string; chunk: string } ✓
- { kind:'tools_complete'; completed: CompletedToolCall[] } ✓
- { kind:'awaiting_approval'; toolCalls: ToolCall[] } ✓

AgenticLoop (agents/src/core/agenticLoop/AgenticLoop.ts):
- `private readonly agentClient` (line 162-163); assigned in ctor (line 183); never re-resolved. ✓
- `async *run(message, signal, promptId?)` (line 270-273); cancellation via `while(!signal.aborted)`
  (line 283); throws on concurrent run (line 276). ✓
- NO `cancel`/`dispose` method exists (grep for `(cancel|dispose)(` method decl => none). ✓
- `finally { unsubscribe(); this.isRunning = false; }` (lines 302-304). ✓
- `sendMessageStream` driven off `this.agentClient` (line 381). ✓
- Per-turn scheduler: `config.getOrCreateScheduler(...)` (line 436); `config.disposeScheduler(sessionId)`
  in loop finally (line 552). ✓
- Emission sites: tools_complete (357), stream (388), tool_output (444), tool_update (455/467),
  awaiting_approval (457). Pseudocode citations (tool_update:455 / tool_output:444 / tools_complete:357)
  are EXACT. ✓
- Safe-denial: approvalHandler rejection => bus.respondToConfirmation(correlationId,
  ToolConfirmationOutcome.Cancel) (lines 247-250); no-handler => wireApprovalHandler no-op (lines 207-209). ✓

HistoryService accessor:
- AgentClientContract.getHistoryService(): HistoryService | null (clientContract.ts:54,73). ✓
- Config does NOT define its own getHistoryService; config.ts:264 and :376 CALL
  previousAgentClient.getHistoryService() / this.agentClient.getHistoryService() — i.e. it reads
  the service FROM the client. Confirms G2 (HistoryService lives on the AgentClient, not Config). ✓

switchActiveProvider (providers/runtime/providerSwitch.ts):
- export async function switchActiveProvider(providerName: string, options: ProviderSwitchOptions = {})
  (line 803-805). ✓
- ProviderSwitchOptions members: { autoOAuth?, preserveEphemerals?, skipModelDefaults?, addItem? }
  (lines 58-63) — NO `model` field. ✓
- Internal rebuild: initializeContentGeneratorConfigIfSupported(context.config) (line 833) ->
  config.initializeContentGeneratorConfig() (line 707). Model resolved internally via
  resolveModelToApply (line 417, used 467). ✓

setActiveModel (providers/runtime/providerMutations.ts:406):
- Calls config.setModel(modelName); does NOT call initializeContentGeneratorConfig (none in body
  406-470) — confirms model-only path requires explicit facade rebuild. ✓

config.initializeContentGeneratorConfig (config.ts:329): extractExistingState ->
transferHistoryToNewClient -> previousAgentClient.dispose() (rebuild + history transfer + prev dispose). ✓

CoreToolScheduler callbacks (coreToolScheduler.ts): outputUpdateHandler (96/189),
onAllToolCallsComplete (97/190), onToolCallsUpdate (98), setCallbacks (188). NO `onStatusUpdate`.
Repo-wide grep for `onStatusUpdate` => ZERO matches. ✓
- CoreToolScheduler.dispose() (line 201) -> this.confirmationCoordinator.dispose() (202). ✓
- ConfirmationCoordinator.dispose() (confirmation-coordinator.ts:170). ✓
- Coordinator throw scoped to !config.isInteractive() (confirmation-coordinator.ts ~320). ✓

createIsolatedRuntimeContext (providers/runtime/runtimeContextFactory.ts:449-451):
returns IsolatedRuntimeContextHandle. Handle (217-228) = { runtimeId, metadata, settingsService,
config, providerManager, oauthManager, activate(opts?):Promise<void>|void, cleanup():Promise<void>|void }. ✓
- activate is built by buildActivateClosure (309) returning an async closure (Promise<void>) =>
  MUST be awaited. ✓
- Builds its OWN sessionMessageBus = new MessageBus(...) (line 472) and passes it to
  resolveOAuthManager(sessionMessageBus,...) (476). Confirms B2 concern: the context-built OAuthManager
  binds to the context bus unless a messageBus is injected. ✓
- IsolatedRuntimeContextOptions (185-208) members TODAY: runtimeId?, metadata?, settingsService?,
  config?, model?, debugMode?, workspaceDir?, oauthManager?, prepare?, onCleanup?. There is NO
  `messageBus` field TODAY. The pseudocode honestly flags adding `messageBus?: MessageBus` as a
  P15 production seam (B2) — it does NOT misrepresent the current API. config? adoption supported. ✓

createAgentRuntimeState (core/runtime/AgentRuntimeState.ts:203): throws
RuntimeStateError(RUNTIME_ID_MISSING) when runtimeId absent (207-209). ✓

config.dispose() (config.ts:882-888): agentClient?.dispose() + mcpClientManager.stop() — matches the
dispose ownership table "partial today". ✓

LSP teardown: config.shutdownLspService() (configBase.ts:159) -> shutdownLsp (lspIntegration.ts:388).
No lspManager object. Config.dispose() does NOT call it today => NET-NEW wiring, exactly as dispose.md states. ✓

config.disposeScheduler defined on configBase.ts:129 (delegates schedulerSingleton.ts:365);
config.getOrCreateScheduler on config.ts:823. ✓

================================================================================
## MANDATORY SEMANTIC VERIFICATION CHECKLIST (7) — per file
================================================================================

### Q1. INPUTS/OUTPUTS/DEPENDENCIES explicit and real (injected, not stubbed)?

- createAgent.md: YES. Interface Contracts give CreateAgentInput{config:AgentConfig},
  CreateAgentOutput=Promise<Agent>, and a Dependencies block of REAL injected/imported symbols
  (createIsolatedRuntimeContext, MessageBusCtor, ConfigCtor, toConfigParameters, buildAgentClientFactory,
  createAgentRuntimeState, AgenticLoopCtor, buildAgent). Each is grounded above. No stubs. PASS.
- config-adapter.md: YES. AdapterInput{config:AgentConfig} -> AdapterOutput=ConfigParameters; deps are
  the REQ-002 CLASSIFICATION table + pure mappers. Pure function; no I/O. PASS.
- event-adapter.md: YES. AdapterInput{loopEvents:AsyncIterable<AgenticLoopEvent>} ->
  AsyncIterable<AgentEvent>; deps are pure projectors with input types pinned to real code
  (ToolCallRequestInfo / ToolCallResponseInfo / CompletedToolCall / ToolCall /
  ServerToolCallConfirmationDetails). PASS.
- switch-rebind.md: YES. SetProviderInput/SetModelInput/ApplyProfileInput -> Promise<void>; deps are
  the verified providers/runtime exports + config + resolveClient + rebuildLoop + loopHolder. All
  grounded. PASS.
- tool-confirmation-merge.md: YES. ConfirmRequestInput/RespondInput; deps messageBus,
  loopEvents (the SAME AgenticLoopEvent stream), confirmationCoordinator, pure projectors. PASS.
- dispose.md: YES. DisposeInput{ownership:OwnershipRecord} -> Promise<void>; Dependencies are the
  concrete resources createAgent recorded. PASS.

### Q2. Does each numbered line describe ONE algorithmic step (no hidden complexity)?

- createAgent.md: YES. Each line is a single ordered step (validate -> adapter -> Config ctor -> bus ->
  context -> activate -> provider/model/auth mutators -> initialize -> refreshAuth -> runtimeState ->
  bind post-auth client -> rebuildLoop -> assemble facade -> SessionStart). rebuildLoop is delegated to
  switch-rebind.md (single named routine), not inlined complexity. PASS.
- config-adapter.md: YES. Field-by-field 1:1 copies, classification loop, settings escape-hatch guard,
  freeze. Each line atomic. PASS.
- event-adapter.md: YES. Outer loop SWITCH on ev.kind, inner 21-variant SWITCH each one
  YIELD/state-set. makeDone/ensureDone are single helpers. PASS.
- switch-rebind.md: YES. rebuildLoop steps 10-27 atomic; setProvider/setModel/applyProfile each a clear
  capture->mutate->rebuild->assert->rebuildLoop sequence; model-param mutators single-line. PASS.
- tool-confirmation-merge.md: YES. subscribe handler, loopEvents SWITCH, respond method each atomic.
  PASS.
- dispose.md: YES. Idempotency guard, ordered awaited safe() calls, error aggregation, safe() helper.
  PASS.

### Q3. event-adapter: ALL 21 variants mapped reading payload from the CORRECT place; exactly-one done
for max-turns/context-overflow/loop-detected/error/BeforeAgent-block; Blocked NON-terminal; Stopped
terminal; idle-timeout terminal?

YES — fully grounded and correct:
- Outer `kind:'stream'` reads `ev.event` (line 33) — CORRECT (union field is `.event`, NOT `.value`). ✓
- All 21 GeminiEventType variants are present in mapStreamEvent (lines 212-244) and the decision table.
- Value-bearing variants read `.value`: Content(212), Thought(213), ToolCallRequest(214),
  ToolCallResponse(215), ToolCallConfirmation(216), UsageMetadata(217), ModelInfo(218),
  SystemNotice(219), ChatCompressed(220), Citation(221), ContextWindowWillOverflow(226-227 reads
  e.value.estimatedRequestTokenCount/remainingTokenCount), StreamIdleTimeout(232 reads e.value.error),
  Error(236 reads e.value.error), Finished(243-244 reads e.value / e.value.reason). ALL match real shapes. ✓
- FLAT no-payload variants handled without `.value`: Retry(222), InvalidStream(223),
  UserCancelled(238-239), MaxSessionTurns(231), LoopDetected(229-230). ✓
- The two FLAT stop/block variants read the event itself: AgentExecutionBlocked(240) and
  AgentExecutionStopped(241) pass `e` to toStopInfo (NOT e.value). The pinned toStopInfo contract
  (reads e.reason/e.systemMessage?/e.contextCleared?) matches turn.ts:274-286 EXACTLY. This was the
  top-risk DEFECT-1 and it is correctly fixed. ✓
- Exactly-one `done`: state.emittedDone tracked; makeDone sets it; the no-Finished terminal paths set
  state.pendingDoneReason and line 200-204 synthesizes the single terminal done at loop end:
  context-overflow(228), loop-detected(230), max-turns(231), error/idle(233,237). BeforeAgent block is
  the Error path (no Finished) -> error event + synthesized done{error}. UserCancelled emits
  done{aborted} inline and marks emittedDone. Finished/Stopped emit makeDone inline + emittedDone. No
  path can emit two `done`. ✓
- AgentExecutionBlocked is NON-terminal: yields hook-blocked, does NOT set emittedDone or
  pendingDoneReason, turn continues (240). ✓
- AgentExecutionStopped is TERMINAL: sets lastStop, yields makeDone(hook-stopped), emittedDone=true
  (241-242). ✓
- StreamIdleTimeout is TERMINAL: yields idle-timeout, sets pendingDoneReason='error', single done at
  loop end (232-233). ✓
PASS.

### Q4. switch-rebind: same-HistoryService reuse via a REAL accessor on the correct type; stripThoughts
referenced to the real Config transfer path; provider/model applied via REAL mechanisms?

YES:
- Same-HistoryService assertion: lines 38/56/75 ASSERT
  `newClient.getHistoryService() === prevHistoryService`, where both are read via
  AgentClient.getHistoryService() (clientContract.ts:54,73) — the REAL accessor on the CORRECT type
  (the AgentClient, NOT Config). prevHistoryService captured from the OLD client (lines 32/52/72)
  before the switch. G2 grounding is correct (config.ts:264,376 read it from the client). ✓
- stripThoughts normalization: correctly attributed to the EXISTING Config transfer path
  (initializeContentGeneratorConfig -> extractExistingState -> transferHistoryToNewClient), NOT a facade
  method (anti-pattern note + Notes). Verified behaviorally by T4f. The pseudocode does NOT invent a
  facade stripThoughts call. ✓
- Provider/model applied via REAL mechanisms: switchActiveProvider(provider) with the REAL 1-arg/options
  signature (NO model field) — model applied AFTER via setActiveModel(model) +
  config.initializeContentGeneratorConfig() (lines 33-36). switchActiveProvider rebuilds internally;
  setActiveModel does NOT (explicit rebuild required) — both grounded. applyProfileSnapshot rebuilds
  internally. Model-param mutators (setActiveModelParam/clearActiveModelParam/getActiveModelParams) are
  lazy, no rebuild (B7). ✓
- B1 loop reconstruction: rebuildLoop (steps 10-27) reconstructs AgenticLoop because the loop caches its
  client readonly; reattach happens INSIDE rebuildLoop on the new loop; cancellation via facade-owned
  AbortController (no loop cancel/dispose method). All grounded. ✓
PASS.

### Q5. tool-confirmation-merge: respond keyed on correlationId; tool-status from a REAL mechanism (not
onStatusUpdate); public Agent path uses AgenticLoop safe-denial; raw coordinator throw scoped to
power-user path?

YES:
- respondToConfirmation publishes TOOL_CONFIRMATION_RESPONSE keyed on correlationId/confirmationId
  (lines 87-89), NOT toolCallId. confirmationId(correlationId) and toolCallId kept distinct (lines
  22-24). dedup via `seen`; editor-modify retires the old correlationId (84-86). ✓
- tool-status sourced from the AgenticLoopEvent STREAM (kinds tool_update/tool_output/tools_complete,
  lines 49-65) — the SAME stream event-adapter.md consumes. The pseudocode EXPLICITLY states there is NO
  onStatusUpdate (grep => ZERO matches, which I independently confirmed) and that where a directly-owned
  scheduler is genuinely used, status comes ONLY from the REAL callbacks
  onToolCallsUpdate/outputUpdateHandler/onAllToolCallsComplete (coreToolScheduler.ts:96-98,188-191). No
  phantom API. ✓
- Public Agent path uses AgenticLoop safe-denial: lines 100-109 document that no-handler ASK_USER ->
  SAFE TOOL DENIAL and handler rejection -> ToolConfirmationOutcome.Cancel, owned by AgenticLoop
  (verified AgenticLoop.ts:207-209 no-op + 247-250 Cancel). This control surface never throws on the
  high-level path. ✓
- Raw coordinator throw scoped to the documented power-user `./internals.js` path only
  (!isInteractive(), confirmation-coordinator.ts ~320) — correctly NOT the default Agent surface. ✓
PASS.

### Q6. dispose: idempotent, awaited, error-collecting, ownership-table-driven; active-run cancellation
via a real mechanism; scheduler/coordinator scoping correct; LSP via config.shutdownLspService()?

YES:
- Idempotent: ownership.disposed guard (11-13). ✓
- Awaited + error-collecting: safe(errors, fn) awaits each fn and pushes errors (110-113); aggregate
  thrown at end (100-101). ✓
- Ownership-table-driven: the Resource-ownership table drives T13; each row tied to a teardown step. ✓
- Active-run cancellation via a REAL mechanism: ownership.activeRunController.abort() (line 30) — the
  facade-owned AbortController whose .signal was passed to loop.run(message, signal). Correctly NOT a
  loop method (AgenticLoop has none; self-cleans in run finally 302-304). ✓
- Scheduler/coordinator scoping CORRECT: per-turn loop schedulers are AgenticLoop-owned/transient
  (config.getOrCreateScheduler @436 / config.disposeScheduler @552 in loop finally) and dispose() does
  NOT touch them (lines 40-47 + table). ONLY the conditional injected-factory scheduler/coordinator the
  facade retained is facade-disposed (T19, guarded existence checks). This reconciles exactly with
  tool-confirmation-merge.md D1 (facade owns no stable scheduler). ✓
- LSP teardown via config.shutdownLspService() (line 70) — grounded (configBase.ts:159 -> shutdownLsp
  lspIntegration.ts:388); flagged NET-NEW because config.dispose() does not call it today; no lspManager
  object invented. ✓
- runtimeHandle.cleanup() unregisters the runtime context (line 55), OAuth torn down within (90-92
  defensive). config.dispose() disposes agentClient + mcp (line 60). ✓
PASS.

### Q7. Are anti-pattern warnings concrete (no hardcoded returns, no test doubles in prod, no invented
APIs)?

YES, across all six files the [ERROR]/[OK] tables are concrete and aimed at the exact real-code traps:
- createAgent.md: do-not-use bare createHeadlessProviderManager; do-not build 2 SettingsServices;
  do-not call createAgentRuntimeState without runtimeId; do-not cache AgentClient; do-not bind transient
  pre-auth client; do-not refreshAuth before context active; do-not pass model to switchActiveProvider.
- config-adapter.md: do-not drop CLI field into settings; do-not invent ConfigParameters fields; do-not
  mutate input; do-not JSON-stringify callbacks.
- event-adapter.md: do-not emit done at inner stream end; do-not emit >1 done; do-not leak
  ToolCallRequestInfo/ResponseInfo; do-not fold Blocked into done; do-not assume Finished exists; plus
  the pinned toStopInfo(e) NOT e.value rule.
- switch-rebind.md: do-not pass config/model to switchActiveProvider; do-not invent switch-refresh
  helper; do-not double-rebuild after switchActiveProvider; do-not resetChat; do-not copy history; do-not
  cache old client; do-not keep OLD AgenticLoop; do-not rebuild CG on model-param change.
- tool-confirmation-merge.md: do-not key on tool name; do-not collapse the two response paths; do-not
  leak raw scheduler status union; do-not allow >1 response; do-not subscribe to a non-existent
  status-update method (explicit ZERO-matches warning).
- dispose.md: do-not dispose caller-supplied resources; do-not fire-and-forget; do-not throw if already
  disposed; do-not leave bus subscriptions attached.
No hardcoded returns, no test doubles in production, no invented APIs anywhere. PASS.

================================================================================
## HOLISTIC FUNCTIONALITY ASSESSMENT
================================================================================

### Algorithm summaries (in my own words)

1. createAgent: Validate AgentConfig (zod). Build ConfigParameters via the adapter, inject
   agentClientFactory (+ optionally wrap a caller toolSchedulerFactory). Construct Config, then ONE
   shared MessageBus. Build a SHARED runtime context via createIsolatedRuntimeContext (adopting our
   Config + SettingsService, threading the bus via the P15 seam so the context OAuthManager binds to
   it), then `await handle.activate()` so the providers runtime registry resolves THESE instances.
   Apply initial provider/model/auth through REAL runtime mutators (switchActiveProvider rebuilds
   internally; model-only applied via setActiveModel + explicit initializeContentGeneratorConfig). Run
   config.initialize({messageBus}) (transient pre-auth client — NOT bound), then refreshAuth (real
   post-auth client, history transferred, prev disposed). Create runtimeState (runtimeId REQUIRED). Bind
   the POST-auth client. Build the first AgenticLoop via the shared rebuildLoop routine. Assemble the
   facade with an ownership record. Fire SessionStart. Return agent.

2. config-adapter: Pure translation AgentConfig -> ConfigParameters. Copy identity/context/tools/MCP/
   approval/policy; map the REQ-002 classification table's typed fields 1:1; never place sub-surface
   concerns here; merge the UNSTABLE settings escape hatch but THROW if it shadows a typed target;
   callbacks are intentionally excluded; return a frozen object.

3. event-adapter: Consume the AgenticLoopEvent stream; map kind:'stream' inner ServerGeminiStreamEvent
   through the 21-variant table (reading each payload from the correct place: value-bearing from
   `.value`, the two FLAT stop/block variants and the no-payload variants from the event itself); surface
   tool_update/tool_output/tools_complete/awaiting_approval; and guarantee EXACTLY ONE terminal `done`
   (synthesized at loop end when no Finished was emitted: max-turns / context-overflow / loop-detected /
   error/idle / BeforeAgent-block). Blocked is non-terminal; Stopped/idle-timeout are terminal.

4. switch-rebind: For setProvider/setModel/applyProfile — capture the OLD client's HistoryService, run
   the REAL runtime mutator (switchActiveProvider/applyProfileSnapshot rebuild internally; setActiveModel
   needs an explicit config.initializeContentGeneratorConfig), assert the NEW client reuses the SAME
   HistoryService by reference and is a different client, then rebuildLoop so the next run drives the new
   client. Model-param mutators are lazy (no rebuild). rebuildLoop (shared with createAgent) aborts the
   facade AbortController, unsubscribes facade-recorded subscriptions, builds a fresh AgenticLoop bound
   to config.getAgentClient(), and reattaches subscriptions.

5. tool-confirmation-merge: Subscribe to TOOL_CONFIRMATION_REQUEST and project to ToolConfirmation
   (confirmationId=correlationId, toolCallId distinct), dedup, emit + fire callback. Source tool status/
   output from the SAME AgenticLoopEvent stream (tool_update/tool_output/tools_complete). respond by
   publishing TOOL_CONFIRMATION_RESPONSE keyed on correlationId; editor-modify retires the old id.
   No-handler/handler-rejection safe-denial is owned by AgenticLoop; the coordinator throw is the raw
   internals power-user path only.

6. dispose: Idempotent, ordered, awaited teardown driven by the ownership table: SessionEnd hook ->
   abort the facade AbortController -> conditionally dispose an injected-factory scheduler/coordinator ->
   unsubscribe bus subs -> runtimeHandle.cleanup() -> config.dispose() -> config.shutdownLspService()
   (NET-NEW) -> extensions/session-locks (NET-NEW) -> defensive OAuth dispose. Collect errors and throw
   an aggregate; never swallow.

### Cross-file consistency

- Client resolution <-> reattach <-> teardown are consistent: createAgent binds the POST-auth client and
  builds the first loop via rebuildLoop; switch-rebind reuses the IDENTICAL rebuildLoop routine and the
  same RebuildLoopDeps shape after every client rebind; dispose tears down the SAME runtime context
  (runtimeHandle.cleanup), config (config.dispose), bus subs, and active-run controller. The loop/
  controller live in mutable slots (loopHolder + activeRunController) shared by all three. No drift.
- Per-run cancellation mechanism is consistent everywhere: a facade-owned AbortController whose .signal
  is passed to AgenticLoop.run(); abort in rebuildLoop (switch-rebind) and in dispose; AgenticLoop has NO
  cancel/dispose and self-cleans in run()'s finally. createAgent.md, switch-rebind.md, and dispose.md all
  describe this identically and it matches AgenticLoop.ts:270-305.
- Tool-status source is consistent between tool-confirmation-merge.md and event-adapter.md: BOTH consume
  the SAME AgenticLoopEvent stream (tool_update/tool_output/tools_complete); neither invents an
  onStatusUpdate; the facade owns no stable scheduler.
- Scheduler ownership is consistent between dispose.md and tool-confirmation-merge.md: per-turn schedulers
  are AgenticLoop-owned/transient (config.getOrCreateScheduler/disposeScheduler); the facade disposes ONLY
  the conditional injected-factory instance (T19). The dispose ownership table and the
  tool-confirmation D1 note agree exactly.
- HistoryService accessor consistent: switch-rebind reads it from the AgentClient (G2), matching the real
  contract and how Config itself reads it internally.

### Remaining ambiguity / wrong-symbol / wrong-signature / wrong-payload defects

NONE that block implementation. Every cited symbol, signature, line reference, and event-payload shape
was grounded against real source and matched:
- The previously most-dangerous risk — AgentExecutionStopped/Blocked payload shape — is CORRECT: both are
  FLAT (turn.ts:274-286), and event-adapter.md lines 240-241 pass `e` (NOT e.value) to toStopInfo, with an
  explicit pinned note. Value-bearing variants (Error/StreamIdleTimeout via .value.error; Finished via
  .value.reason; ContextWindowWillOverflow via .value.*; etc.) all read `.value`. No wrong-payload defect.
- switchActiveProvider signature (no model field), setActiveModel (no internal rebuild),
  getHistoryService location (AgentClient not Config), CoreToolScheduler callbacks (no onStatusUpdate),
  config.shutdownLspService (no lspManager), AgenticLoop run/no-cancel/getOrCreateScheduler/disposeScheduler
  — all match. No wrong-symbol/wrong-signature defect.
- The single production change the pseudocode depends on (adding `messageBus?: MessageBus` to
  IsolatedRuntimeContextOptions) is HONESTLY disclosed as a P15 seam, not presented as already existing.
  IsolatedRuntimeContextOptions today has no messageBus field — the pseudocode states this explicitly and
  scopes the change to P15. This is a planned, bounded production edit, not an invented API, so it does not
  constitute a wrong-symbol defect; an impl worker is correctly instructed to ADD it (and given a
  conforming fallback). I note this so P15 must actually implement that seam for the B2 single-bus
  invariant to hold.

The six algorithms are unambiguous, complete, mutually consistent, and every cited API/event-payload
exists and matches on the type it is used on.

Verdict: PASS
