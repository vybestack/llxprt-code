# P23a — Verify Hooks/Lifecycle + Scheduler Factory + Sandbox (REQ-015/REQ-006/REQ-016/REQ-002) — PASS

Plan: PLAN-20260617-COREAPI · Phase 23a · Requirements REQ-015, REQ-006, REQ-016, REQ-002
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**

## Per-axis results

1. **Static gates** — PASS. @plan:P23 + correct @requirement tags on all four changed
   production files (control/hooks.ts REQ-015; agentImpl.ts REQ-015/REQ-016;
   agentBootstrap.ts REQ-006/REQ-016; createAgent.ts REQ-006/REQ-016/REQ-015). No
   REQ-002 marker in changed files because sandbox was already routed through existing
   config adapter/schema plumbing and no new sandbox surface was added (accepted).
   Deferred-impl grep clean; no production `__tests__` import; hooks.ts strict (no
   `as any`/`: any`/`!`-abuse); SafeHookControl fully removed (src + dist). typecheck 0.

2. **Tests** — PASS. Rebuild chain core→mcp→ide-integration→agents exit 0. Targeted
   (hooks/scheduler-factory/sandbox-boundary) **7/7 pass**. Full agents api suite
   **110 passed / 10 failed**; the 10 are exactly the later-phase REDs: disposal.spec.ts
   (9, P24 — clean assertion failures, no crashes) + app-service-boundary.spec.ts
   (1, P27 — missing src/app-service.js). hooks.spec.ts + scheduler-factory.spec.ts
   moved OUT of the failing set. No regressions.

3. **Anti-mock-theater** — PASS. hooks.ts triggerSessionStart/End call REAL core
   triggers (lifecycleHookTriggers.ts fireSessionStart/EndEvent via config.getHookSystem);
   bus-mediated observation subscribes to HOOK_EXECUTION_REQUEST/RESPONSE and correlates
   by correlationId (hooks.ts:182-216); inputs/outputs from live session/cwd/trigger
   result — no test-literal branching. Single-shared-MessageBus invariant holds
   (createAgent constructs one bus and threads it to runtime/config/loop/facade/HookControl;
   HookControl instantiates none). T19 teardown disposes the genuine injected-factory
   AgentSchedulerHandle (real `disposed` boolean, asserted directly — not a spy); caller
   factory function never disposed.

4. **Scope boundary** — PASS. T19 slice did NOT overreach into P24: no AggregateDisposeError,
   no ordered teardown, no probe-readable injectedFactoryScheduler/confirmationCoordinator
   fields, no SessionEnd-on-dispose. disposal.spec.ts stays RED for the right reasons
   (clean assertions awaiting P24 wiring, including the messageBus pre-count `-1>=0`
   probe). Respects dispose.md lines 40-47 (only facade-retained injected-factory handles
   disposed; per-turn loop schedulers remain AgenticLoop-owned). Fixture extension
   (tool-call-then-answer.jsonl +3rd turn) is additive and safe — single-turn consumers
   (core-tools, disposal, scheduler-factory) unaffected; no consumer asserts exact turn count.

5. **Sandbox decision** — PASS (accepted). Independent grep of __tests__ found NO
   sandbox-status accessor expectation. Public Agent interface has no sandbox status
   surface; AgentConfig.sandbox routes via config-types.ts:174 → config-schema.ts:189 →
   agentConfig.adapter.ts:86. Adding an untested accessor would be speculative; the 23a
   checklist "status reported" clause is a plan/checklist mismatch to be driven by a
   future locked RED spec if desired. T18e green (accept + first turn + recreate path).

## Semantic checklist (23a five questions)
1. onHookExecution observes a real firing — YES (observers + lifecycle triggers + bus correlation; hooks.spec 4/4).
2. SessionStart (create) + SessionEnd fire — YES for scoped behavior (createAgent fires real triggerSessionStart; explicit triggerSessionEnd green). SessionEnd-on-dispose is P24.
3. save_memory/updateSystemInstruction refresh reflected next turn — YES (T15c green; exercises updateSystemInstruction per the locked contract).
4. Injected scheduler factory used + torn down; factory fn not disposed — YES (real handle, disposed=true, count unchanged).
5. Sandbox routes from first turn — YES; status accessor intentionally absent (no RED test), accepted under TDD discipline.

## Non-blocking observations (not defects)
- hooks.spec.ts T15c prose mentions save_memory; executable assertions use
  updateSystemInstruction after a read_file tool turn — inherited locked-test scope.
- Agent.dispose does not yet fire SessionEnd — explicitly deferred to P24; disposal
  specs correctly remain RED.

Status: **PASS** — P23 complete. Proceed to P24 (full dispose/teardown).
