# Phase 17a Complete — Tools/Approval/Loop Verification

Phase ID: `PLAN-20260617-COREAPI.P17a`
Reviewer: architect (Opus) | Coordinator-corroborated
Prerequisite: P17 PASS (`.completed/P17.md` present)

## Verdict: PASS

Pseudocode-compliance + semantic review of the public Agent tools/approval/confirmation-merge surface against `analysis/pseudocode/tool-confirmation-merge.md` (REQ-006, REQ-007). All contractual invariants hold with file:line evidence.

## Files reviewed
- `packages/agents/src/api/control/toolControl.ts` (primary merge surface)
- `packages/agents/src/api/confirmationForcing.ts` (confirmation-forcing seam)
- `packages/agents/src/api/createAgent.ts` (default scheduler factory + bus/approval/display wiring)
- `packages/agents/src/api/agentImpl.ts` (tools wiring + chat/stream delegation)
- `packages/agents/src/api/eventAdapter.ts` (loop/stream → public AgentEvent projection)
- `packages/agents/src/scheduler/status-transitions.ts` (correlationId propagation)

## Semantic checklist (all PASS, with evidence)
1. **Correlation** — respondToConfirmation keys on confirmationId/correlationId, never tool name. correlationId originates as randomUUID() in setupConfirmationPrompt (confirmation-coordinator.ts:350), flows onto the awaiting_approval ToolCall (status-transitions.ts:159-185), surfaces as confirmationId (eventAdapter.ts:147-157), routes back via pendingConfirmations.get(correlationId)→callId (confirmation-coordinator.ts:402). T21-safe.
2. **Dedup** — `seen` Set keyed by confirmationId (toolControl.ts:198-200); duplicate REQUEST dropped, no double-emit/double-respond.
3. **Modify re-key** — decision===ModifyWithEditor deletes confirmationId from seen (toolControl.ts:152-155); coordinator mints newCorrelationId + registers old as stale.
4. **B7 safe denial** — no-handler → no-op wire (AgenticLoop.ts:208); handler-rejection → Cancel (AgenticLoop.ts:241-247); public chat()/stream() delegate to loop.run() and NEVER throw. Coordinator non-interactive throw (confirmation-coordinator.ts:336) fenced off by params.interactive=true (createAgent.ts:108) — reachable only via raw ./internals.js. Confirmed by T3/T11.
5. **Dual path** — projectConfirmationFromToolCall (coordinator/interactive) vs projectConfirmationFromDetails (raw a2a stream) are distinct, not conflated (T2b).
6. **Loop delegation** — multi-turn loop is AgenticLoop.run→runTurn→scheduleAndAwait→buildNextMessage (AgenticLoop.ts:271-360); facade only adapts the event stream, does not re-implement scheduling/continuation.
7. **Confirmation field sourcing** — real bus ToolConfirmationRequest = {type, toolCall, correlationId, serverName?} (no top-level callId/name/details); impl correctly sources details from tc.confirmationDetails and maps confirmationId=tc.correlationId, toolCallId=tc.request.callId (eventAdapter.ts:147-157). T3c satisfied.
8. **Layering** — denied→isError projection (cancelled+Cancel⇒isError) in PUBLIC eventAdapter.projectToolResult (eventAdapter.ts:70-77); AgenticLoop.ts:357 yields raw unmutated CompletedToolCall[]. Integration tests 29/29 — shared event not mutated.
9. **Confirmation-forcing seam** — clean Proxy-based seam (Reflect.get + typed returns, no any-abuse); priority-4.0 ASK rule + real info-variant details; produces BOTH the published REQUEST and the awaiting_approval ToolCall carrying confirmationDetails (T3c). Scoped to tool build(); does not corrupt non-tool turns.
10. **Standards** — typecheck exit 0; eslint exit 0 (6 files); @plan:P17 + @requirement:REQ-006/REQ-007 + @pseudocode markers present (toolControl.ts 8 @pseudocode refs); zero deferred-impl language; no any/non-null-assertion abuse; public outputs frozen (list/getModelParams).

## End-to-end trace (verified)
loop.run delegation (agentImpl.ts:233) → injectConfirmationForcingPolicy ASK rule (confirmationForcing.ts:62-72) → shouldConfirmExecute truthy details → setupConfirmationPrompt mints correlationId + publishes TOOL_CONFIRMATION_REQUEST + setAwaitingApproval (confirmation-coordinator.ts:350-380) → buildAwaitingApprovalTransition stamps tc.correlationId (status-transitions.ts:159-185) → projectConfirmationFromToolCall public confirmation with details (eventAdapter.ts:147-157) → tools.notifyConfirmation dedup+callbacks (toolControl.ts:198-209) → respondToConfirmation publishes RESPONSE by correlationId (toolControl.ts:147-156) → coordinator pendingConfirmations.get(correlationId)→callId (confirmation-coordinator.ts:402) → handleApproval→attemptExecution → tools_complete (AgenticLoop.ts:357) → projectToolResult tool-result (eventAdapter.ts:430-435) → buildNextMessage functionResponse continuation → exactly one terminal done.

## Independent verification (architect-run, coordinator-corroborated)
- typecheck: exit 0
- eslint (6 files): exit 0, zero findings
- core-tools.spec.ts: 8/8 passed
- T2p property: deterministic across 3 runs
- agenticLoop.integration.test.ts: 29/29 (no regression)
- deferred-impl grep: no matches
- @pseudocode markers: present (8 refs in toolControl.ts)

## Non-blocking observations (informational, no action)
- Steps 10-67 realized as a stream-tap (AgentImpl.stream forwards mapLoopStream events into ToolControl.notify*) — honors pin D1 (same stream the eventAdapter consumes) and the "no scheduler status-update subscription" anti-pattern. Faithful realization.
- Pseudocode bus fields msg.callId/name/details (steps 20-25) don't exist on the real ToolConfirmationRequest type; impl sources details from the awaiting_approval ToolCall projection — correct, necessary deviation (already documented in P17.md).
- ToolDecision = ToolConfirmationOutcome (agent.ts:125) vs pseudocode "approve|deny|modify" string — type-correct, behavior-consistent.
