# P22a — Verify MCP Control + Discovery Gate + IDE (REQ-013, REQ-014) — PASS

Plan: PLAN-20260617-COREAPI · Phase 22a · Requirements REQ-013, REQ-014
Reviewer: deepthinker (independent verification) · VERDICT: **PASS**

## Targeted specs
12 passed / 0 failed. `npx vitest run --root packages/agents src/api/__tests__/mcp-discovery.spec.ts src/api/__tests__/ide.spec.ts` →
ide.spec.ts 5/5; mcp-discovery.spec.ts 7/7. T12, T12b, T20, T15 all GREEN.

## Full api suite (no regression)
105 passed / 15 failed (12 files passed / 4 failed). The 15 failures are EXACTLY the
later-phase-owned RED baseline: disposal.spec.ts 9 (P24), hooks.spec.ts 4 (P23),
scheduler-factory.spec.ts 1 (P23), app-service-boundary.spec.ts 1 (P27). No P22-owned
failures or regressions. Green: boundary (T17), provider-bootstrap, event-characterization,
core-tools, switch-context, core-history, auth-profiles, core-conversation, generate.

## Per-axis results
1. **Schema decision** — PASS. config-schema.ts McpServerConfigSchema is a genuine
   structural z.object across stdio/url/http/websocket + metadata + OAuth fields (NOT
   z.any()/weakened); boundary widened to z.ZodType<MCPServerConfig> so the public
   AgentConfig.mcpServers type stays meaningful. No P03/P14 regression.
2. **Seam decision** — PASS. Shipped env-driven test doubles modeled on FakeProvider:
   fakeMcpDiscovery.ts (LLXPRT_FAKE_MCP) drives the REAL ToolRegistry + REAL
   MCPDiscoveryState machine + real failure recording + whenDiscoverySettled();
   fake-ide-environment.ts (LLXPRT_FAKE_IDE) feeds REAL IdeControl, else detectIdeFromEnv().
   No production import from __tests__. Behavioral assertions in the locked P12 specs
   unchanged.
3. **Gate correctness** — PASS. Default awaits manager.whenDiscoverySettled() before the
   model turn; mcpDiscovery:'skip' opts out; chat() re-streams with 'skip' to avoid
   double-gating; discovery failure → AgentError{code:'mcp_discovery_failed'} surfaced
   through the buffered AgentResult.error.code AND exactly one stream done{reason:'error'};
   mcp.status()/discoveryState()/listTools() remain callable while pending.
4. **No mock theater** — PASS. MCP methods delegate to the injected manager + tool
   registry; IDE reads fixture-or-detectIdeFromEnv; editor callbacks fire via the shared
   holder. Fake MCP tool execute() is intentionally inert (P22 requires discovery/listing/
   gating, not fake tool execution) — not a canned public-API return.
5. **Static gates** — PASS. typecheck 0; eslint 0 on all changed production files;
   deferred-impl scans clean on mcpControl.ts/ideControl.ts; P22 + REQ markers present on
   all changed production files.
6. **Scope** — PASS (clean). Durable MCP add/remove NOT implemented on Agent (deferred to
   P27); no slash-command parser on Agent; no P23+/P24/P27 leakage.

## Files verified
- packages/agents/src/api/control/mcpControl.ts (REQ-013)
- packages/agents/src/api/control/ideControl.ts (REQ-014)
- packages/agents/src/api/agentImpl.ts (gate + buildMcpControl/buildIdeControl + listTools)
- packages/agents/src/api/config-schema.ts (structural MCP schema)
- packages/mcp/src/client/mcp-client-manager.ts (fake discovery branch + whenDiscoverySettled)
- packages/mcp/src/fake/fakeMcpDiscovery.ts (shipped MCP discovery test double)
- packages/ide-integration/src/ide/fake-ide-environment.ts (shipped IDE env test double)

Status: **PASS** — P22 complete. Proceed to P23 (hooks/lifecycle + scheduler factory + sandbox).
