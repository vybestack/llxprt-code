# Phase 06a: Stubs Verification — Completion Marker

**Phase ID:** `PLAN-20260617-COREAPI.P06a`
**Reviewer role:** typescriptreviewer (skeptical verification gate)
**Date:** 2026-06-17
**Branch:** issue1594

---

## 1. Verification Command Outputs

### `npm run typecheck` (monorepo)

```
> @vybestack/llxprt-code-core@0.10.0 typecheck   -> tsc --noEmit
> @vybestack/llxprt-code-lsp@0.10.0 typecheck    -> tsc --noEmit
> @vybestack/llxprt-code-providers@0.10.0 typecheck -> tsc --noEmit
> @vybestack/llxprt-code-agents@0.10.0 typecheck -> tsc --noEmit
> @vybestack/llxprt-code@0.10.0 typecheck        -> tsc --noEmit
> @vybestack/llxprt-code-a2a-server@0.10.0 typecheck -> tsc --noEmit
> @vybestack/llxprt-code-test-utils@0.10.0 typecheck -> tsc --noEmit
Exit Code: 0
```

PASS — exit 0, all workspaces clean.

### `npm run typecheck --workspace @vybestack/llxprt-code-agents` (barrel/collision check)

```
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit
===EXIT:0===
```

PASS — exit 0. No barrel collision / no duplicate-symbol error (a duplicate `Agent`
symbol or unimplemented interface member would have errored here).

### `npm run lint --workspace @vybestack/llxprt-code-agents`

```
✖ 138 problems (0 errors, 138 warnings)
===EXIT:0===
```

Filtered to `src/api/`:

```
packages/agents/src/api/config-types.ts
  52:13  warning  Remove this redundant type alias and replace its occurrences with "AgentHooks"  sonarjs/redundant-type-aliases
```

PASS — lint exit code 0. ZERO ERRORS. The ONLY warning in `packages/agents/src/api/`
is the single pre-existing P03 `sonarjs/redundant-type-aliases` warning in
`config-types.ts:52` — explicitly listed as acceptable by the phase spec. All other
138 warnings are in pre-existing files elsewhere (core/, compression/, scheduler/,
tools/), not in the P06 API surface.

### `npx prettier --check "packages/agents/src/api/**/*.ts"`

```
Checking formatting...
All matched files use Prettier code style!
===EXIT:0===
```

PASS — all formatted.

### `grep -rc "@plan:PLAN-20260617-COREAPI.P06" packages/agents/src/api/`

```
agent.ts:0                       (P05 interface — correctly NOT a P06 file)
config-classification.ts:0       (P03)
config-types.ts:0                (P03)
config-schema.ts:0               (P04)
event-types.ts:0                 (P03/P04)
createAgent.ts:1
discovery.ts:1
index.ts:1
event-schema.ts:0                (P04)
agentImpl.ts:1
control/authControl.ts:1
control/ideControl.ts:1
control/hookControl.ts:1
control/sessionControl.ts:1
control/authKeysControl.ts:1
control/mcpControl.ts:1
control/toolControl.ts:1
control/profilesControl.ts:1
```

PASS — every new P06 artifact (createAgent, agentImpl, discovery, index, all 8
control classes) carries the `@plan:PLAN-20260617-COREAPI.P06` marker. Pre-existing
P03/P04/P05 files correctly carry their own (non-P06) markers.

### `grep -rn "NotYetImplemented" packages/agents/src/api/ | wc -l`

```
81
```

PASS — 81 occurrences (>0). Stubs present.

### `find packages/agents/src/api -name "*V2*" -o -name "*New*" -o -name "*Copy*"`

```
(empty)
```

PASS — no parallel/duplicate files.

### `grep -rnE "(TODO|FIXME|HACK|XXX)" packages/agents/src/api/ | grep -v ".spec.ts"`

```
(empty)
```

PASS — no TODO/FIXME/HACK/XXX.

### `grep -nE "export (interface|class) Agent\b" packages/agents/src/api/agent.ts`

```
321:export interface Agent {
```

PASS — `Agent` is an `export interface`, NOT a class. The verified P05 interface is
preserved. (Note: the `06-stubs.md` "Files to Create" prose mentions an "`Agent`
class"; the authoritative ground truth — the verified P05 interface and the task's
explicit requirement — is that `agent.ts` retains `export interface Agent` and the
implementation lives in a separate `AgentImpl` class. The artifacts correctly follow
the interface-preservation requirement.)

### `grep -rnE "class AgentImpl implements Agent" packages/agents/src/api/agentImpl.ts`

```
agentImpl.ts:32:export class AgentImpl implements Agent {
```

PASS — the implementation class exists and `implements Agent`.

### `git diff --stat packages/agents/src/index.ts`

```
(empty)
```

PASS — the PACKAGE top-level barrel is untouched (P07 handles package exports
additively). `git status --short` confirms only the new `?? packages/agents/src/api/`
tree is added; no existing files modified.

### Reverse-test hook scan

```
grep -rn "toThrow('NotYetImplemented')|expect.*not\.toThrow" packages/agents/src/api/
-> OK (none)

grep -rn "export.*NotYetImplemented | NotYetImplemented =" packages/agents/src/api/
-> OK (only thrown inline)
```

PASS — `NotYetImplemented` only appears as `throw new Error('NotYetImplemented')`
inside method bodies. It is NOT exported as a sentinel/property/constant, and nothing
asserts against it.

### Strict-TS scan (`any` / `as`)

```
grep -rnE ":\s*any\b|<any>|as any" packages/agents/src/api/  -> OK (no any)
grep -rnE "\bas\s+[A-Z]" ... | grep -v "as const"            -> OK (no as-type-assert)
```

### Control directory listing (exactly 8)

```
authControl.ts  authKeysControl.ts  hookControl.ts  ideControl.ts
mcpControl.ts   profilesControl.ts  sessionControl.ts  toolControl.ts
```

### eslint-disable scan

```
agentImpl.ts:47:  // eslint-disable-next-line require-yield, sonarjs/generator-without-yield
```

PASS — the sole eslint-disable is the acceptable one on the throwing `stream`
async-generator stub, exactly as permitted by the phase spec.

---

## 2. Structural / Semantic Checklist

### Q1 — Are ALL Agent methods + sub-surfaces stubbed?

**YES.** `AgentImpl implements Agent` and TypeScript (`tsc --noEmit`, exit 0) confirms
every interface member is implemented; an omission would error. Verified explicitly:

Top-level Agent methods present in `agentImpl.ts`: `chat`, `stream`, `getProvider`,
`setProvider`, `getProviderStatus`, `getModel`, `setModel`, `getCurrentSequenceModel`,
`getModelParams`, `setModelParam`, `clearModelParam`, `getUserTier`, `getHistory`,
`setHistory`, `addHistory`, `restoreHistory`, `resetChat`, `updateSystemInstruction`,
`addDirectoryContext`, `compress`, `getStats`, `onStats`, `generate`, `generateJson`,
`generateEmbedding`, `listProviders`, `listTools`, `dispose` — all present, all throw
`NotYetImplemented`.

All 7 sub-surface control classes implement their full interfaces:
- `ToolControl implements AgentToolControl` — list, setEnabled, onConfirmationRequest, respondToConfirmation, onToolUpdate, setEditorCallbacks ✓
- `McpControl implements AgentMcpControl` — listServers, status, toolsByServer, auth, discoveryState, refresh ✓
- `AuthControl implements AgentAuthControl` — login, logout, status, enableOAuth, disableOAuth, listBuckets, switchBucket, mcpLogin, setBaseUrl, plus nested `readonly keys` ✓
- `AuthKeysControl implements AgentAuthKeysControl` — list, save, use, delete, setRaw, setKeyFile ✓
- `IdeControl implements AgentIdeControl` — current, detected, trust, status, openEditor, closeEditor ✓
- `SessionControl implements AgentSessionControl` — resume, createCheckpoint, restoreCheckpoint, listCheckpoints, setRecording, getRecording ✓
- `ProfilesControl implements AgentProfileControl` — list, get, create, saveCurrent, delete, apply, setDefault, getDefault ✓
- `HookControl implements AgentHookControl` — onHookExecution, triggerSessionStart, triggerSessionEnd, clear ✓

### Q2 — Does everything compile?

**YES.** Monorepo `npm run typecheck` exit 0; agents-workspace `tsc --noEmit` exit 0.

### Q3 — Is the verified `Agent` INTERFACE preserved; exactly ONE `Agent` symbol?

**YES.** `agent.ts:321: export interface Agent` is intact, and all 7 sub-surface
interfaces (AgentToolControl, AgentMcpControl, AgentAuthControl with nested
`readonly keys: AgentAuthKeysControl`, AgentIdeControl, AgentSessionControl,
AgentProfileControl, AgentHookControl) are unchanged from P05. A repo-wide grep over
`packages/agents/src/api/` finds exactly ONE declaration of an `Agent` symbol
(`export interface Agent`); `AgentImpl` is the distinct implementing class. No
duplicate/parallel `Agent`.

### Q4 — NO reverse-test hooks / parallel files / TODOs?

**YES (clean).** `NotYetImplemented` is used exclusively as
`throw new Error('NotYetImplemented')` inside method bodies; it is never exported as a
sentinel/constant/property. No `*V2`/`*New`/`*Copy` files. No TODO/FIXME/HACK/XXX.

### Q5 — Are the 7 sub-surface accessors constructed instances (navigable, NOT throwing)?

**YES.** `AgentImpl` declares the 7 sub-surfaces as eagerly-constructed readonly
fields: `profiles = new ProfilesControl()`, `tools = new ToolControl()`,
`mcp = new McpControl()`, `auth = new AuthControl()`, `ide = new IdeControl()`,
`session = new SessionControl()`, `hooks = new HookControl()`. Accessing the shape does
NOT throw — only invoking the methods throws. `agent.auth.keys` resolves to a
constructed `AuthKeysControl` instance: `AuthControl` declares
`readonly keys: AgentAuthKeysControl = new AuthKeysControl()`. The control plane shape
is fully navigable.

### Q6 — Does `api/index.ts` export the public surface?

**YES.** It exports `createAgent` (from createAgent.js), `listProviders`/`listTools`
(from discovery.js), and re-exports public types via `export *` from `config-types.js`,
`event-types.js`, `agent.js` (Agent + handlers), `config-schema.js`, and
`event-schema.js`. Harness phases can import the full public surface from this
sub-barrel.

### Q7 — Is `packages/agents/src/index.ts` (package barrel) untouched?

**YES.** `git diff --stat packages/agents/src/index.ts` is empty; `git status --short`
shows only the new untracked `packages/agents/src/api/` tree. P07 will wire package
exports additively.

### Q8 — Strict TS: no `any`, no `as` assertions, explicit return types?

**YES.** No `any`, no `<any>`, no `as any`; no `as <Type>` assertions (only allowed
construct would be `as const`, none present). Every method in agentImpl.ts and all 8
control classes carries an explicit return type (e.g. `Promise<AgentResult>`,
`readonly ToolInfo[]`, `IdeInfo | null`, `void`). The single permitted eslint-disable
is on the throwing `stream` async-generator stub
(`require-yield, sonarjs/generator-without-yield`), which the spec explicitly allows.

---

## 3. Holistic Functionality Assessment

The Phase 06 stub surface delivers a faithful, fully-compiling skeleton of the entire
public Agent control plane:

- `createAgent(config: AgentConfig): Promise<Agent>` constructs and returns an
  `AgentImpl`, giving the harness a real, navigable object shaped exactly like the
  verified `Agent` interface.
- `AgentImpl implements Agent` stubs every top-level method (chat/stream, provider &
  model control, model params, user tier, full history surface, compress/stats,
  generate/generateJson/generateEmbedding, listProviders/listTools, dispose) with
  `throw new Error('NotYetImplemented')`.
- The 7 sub-surfaces are eagerly constructed readonly instances, so the entire control
  plane (`agent.tools`, `agent.mcp`, `agent.auth`, `agent.auth.keys`, `agent.ide`,
  `agent.session`, `agent.profiles`, `agent.hooks`) is structurally navigable without
  throwing; only method invocation throws.
- Module-level `listProviders`/`listTools` discovery stubs exist.
- The `api/index.ts` sub-barrel re-exports the public surface (factory + discovery +
  types/schemas/handlers) for downstream harness imports, while the package-level
  barrel remains untouched (deferred to P07).

This precisely satisfies REQ-001's stub intent: the upcoming test-harness phases
(P09–P13) can compile against the public surface and fail NATURALLY at runtime
(methods throw, shape resolves), with NO reverse-test artifacts to assert against. The
verified P05 `Agent` interface and its 7 sub-surface interfaces are preserved exactly;
the implementation is cleanly separated into `AgentImpl` + control classes. Strict-TS
discipline (no any/as, explicit return types) is maintained, typecheck is clean across
the monorepo, lint has zero errors in the API surface, and formatting is clean.

No gaps found. The work meets the bar.

---

**Verdict: PASS**
