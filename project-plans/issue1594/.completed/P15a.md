# P15a — Verify createAgent pseudocode-compliance

Plan: PLAN-20260617-COREAPI
Phase: 15a (verification of P15 — createAgent bootstrap + stream/chat + initial rebuildLoop + messageBus seam)
Reviewer: architect (Opus)
Verdict: **PASS**

## Scope verified

createAgent bootstrap (`packages/agents/src/api/createAgent.ts`) and its support
modules (`agentBootstrap.ts`, `agentImpl.ts`, `loop/rebuildLoop.ts`), plus the
shared-engine fix in `packages/agents/src/core/StreamProcessor.ts` and the
single-shared-MessageBus seam in
`packages/providers/src/runtime/runtimeContextFactory.ts`.

## Pseudocode-compliance (createAgent.md steps 10-176)

All 12 architect checklist items PASS with file:line evidence:

- Bootstrap order matches createAgent.md await-for-await sequence
  (parse → resolveAuth → runtimeId → ConfigParameters(+factories) → new Config →
  shared MessageBus → createIsolatedRuntimeContext({config, messageBus,
  settingsService, runtimeId, model}) → await handle.activate() → initial
  provider/model/auth mutators → config.initialize({messageBus}) →
  config.refreshAuth → createAgentRuntimeState({runtimeId,...}) → bind post-auth
  client → rebuildLoop (initial) → recordOwnership → buildAgent →
  hooks.triggerSessionStart → return).
- Invariants hold: B2 (one shared MessageBus threaded to runtime-context →
  OAuthManager + config.initialize + AgenticLoop), B4 (createAgentRuntimeState
  receives runtimeId), B6 (shared isolated runtime context — one runtimeId,
  shared Config/SettingsService/ProviderManager/OAuthManager; no
  createHeadlessProviderManager), R-CLIENT (facade resolves AgentClient lazily via
  config.getAgentClient(); never caches; transient pre-auth client never bound).
- rebuildLoop builds the initial AgenticLoop via the same routine future
  provider/model switches reuse, bound to the current client.
- StreamProcessor `_buildRuntimeContext` fix is type-correct and safe for the
  shared CLI/a2a code paths (root cause: an object-spread stripped the live
  `Config` prototype → `config.getModel is not a function`; fix preserves the live
  Config instance).

## Independent coordinator gate (filesystem ground truth)

- `npx eslint packages/agents/src/api` → **0 errors** (1 non-blocking warning:
  config-types.ts:52 redundant-type-alias — out of P15 scope; lint gate has no
  --max-warnings).
- `npm run typecheck --workspace @vybestack/llxprt-code-agents` → **PASS** (exit 0).
- `npx vitest run --root packages/agents core-conversation + provider-bootstrap +
  event-characterization` → **45 passed**; T1/T9/T25a-d GREEN; event-characterization
  38/38 (T16 stays GREEN). T10 (P21 generate) and T14 (P17 continuation) remain
  RED as expected for later phases.
- `npx vitest run --root packages/providers runtimeContextFactory.messageBus.test.ts`
  → **2/2 PASS** (P12 single-shared-MessageBus seam, including provided-bus identity).
- `npx prettier --check` across all api/ + modified providers/core P15 files →
  **clean**.
- Deferred-impl language scan (TODO/FIXME/HACK/XXX/WIP/"in a real"/"for now"/
  "placeholder"/"not yet"/"will be implemented") on P15 core files → **empty**
  (NotYetImplemented stubs for later phases excluded by design).

## Concerns log (from architect review) + actions taken

1. **(Non-blocking) Undocumented `prepare`/`registerProvidersOntoManager`
   provider-registration deviation** from incomplete pseudocode steps 40-46:
   `createIsolatedRuntimeContext` returns a bare ProviderManager; createAgent
   fills it via the `prepare` callback (canonical pattern used by CLI integration
   tests). Documented here so P16 does not treat it as accidental. Pseudocode
   addendum recommended (optional follow-up).
2. **(Resolved) `prepare` closure captured `handle` before assignment** — refactored
   to use the `prepare` callback context arg (`ctx.providerManager/settingsService/
   runtimeId/metadata`) instead of the outer `handle`. Verified typecheck + tests +
   lint after the change.
3. **(P14 scope, deferred) eventAdapter multi-`Finished` could emit multiple
   `done`** — tracked for P14/event-adapter follow-up; not in P15 scope.
4. **(P16 scope, deferred) `getModelParams()` source switch** — currently reads
   `runtimeState.modelParams`; switch to `getActiveModelParams()` when P16
   setModelParam lands.

## Lint debt discovered + fixed during P15a (worker had only linted StreamProcessor)

7 ESLint errors across createAgent.ts / agentBootstrap.ts / agentImpl.ts fixed:
type-only import of ToolConfirmationOutcome; concise arrow body in
buildAgentClientFactory; removed redundant `public` accessibility modifier;
widened post-auth client local to `AgentClientContract | undefined` so the
mandated defensive guard (steps 115-118) is legitimate; removed a genuinely
unnecessary `!== undefined` guard around setActiveProvider (getActiveProvider
throws, never returns undefined); extracted pseudocode steps 105-166 into a new
`async function finalizeAgent(...): Promise<Agent>` to bring createAgent under the
max-lines-per-function limit (all @pseudocode comments preserved).

Result: P15 + P15a complete. Ready for P16 (switch + context-preservation).
