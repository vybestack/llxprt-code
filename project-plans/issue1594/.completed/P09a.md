Phase: P09a (Harness Layer 1 — Static / Boundary Verification)
Plan: PLAN-20260617-COREAPI (issue #1594)
Verified: 2026-06-17
Reviewer: independent verification gate (first-principles, no prior-review assumption)

================================================================================
VERIFICATION COMMAND OUTPUTS (re-run independently — not the worker's pasted output)
================================================================================

(1) npm test --workspace @vybestack/llxprt-code-agents -- --run --testNamePattern "@plan:.*P09"
----------------------------------------------------------------
 FAIL  src/api/__tests__/app-service-boundary.spec.ts > Runtime-vs-app-service
       boundary (T23/T24) ... > durable app-service subpaths are importable with
       their named export (T23) ...
 Error: Cannot find module '.../packages/agents/src/app-service.js' imported from
        '.../packages/agents/src/api/__tests__/app-service-boundary.spec.ts'
   at src/api/__tests__/app-service-boundary.spec.ts:94:44
       94|   const mod: Record<string, unknown> = await import(entry.target);
 Caused by: Failed to load url .../packages/agents/src/app-service.js. Does the file exist?

 Test Files  1 failed | 1 passed | 91 skipped (93)
      Tests  1 failed | 7 passed | 1604 skipped (1612)

   => EXACT expected RED: "1 failed | 7 passed". Suite RUNS; only the dynamic
      subpath-resolution assertion fails. Failure cause = missing P27 artifact
      app-service.js (resolved from @vybestack/llxprt-code-agents/app-service.js).

(2) grep -rn "toHaveBeenCalled|toThrow('NotYetImplemented')|not\.toThrow" packages/agents/src/api/__tests__/
----------------------------------------------------------------
 OK   (no matches — no mock-theater, no reverse-test assertions)

(3) npm run typecheck --workspace @vybestack/llxprt-code-agents
----------------------------------------------------------------
 > tsc --noEmit
 (Exit 0, 0 errors)

(4) npm run lint --workspace @vybestack/llxprt-code-agents
----------------------------------------------------------------
 ✖ 138 problems (0 errors, 138 warnings)
 => Exactly the pre-existing baseline (138 warnings / 0 errors). Filtering lint
    output for the three P09 files => "NO P09 LINT ISSUES". No NEW errors or
    warnings introduced by P09.

(5) grep -rc "@plan:PLAN-20260617-COREAPI.P09" <three files>
----------------------------------------------------------------
 command-api-map.ts:1   boundary.spec.ts:5   app-service-boundary.spec.ts:7
 @requirement markers (REQ-019/REQ-021) present in all three (1 / 5 / 7).
 it() titles carrying BOTH @plan + @requirement markers: 8 of 8 (3 in boundary,
 5 in app-service-boundary).

(6) Hygiene grep (any / as-assertion / TODO / FIXME / HACK / STUB / NotYetImplemented)
----------------------------------------------------------------
 NONE FOUND. Only `as const` (line 50 app-service-boundary) which is permitted.

(7) sonarjs/regular-expr disable convention repo-wide => 261 occurrences.
    The 4 disables in boundary.spec.ts match the established convention and are
    not masking a real defect (they sit on simple import-extraction regexes).

(8) Map / interface cross-check
----------------------------------------------------------------
 37 map entries (the prompt's "39" and a naive grep's "38" are miscounts; "38"
 included the `readonly command: string` interface field). 37 is the real entry
 count and is COMPLETE — see semantic check 3.

================================================================================
POINT-BY-POINT SEMANTIC CHECKS (09a spec)
================================================================================

1. BEHAVIORAL, NOT MOCK THEATER — PASS
   - boundary.spec.ts performs a REAL static scan: readdirSync/readFileSync/
     statSync (node:fs) walk __tests__, extract import specifiers via regex over
     actual file contents, and assert against an allowlist + forbidden-deep set.
     It asserts real data (zero violations), not "method called".
   - app-service-boundary.spec.ts asserts real map data (no orphan, durable set
     present + classified subpath, every subpath targets the pinned specifier and
     names an export) AND does a REAL dynamic `await import(entry.target)` to test
     actual module resolvability + that the named export is a function.
   - No toHaveBeenCalled, no spies anywhere. Confirmed.

2. NATURAL RED, NOT REVERSE TEST — PASS
   - The single failing assertion fails because the REAL artifact
     @vybestack/llxprt-code-agents/app-service.js (-> src/app-service.js) does not
     exist yet (lands in P27). Error is "Cannot find module ... app-service.js",
     NOT toThrow('NotYetImplemented') / not.toThrow.
   - CRITICAL DYNAMIC-IMPORT NUANCE CONFIRMED: the only imports of the missing
     subpath are the in-test-body dynamic `await import(entry.target)` at line 94.
     Top-level static imports (lines 24-25) are ONLY `vitest` and
     `./command-api-map.js`. Therefore the harness module LOADS and the suite RUNS;
     run shows "1 failed | 7 passed" exactly as required.

3. MAP COMPLETENESS (independent cross-check vs overview §4.7 + slash-command
   note lines 1184-1194, and spec REQ-021 T23 durable set) — PASS
   Required commands all present and correctly classified:
     /auth(runtime), /key(runtime), /keyfile(runtime), /provider(runtime),
     /model(runtime), /profile {apply=runtime, save/list/delete=subpath},
     /compress(runtime), /mcp {status=runtime, auth=runtime (OAuth),
     add=subpath, remove=subpath}, /restore(runtime),
     /chat {save/resume/clear = runtime}, /tools(runtime), /directory(runtime),
     /memory {show=runtime, edit=subpath}, /skills(subpath), /extensions(subpath),
     /ide(runtime), /stats(runtime), /about(subpath), settings mutation(subpath),
     diagnostics(subpath), completions:{prompt,command,at-command,mcp-prompt}=cli-local,
     plus /help,/clear,/theme,/quit = cli-local.
   - NO ORPHAN: every entry has a kind in {runtime,subpath,cli-local}; the test
     proves it (ORPHANS length 0, unique-command-count == count).
   - DURABLE SET CORRECTLY classified as subpath (NOT crammed on runtime Agent):
     test's REQUIRED_DURABLE_COMMANDS = [/mcp add, /mcp remove, /extensions,
     /skills, /memory edit, settings mutation, diagnostics] — matches REQ-021/T23
     exactly (with /memory split into show=runtime / edit=subpath, the durable
     portion). Matches §4.7 "MCP server config add/remove, extension management,
     memory-file edits, diagnostics/about, settings mutation" => app-service subpaths.
   - RUNTIME targets point at real P05 Agent methods (packages/agents/src/api/
     agent.ts): setProvider(326), setModel(329), compress(354), restoreHistory(350),
     resetChat(351), updateSystemInstruction(352), addDirectoryContext(353),
     getStats(355), auth.login(259), auth(setKeyFile), profiles.apply(307),
     mcp.status(232), mcp.auth(234), tools.list(222), ide.status(281),
     session.resume(287). All resolve. The /chat {save/resume/clear}=runtime
     classification is corroborated by overview line 467 (agent.session is the
     runtime control plane for resume/checkpoint/recording).
   - COMPLETIONS classified cli-local per §4.7 decision (a); no orphan (test
     proves COMPLETION_UNCLASSIFIED length 0).

   NON-BLOCKING OBSERVATION (documentation-label imprecision, not a contract
   defect): the runtime `target` string for `/chat save` reads `agent.session.save`,
   but the P05 interface method is `session.createCheckpoint` (there is no
   `session.save`). Runtime `target` strings are PURELY DESCRIPTIVE — the test
   never asserts runtime targets against the interface (only subpath targets are
   asserted). Classification (runtime) is correct and REQ-021 is satisfied. This
   is a cosmetic label that the P27/doc phase should reconcile to `createCheckpoint`;
   it does not affect the RED outcome, the contract, or any assertion.

4. FUTURE-CORRECTNESS (real teeth) — PASS
   - The T23 assertion WILL PASS once P27 ships app-service.js exporting the named
     functions (saveCurrentProfile, listProfiles, deleteProfile, addMcpServer,
     removeMcpServer, editMemory, manageSkills, manageExtensions, getAbout,
     mutateSettings, getDiagnostics) as functions — `typeof exported === 'function'`.
   - The T17 guard is NON-VACUOUS: it collects 3 real consumer specs
     (boundary.spec.ts, quality-gate-smoke.spec.ts, app-service-boundary.spec.ts);
     CONSUMER_FILES is non-empty. isForbiddenDeep() scans actual `from '...'` and
     `import('...')` specifiers and flags `/dist/` + 7 `@vybestack/llxprt-code-*/`
     deep prefixes. Adding a deep `@vybestack/llxprt-code-core/...` import to any
     consumer spec WOULD register a violation and FAIL T17. Verified the guard is
     not falsely passing: quality-gate-smoke.spec.ts imports only vitest/fast-check/
     in-package relative `../quality-gate-smoke.js` (all allowlisted), so the
     current PASS is honest. internals.js is correctly DENIED on consumer specs
     (isAllowed returns false) but helpers/ are excluded by design.

5. RULES.md HYGIENE — PASS
   - No `any`, no `as X` casts (only `as const`), no TODO/FIXME/HACK/STUB/
     NotYetImplemented. Colon-form `@plan:PLAN-20260617-COREAPI.P09` +
     `@requirement:REQ-019`/`REQ-021` present in every file AND embedded in all 8
     it() titles. The sonarjs/regular-expr eslint-disable lines match the repo
     convention (261 occurrences) and mask no real problem.

================================================================================
HOLISTIC FUNCTIONALITY ASSESSMENT
================================================================================
The two boundary specs prove two enforceable architectural contracts for the new
public agents API:

 - T17/REQ-019 (boundary.spec.ts) is a genuine compile-independent static
   import-boundary guard. By scanning real file bytes of every consumer-facing
   *.spec.ts under __tests__, it guarantees the harness (and, as #1595's gate, the
   CLI) imports ONLY the public root `@vybestack/llxprt-code-agents`, the documented
   `app-service.js` subpath, vitest/fast-check/node builtins, and in-package
   relatives — and that it never reaches into deep `…/dist/…` or any sibling
   `@vybestack/llxprt-code-*` internal. This is the durable wall that stops #1595
   from silently recreating deep internal imports once the public API exists. It
   currently PASSES because the harness is genuinely clean (honest pass, not forced).

 - T23/T24/REQ-021 (app-service-boundary.spec.ts over COMMAND_API_MAP) proves the
   §4.7 runtime-vs-app-service split is real and import-safe: every one of the 37
   CLI touchpoints is classified into exactly one of runtime|subpath|cli-local with
   no orphan; durable mutations resolve to a single pinned public subpath rather
   than bloating the live Agent; completions are explicitly cli-local. The contract-
   data assertions PASS (7 passed). The single FAILING assertion is the dynamic
   `await import('@vybestack/llxprt-code-agents/app-service.js')` resolvability
   check — and it fails for exactly the RIGHT reason: that subpath/module is built
   in P27 and does not exist yet. Because the import is dynamic and inside the test
   body, the suite still loads and runs; only the resolvability assertion reds.
   When P27 lands the subpath with the named function exports, this test flips green
   with no test change required.

This is a correct Layer-1 RED harness: behavioral (real fs scan + real dynamic
import resolution + real map data), failing naturally on a missing future artifact,
with a complete and correctly-classified command→API map, clean typecheck/lint, and
correct plan/requirement markers throughout.

Verdict: PASS
