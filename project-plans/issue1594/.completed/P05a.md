# P05a — Agent Control-Plane Interface Verification

Phase ID: `PLAN-20260617-COREAPI.P05a`
Reviewer role: typescriptreviewer (skeptical verification gate)
Artifacts: `packages/agents/src/api/agent.ts`, `packages/agents/src/api/index.ts`
Consumed public types: `packages/agents/src/api/config-types.ts`, `packages/agents/src/api/event-types.ts`
Grounded against: `specification.md` §4.3/§4.5/§4.6/REQ-008..REQ-017, `overview.md` §4.3 (L406-486) + auth contract (L941-958), `packages/core/src/core/clientContract.ts`

---

## 1. Verification Command Outputs

### `npm run typecheck` (monorepo) — exit 0
All workspaces compiled with `tsc --noEmit`, no errors:
```
> @vybestack/llxprt-code-ide-integration typecheck  ✓
> @vybestack/llxprt-code-policy typecheck            ✓
> @vybestack/llxprt-code-mcp typecheck               ✓
> @vybestack/llxprt-code-core typecheck              ✓
> @vybestack/llxprt-code-lsp typecheck               ✓
> @vybestack/llxprt-code-providers typecheck         ✓
> @vybestack/llxprt-code-agents typecheck            ✓
> @vybestack/llxprt-code typecheck                   ✓
> @vybestack/llxprt-code-a2a-server typecheck        ✓
> @vybestack/llxprt-code-test-utils typecheck        ✓
Exit Code: 0
```

### `npm run typecheck --workspace @vybestack/llxprt-code-agents` (barrel/collision check) — exit 0
```
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit
Exit Code: 0
```
The `index.ts` `export *` re-exports of config-types, config-schema, event-types,
event-schema, and agent produce NO name collisions and compile cleanly.

### Method-presence loop — exit 0 (missing=0)
```
=== METHOD PRESENCE ===      (chat stream setProvider setModel getHistory setHistory
                              compress getStats onStats generate dispose) — all present
=== SUB-SURFACE PRESENCE === (AgentToolControl AgentMcpControl AgentAuthControl
                              AgentIdeControl AgentSessionControl AgentProfileControl
                              AgentHookControl) — all present
=== MCP DISCOVERY/ERROR CONTRACT === (mcpDiscovery AgentError mcp_discovery_failed) — all present
=== ANY / ASSERTION GREP === (no any/assertion matches)
=== RESULT missing=0 ===
```

### any/assertion grep — clean
- Phase-05a pattern `: any\b|as [A-Z][A-Za-z]+` → no matches.
- Broader Phase-05 pattern `: any\b|as [A-Z]` → no matches. Zero `any`, zero type assertions.

### `npm run lint --workspace @vybestack/llxprt-code-agents` — exit 0
```
✖ 138 problems (0 errors, 138 warnings)
=== LINT EXIT CODE: 0 ===
```
- **ZERO errors** repo-wide; exit code 0.
- `agent.ts`: **zero** lint findings (no entries in output).
- `index.ts`: **zero** lint findings.
- `config-types.ts`: a single **warning** (`52:13 sonarjs/redundant-type-aliases` on
  `AgentProjectHooks = AgentHooks`) — a warning, not an error; consumed-type file from P03,
  not introduced by this phase. Acceptable per the gate (zero ERRORS required).
- All other 137 warnings are in pre-existing non-api files (core/, scheduler/, tools/,
  compression/) — unused eslint-disable directives and sonarjs nits, out of scope.

### Deferred-implementation / stub-body scan — clean
- `grep TODO|FIXME|HACK|STUB|XXX` over agent.ts + index.ts → none.
- No function bodies / `return` / `console.` / `throw` in agent.ts. The only `=>`
  occurrences are within type positions (`Unsubscribe = () => void` and callback
  parameter signatures), which is correct interface syntax — not implementation.

### Plan markers
- `@plan:PLAN-20260617-COREAPI.P05` present in agent.ts (1) and index.ts (1). `@requirement:REQ-001, REQ-017` present in both. One marker block per file, as required.

---

## 2. Semantic Verification Checklist (MANDATORY — 6)

### Q1 — Does `Agent` match specification.md §4.3 / overview.md L406-486 EXACTLY (names, params, async-ness)? Method-by-method.

| overview.md §4.3 | agent.ts | Verdict |
|---|---|---|
| `chat(input: string\|AgentInput, opts?: TurnOptions): Promise<AgentResult>` | `chat(input: AgentInput, opts?: TurnOptions): Promise<AgentResult>` | ✓ — `AgentInput` is `string \| {text;role?}`, so it subsumes the union |
| `stream(...): AsyncIterable<AgentEvent>` | identical | ✓ |
| `getProvider(): string` | identical | ✓ |
| `setProvider(provider, model?): Promise<void>` | identical | ✓ async |
| `getProviderStatus(): ProviderStatus` | identical | ✓ |
| `getModel(): string` | identical | ✓ |
| `setModel(model): Promise<void>` | identical | ✓ async |
| `getCurrentSequenceModel(): string \| null` | identical | ✓ |
| `getModelParams(): Record<string,unknown>` | `: Readonly<Record<string,unknown>>` | ✓ — immutability hardening (RULES.md readonly), structurally compatible |
| `setModelParam(key, value): void` (sync) | identical (sync) | ✓ |
| `clearModelParam(key): void` | identical | ✓ |
| `getUserTier(): UserTierId \| undefined` | identical | ✓ |
| `readonly profiles: AgentProfileControl` | identical | ✓ |
| `readonly tools: AgentToolControl` | identical | ✓ |
| `readonly mcp: AgentMcpControl` | identical | ✓ |
| `readonly auth: AgentAuthControl` | identical | ✓ |
| `readonly ide: AgentIdeControl` | identical | ✓ |
| `getHistory(): Promise<readonly AgentMessage[]>` | identical | ✓ async |
| `setHistory(history, {stripThoughts?}): Promise<void>` | identical | ✓ async |
| `addHistory(message): Promise<void>` | identical | ✓ async |
| `restoreHistory(items): Promise<void>` | `restoreHistory(items: readonly AgentHistoryItem[])` | ✓ — overview comments "IContent-level restore"; `AgentHistoryItem = IContent` precisely matches `AgentClientContract.restoreHistory(historyItems: IContent[])`. Stronger/correct vs the overview's loose `AgentMessage[]`. |
| `resetChat(): Promise<void>` | identical | ✓ async |
| `updateSystemInstruction(): Promise<void>` | identical | ✓ async |
| `addDirectoryContext(): Promise<void>` | identical | ✓ async |
| `compress(opts?:{promptId?}): Promise<CompressionResult>` | identical | ✓ async |
| `getStats(): SessionStats` | identical (sync snapshot) | ✓ |
| `onStats(cb): Unsubscribe` | identical | ✓ |
| `readonly session: AgentSessionControl` | identical | ✓ |
| `generate(input, opts?): Promise<string>` | identical | ✓ async |
| `generateJson(contents, schema, opts?): Promise<Record<string,unknown>>` | identical | ✓ async |
| `generateEmbedding(texts): Promise<number[][]>` | identical | ✓ async |
| `readonly hooks: AgentHookControl` | identical | ✓ |
| `listProviders(): ProviderInfo[]` | `: readonly ProviderInfo[]` | ✓ readonly-hardened |
| `listTools(): ToolInfo[]` | `: readonly ToolInfo[]` | ✓ readonly-hardened |
| `dispose(): Promise<void>` | identical | ✓ async |

No missing method, no wrong sync/async, no wrong param. The minor deltas
(`Readonly<Record>`, `readonly` arrays, `AgentHistoryItem`/`IContent` for restore) are
strictly-correct immutability/typing hardenings mandated by RULES.md and the
`AgentClientContract`, not deviations. **PASS.**

### Q2 — All 7 sub-surfaces present and COMPLETE?

- **AgentToolControl** (§4.6): `list()`, `setEnabled(names): Promise<void>`,
  `onConfirmationRequest(cb: (ToolConfirmation)=>void): Unsubscribe`,
  `respondToConfirmation(confirmationId, decision: ToolDecision): void`,
  `onToolUpdate(cb): Unsubscribe`, `setEditorCallbacks(cbs: EditorCallbacks): void`.
  Matches §4.6 verbatim incl. correlation-id keyed `respondToConfirmation` and dual-id
  `ToolConfirmation`. ✓
- **AgentMcpControl** (§4.3/REQ-013): `listServers()`, `status(): McpStatus`,
  `toolsByServer()`, `auth(server): Promise<McpServerAuthStatus>`, `discoveryState()`,
  `refresh(server?): Promise<void>`. Matches `listServers/status/toolsByServer/auth/discoveryState/refresh`. ✓
- **AgentAuthControl** — see Q2-auth below. ✓
- **AgentIdeControl** (REQ-014): `current()`, `detected()`, `trust(name): Promise<void>`,
  `status(): IdeStatus`, `openEditor(): Promise<void>`, `closeEditor(): Promise<void>`.
  Covers current/detected/trust/status + editor open/close. ✓
- **AgentSessionControl** (REQ-010): `resume('latest'\|string,{prefix?}): Promise<void>`,
  `createCheckpoint(label?): Promise<SessionCheckpoint>`, `restoreCheckpoint(id): Promise<void>`,
  `listCheckpoints()`, `setRecording(state): Promise<void>`, `getRecording()`.
  Covers resume(latest/id/prefix) + checkpoint create/restore + recording swap. ✓
- **AgentProfileControl** (REQ-009): `list()`, `get(name)`, `create(name, detail): Promise<void>`,
  `saveCurrent(name): Promise<void>`, `delete(name): Promise<void>`, `apply(name): Promise<void>`,
  `setDefault(name): Promise<void>`, `getDefault()`. Matches the full
  `list/get/create/saveCurrent/delete/apply/setDefault/getDefault` set verbatim. ✓
- **AgentHookControl** (REQ-015): `onHookExecution(cb): Unsubscribe`,
  `triggerSessionStart(): Promise<void>`, `triggerSessionEnd(): Promise<void>`, `clear(): void`.
  Covers onHookExecution + trigger SessionStart/SessionEnd + clear. ✓

**AgentAuthControl vs overview.md L941-958 (line-by-line):**
| overview L941-958 | agent.ts AgentAuthControl | Verdict |
|---|---|---|
| `login(provider, {bucket?}): Promise<void>` | identical | ✓ |
| `logout(provider, {bucket?,all?}): Promise<void>` | identical | ✓ |
| `status(provider?): AuthStatus` | identical (sync) | ✓ |
| `enableOAuth(provider): Promise<void>` | identical | ✓ |
| `disableOAuth(provider): Promise<void>` | identical | ✓ |
| `listBuckets(provider?): AuthBucket[]` | `: readonly AuthBucket[]` | ✓ readonly-hardened |
| `switchBucket(provider, bucket): Promise<void>` | identical | ✓ |
| `mcpLogin(server): Promise<void>` | identical | ✓ |
| `setBaseUrl(string\|null, {provider?}): Promise<void>` | identical | ✓ |
| NESTED `keys.list/save/use/delete/setRaw/setKeyFile` | `readonly keys: AgentAuthKeysControl` | ✓ |

`AgentAuthKeysControl`: `list(): Promise<readonly KeyInfo[]>`,
`save(name, apiKey, {provider?}): Promise<void>`, `use(name,{provider?})`,
`delete(name,{provider?})`, `setRaw(apiKey: string\|null,{provider?})`,
`setKeyFile(path: string\|null,{provider?})` — all 6 present, all async, matching
REQ-008 exactly. The nested namespace makes `agent.auth.keys.save(name, apiKey, {provider?})`
(harness T18b) directly expressible. **No raw-secret leak:** `KeyInfo` is `{readonly name; readonly provider?}`
— a reference only, never the secret value. **PASS.**

### Q3 — Async signatures match AgentClientContract?

`clientContract.ts` is authoritative. Mapping:
- `getHistory(): Promise<Content[]>` → Agent `getHistory(): Promise<readonly AgentMessage[]>` (AgentMessage=Content). ✓ async
- `setHistory(history, {stripThoughts?}): Promise<void>` → Agent identical. ✓ async
- `restoreHistory(historyItems: IContent[]): Promise<void>` → Agent `restoreHistory(items: readonly AgentHistoryItem[]): Promise<void>` (AgentHistoryItem=IContent). ✓ async, correct element type
- `resetChat(): Promise<void>` → ✓ async
- `addHistory(content: Content): Promise<void>` → ✓ async
- `updateSystemInstruction(): Promise<void>` → ✓ async
- `addDirectoryContext(): Promise<void>` → ✓ async
- `generateEmbedding(texts: string[]): Promise<number[][]>` → Agent `generateEmbedding(texts: readonly string[]): Promise<number[][]>`. ✓ exact return type
- `getUserTier(): UserTierId \| undefined` → ✓
- `getCurrentSequenceModel(): string \| null` → ✓

Every contract-async member is async on the public surface; sync contract members
(`getUserTier`, `getCurrentSequenceModel`) are sync. **PASS.**

### Q4 — Can the surface express every harness T-row's inputs/outputs?

| T-row | Capability | Interface home |
|---|---|---|
| T1 | drain stream, ordered events | `stream(): AsyncIterable<AgentEvent>` |
| T2/T2b | tool call + confirmation + result | `tools.onConfirmationRequest`, `respondToConfirmation`, `onToolUpdate`; raw path via `stream` building block |
| T3/T3b/T3c | deny / live output / editor cb | `respondToConfirmation(deny)`, `onToolUpdate`, `tools.setEditorCallbacks` |
| T4/T4c/T4d/T4e/T4f | provider switch + rebinding + context preserve + LB + stripThoughts | `setProvider`, `getProvider`, `setHistory({stripThoughts})`, `profiles.apply` |
| T4b/T18d | apply/CRUD profile | `profiles.apply/saveCurrent/create/delete/list/get/setDefault/getDefault` |
| T5 | model/params | `setModel`, `setModelParam`, `getModel`, `getModelParams` |
| T6/T6b/T7/T14b | history restore/resume/reset/direct | `setHistory`,`getHistory`,`session.resume`,`session.createCheckpoint/restoreCheckpoint`,`resetChat`,`addHistory`,`updateSystemInstruction`,`addDirectoryContext` |
| T8 | compression explicit + auto | `compress(): Promise<CompressionResult>` + `compression` stream event (event-types) |
| T8b | live stats | `onStats`, `getStats`, `SessionStats` |
| T9 | cancellation | `TurnOptions.signal` + `done:aborted` DoneReason |
| T10 | side-channel generate | `generate`, `generateJson`, `generateEmbedding` + `GenerateOptions.readHistory/writeHistory` |
| T11/T22 | headless approval / non-interactive | `AgentConfig.onApproval` (config-types) + `AgentResult{text,toolCalls,finishReason,error,usage}` for output-format/exit-code |
| T12/T12b/T20 | discovery / MCP status / gating | `listProviders`,`listTools`,`mcp.*`,`TurnOptions.mcpDiscovery`,`AgentError.code:mcp_discovery_failed` |
| T13 | disposal | `dispose(): Promise<void>` |
| T14 | todo continuation | `stream`/`chat` |
| T15/T15b/T15c | IDE / hooks / save_memory | `ide.*`, `hooks.*`, `updateSystemInstruction` |
| T16 | event-variant coverage | full `AgentEvent` union (event-types) incl. hook-blocked/done variants |
| T17/T23/T24 | no-deep-import / app-service / completions | static/contract tests against the public entry (index.ts re-export root) |
| T18 | key/keyfile/key-name precedence | `auth.status`, `getProviderStatus`, `auth.keys.*` |
| **T18b** | **key store + profile-save** | **`auth.keys.save/use/delete/setRaw/setKeyFile` (nested namespace) → has an interface home** ✓ |
| T18c | OAuth/buckets/MCP auth | `auth.login/logout/status/listBuckets/switchBucket/mcpLogin` + `onOAuthPrompt` (config) |
| T18e | sandbox | `AgentConfig.sandbox` (config-types) — recreate/app-service classification, not on Agent (correct per §4.7) |
| T19 | scheduler factory | `AgentConfig.toolSchedulerFactory` (config-types) |
| T21 | tool-loop sequencing | `stream`/`chat` (AgenticLoop facade) |
| T25 | provider-by-name bootstrap | `createAgent(config)` (P06) + `listProviders` |

Every capability area in T1..T25 has a member that can carry it. **T18b specifically has
an interface home** via the nested `auth.keys.*` namespace. No T-row lacks interface
support. **PASS.**

### Q5 — Can T20 compile against `TurnOptions.mcpDiscovery` and `AgentError.code:'mcp_discovery_failed'`?

- `TurnOptions.mcpDiscovery?: McpDiscoveryMode` where `McpDiscoveryMode = 'await' | 'skip'`. ✓ both literals present.
- `AgentErrorCode` union includes `'mcp_discovery_failed'`; `AgentError = {code: AgentErrorCode; message; cause?}`. ✓
- Carriage: `AgentResult.error?: AgentError` carries the code in the buffered path. For the
  streamed error path, the public `AgentEvent` union's error event (`event-types.ts`
  `AgentErrorEvent`) carries a `StructuredError`; the structured MCP-discovery error +
  terminal `done:error` (DoneReason includes `'error'`) is expressible. The buffered
  `AgentResult.error.code:'mcp_discovery_failed'` is the precise REQ-013 carrier and compiles.
**PASS.**

### Q6 — No `any`, no assertions, explicit return types, readonly where appropriate?

- No `any` anywhere (grep clean, both patterns). ✓
- No type assertions (`as X`) (grep clean). ✓
- Every interface member has an explicit return type (verified by reading file). ✓
- All 7 sub-surface members on `Agent` are `readonly` (lines 336-342). ✓
- DTO fields are `readonly`; arrays are `readonly T[]`; record types are `Readonly<Record<...>>`. ✓
**PASS.**

---

## Interface-Purity & New-Type-Shape Findings

**Purity:** This file is interface-only. No stub bodies, no `return`/`throw`/`console`,
no TODO/FIXME/STUB markers. The only `=>` tokens are in type positions (`Unsubscribe`
and callback parameter signatures), which is required interface syntax. No implementation
leaks in.

**New-type shapes (no internal-field leakage):**
- `SessionStats` — normalized public token/context shape (promptTokens, candidateTokens,
  totalTokens, cachedTokens, contextWindowSize/Used, turnCount). No HistoryService/uiTelemetry internals.
- `ProviderStatus` / `ProviderInfo` — public auth/baseUrl/keyName projections; no provider-manager internals.
- `AuthStatus` — string-literal union; `AgentResult`/`AgentError`/`AgentErrorCode` — clean public DTOs.
- `KeyInfo` — `{name, provider?}` reference only; **no raw secret**.
- `ToolInfo`/`McpServerInfo`/`McpStatus`/`AuthBucket`/`IdeInfo`/`ProfileSummary`/`ProfileDetail` — normalized projections.
- `ToolDecision = ToolConfirmationOutcome` and `ToolUpdate`/`ToolConfirmation` reuse the
  P04 public event-types (normalized, not raw scheduler unions).
- Imports of `Content`, `IContent`, `UserTierId`, `ApprovalMode`, `MCPServerConfig`,
  Hook types, `ToolConfirmationOutcome` are `import type` of stable public core/tools symbols;
  monorepo typecheck confirms they resolve. agent.ts correctly references `EditorCallbacks`
  from config-types and `AgentEvent`/`AgentToolCall`/`DoneReason`/`ToolConfirmation`/`ToolUpdate`
  from event-types.

---

## Holistic Functionality Assessment

The public control-plane surface is a single `Agent` interface that gives the #1595 CLI
(and a2a-server) everything needed to be a thin UI with zero deep imports. At the top
level it exposes conversation (`chat`/`stream` over the stable `AgentEvent` union),
provider/model control (`getProvider`/`setProvider`/`getModel`/`setModel`/model-params/
`getCurrentSequenceModel`/`getUserTier`/`getProviderStatus`), async history/session
management grounded directly in `AgentClientContract` (`getHistory`/`setHistory`/
`addHistory`/`restoreHistory`/`resetChat`/`updateSystemInstruction`/`addDirectoryContext`),
compression, live stats, side-channel generation, instance-scoped discovery, and
`dispose`. Cross-cutting concerns are grouped into seven `readonly` sub-surfaces —
tools/scheduler, MCP, auth (with a nested `keys.*` namespace), IDE, session, profiles,
and hooks — each matching the specification's §4.3/§4.5/§4.6 and REQ-008..REQ-017 shapes.
T20's MCP-discovery contract (`TurnOptions.mcpDiscovery: 'await'|'skip'` and
`AgentError.code:'mcp_discovery_failed'`) is expressible, and T18b's secure-store/key
operations now have a precise home via `auth.keys.*` with `KeyInfo` carrying a reference
rather than a secret. The surface is strict (no `any`, no assertions, explicit return
types, readonly throughout), pure interface (no implementation), async exactly where the
underlying contract is async, and the agents-scoped barrel re-export compiles without
collision.

It can express every harness T-row (T1..T25); no capability area is orphaned. The minor
type deltas versus the overview prose (readonly arrays, `Readonly<Record>`,
`AgentHistoryItem=IContent` on `restoreHistory`) are correct hardenings consistent with
RULES.md and `AgentClientContract`, not deviations.

Gaps: none blocking. (Non-blocking observation: the single `config-types.ts`
`redundant-type-aliases` warning on `AgentProjectHooks = AgentHooks` originates in P03,
is a warning not an error, and is outside this phase's scope.)

---

Verdict: PASS
