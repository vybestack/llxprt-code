# P21 — Impl Side-Channel Generate (REQ-012) — COMPLETE

Plan: PLAN-20260617-COREAPI · Phase 21 · Requirement REQ-012
Worker: typescriptexpert · Coordinator-verified from filesystem ground truth.

## Scope
Implement the three detached side-channel generation methods on the public Agent
surface (`AgentImpl`) so behavioral test **T10** goes GREEN, without mutating
conversation history and without running a tool loop.

## File modified (ONLY)
`packages/agents/src/api/agentImpl.ts` (confirmed sole source file changed by mtime).

## Implementation (verified by reading code L644-687)
- `generate(input, opts?)` → `client.generateDirectMessage({ message: toPartListUnion(input) }, promptId)`
  then `getResponseText(response) ?? ''`. `generateDirectMessage` is the DETACHED
  single-shot path — does NOT append to history (exactly T10's contract). promptId
  defaults to `generate-${Date.now()}`.
- `generateJson(contents, schema, opts?)` → `client.generateJson([...contents] as …, { ...schema }, signal, model)`;
  signal defaults to a fresh `AbortController().signal`; model defaults to `this.providerState.model`.
- `generateEmbedding(texts)` → `client.generateEmbedding([...texts])`.
- Import added: `getResponseText` from `@vybestack/llxprt-code-core/utils/generateContentResponseUtilities.js`
  (canonical helper; same one used in `baseLlmClient.ts:157`).
- Markers: `@plan:PLAN-20260617-COREAPI.P21` + `@requirement:REQ-012` on all 3 methods.
- No mock theater — genuine delegation to the AgentClientContract side-channel methods.

## Independent verification (coordinator-run)
- `core-conversation.spec.ts`: **5 passed (5)** — T1, T9, **T10**, T14 all green.
- Full api-spec: **26 failed | 94 passed (120)** — moved from 27/93; exactly T10 flipped.
- Keep-green set intact: switch-context 11/11, core-tools 8/8, event-characterization 38/38,
  provider-bootstrap 4/4, boundary 3/3, core-history 7/7, auth-profiles 8/8.
- typecheck (agents): 0 errors. eslint agentImpl.ts: 0 errors. prettier: clean.
- Deferred-impl scan: clean on the 3 generate methods; only the legitimate
  SafeHookControl P23 stub remains at L970/974 (`NotYetImplemented`).
- build (agents): success.

## Remaining failures (all later-phase-owned, NOT regressions)
disposal 9 → P24 · mcp-discovery 6 → P22/P25 · ide 5 → P22 · hooks 4 → P23 ·
scheduler-factory 1 → P23 · app-service-boundary T23 1 → P27.

Status: **PASS** — ready for P21a architect verification.
