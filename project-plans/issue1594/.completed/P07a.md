# Phase 07a Completion — Export Strategy Verification

## Phase ID

`PLAN-20260617-COREAPI.P07a`

## Reviewer

typescriptreviewer (rigorous, skeptical gate — NOT a rubber stamp)

## Scope under review

Non-breaking, additive export strategy for `@vybestack/llxprt-code-agents`:

- `packages/agents/src/internals.ts` (NEW low-level barrel)
- `packages/agents/src/index.ts` (MODIFIED top-level barrel)
- `packages/agents/package.json` (MODIFIED exports map)
- `packages/agents/src/api/index.ts` (public API barrel surfaced at top-level)

Constraint: additive only — no top-level low-level export removed; `packages/core/src/index.ts` not modified (final trim + CLI/a2a migration deferred to #1595).

---

## Command Outputs

### 1. internals subpath present in package.json

```
$ node -e "const p=require('./packages/agents/package.json'); if(!p.exports['./internals.js']){console.error('FAIL no internals subpath');process.exit(1)} console.log('OK subpath')"
OK subpath
```

### 2. Plan marker count + AgentClient in internals.ts

```
$ grep -c "@plan:PLAN-20260617-COREAPI.P07" packages/agents/src/internals.ts
1
$ grep -n "AgentClient" packages/agents/src/internals.ts
38:export { AgentClient, PostTurnAction } from './core/client.js';
```

(`@requirement:REQ-018` also present in the internals.ts header, line 2.)

### 3. Monorepo typecheck — exit 0 (cli + a2a-server included)

```
$ npm run typecheck   (tail)
> @vybestack/llxprt-code-core@0.10.0 typecheck
> @vybestack/llxprt-code-lsp@0.10.0 typecheck
> @vybestack/llxprt-code-providers@0.10.0 typecheck
> @vybestack/llxprt-code-agents@0.10.0 typecheck
> @vybestack/llxprt-code@0.10.0 typecheck            # cli
> @vybestack/llxprt-code-a2a-server@0.10.0 typecheck # a2a-server
> @vybestack/llxprt-code-test-utils@0.10.0 typecheck
TYPECHECK_EXIT=0
```

### 4. Monorepo build — exit 0

```
$ npm run build   (tail)
[watch] build finished
BUILD_EXIT=0
```

### 5. Both export targets resolve to real built files

```
$ ls -la packages/agents/dist/index.js packages/agents/dist/index.d.ts \
         packages/agents/dist/src/internals.js packages/agents/dist/src/internals.d.ts
-rw-r--r--  193  packages/agents/dist/index.d.ts
-rw-r--r--  226  packages/agents/dist/index.js
-rw-r--r--  2718 packages/agents/dist/src/internals.d.ts
-rw-r--r--  2612 packages/agents/dist/src/internals.js
LS_EXIT=0
```

Shim confirmed (matches providers convention — `.` → dist/index.js shim, subpath → dist/src/...):

```
$ cat packages/agents/dist/index.js
...
export * from './src/index.js';
```

### 6. Lint — zero ERRORS (pre-existing warnings acceptable)

```
$ npm run lint --workspace @vybestack/llxprt-code-agents   (tail)
✖ 138 problems (0 errors, 138 warnings)
LINT_EXIT=0
```

All 138 are pre-existing warnings in unrelated files (turn.ts, task.ts, test files, eslint-comments pairs). Zero errors.

### 7. Deferred-implementation markers — empty

```
$ grep -rnE "(TODO|FIXME|HACK)" packages/agents/src/internals.ts packages/agents/src/index.ts | grep -v ".spec.ts"
EXIT_GREP=1   (no matches)
```

### 8. NON-BREAKING PROOF — symbol-set diff (HEAD index.ts vs current internals.ts)

`git show HEAD:packages/agents/src/index.ts` export lines were compared (sorted) against
current `internals.ts` export lines. The set difference (excluding
`createTaskToolRegistration`, which legitimately remains defined in index.ts) is EMPTY:

```
$ comm -23 <head_exports> <internals_exports> | grep -v createTaskToolRegistration
(empty)
```

Every low-level export statement present at HEAD is reproduced verbatim in internals.ts:
`AgentClient`, `PostTurnAction`, `ChatSession`/`StreamEventType`/`InvalidStreamError`/`StreamEvent`,
`ChatSessionFactory.*`, `CoreToolScheduler`, `executeToolCall`, `SubagentOrchestrator`,
`TaskTool`, `TaskToolDependencies`/`TaskToolParams`, `core/turn.*`, `subagent.*`,
`subagentExecution.*`, `subagentRuntimeSetup.*`, `subagentToolProcessing.*`,
`subagentScheduler.*`, `buildToolGovernance`/`isToolBlocked`/`ToolGovernance`/`ToolGovernanceConfig`,
`compression/*`, `agents/types.*`, `agents/invocation.*`, `agents/executor.*`,
`core/agenticLoop/*`. **No symbol dropped.**

The top-level (`index.ts`) re-exports all of these via `export * from './internals.js'`
and adds the public API via `export * from './api/index.js'`.

Runtime reachability against the BUILT artifacts:

```
$ node --input-type=module  (import dist/index.js + dist/src/internals.js)
low-level missing from TOP: []
low-level missing from INTERNALS: []
public API missing from TOP: []   # createAgent, listProviders, listTools all present
createTaskToolRegistration in internals (should be false): false
```

`createTaskToolRegistration` is intentionally NOT in internals.ts (avoids the
internals.ts ← index.ts circular dependency) and remains reachable at the top-level.

### 9. core/index.ts untouched

```
$ git diff --stat packages/core/src/index.ts
(empty)   EXIT=0
```

### 10. Prettier clean on artifacts

```
$ npx prettier --check packages/agents/src/internals.ts packages/agents/src/index.ts packages/agents/src/api/index.ts
All matched files use Prettier code style!
PRETTIER_EXIT=0
```

---

## Semantic Verification Checklist

**1. Does `./internals.js` resolve to real build output (dist/src/internals.js + .d.ts)?**
YES. Both `packages/agents/dist/src/internals.js` (2612 B) and `internals.d.ts` (2718 B)
exist after build. The package.json `./internals.js` export maps types →
`./dist/src/internals.d.ts`, import → `./dist/src/internals.js` — both present.

**2. Does top-level `.` additively expose the new public Agent surface WITHOUT breaking
existing low-level imports?**
YES. `index.ts` adds `export * from './api/index.js'`, surfacing `createAgent`, `Agent`,
`AgentConfig`, `AgentEvent`, `listProviders`, `listTools`, config/event schemas, and
handler types. Runtime check confirms `createAgent`/`listProviders`/`listTools` present at
top-level. All prior low-level symbols remain (via `export * from './internals.js'`).
Monorepo typecheck (incl. cli + a2a-server) exits 0.

**3. Are low-level symbols reachable via BOTH top-level AND `./internals.js`?**
YES. internals.ts is the single source of the low-level surface; index.ts re-exports it
via `export * from './internals.js'`. Runtime check: `AgentClient`, `ChatSession`,
`CoreToolScheduler`, `AgenticLoop`, `SubagentOrchestrator`, `TaskTool`, `executeToolCall`,
turn.js exports, compression exports, subagent* exports, toolGovernance all reachable from
both. (`createTaskToolRegistration` stays at top-level only — by design, to avoid a circular
dep; it is app-glue, not a power-user primitive.)

**4. Do cli + a2a-server still typecheck with no mandatory migration this phase?**
YES. `npm run typecheck` exits 0 with both `@vybestack/llxprt-code` (cli) and
`@vybestack/llxprt-code-a2a-server` typechecking against the unchanged additive top-level.

**5. Was `packages/core/src/index.ts` left unmodified?**
YES. `git diff --stat` is empty.

**6. Name-collision handling (ApprovalHandler, CompressionResult, ModelInfo,
ChatCompressionInfo, StructuredError).**

- **ModelInfo / ChatCompressionInfo / StructuredError** — re-exported by BOTH the internals
  side (via `./core/turn.js`) and the api side (via `./api/event-types.ts`). Verified that
  BOTH paths re-export these from the IDENTICAL source module
  `@vybestack/llxprt-code-core/core/turn.js` (turn.ts lines 144–147; event-types.ts lines
  7–18). Because the re-exports trace to the same symbol, TypeScript deduplicates them at
  the `export *` boundary — no ambiguous-reexport error. Confirmed empirically: typecheck +
  build clean, and the runtime import of dist/index.js succeeds. No disambiguation needed,
  and none introduced. Low-level identity is preserved (same source symbol).

- **ApprovalHandler** — GENUINE collision: api/config-types.ts (line 109) defines its own
  `ApprovalHandler` (a confirmation callback type) while core/agenticLoop/types.js exports a
  different low-level `ApprovalHandler`. index.ts resolves this with an explicit
  `export type { ApprovalHandler } from './core/agenticLoop/types.js';`, which takes
  precedence over the ambiguous `export *` merge — preserving the LOW-LEVEL definition at the
  top-level (non-breaking for existing consumers).

- **CompressionResult** — GENUINE collision: api/agent.ts (line 92) defines its own
  `CompressionResult` interface while core compression types export a different one. index.ts
  resolves with explicit `export type { CompressionResult } from
  '@vybestack/llxprt-code-core/core/compression/types.js';` — again preserving the low-level
  symbol at top-level.

- **Harmlessness check**: grep of `packages/cli/src` and `packages/a2a-server/src` for
  imports of `ApprovalHandler`/`CompressionResult` from the agents top-level returned NONE.
  So even though the explicit re-export makes the low-level versions win at the top-level,
  there is no current monorepo consumer relying on either name from the agents top-level —
  the resolution is both correct (preserves low-level for any future/legacy consumer) and
  provably harmless to existing consumers. The resolution did NOT remove or shadow any
  low-level symbol existing consumers rely on.

**7. Strict TS hygiene.**
No new `any`. The single `as` (`config as Config` in `createTaskToolRegistration`) is
PRE-EXISTING — confirmed identical at HEAD (line 66) — not introduced by this phase.
Plan/requirement markers present (`@plan:PLAN-20260617-COREAPI.P07` ×1 +
`@requirement:REQ-018`). Prettier clean on all three artifacts. No new `as` assertion added.

---

## Holistic Functionality Assessment

**Resulting export topology:**

- **Top-level (`.` → dist/index.js shim → dist/src/index.js):** the union of (a) the NEW
  curated public Agent API (`export * from './api/index.js'`: createAgent, Agent, AgentConfig,
  AgentEvent, ApprovalMode, listProviders, listTools, config/event schemas, handler types),
  (b) the FULL retained low-level surface (`export * from './internals.js'`), (c) two explicit
  collision-resolving `export type` lines that preserve the low-level `ApprovalHandler` and
  `CompressionResult`, and (d) `createTaskToolRegistration` (app-glue, defined inline).
- **`./internals.js` subpath (→ dist/src/internals.js / .d.ts):** the SINGLE SOURCE of the
  low-level re-export surface — the documented power-user path that #1595 will migrate
  CLI/a2a consumers to before trimming the top-level.
- **Single-source design:** index.ts derives its low-level surface FROM internals.ts, so the
  two can never drift.

**Non-breaking confirmation:** The symbol-set diff between HEAD's top-level and the current
internals barrel is empty — zero low-level symbols dropped from the top-level. All prior
top-level symbols remain reachable, the whole monorepo (cli + a2a-server included) typechecks
and builds, and core/index.ts is untouched. The internals subpath is real (both build targets
exist) and documented (detailed header in internals.ts explaining single-source rationale,
the createTaskToolRegistration exclusion, the collision handling, and the non-breaking proof).
This faithfully matches REQ-018/REQ-019 intent: additive public API + power-user subpath now,
final curated trim + consumer migration deferred to #1595.

---

Verdict: PASS
