Phase: P02 (Pseudocode Finalization)
Plan ID: PLAN-20260617-COREAPI.P02
Completed: 2026-06-17
Remediated: 2026-06-17 (post P02a FAIL — defects G1/G2/G3 fixed)
Re-remediated: 2026-06-17 (post P02a deep-gate round 2 — DEFECT1/2/3 + minor fixed)

Prerequisite: P01a (PASS) — verified `project-plans/issue1594/.completed/P01a.md` exists.
Remediation basis: P02a (FAIL) — `project-plans/issue1594/.completed/P02a.md`.

Files Modified (all under `project-plans/issue1594/analysis/pseudocode/`):
- createAgent.md
- config-adapter.md
- event-adapter.md
- switch-rebind.md
- tool-confirmation-merge.md
- dispose.md

No files outside `analysis/pseudocode/` were modified (other than this marker).

---

## Per-file confirmation/changes

### createAgent.md (REQ-001)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-001 -->`
- Added explicit INPUTS/OUTPUTS/DEPENDENCIES section reference header.
- Confirmed ordering: provider-manager/shared-runtime-context established via
  `createIsolatedRuntimeContext` (step 3, line 41) and activated via
  `await handle.activate()` (step 4, line 58) BEFORE `refreshAuth` (step 6, line 91).
- Confirmed uses `createIsolatedRuntimeContext` + `handle.activate()` per P00a
  corrections (NOT bare `createHeadlessProviderManager` — explicit anti-pattern warning
  present).
- Confirmed binds ONLY the post-auth client (line 111 `config_.getAgentClient()`);
  transient pre-auth client is never bound (line 76 comment).
- Confirmed `rebuildLoop()` used for initial loop build (steps 125-140); AgenticLoop
  caches its client readonly, so the facade owns rebuildLoop for every rebind.
- Confirmed `createAgentRuntimeState` called with required `runtimeId` (step 7, B4).

### config-adapter.md (REQ-002)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-002 -->`
- Confirmed classification table (CLASSIFICATION.typed) present with full typed-field
  enumeration (lines 61-71).
- Confirmed escape-hatch THROW-on-shadow behavior (lines 83-84: `THROW AdapterError`
  when a settings key shadows a typed target).
- Confirmed INPUTS/OUTPUTS/DEPENDENCIES interface contracts present.

### event-adapter.md (REQ-003)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-003 -->`
- Confirmed all 21 GeminiEventType variants mapped (mapStreamEvent switch, lines
  210-246: Content, Thought, ToolCallRequest, ToolCallResponse, ToolCallConfirmation,
  UserCancelled, StreamIdleTimeout, Error, ChatCompressed, UsageMetadata,
  MaxSessionTurns, Finished, LoopDetected, Citation, Retry, SystemNotice,
  InvalidStream, ContextWindowWillOverflow, ModelInfo, AgentExecutionStopped,
  AgentExecutionBlocked).
- Confirmed terminal decision table present (all 21 variants classified
  terminal/intermediate).
- Confirmed exactly-one synthesized `done` at the AgenticLoop boundary (ensureDone at
  line 200-204; `done` is decided at `AgenticLoop.run` boundary, not inner stream end).
- Confirmed AgentExecutionBlocked is NON-terminal (line 240: `hook-blocked`, turn
  continues).
- Confirmed AgentExecutionStopped is terminal (line 242: `done{hook-stopped}`).
- Confirmed idle-timeout is terminal (lines 232-233: emits `idle-timeout` then
  `pendingDoneReason='error'`; line 200 synthesizes the single `done`).

### switch-rebind.md (REQ-004/REQ-005)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-004/REQ-005 -->`
- Confirmed same-HistoryService assertion present: `ASSERT config.getHistoryService()
  === prevHistoryService` (lines 34, 55, 74 for setProvider/setModel/applyProfile).
- Confirmed stripThoughts normalization (anti-pattern warning + verified notes:
  transfer path normalizes per target provider).
- Confirmed reattach subscriptions inside `rebuildLoop` (line 23
  `attachPerTurnSubscriptions` on the NEW loop).
- Confirmed rebuild-hook name pinned to `initializeContentGeneratorConfig`
  (P00a B1/B5): explicit model-only rebuild at line 54
  (`await config.initializeContentGeneratorConfig()`); `switchActiveProvider` and
  `applyProfileSnapshot` rebuild internally (verified, no separate rebuild needed).

### tool-confirmation-merge.md (REQ-006)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-006 -->`
- Confirmed dedup by confirmationId (lines 11, 26-28: `seen = new Set()`,
  `seen.add(confirmationId)`, dedup RETURN).
- Confirmed respond keyed on correlationId (lines 67-68: `correlationId:
  confirmationId` in published response).
- Confirmed editor-modify produces new correlationId (lines 64-66: `seen.delete` +
  coordinator issues new correlationId for re-confirmation).
- Confirmed public Agent no-handler ASK_USER => AgenticLoop safe-denial (NOT throw)
  (lines 80-89: SAFE TOOL DENIAL, delegated to AgenticLoop; NOT a throw on the
  public path).
- Confirmed raw coordinator internals throw ONLY on the documented power-user
  `internals` subpath (lines 87-89: coordinator throw scoped to raw `internals`
  path only).

### dispose.md (REQ-016)
- Added HTML marker: `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-016 -->`
- Confirmed idempotent (lines 11-13: `ownership.disposed` guard).
- Confirmed awaited teardown (all `await safe(errors, ...)` calls, lines 20-92).
- Confirmed ownership/teardown table present with all resources.
- Confirmed net-new cleanup flags (LSP line 70, extensions line 80, session locks
  line 82 — all marked NET-NEW).
- Changed `ownership.loop` to `ownership.loopHolder.current` (B1 consistency: the loop
  lives in a mutable slot since `rebuildLoop` reconstructs it on every client rebind).
- Added `runtimeHandle.cleanup()` teardown step (line 55) to unregister the runtime
  context (B5) — OAuth infra disposed within cleanup.
- Added `runtimeHandle` to dependencies + ownership table (runtime context row).

---

## Structural Verification Checklist

- [x] All six files numbered, contract-first (numbered step lines `^[0-9]+:` present)
- [x] Anti-pattern warnings present in all six files
- [x] Rebuild-hook name pinned to `initializeContentGeneratorConfig` (P00a B1) in
      switch-rebind.md
- [x] No TypeScript implementation (grep for impl patterns returns OK)
- [x] HTML marker `<!-- @plan:PLAN-20260617-COREAPI.P02 @requirement:REQ-XXX -->`
      present in all six files with correct REQ IDs

---

## Verification Outputs

### Verification 1: Files present + numbered lines
```
--- Verification 1: files + numbered lines ---
(no output above = all present + all have numbered lines)
```
Result: PASS (no MISSING, no NO NUMBERED LINES)

### Verification 2: Contract sections (INPUTS/OUTPUTS/DEPENDENCIES)
```
--- Verification 2: contract sections ---
(no output above = all sections present)
```
Result: PASS (no NO INPUTS, no NO OUTPUTS, no NO DEPS)

### Verification 3: No real TS impl
```
--- Verification 3: no real TS impl ---
OK
```
Result: PASS (prints "OK", not "POSSIBLE IMPL CODE")

Verdict: PASS

================================================================================
REMEDIATION (post P02a FAIL) — defects G1, G2, G3 fixed 2026-06-17
================================================================================

P02a (deep review) FAILED the pseudocode with three source-verified defects. All
three were REAL: the pseudocode named APIs that DO NOT EXIST on the cited types.
The underlying designs (loop rebuild on rebind; HistoryService-identity continuity;
idempotent awaited teardown) were sound; only the named real-code surfaces were
wrong. Fixes were surgical and confined to the three files with defects
(createAgent.md, switch-rebind.md, dispose.md). event-adapter.md,
config-adapter.md, and tool-confirmation-merge.md were marked CLEAN by P02a and
were NOT modified.

Grounding re-verified against real source:
- AgenticLoop (`packages/agents/src/core/agenticLoop/AgenticLoop.ts`): public API is
  `async *run(message, signal, promptId?)` (line 270). Cancellation is via the
  AbortSignal; the loop self-cleans in run()'s `finally`
  (`unsubscribe(); this.isRunning = false`, lines ~300-305). There is NO
  `cancelActiveRun()`, NO `cancelActiveRunIfSupported()`, NO `dispose()`.
- HistoryService accessor: `AgentClientContract.getHistoryService(): HistoryService
  | null` (`packages/core/src/core/clientContract.ts:54,73`). Config has NO
  `getHistoryService()`; config's own continuity logic reads it from the client
  (`previousAgentClient.getHistoryService()` config.ts:264;
  `this.agentClient.getHistoryService()` config.ts:376).

---

### G1 (BLOCKING) — Nonexistent loop-cancellation API — FIXED

Replaced all references to `AgenticLoop.cancelActiveRun()` /
`cancelActiveRunIfSupported()` / `dispose()` with the facade-owned-AbortController
abort + facade-recorded-subscription-teardown model. The Agent facade OWNS an
AbortController per active run() and aborts it to cancel; per-turn MessageBus/
scheduler subscriptions are RECORDED BY THE FACADE and unsubscribed by the facade
(no loop method involved).

- createAgent.md (G1a):
  - rebuildLoop contract note (after the numbered block): rewrote step (1) to abort
    the facade-owned `AbortController` for the active run (the `signal` passed into
    `AgenticLoop.run(message, signal)`) and unsubscribe facade-RECORDED per-turn
    subscriptions; cited the real self-cleanup in run()'s finally
    (AgenticLoop.ts:300-305). Step (3) now records a FRESH facade-owned
    AbortController for the new loop's next run.
  - `rebuildLoop` ownership blockquote: "disposes the prior loop + subscriptions" →
    "aborts the prior run's facade-owned AbortController + unsubscribes
    facade-RECORDED per-turn subscriptions".
  - Numbered comment block (Step 9): expanded to state there is NO loop
    cancel/dispose method; the facade aborts its AbortController and unsubscribes
    facade-recorded subscriptions.
  - Notes for impl phase: added the G1 clarification to the loop-build note.
- switch-rebind.md (G1b):
  - rebuildLoop line 13: `await oldLoop.cancelActiveRunIfSupported()` →
    `deps.activeRunController?.abort()` (facade-owned AbortController; cited
    AgenticLoop.ts:300-305).
  - rebuildLoop line 14: `unsubscribeLoopBusAndSchedulerSubscriptions(oldLoop)` →
    `unsubscribeFacadeRecordedSubscriptions(oldLoop)` (facade-recorded; NO loop
    method).
  - rebuildLoop line 23 (new): `deps.activeRunController = createAbortController()`
    so the next `run(message, signal)` gets a fresh facade-owned signal.
  - rebuildLoop line 24: clarified `attachPerTurnSubscriptions` records into
    `deps.busSubscriptions` (unsubscribed by rebuildLoop/dispose, not a loop method).
- dispose.md (G1c):
  - Line 30: `ownership.loopHolder.current?.cancelActiveRun()` →
    `ownership.activeRunController?.abort()` (facade-owned AbortController; cited
    AgenticLoop.ts:300-305).
  - DEPENDENCIES: added `activeRunController`.
  - Integration Points Line 30: rewrote to describe aborting the facade-owned
    activeRunController.
  - Ownership/teardown table "AgenticLoop active run" row:
    `loopHolder.current.cancelActiveRun()` → "facade-owned activeRunController.abort()
    (the signal passed to loop.run; loop self-cleans in run()'s finally)".
  - Notes for impl phase: rewrote the line-30 note to explain the
    facade-owned-controller cancellation model (B1 + G1).

### G2 (BLOCKING) — Nonexistent config.getHistoryService() — FIXED

The REQ-005 invariant intent (existingHistoryService === newHistoryService) is
CORRECT and was KEPT; only the accessor changed. HistoryService is now read via the
CLIENT (`AgentClient.getHistoryService()`), not Config. `resolveClient()` (the
facade's always-current client accessor, consistent with createAgent.md) is captured
as the OLD client BEFORE the switch/rebuild, and a NEW `resolveClient()` call reads
the post-mutation client AFTER. The pseudocode notes getHistoryService() can return
null and that continuity transfer is expected to preserve a non-null service.

- switch-rebind.md (G2) — setProvider / setModel / applyProfile (all three methods):
  - BEFORE the mutation: `oldClient = resolveClient()` then
    `prevHistoryService = oldClient.getHistoryService()` (with citations to
    clientContract.ts:54,73 and config.ts:264,376).
  - AFTER the mutation: `newClient = resolveClient()` then
    `ASSERT newClient.getHistoryService() === prevHistoryService` (REQ-005; read via
    the NEW client, NOT config; expected non-null).
  - Updated Notes for impl phase to reflect the corrected accessor + shifted line
    numbers (assertions now at 35/56/75; client replacement 36/57/76; rebuildLoop
    37/58/77).

### G3 (MINOR) — Duplicate / out-of-order numbered labels — FIXED

- dispose.md: the OAuth block had two `91:` labels (the second after `92:`). Fixed
  to a monotonic sequence (90 comment, 91 comment, 92 await, 93 blank). Audited the
  ENTIRE file: all 39 step labels are now unique and monotonic.
- createAgent.md (side-effect of the G1a comment expansion): the expanded Step 9
  comment block collided the code labels 132/133/134 with comment lines; renumbered
  the rebuildLoop code block to 135-143 and shifted the subsequent ownership/assemble/
  SessionStart/return labels accordingly; updated all stale cross-references
  (Integration Points, Notes, "steps 10-26" → "10-27").
- switch-rebind.md (side-effect of G1b/G2 line additions): four trailing blank
  separator lines after `END METHOD` (at labels 27/39/60/79) collided with the
  `END METHOD` lines; renumbered the blanks to 28/40/61/80 so every label is unique
  and monotonic.

---

## Verification Outputs (POST-REMEDIATION)

Run from repo root `/Users/acoliver/projects/llxprt/branch-3/llxprt-code`:

### Suite 1: files exist + numbered lines
```
>>> SUITE 1: files exist + numbered lines
(no output — no MISSING, no NO NUMBERED LINES for any of the 6 files)
```
Result: PASS

### Suite 2: INPUTS/OUTPUTS/DEPENDENCIES present
```
>>> SUITE 2: INPUTS/OUTPUTS/DEPENDENCIES
(no output — no NO INPUTS / NO OUTPUTS / NO DEPS for any of the 6 files)
```
Result: PASS

### Suite 3: no real TS impl
```
>>> SUITE 3: impl-code grep
OK
```
Result: PASS (prints "OK", not "POSSIBLE IMPL CODE")

### Suite 4: G1 phantom loop API
```
>>> SUITE 4: G1 phantom loop API
G1 CLEAN
```
Result: PASS (no cancelActiveRun / cancelActiveRunIfSupported anywhere)

### Suite 5: G2 phantom config accessor
```
>>> SUITE 5: G2 phantom config accessor
G2 CLEAN
```
Result: PASS (no config.getHistoryService anywhere)

### Suite 6: dispose duplicate labels
```
>>> SUITE 6: dispose duplicate labels
(no DUP LABEL output)
```
Result: PASS

### Supplementary: dup-label audit on the two other touched files
```
createAgent.md: (none above = ok)
switch-rebind.md: (none above = ok)
```
Result: PASS (all step labels unique and monotonic across all three touched files)

### Supplementary: CLEAN files untouched
event-adapter.md, config-adapter.md, tool-confirmation-merge.md: 0 phantom-API
matches; older mtimes than the three remediated files — NOT modified.

Verdict (post-remediation): PASS — all six files contract-first, no phantom APIs,
no duplicate labels, no implementation code.

---

## D1/D2/D3 Remediation (post second P02a FAIL — wrong-signature/wrong-symbol defects)

Remediation date: 2026-06-17
Basis: P02a (second FAIL) — `project-plans/issue1594/.completed/P02a.md` found three
BLOCKING wrong-signature/wrong-symbol defects where the pseudocode called APIs with
signatures that DO NOT MATCH the real source. All three verified against actual source.

Files Modified (only the three defective files):
- `project-plans/issue1594/analysis/pseudocode/tool-confirmation-merge.md` (D1)
- `project-plans/issue1594/analysis/pseudocode/createAgent.md` (D2)
- `project-plans/issue1594/analysis/pseudocode/switch-rebind.md` (D3)

Files NOT modified (verified correct, untouched): config-adapter.md, event-adapter.md,
dispose.md.

### D1 — tool-confirmation-merge.md: scheduler.onStatusUpdate does not exist

VERIFIED TRUTH: `grep -rn onStatusUpdate packages/agents/src packages/core/src
packages/tools/src` => ZERO matches. CoreToolScheduler surfaces tool status ONLY via
constructor/setCallbacks handlers (onToolCallsUpdate, outputUpdateHandler,
onAllToolCallsComplete — coreToolScheduler.ts:89-91,185-191). The Agent facade does NOT
own a stable scheduler; AgenticLoop creates its OWN transient per-turn scheduler
internally (AgenticLoop.ts getOrCreateScheduler, ~173-189) and surfaces tool status
through the AgenticLoopEvent stream as kinds tool_update (:455), tool_output (:444),
tools_complete (:357) — the SAME stream event-adapter.md consumes.

CHANGES:
- DEPENDENCIES: replaced `scheduler: CoreToolScheduler // status updates source` with
  `loopEvents: AsyncIterable<AgenticLoopEvent>` documenting it as the SAME loop-event
  stream event-adapter.md consumes (tool_update / tool_output / tools_complete).
- Integration Points line 40: replaced `scheduler.onStatusUpdate` with
  `loopEvents tool_update/tool_output/tools_complete`.
- Numbered step 40: replaced the phantom `scheduler.onStatusUpdate((raw) => ...)`
  subscription with a `FOR AWAIT ev IN loopEvents` loop that SWITCHes on ev.kind
  (tool_update / tool_output / tools_complete), projecting each to ToolUpdate via
  fireOnToolUpdate + emit. Added explanatory comments citing the real source locations
  and the real callback-handler alternative.
- Renumbered the respondToConfirmation method (80-91) and B7 comment block (100-109) to
  stay unique and monotonic after the expanded status block (40-68).
- Added anti-pattern warning: "DO NOT subscribe to a status-update method on the
  scheduler" with the real alternatives.
- Updated Notes for impl phase cross-references.

### D2 — createAgent.md: switchActiveProvider passed model as 2nd arg

VERIFIED TRUTH: `switchActiveProvider(providerName: string, options: ProviderSwitchOptions
= {})` (providerSwitch.ts:803-805). ProviderSwitchOptions members are { autoOAuth?,
preserveEphemerals?, skipModelDefaults?, addItem? } (providerSwitch.ts:56-63) — there is
NO model field. Passing a string is a type error. The model is applied via
`setActiveModel(modelName: string)` (providerMutations.ts:406) +
`config.initializeContentGeneratorConfig()` (model-only changes do NOT auto-rebuild —
P00a/B5). switchActiveProvider resolves a default model internally via
resolveModelToApply.

CHANGES:
- Line 63: corrected `switchActiveProvider(config.provider, config.model)` to
  `switchActiveProvider(config.provider)` with a comment documenting the real signature
  and that ProviderSwitchOptions has NO model field.
- Added lines 64-66: after the switch, IF config.model differs from active model, call
  `setActiveModel(config.model)` + `config_.initializeContentGeneratorConfig()`.
- Lines 67-69 (ELSE IF model-only branch): added explicit
  `config_.initializeContentGeneratorConfig()` rebuild after setActiveModel.
- Added anti-pattern warning: "DO NOT pass a model to switchActiveProvider".
- Renumbered Step 5 onward (80-176) to stay unique and monotonic after the expanded
  Step 4b block (61-79).
- Updated Integration Points and Notes for impl phase cross-references.

### D3 — switch-rebind.md: switchActiveProvider passed { model }

VERIFIED TRUTH: same as D2 — `{ model }` is NOT a member of ProviderSwitchOptions
(excess-property; silently ignored). switchActiveProvider resolves the model internally
via resolveModelToApply (providerSwitch.ts:467,838-847).

CHANGES:
- Line 33: corrected `switchActiveProvider(provider, { model: model })` to
  `switchActiveProvider(provider)` with a comment documenting the real signature.
- Added lines 34-36: after the switch, IF model IS defined AND differs from active
  model, call `setActiveModel(model)` + `config.initializeContentGeneratorConfig()`
  (consistent with the setModel path in the same file, lines 53-54).
- Renumbered the setProvider method (30-42) to stay unique and monotonic.
- Updated Integration Points line 30 entry to document the real signature and the
  post-switch model-application path.
- Added anti-pattern warning: "DO NOT pass a model to switchActiveProvider".
- Updated Notes for impl phase: added D3 pinned note, corrected continuity-assertion
  line references (38/56/75, 39/57/76, 40/58/77).

### Verification outputs (post D1/D2/D3 remediation)

```
$ grep -rn "onStatusUpdate" project-plans/issue1594/analysis/pseudocode/ && echo "STILL HAS PHANTOM onStatusUpdate" || echo "D1 CLEAN"
D1 CLEAN

$ grep -rnE "switchActiveProvider\([^,]+, *\{ *model|switchActiveProvider\([^,]+, *[A-Za-z_]+\.model|switchActiveProvider\([^,)]+, *config\.model" project-plans/issue1594/analysis/pseudocode/ && echo "STILL PASSES MODEL TO switchActiveProvider" || echo "D2D3 CLEAN"
D2D3 CLEAN

$ grep -rn "setActiveModel" project-plans/issue1594/analysis/pseudocode/createAgent.md project-plans/issue1594/analysis/pseudocode/switch-rebind.md
(24 matches across both files — setActiveModel now the documented model-application mechanism in both)

$ for f in createAgent config-adapter event-adapter switch-rebind tool-confirmation-merge dispose; do test -f ...$f.md || echo "MISSING $f"; grep -qE "^[0-9]+:" ... || echo "NO NUMBERED LINES $f"; grep -qi "INPUTS" ... || echo "NO INPUTS $f"; grep -qi "OUTPUTS" ... || echo "NO OUTPUTS $f"; grep -qi "DEPENDENCIES" ... || echo "NO DEPS $f"; done
(no output — all 6 files pass: present, numbered lines, INPUTS/OUTPUTS/DEPENDENCIES)

$ grep -rnE "import .* from|const .*= async|=> \{ *$" project-plans/issue1594/analysis/pseudocode/ && echo "POSSIBLE IMPL CODE" || echo "OK"
OK

$ for f in createAgent switch-rebind tool-confirmation-merge dispose; do awk -F: -v F="$f" '/^[0-9]+:/{if(seen[$1]++) print "DUP LABEL "$1" in "F}' ...$f.md; done
(no output — no duplicate labels)
```

Verdict (post D1/D2/D3 remediation): PASS — D1 CLEAN, D2D3 CLEAN, setActiveModel present
in both files, all six files contract-first with unique monotonic labels, no
implementation code, status source consistent with event-adapter.md.

---

## P02 remediation round 2 (DEFECT1/2/3 + minor) — post P02a deep-gate FAIL

Basis: `project-plans/issue1594/.completed/P02a.md` (deep pseudocode-compliance gate,
verdict FAIL) flagged three defects where pseudocode referenced event payload shapes /
symbols / ownership that DO NOT match real source, plus one minor tightening.

Files Modified (only under `project-plans/issue1594/analysis/pseudocode/`):
- event-adapter.md
- dispose.md
- switch-rebind.md

No other files were modified (other than this marker). tool-confirmation-merge.md,
createAgent.md, config-adapter.md were NOT edited (read for consistency only).

### DEFECT 1 (CRITICAL — wrong payload shape): event-adapter.md
GROUND: `packages/core/src/core/turn.ts:273-286` —
`ServerGeminiAgentExecutionStoppedEvent` and `ServerGeminiAgentExecutionBlockedEvent`
carry FLAT fields (`reason`/`systemMessage?`/`contextCleared?`) with NO `.value` wrapper
(unlike Content/Thought/ToolCall*/UsageMetadata/ModelInfo/SystemNotice/ChatCompressed/
Citation/ContextWindowWillOverflow/StreamIdleTimeout/Error/Finished, which all DO carry
a `value:` field).

CHANGES:
- Lines 240-241: `toStopInfo(e.value)` -> `toStopInfo(e)`, with an inline comment pinning
  the FLAT event shape (turn.ts:274-286 / 281-286) and the "NO .value wrapper" caveat.
- INPUTS contract note: added an EXCEPTION line pinning that AgentExecutionStopped/
  AgentExecutionBlocked carry flat fields with no `.value` wrapper, while all other
  value-bearing variants DO use `.value`.
- "Notes for impl phase": added a pinned `toStopInfo` contract note stating it reads
  `e.reason` / `e.systemMessage?` / `e.contextCleared?` directly off the FLAT event,
  and that passing `e.value` yields undefined and silently drops the stop/block info
  (defeating AgentStopInfo + the hook-stopped `stop` payload).
- The OTHER `e.value` accesses (lines 212-238, 243-244) were LEFT UNCHANGED — only the
  two stop/block variants were flat. Decision-table rows for the two variants reference
  only terminal/hook-blocked status, not `.value`, so needed no change.

### DEFECT 2 (MEDIUM — wrong symbol): dispose.md
GROUND: `packages/core/src/config/configBase.ts:159` — real LSP teardown is
`config.shutdownLspService()` (calls `shutdownLsp(this._lspState, this.toolRegistry)`
from lspIntegration.ts:388). It is a method on Config (configBase), NOT a separate
`lspManager` object. The method exists on Config but is NOT invoked by `Config.dispose()`
today -> remains NET-NEW wiring.

CHANGES:
- Line 70: `ownership.lspManager.shutdown()` -> `ownership.config.shutdownLspService()`,
  with comment pinning configBase.ts:159 -> shutdownLsp (lspIntegration.ts:388) and the
  "method exists on Config but NOT called by Config.dispose() today" + "no separate LSP
  manager object" caveats.
- DEPENDENCIES: removed `lspManager` (it does not exist as a separate object); the Config
  the facade already owns exposes `shutdownLspService()`.
- Integration Points "Line 70" note: updated to `ownership.config.shutdownLspService()`
  with the configBase.ts:159 / lspIntegration.ts:388 citation and NET-NEW flag.
- Ownership-table LSP row: resource=LSP service, creator=config/configBase,
  teardown=config.shutdownLspService(), exists-today="method exists on Config but NOT
  called by Config.dispose() today (NET-NEW wiring); teardown is a Config method, NOT a
  separate object". Kept flagged NET-NEW.

### DEFECT 3 (MEDIUM — wrong ownership / cross-file inconsistency): dispose.md
GROUND: `packages/agents/src/core/agenticLoop/AgenticLoop.ts:436,552` — AgenticLoop
creates a TRANSIENT per-turn scheduler via `config.getOrCreateScheduler(...)` and
DISPOSES it ITSELF via `config.disposeScheduler(sessionId)` in its finally. The Agent
facade does NOT own a stable scheduler/coordinator (matches tool-confirmation-merge.md
D1). The ONLY genuinely Agent-owned scheduler case is the CONDITIONAL one where an
injected `toolSchedulerFactory` creates an instance the facade holds (T19).

CHANGES (chose the preferred SCOPE-to-conditional-T19 option, NOT removal):
- Lines 40-42 / 45-47: wrapped the scheduler/coordinator disposal in existence guards
  (`IF ownership.injectedFactoryScheduler EXISTS` / `IF ownership.injectedFactoryCoordinator
  EXISTS`) so a facade-held instance is disposed ONLY IF one was created via the injected
  toolSchedulerFactory and retained by the facade. Inline comments cite
  config.getOrCreateScheduler (AgenticLoop.ts:436) / config.disposeScheduler
  (AgenticLoop.ts:552) and state dispose() does NOT touch loop-owned schedulers.
- DEPENDENCIES: replaced `scheduler, confirmationCoordinator` with
  `injectedFactoryScheduler?, injectedFactoryCoordinator?` (optional, T19-conditional).
- Integration Points "Line 40"/"Line 45" notes: rewritten to CONDITIONAL T19-only,
  citing getOrCreateScheduler/disposeScheduler and the loop's finally.
- Ownership-table scheduler row: split into two rows —
  (a) per-turn loop scheduler (TRANSIENT): AgenticLoop-owned via
      config.getOrCreateScheduler (:436), disposed by AgenticLoop via
      config.disposeScheduler (:552) in the loop's finally, NOT by dispose();
  (b) facade-held injected-factory instance (T19, CONDITIONAL): created via injected
      toolSchedulerFactory, disposed by injectedFactoryScheduler.dispose() (guarded).
  ConfirmationCoordinator row: owned by whichever scheduler created it (per-turn =>
  loop-owned; facade-held => only the conditional T19 case). Wording now consistent with
  tool-confirmation-merge.md D1 (the facade does NOT own a stable scheduler).
- "Notes for impl phase": added a new bullet explaining lines 40-47 are CONDITIONAL (T19)
  and that per-turn schedulers are transient/loop-owned, reconciling with
  tool-confirmation-merge.md D1.
- Renumbered labels (introduced 42/47) so every numbered label in the file stays UNIQUE
  and MONOTONIC; removed a stray duplicate `46:` blank label that would have been a
  DUP LABEL.

### MINOR (non-blocking): switch-rebind.md
GROUND: the existing anti-pattern line asserted stripThoughts normalization happens "in
the transfer path" without pinning the exact symbol/step.

CHANGES:
- Anti-pattern "DO NOT skip stripThoughts normalization" line: pinned to the EXISTING
  Config transfer path (`config.initializeContentGeneratorConfig ->
  extractExistingState -> transferHistoryToNewClient`), describing the
  provider-incompatible thought-signature stripping (stripThoughtSignatures when
  switching into Vertex, per overview/spec) as performed by the transfer path, NOT the
  facade (the facade calls no separate normalization method). Added a one-line note that
  this is verified behaviorally by T4f. Kept minimal — no invented method name.

### Verification outputs (post DEFECT1/2/3 + minor remediation)

```
$ grep -nE "AgentExecutionStopped|AgentExecutionBlocked" project-plans/issue1594/analysis/pseudocode/event-adapter.md
(toStopInfo(e) on both lines 240/241; FLAT-event caveats present; no .value on either)

$ grep -n "toStopInfo(e.value)" project-plans/issue1594/analysis/pseudocode/event-adapter.md && echo "STILL READS e.value FOR STOP EVENTS" || echo "DEFECT1 CLEAN"
DEFECT1 CLEAN

$ grep -n "lspManager" project-plans/issue1594/analysis/pseudocode/dispose.md && echo "STILL HAS lspManager" || echo "DEFECT2 CLEAN"
DEFECT2 CLEAN

$ grep -n "shutdownLspService" project-plans/issue1594/analysis/pseudocode/dispose.md
(Line 70 numbered line, Integration Points "Line 70" note, ownership-table LSP row — 3 occurrences)

$ grep -nE "getOrCreateScheduler|disposeScheduler|toolSchedulerFactory|conditional|T19" project-plans/issue1594/analysis/pseudocode/dispose.md
(matches on Integration Points notes lines 32/33, numbered lines 40-47, ownership-table rows 103-105, Notes bullet 114-117)

$ for f in createAgent config-adapter event-adapter switch-rebind tool-confirmation-merge dispose; do test -f ...$f.md || echo "MISSING $f"; grep -qE "^[0-9]+:" ... || echo "NO NUMBERED LINES $f"; grep -qi "INPUTS" ... || echo "NO INPUTS $f"; grep -qi "OUTPUTS" ... || echo "NO OUTPUTS $f"; grep -qi "DEPENDENCIES" ... || echo "NO DEPS $f"; awk -F: -v F="$f" '/^[0-9]+:/{if(seen[$1]++) print "DUP LABEL "$1" in "F}' ...$f.md; done
(no output — all 6 files present, numbered lines, INPUTS/OUTPUTS/DEPENDENCIES, no duplicate labels)

$ grep -rnE "import .* from|const .*= async|=> \{ *$" project-plans/issue1594/analysis/pseudocode/ && echo "POSSIBLE IMPL CODE" || echo "OK"
OK
```

Verdict (post DEFECT1/2/3 + minor remediation): PASS — DEFECT1 CLEAN, DEFECT2 CLEAN,
shutdownLspService present, scheduler ownership scoped to conditional T19 with
getOrCreateScheduler/disposeScheduler cited, all six files contract-first with unique
monotonic labels, no implementation code, dispose.md scheduler wording consistent with
tool-confirmation-merge.md D1.
