# Phase: P11a — Core Behavior Harness Verification

Independent verification of Phase 11 (P11) — Layer-3 "core Agent behavior" RED
harness for `@vybestack/llxprt-code-agents`. Plan: PLAN-20260617-COREAPI.

## 1. `npm test … --testNamePattern "@plan:.*P11"` — RUN + NATURAL RED

All three new spec files COLLECTED (0 collection errors) and RAN. Result:

```
 ❯ src/api/__tests__/core-history.spec.ts      (7 tests | 7 failed)
 ❯ src/api/__tests__/core-tools.spec.ts        (8 tests | 8 failed)
 ❯ src/api/__tests__/core-conversation.spec.ts (5 tests | 5 failed)
 ⎯⎯⎯⎯⎯⎯ Failed Tests 20 ⎯⎯⎯⎯⎯⎯
```

20 total / 20 failed — exactly the RED state P11 requires. Every failure
originates from a REAL stub method body reached THROUGH the harness/public API
(not a `toThrow` matcher, not a "module Failed to load"). Representative quoted
natural-failure stacks proving the right reason:

```
FAIL  core-conversation.spec.ts > T1 stream yields ordered thinking then text …
Error: NotYetImplemented
 ❯ AgentImpl.stream src/api/agentImpl.ts:52:11
 ❯ drain src/api/__tests__/helpers/agentHarness.ts:125:20
 ❯ src/api/__tests__/core-conversation.spec.ts:41:28

FAIL  core-conversation.spec.ts > T9 abort mid-stream …
Error: NotYetImplemented
 ❯ AgentImpl.stream src/api/agentImpl.ts:52:11
 ❯ src/api/__tests__/core-conversation.spec.ts:88:26

FAIL  core-tools.spec.ts > T2 tool call → confirm → result …
Error: NotYetImplemented
 ❯ ToolControl.onConfirmationRequest src/api/control/toolControl.ts:25:11
 ❯ respondToFirstConfirmation src/api/__tests__/helpers/agentHarness.ts:226:35
 ❯ src/api/__tests__/core-tools.spec.ts:52:25

FAIL  core-history.spec.ts > T8b onStats receives SessionStats …
Error: NotYetImplemented
 ❯ AgentImpl.onStats src/api/agentImpl.ts:137:11
 ❯ src/api/__tests__/core-history.spec.ts:141:27
```

The two property tests also fail NATURALLY (fast-check surfaces a counterexample,
then `Caused by: Error: NotYetImplemented` at `AgentImpl.setHistory` /
`ToolControl.onConfirmationRequest`):

```
FAIL core-history.spec.ts > T6p property …
Counterexample: [[]]
Caused by: Error: NotYetImplemented
 ❯ AgentImpl.setHistory src/api/agentImpl.ts:103:11
```

Stub bodies hit across the suite: `AgentImpl.stream`, `.chat`, `.getHistory`,
`.setHistory`, `.onStats`, `ToolControl.onConfirmationRequest`,
`.setEditorCallbacks`. All reached via the real public Agent built over the real
FakeProvider seam — no reverse tests.

## 2. Hard guards

### N4 — all 17 T-rows present
```
=== T-row presence ===
(no MISSING above = OK)
```
T1 T2 T2b T3 T3b T3c T6 T7 T8 T8b T9 T10 T11 T14 T14b T21 T22 — ALL present.

### N4 — mock theater / reverse-test guard
```
grep -rn "toHaveBeenCalled\|not\.toThrow\|NotYetImplemented" core-*.spec.ts
=== mock/reverse guard ===
OK: no matches
```
No `toHaveBeenCalled`, no `.not.toThrow`, no `NotYetImplemented` literal in the
consumer specs. PASS.

### B9 — layer property-based report (informational; HARD gate is global in P29)
```
layer property-based: 2 / 20
```
2 genuine property tests (T2p tool-arg projection over `fc.dictionary`; T6p
history round-trip over `fc.array` of generated messages). 9 `fc.*` call sites.
This layer ratio (10%) is INFORMATIONAL per the P11a contract — the hard ≥30%
gate is GLOBAL across the full harness and enforced in P29.

## 3. `boundary.spec.ts` (T17) — 3/3 PASS
```
 ✓ src/api/__tests__/boundary.spec.ts (3 tests) 2ms
```
The new specs do NOT deep-import: the static scan over consumer-facing
`*.spec.ts` is clean. All deep imports (FakeProvider seam, ToolConfirmationOutcome
value, IContent types) live only in `helpers/agentHarness.ts`, which the scan
excludes. (Note: `app-service-boundary.spec.ts` has 1 unrelated P09 failure —
`app-service.js` not yet implemented; it is not a P11 file and is outside P11
scope. The P11a contract scopes the boundary check to `boundary.spec.ts`, which
is 3/3 green.)

## 4. `npm run typecheck` — 0 errors
```
> tsc --noEmit
(exit 0, no output)
```

## 5. `npx eslint <4 deliverable files>` — 0 errors / 0 warnings
```
EXIT: 0   (no output)
```

### RULES.md spot checks
- License header + `@plan:PLAN-20260617-COREAPI.P11` + `@requirement:` present in
  all four files; every `it`/property title embeds the markers AND its T-row token.
- No `any` types, no `as X` casts (type narrowing done via exported predicate
  helpers in the harness); the only "any" hit is the English word inside a comment.
- No TODO/FIXME/STUB.
- Fixtures are real `FakeResponseTurn` JSONL (`{chunks: IContent[]}`) with real
  `text`/`thinking`/`tool_call` blocks and `{{CWD}}` placeholders that
  FakeProvider genuinely substitutes (verified in FakeProvider.ts:56-62, 238-250;
  seam in providerManagerInstance.ts:537-551 registers FakeProvider from
  `LLXPRT_FAKE_RESPONSES` with `process.cwd()`).

## Per-T-row semantic assessment

- **T1** (conversation): PASS. Asserts thinking-before-text ordering, real fixture
  text value `'hello with a thought'`, exactly ONE terminal `done` whose reason is
  in `DONE_REASONS`, and `done` is last. chat() asserts non-empty `text`,
  `finishReason === 'stop'`, `toolCalls` array. Behavioral.
- **T9** (abort): PASS. Real `AbortController` passed via public `stream(..,{signal})`;
  asserts exactly one `done` with `reason==='aborted'` and that `done` is the
  terminal event (nothing after). Genuine abort semantics, not hollow.
- **T10** (generate side-channel): PASS. Captures history length before, calls
  `generate()`, asserts return is a string AND history length is UNCHANGED — the
  real side-channel/no-mutation contract.
- **T14** (multi-step continuation): PASS. Two sequential `stream` turns each
  yield ≥1 text and exactly one terminal `done`; per-turn ordering asserted.
- **T2** (tool confirm): PASS. Asserts tool-call surfaced with `{id,name:'read_file'}`,
  matching tool-result `id`, final text + one done, and history records ≥2 items.
  Confirm via public `respondToConfirmation`.
- **T2b** (both ids): PASS. Captures `ToolConfirmation` via public
  `onConfirmationRequest`; asserts BOTH `confirmationId` and `toolCallId` truthy +
  `name==='read_file'`.
- **T3** (deny): PASS. Cancel decision → asserts a tool-result with
  `isError===true` exists, exactly one done, history records the denied call.
- **T3b** (status before result): PASS. Asserts a `tool-status` index strictly
  precedes the first `tool-result` index — real ordering guarantee.
- **T3c** (editor callbacks): PASS (with honest scope). Drives the real
  `setEditorCallbacks` stub (fails naturally there) and asserts the confirmation
  carries a defined `details` payload. Author honestly documents that FakeProvider
  cannot drive a real editor edit, so it asserts registration + confirmation
  details rather than faking an edit. Acceptable per contract; the
  `getPreferredEditor()` self-assertion is a minor belt-and-suspenders line, not a
  fig-leaf substitute for the real assertions.
- **T11** (headless onApproval): PASS. Configures `onApproval` in AgentConfig
  (no manual wiring), asserts ≥1 successful (`isError!==true`) result + one done.
- **T21** (multi-tool): PASS. ≥2 tool-calls, all calls precede the continuation
  text, exactly one done, per-call call→result deltas all strictly positive,
  unique call ids (no overlap). Strong sequencing assertions.
- **T6** (history round-trip): PASS. setHistory two messages, getHistory returns
  same length and exact text CONTENTS; follow-up turn emits text+done.
- **T7** (resetChat): PASS. After reset, getHistory length 0; next turn done;
  post-turn history bounded.
- **T8** (compress): PASS (with honest scope). Asserts explicit `compress()`
  returns a `CompressionResult` with valid `status` and monotonic numeric token
  fields when compressed. Author honestly documents the auto-compression event
  path can't be FakeProvider-driven, so only the EXPLICIT contract is asserted —
  permitted by the phase contract.
- **T8b** (stats): PASS. Asserts ≥1 SessionStats delivered to onStats with all
  documented numeric fields (promptTokens, candidateTokens, totalTokens,
  contextWindowSize, contextWindowUsed, turnCount) + getStats snapshot numerics.
- **T14b** (context mutators): PASS. addHistory increments length by 1 and the
  injected text is the last message; updateSystemInstruction/addDirectoryContext
  accepted; subsequent turn done; injected message still present after the turn.
- **T22** (non-interactive AgentResult): PASS. Asserts text non-empty,
  toolCalls array, truthy string finishReason, and (when present) usage/error
  shapes — exactly the fields needed for output-format + exit/error mapping.
- **T2p / T6p** (property): GENUINE. Real generated-input round-trip/projection
  properties, not fig-leaf; both fail naturally at the stub.

### Honesty assessment
The author made HONEST, documented decisions where FakeProvider cannot drive a
scenario (T8 auto-compression, T3c real editor edit) instead of faking behavior,
and the assertion shapes are stable so they pass unchanged at GREEN. Fixtures are
real file-based FakeProvider JSONL. No mock theater anywhere; no
`toHaveBeenCalled`. Tests reach real stubs through the public API.

## Verdict: PASS
