# Batch 05 Audit

## Intent
Move the `web_fetch` tool name to centralized constants in `tool-names.ts`.

## Claimed Action
- Reimplemented upstream `c9c633be` by replacing hardcoded `web_fetch`/`direct_web_fetch` strings with `GOOGLE_WEB_FETCH_TOOL` and `DIRECT_WEB_FETCH_TOOL` constants in `google-web-fetch.ts` and `direct-web-fetch.ts`.
- Noted upstream policy/web-fetch files as NO_OP for LLxprt.

## Code Evidence
- `packages/core/src/tools/tool-names.ts:43` defines `GOOGLE_WEB_FETCH_TOOL` and `DIRECT_WEB_FETCH_TOOL` constants.
- `packages/core/src/tools/google-web-fetch.ts:16` imports `GOOGLE_WEB_FETCH_TOOL`.
- `packages/core/src/tools/google-web-fetch.ts:320` uses `GOOGLE_WEB_FETCH_TOOL` in `supportedTools.includes()` check.
- `packages/core/src/tools/direct-web-fetch.ts:21` imports `DIRECT_WEB_FETCH_TOOL`.
- `packages/core/src/tools/direct-web-fetch.ts:40` uses `DIRECT_WEB_FETCH_TOOL` for the tool name.

## Gaps/Mismatches
- `packages/core/src/tools/google-web-fetch.ts:335` still invokes the server tool with the literal `'web_fetch'` instead of `GOOGLE_WEB_FETCH_TOOL`, so the hardcoded string was not fully removed.
- Related integration tests still use `'web_fetch'` literals (e.g. `packages/core/src/tools/google-web-fetch.integration.test.ts:31`), indicating full centralization did not occur.

## Verdict
partial
