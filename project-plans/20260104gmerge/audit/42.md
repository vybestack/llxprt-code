# Batch 42 Audit

## Planned
- `62dc9683`: MCP add array handling + tests.
- `e72c00cf`: Proxy agent error handling.
- `cf16d167`: Repo tooling: tsconfig linter for exclude list.

## Reported
- `62dc9683` marked VERIFIED, SKIP in `project-plans/20260104gmerge/AUDIT.md` row 126 (LLxprt uses `unknown-options-as-args`).
- `e72c00cf` marked VERIFIED, COMMITTED as `f3d6f58e2` in `project-plans/20260104gmerge/AUDIT.md` row 127.
- `cf16d167` marked VERIFIED, COMMITTED as `ba3c2f7a4` in `project-plans/20260104gmerge/AUDIT.md` row 129.

## Actual (evidence)
- `62dc9683` (MCP add array handling): `packages/cli/src/commands/mcp/add.ts` enables `yargs` parser configuration to pass unknown options as args, which covers array handling for server args without the upstream change (`'unknown-options-as-args': true`). Evidence: `packages/cli/src/commands/mcp/add.ts:143`.
- `e72c00cf` (proxy agent error handling): `packages/core/src/utils/fetch.ts` wraps proxy setup in a try/catch and logs failures (`setGlobalProxy()`), providing the error handling behavior. Evidence: `packages/core/src/utils/fetch.ts:86`.
- `cf16d167` (tsconfig exclude linter): `scripts/lint.js` implements `runTSConfigLinter()` that validates tsconfig `exclude` entries against an allowed set and exits non-zero on violations. Evidence: `scripts/lint.js:262`.

## Gaps
- `62dc9683`: Upstream tests for array handling are not present; LLxprt relies on parser configuration rather than the upstream change. Functionality appears covered, but no direct test parity was found.

## Confidence
- Medium-high. Evidence confirms functional equivalents for array handling, proxy error handling, and tsconfig exclude linter. Remaining uncertainty is limited to the absence of upstream test parity for `62dc9683`.
