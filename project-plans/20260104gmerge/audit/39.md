# Batch 39 Audit (7dd2d8f7)

## Planned
- Reimplement upstream `7dd2d8f7` to restore static tool names for configuration exclusions in core tools.

## Reported
- `project-plans/20260104gmerge/AUDIT.md` lists `7dd2d8f7` as **SKIP/NO_OP** with the note that LLxprt already defines static tool names across tool classes and centralizes names in `tool-names.ts`.

## Actual (evidence)
- Core tool classes already define static names that are fixed strings or centralized constants, e.g.:
  - `EditTool` uses a static name constant: `packages/core/src/tools/edit.ts:691`.
  - `GrepTool` exposes a static tool name string: `packages/core/src/tools/grep.ts:894`.
  - `LSTool` exposes a static tool name string: `packages/core/src/tools/ls.ts:281`.
  - `ReadFileTool` and `ReadManyFilesTool` expose static tool names: `packages/core/src/tools/read-file.ts:292`, `packages/core/src/tools/read-many-files.ts:591`.
  - `RipGrepTool` exposes a static tool name string: `packages/core/src/tools/ripGrep.ts:399`.
  - `ShellTool` sets a static tool name: `packages/core/src/tools/shell.ts:585`.
  - `MemoryTool` exposes a static tool name derived from schema: `packages/core/src/tools/memoryTool.ts:318`.
- Centralized tool name constants exist and are used for key tools, including `EDIT_TOOL_NAME`, `GOOGLE_WEB_FETCH_TOOL`, and `DIRECT_WEB_FETCH_TOOL`, which prevents configuration exclusion drift. See `packages/core/src/tools/tool-names.ts:12`.

## Gaps
- No gaps found. The code already has static tool name properties and centralized name constants, matching the intent of the upstream fix without requiring changes.

## Confidence
- **Medium** — evidence is from code inspection of tool declarations and constants; no runtime verification performed in this audit.
