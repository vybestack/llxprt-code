# Batch 09 Audit

## Intent
Remove deprecated `--all-files` CLI flag and related `allFiles/fullContext` config plumbing across CLI, core, a2a-server, tests, and docs.

## Claimed Action
- PROGRESS marks Batch 09 as QUICK REIMPLEMENT for `937c15c6`.
- AUDIT claims `--all-files` option removed, `allFiles/fullContext` removed across 3 packages, and tests/docs updated.

## Code Evidence
- Batch list shows reimplementation recorded for Batch 09 in progress tracking: `project-plans/20260104gmerge/PROGRESS.md:30`.
- Audit table claims reimplementation and describes removals: `project-plans/20260104gmerge/AUDIT.md:55`.
- CLI args interface no longer exposes `allFiles` or `fullContext`: `packages/cli/src/config/config.ts:129`.
- CLI option list around extensions/include-directories shows no `--all-files` option or deprecate entry: `packages/cli/src/config/config.ts:300`.
- Core config file filtering options only include git/llxprt ignore toggles (no full-context flag): `packages/core/src/config/constants.ts:7`.

## Gaps/Mismatches
- `project-plans/20260104gmerge/NOTES.md` has no Batch 09 execution/verification record, so there is no logged proof of the reimplementation steps or tests.
- AUDIT claims tests/docs updated, but no concrete evidence was found in NOTES for those actions.

## Verdict
partial
