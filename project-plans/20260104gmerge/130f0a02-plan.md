# Reimplement 130f0a02 — chore(subagents): Remove legacy subagent code (#11175)

Upstream: https://github.com/google-gemini/gemini-cli/commit/130f0a02530db9a4d88fb9f08cb964d5b2313a1a
Areas: core
Rationale: Subagent legacy cleanup is upstream-only; llxprt subagent architecture diverges

## Upstream Files
- `packages/core/src/core/subagent.test.ts` (exists: YES)
- `packages/core/src/core/subagent.ts` (exists: YES)

## Implementation Steps
1. Inspect upstream diff: `git show 130f0a02 --stat`.
2. Review each touched file: `git show 130f0a02 -- <file>`.
3. Apply equivalent changes in LLxprt, adjusting for:
   - Multi-provider architecture (no Google-only auth paths).
   - No Clearcut telemetry; keep llxprt logging model.
   - Canonical tool names and policy files per dev-docs/cherrypicking.md.
   - A2A server remains private.
4. If a referenced file is missing in LLxprt, document NO_OP in AUDIT.md and explain in NOTES.md.
5. Run quick verify after implementation: `npm run lint` and `npm run typecheck`.
6. Commit with: `reimplement: chore(subagents): Remove legacy subagent code (#11175) (upstream 130f0a02)`.
7. Update PROGRESS.md, append NOTES.md, and record in AUDIT.md.

