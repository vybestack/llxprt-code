# Reimplement Plan: A2A ModelInfo Propagation

**Upstream SHA:** `806cd112ac974ca54e39d6c28d2d243839aa9fd0`
**Batch:** 11

## What upstream does

Adds `modelInfo` field to A2A Task class. When an agent reports a model change via `GeminiEventType.ModelInfo`, the task stores it and uses it in metadata and status updates instead of the default config model.

## LLxprt approach

Same concept adapted to LLxprt's private A2A task architecture.

## Files to modify

1. `packages/a2a-server/src/agent/task.ts` — add `modelInfo` field, handle ModelInfo event, use in getMetadata/setTaskState
2. `packages/a2a-server/src/utils/testing_utils.ts` — update mock config
3. Tests

## Verification

- Unit tests for modelInfo propagation
- Verify getMetadata returns updated model name
