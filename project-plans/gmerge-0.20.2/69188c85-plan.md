# Reimplement Plan: Stats Quota Display

**Upstream SHA:** `69188c8538af44f6cbae7c57f4d8478a474802d0`
**Batch:** 10

## What upstream does

Adds quota/usage-limit-remaining display to /stats command via `retrieveUserQuota` API.

## LLxprt approach

Treat Gemini quotas generically like Anthropic/Codex quotas. Display when authenticated or when a rate-limit bucket exists.

## Files to modify

1. `packages/core/src/code_assist/types.ts` — add quota types if missing
2. `packages/core/src/code_assist/server.ts` — add quota retrieval
3. `packages/cli/src/ui/commands/statsCommand.ts` — add quota subcommand/section
4. `packages/cli/src/ui/components/StatsDisplay.tsx` — render quota data
5. `packages/cli/src/ui/types.ts` — UI types for quota display
6. Tests

## Key design

- Generic quota interface works across providers
- Display format: used/limit with percentage
- Graceful fallback if quota API unavailable

## Verification

- Unit tests for quota retrieval and display
- Verify /stats shows quota when available
