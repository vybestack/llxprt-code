# Reimplement Plan: JIT Context Manager

**Upstream SHA:** `752a521423630589e49f9b5c1aed3b05173f686f`
**Batch:** 12

## What upstream does

Implements a JIT (Just-In-Time) context manager service that loads subdirectory-specific context on demand, controlled by a setting.

## LLxprt approach

LLxprt already has `loadJitSubdirectoryMemory` in `memoryDiscovery.ts`. Wire it into settings/config/service layer.

## Files to modify

1. `packages/core/src/config/config.ts` — add getter for JIT context setting
2. `packages/cli/src/config/settingsSchema.ts` — add setting
3. `packages/core/src/services/contextManager.ts` — create or enhance
4. `packages/core/src/utils/memoryDiscovery.ts` — integrate with config
5. `schemas/settings.schema.json` — add schema entry
6. Tests

## Verification

- Unit tests for context manager service
- Verify JIT context loads when setting enabled
