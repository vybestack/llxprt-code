# P09 Cleanup And No-Shim Removal Completion

Phase: `PLAN-20260608-ISSUE1588.P09`

Status: PASS

## Summary

P09 cleanup removed the legacy core settings/profile/storage/modelParams implementation and export surface. Moved APIs are owned by `@vybestack/llxprt-code-settings`; core keeps only the runtime adapter bridge required for provider runtime-context integration.

## Remediations Completed

- Removed stale core settings test directory under `packages/core/test/settings/`.
- Fixed tests that still mocked or imported moved `SettingsService`, `ProfileManager`, `Storage`, and profile/model types through core instead of settings.
- Fixed provider runtime-context mocks to include `setProviderRuntimeStateFactory()` for the settings runtime adapter module-load contract.
- Fixed CLI runtime isolation tests by giving each isolated runtime fixture its own `SettingsService` instance.
- Updated runtime registry fallback behavior expectations for no-scope/no-active-provider-context cases.
- Removed the stale `// Export Storage` core barrel comment after the moved storage export was removed.

## Verification Evidence

### Boundary and No-Shim Checks

Command:

    node scripts/check-settings-boundary.js --phase post-p09

Result: PASS. Output included all OK checks and `All requested checks passed.`

Key supplemental scans rerun after remediation:

- No old deep imports for `@vybestack/llxprt-code-core/settings` or core `config/storage` / `config/profileManager`.
- No root-barrel moved-symbol imports from `@vybestack/llxprt-code-core`.
- `packages/core/src/runtime/providerRuntimeContext.ts` has no settings package or settings singleton references.
- `.llxprt` status check printed no modified files.

### Removed File Checks

All of these checks passed:

- `packages/core/src/settings` directory absent.
- `packages/core/src/config/storage.ts` absent.
- `packages/core/src/config/profileManager.ts` absent.
- `packages/core/src/types/modelParams.ts` absent.
- Moved core storage/profile/settings/modelParams tests absent.

### Typecheck

Command:

    npm run typecheck

Result: PASS across settings, core, providers, CLI, a2a-server, test-utils, and lsp workspaces.

### Tests

Focused remediation tests:

    npm run test --workspace @vybestack/llxprt-code -- --run src/config/__tests__/profileOverridePrecedenceParity.test.ts src/config/config.loadMemory.test.ts src/runtime/__tests__/runtimeIsolation.test.ts

Result: PASS, 3 files / 21 tests.

Full CLI workspace test:

    npm run test --workspace @vybestack/llxprt-code

Result: PASS, 507 files / 5745 tests passed, 7 skipped.

Full root test:

    npm run test

Result: PASS. Captured workspace summaries included:

- Settings: 5 files / 156 tests passed.
- Core: 427 files passed, 1 skipped; 7801 tests passed, 30 skipped.
- Providers: 151 files passed, 1 skipped; 1916 tests passed, 14 skipped.
- CLI: completed with passing test file output in the captured log; direct CLI workspace run above passed fully.

### Build and Dist Stale Export Scan

Command:

    npm run build

Result: PASS across all workspaces.

Post-build moved-export scan:

    rg -n "SettingsService|ISettingsService|GlobalSettings|SettingsChangeEvent|ProviderSettings|UISettings|AdvancedSettings|EventListener|EventUnsubscribe|SettingsTelemetrySettings|DiagnosticsInfo|ProfileManager|ModelParams|getSettingsService|registerSettingsService|resetSettingsService|SETTINGS_REGISTRY" packages/core/dist/src/index.js packages/core/dist/index.js

Result: no matches; marker command printed `OK-no-stale-core-dist-moved-settings-exports`.

## Semantic Checklist

- [x] Core no longer re-exports moved settings/profile/storage/modelParams APIs for compatibility.
- [x] Core `package.json` no longer exports legacy settings/profile/storage/modelParams subpaths.
- [x] Moved core source files and moved core tests are absent.
- [x] Consumers import moved APIs from `@vybestack/llxprt-code-settings`.
- [x] Provider runtime context remains settings-package agnostic.
- [x] Core runtime adapter remains the single production bridge.
- [x] Build output contains no moved settings/profile/model type exports in core barrels.
- [x] No `.llxprt` modifications were observed.

## Next Phase

Proceed to P09a semantic verification/review.
