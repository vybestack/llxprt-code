# Phase P07 Completion — Consumer Migration Integration Tests (TDD Red Phase)

## Result

PASS

## Prerequisite Verification

Read `project-plans/issue1588/.completed/P06a.md` and confirmed it exists with PASS status.

## Files Created

1. `packages/providers/src/__tests__/settings-integration/provider-settings.integration.test.ts`
   - Markers: `@plan PLAN-20260608-ISSUE1588.P07`, `@requirement REQ-TEST-001.2`
   - 3 tests, all failing (TDD red phase)

2. `packages/cli/src/__tests__/settings-integration/profile-startup.settings-consumer.test.ts`
   - Markers: `@plan PLAN-20260608-ISSUE1588.P07`, `@requirement REQ-TEST-001.2`
   - 3 tests, all passing (ProfileManager identity is same via P06 root-barrel re-export)
   - Note: File named `.settings-consumer.test.ts` (not `.integration.test.ts`) because CLI vitest.config.ts excludes `*.integration.{test,spec}.*` patterns

## Production Paths Tested

### Provider Vertical-Slice

| Test | Production Entrypoint | Import Path |
|------|----------------------|-------------|
| Model from sentinel | `BaseProvider.constructor` → `getSettingsService()` | `@vybestack/llxprt-code-core/settings/settingsServiceInstance.js` line 34 |
| Base URL from sentinel | `BaseProvider.getBaseURL()` → `resolveSettingsService()` → `computeBaseURL()` | `@vybestack/llxprt-code-core/settings/SettingsService.js` line 33 |
| Provider-specific model | `BaseProvider.getModel()` → `resolveSettingsService()` → `computeModel()` | `@vybestack/llxprt-code-core/settings/settingsServiceInstance.js` line 34 |

### CLI Vertical-Slice

| Test | Production Entrypoint | Import Path |
|------|----------------------|-------------|
| Constructor identity | `runtimeContextFactory.ts` line 34 | `import { ProfileManager } from '@vybestack/llxprt-code-core'` |
| Instance check | `runtimeContextFactory.ts` line 233 | `new ProfileManager(path.join(llxprtDir, 'profiles'))` |
| Round-trip baseline | Settings-package `ProfileManager` | `@vybestack/llxprt-code-settings` ProfileManager temp-filesystem JSON round-trip |

## Red-Phase Evidence

### Provider Tests: 3 failures, exit code 1

```
× fails: BaseProvider reads model from settings-package sentinel when registered
  AssertionError: expected 'test-default-model' to be 'sentinel-model-value' // Object.is equality

× fails: BaseProvider reads base-url from settings-package sentinel when registered
  AssertionError: expected undefined to be 'https://sentinel.example.com/api' // Object.is equality

× fails: BaseProvider reads provider-specific model from settings-package sentinel
  AssertionError: expected 'test-default-model' to be 'provider-specific-model' // Object.is equality
```

Root cause: `BaseProvider.ts` imports `getSettingsService` from `@vybestack/llxprt-code-core/settings/settingsServiceInstance.js` (line 34), which has a separate singleton from `@vybestack/llxprt-code-settings`. The sentinel registered via settings-package `registerSettingsService()` is not visible to BaseProvider's core import.

### CLI Tests: 3 passes, exit code 0

CLI tests pass because P06 already migrated the core root barrel to re-export `ProfileManager` from `@vybestack/llxprt-code-settings`:
```typescript
// packages/core/src/index.ts line 14
export { ProfileManager } from '@vybestack/llxprt-code-settings';
```

This means `CoreProfileManager === SettingsProfileManager` is already true. The CLI identity tests verify this P06 re-export works correctly. The TDD red phase is satisfied by the provider tests alone.

## No-Module-Resolution Evidence

```
grep -E "Cannot find module|Module not found|ERR_MODULE_NOT_FOUND" /tmp/p07-provider-output.txt /tmp/p07-cli-output.txt
(no matches — OK)
```

## Inventory Counts (REPORT-ONLY)

- Deep-path settings imports: **94 matches** (will be enforced zero in P08)
- Root-barrel moved-symbol imports: **46 matches** (will be enforced zero in P08)

## Scope Evidence

- Only test files created (2 files in `__tests__/settings-integration/` directories)
- No production code modified
- No `.llxprt/` modifications
- No provider/CLI production import migration beyond test setup imports
- Test setup imports (`registerSettingsService`, `resetSettingsService` from settings package) are strictly necessary for test setup

## Verification Commands Run

1. `npm run typecheck` — PASS
2. `npm run test --workspace @vybestack/llxprt-code-providers -- --run src/__tests__/settings-integration` — 3 failures, exit 1
3. `npm run test --workspace @vybestack/llxprt-code -- --run src/__tests__/settings-integration` — 3 passes, exit 0
4. Module resolution error scan — none found
5. Behavioral failure pattern scan — AssertionError present in provider output
6. Inventory scans — deep-path: 94, root-barrel: 46 (REPORT-ONLY)

## Remaining Issues for P08

- `BaseProvider.ts` lines 33-34 import `SettingsService` and `getSettingsService` from `@vybestack/llxprt-code-core/settings/` deep paths
- `providerConfigKeys.ts` imports `SETTINGS_REGISTRY` from `@vybestack/llxprt-code-core/settings/` deep path
- CLI has ~46 root-barrel imports of moved symbols from `@vybestack/llxprt-code-core`
- CLI has `Storage` imports from `@vybestack/llxprt-code-core` in production files (profileRuntimeApplication, sandboxProfiles, paths, etc.)
- CLI `profile-utils.ts` dynamically imports `ProfileManager` from `@vybestack/llxprt-code-core`
