# Phase P08 Completion — Consumer Migration Implementation

## Result

PASS

## Completed At

2026-06-09T11:44:37Z

## Phase Scope

P08 migrated consumers of moved settings/profile/storage APIs from old core deep paths and core root-barrel moved-symbol imports to `@vybestack/llxprt-code-settings`.

Old duplicate core settings/profile/storage files remain only for later P09 cleanup/no-shim removal. P08 did not convert old core files into forwarders or compatibility shims.

## Implementation Summary

- Migrated provider, CLI, core, and test imports for moved settings package APIs, including:
  - `SettingsService`
  - `ProfileManager`
  - `Storage`
  - `ModelParams`, `Profile`, `StandardProfile`, `LoadBalancerProfile`, `EphemeralSettings`
  - `SETTINGS_REGISTRY`
  - `getSettingsService`, `registerSettingsService`, `resetSettingsService`
  - settings registry helpers such as `resolveAlias` and `getProfilePersistableKeys`
- Removed P08-inappropriate core root-barrel re-exports of moved settings/profile/storage symbols from `packages/core/src/index.ts` and `packages/core/index.ts`.
- Updated consumer workspace metadata and aliases for settings package imports.
- Preserved P06 single-owner runtime rule:
  - `packages/core/src/runtime/settingsRuntimeAdapter.ts` remains the settings/runtime-context bridge.
  - `packages/core/src/runtime/providerRuntimeContext.ts` remains settings-symbol agnostic.
  - `packages/core/src/config/configConstructor.ts` remains wired through `activateSettingsRuntimeContext()`.
- Converted settings-package test mocks to partial actual mocks where needed so real settings exports remain available while singleton helpers are overridden.
- Updated path-sensitive tests that now use settings-package `Storage` so they remain isolated from real user state.
- Fixed final CLI build/smoke issues caused by remaining moved-symbol root imports and one unexported core deep subpath import.

## Prior Subagent Notes

Earlier P08 `typescriptexpert` attempts timed out during broad consumer migration, and one later attempt failed while investigating a provider API compression error. Manual continuation completed the consumer migration and verification gates recorded below.

## Final Import Inventory

Canonical P08 inventory commands from `project-plans/issue1588/plan/08-consumer-migration-impl.md` were rerun after the final CLI migration/build fixes.

```text
Root-barrel: 0 matches
Deep-path: 0 matches
Type imports: 0 matches
vi.mock paths: 0 matches
Dynamic imports: 0 matches
Core relative ProfileManager: 0 matches
LSP imports: 0 matches
```

The a2a-server supplemental scan returned only unrelated `Storage`/`requestStorage` references:

```text
packages/a2a-server/src/persistence/gcs.ts:7:import { Storage } from '@google-cloud/storage';
packages/a2a-server/src/persistence/gcs.test.ts:7:import { Storage } from '@google-cloud/storage';
packages/a2a-server/src/http/requestStorage.ts:8:import { AsyncLocalStorage } from 'node:async_hooks';
packages/a2a-server/src/http/app.ts:23:import { requestStorage } from './requestStorage.js';
packages/a2a-server/src/agent/executor.ts:39:import { requestStorage } from '../http/requestStorage.js';
```

## Boundary and Static Verification Evidence

### Authoritative boundary script

Command:

```bash
node scripts/check-settings-boundary.js
```

Evidence:

```text
OK: source-imports
OK: all-files-imports
OK: metadata
OK: tsconfig-references
OK: vitest-aliases
OK: export-style
OK: old-paths
OK: root-barrel
OK: anti-shim
OK: core-re-exports
OK: modelParams-subpath
OK: relative-settings-imports
OK: relative-storage-imports
OK: vi-mock-paths
OK: dynamic-import-paths
OK: provider-runtime-context
OK: no-storage-package
OK: core-barrel-shim
OK: adapter-single-owner
OK: lockfile
OK: settings/src/index.ts exists

All requested checks passed.
```

### Workspace typechecks

Commands:

```bash
npm run typecheck --workspace @vybestack/llxprt-code-core
npm run typecheck --workspace @vybestack/llxprt-code-providers
npm run typecheck --workspace @vybestack/llxprt-code
```

Evidence:

```text
> @vybestack/llxprt-code-core@0.10.0 typecheck
> tsc --noEmit

> @vybestack/llxprt-code-providers@0.10.0 typecheck
> tsc --noEmit

> @vybestack/llxprt-code@0.10.0 typecheck
> tsc --noEmit
```

All three commands exited 0.

### Enforcing scans

Commands included the P08 enforcing scans for old deep paths, root-barrel moved-symbol imports, old vi.mock paths, deep dynamic imports, LSP imports, malformed `.@vybestack` imports, `configConstructor.ts` runtime wiring, and `providerRuntimeContext.ts` settings-symbol agnosticism.

Evidence:

```text
OK: no old deep-path imports
OK: no root-barrel moved-symbol imports from core
OK: no old vi.mock paths
OK: no deep dynamic imports
OK: no unmigrated LSP imports
OK: no malformed .@vybestack imports
66:import { activateSettingsRuntimeContext } from '../runtime/settingsRuntimeAdapter.js';
217:    activateSettingsRuntimeContext(providedSettingsService);
OK_PROVIDER_RUNTIME_SETTINGS_AGNOSTIC
```

## Behavioral Verification Evidence

### Provider full suite

Command:

```bash
npm run test --workspace @vybestack/llxprt-code-providers
```

Evidence:

```text
Test Files  151 passed | 1 skipped (152)
Tests  1916 passed | 14 skipped (1930)
Exit Code: 0
```

### Core full suite

Command:

```bash
npm run test --workspace @vybestack/llxprt-code-core
```

Evidence:

```text
Test Files  434 passed | 1 skipped (435)
Tests  8018 passed | 30 skipped (8048)
Exit Code: 0
```

### Provider settings-integration slice

Command:

```bash
npm run test --workspace @vybestack/llxprt-code-providers -- --run src/__tests__/settings-integration/provider-settings.integration.test.ts
```

Evidence:

```text
src/__tests__/settings-integration/provider-settings.integration.test.ts (3 tests)
Test Files  1 passed (1)
Tests  3 passed (3)
Exit Code: 0
```

### CLI settings-integration slice

Command:

```bash
npm run test --workspace @vybestack/llxprt-code -- --run src/__tests__/settings-integration/profile-startup.settings-consumer.test.ts
```

Evidence:

```text
src/__tests__/settings-integration/profile-startup.settings-consumer.test.ts (3 tests)
Test Files  1 passed (1)
Tests  3 passed (3)
Exit Code: 0
```

### Build

Command:

```bash
npm run build
```

Evidence:

```text
> @vybestack/llxprt-code-settings@0.10.0 build
Successfully copied files.
> @vybestack/llxprt-code-core@0.10.0 build
Successfully copied files.
> @vybestack/llxprt-code-providers@0.10.0 build
Successfully copied files.
> @vybestack/llxprt-code@0.10.0 build
Successfully copied files.
> @vybestack/llxprt-code-a2a-server@0.10.0 build
Successfully copied files.
> @vybestack/llxprt-code-test-utils@0.10.0 build
Successfully copied files.
> llxprt-code-vscode-ide-companion@0.10.0 build:dev
[watch] build finished
> @vybestack/llxprt-code-lsp@0.10.0 build
Exit Code: 0
```

### CLI smoke

Command:

```bash
node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
```

Evidence:

```text
Checking build status...
Build is up-to-date.
Error when talking to openai API Full report available at: /var/folders/qd/962lhrjj0232rjykgg3lgmrw0000gn/T/llxprt-client-error-Turn.run-sendMessageStream-2026-06-09T11-43-31-175Z.json
Exit Code: 0
```

The first post-build smoke attempt exposed an unexported core deep subpath import in `packages/cli/src/ui/commands/diagnosticsCommand.ts`; that was fixed by importing `MCPOAuthTokenStorage` from the core root export, then CLI typecheck, build, and smoke were rerun successfully as shown above.

## Scope Evidence

- P08 consumer migration uses `@vybestack/llxprt-code-settings` for moved settings/profile/storage APIs.
- No old core settings/config deep-path imports remain in packages.
- No root-barrel moved-symbol imports from `@vybestack/llxprt-code-core` remain in packages.
- No old settings/profile/storage vi.mock paths remain.
- No old settings/profile/storage dynamic imports remain.
- `packages/lsp` has no unmigrated settings/profile imports.
- `packages/a2a-server` scan hits are unrelated storage names from Google Cloud/request local storage, not moved settings APIs.
- `.llxprt` status check produced no modified `.llxprt` files.

## Follow-up

Proceed to P08a verifier review. Do not start P09 cleanup/no-shim removal until P08a is complete and marked.
