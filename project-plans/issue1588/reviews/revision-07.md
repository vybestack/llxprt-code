# Revision 07: Addressing Review-07

## Summary

Review-07 raised 15 material issues and 10 pedantic improvements. After complete re-inspection of all plan files, analysis artifacts, execution-tracker, and specification, every finding is confirmed as already addressed in current plan files through prior revisions (revision-05, revision-06, and ongoing plan updates). No additional plan file edits are required beyond the three minor corrections already made in this revision cycle.

## Material Issues → Current Plan Assessment

| # | Review Finding | Status | Evidence in Current Plan Files |
|---|---------------|--------|-------------------------------|
| M1 | Root build ordering must explicitly insert settings before core or modify scripts/build.js | **Already addressed** | `plan/03-decoupling-stub.md` §6a requires inserting `packages/settings` before `packages/core` in workspaces OR modifying `scripts/build.js`, with verification commands inspecting workspace order and `scripts/build.js`. `plan/03b-minimal-adapter-wiring.md` has Root Build And Script Ordering Verification section with `predocs:settings` handling. `analysis/package-metadata-constraints.md` has Root Build Ordering Verification section with concrete commands. |
| M2 | P03/P03b must update providers/CLI tsconfig include/rootDir/references | **Already addressed** | `plan/03b-minimal-adapter-wiring.md` Standardized TypeScript Path Alias Strategy section specifies: providers add `{ "path": "../settings" }` to references and settings source to `include`; CLI adds `"../settings/index.ts"`, `"../settings/src/**/*.ts"` to `include` and `{ "path": "../settings" }` to references; core only needs path aliases. Each package has mandatory `npm run typecheck --workspace` verification. |
| M3 | CLI TypeScript references must include settings | **Already addressed** | Same as M2 — P03b explicitly requires CLI add `{ "path": "../settings" }` alongside existing `{ "path": "../core" }` and settings source files to `include` array. `analysis/package-metadata-constraints.md` mirors this requirement. |
| M4 | P05a workspace graph check has typo `@vybestack/llxrt-code` | **Not present in plan files** | Comprehensive grep of all plan, analysis, and tracker files confirms zero occurrences of `llxrt-code`. The typo exists only in review-07.md itself. All deterministic workspace graph scripts use `lodash` startsWith or exact `@vybestack/llxprt-code` strings via Node.js `require()`. |
| M5 | Enforcing rg scans must use rg -n or rg -q, not rg -c | **Already addressed** | All scan commands across all plan files use `rg -n` capture-and-check-empty patterns. `analysis/phase-verification-matrix.md` §Boundary Scans states "all boundary scans use enforcing `rg -n` capture-and-check-empty patterns (not `rg -c`)". `execution-tracker.md` confirms. Zero `rg -c` occurrences in any plan file. |
| M6 | Built-runtime dynamic import verification must require npm install + workspace resolution + validate against package.json export map and built files | **Already addressed** | `analysis/package-metadata-constraints.md` Built-runtime verification section requires `npm install` + `npm run build` prerequisite, validates each declared `dist/src/...` path exists on disk, and validates package exports against `package.json` export map. P05a and P10 verification commands include both existence checks and `await import()` checks. `plan/00-overview.md` Key Behavioral Contracts section documents the prerequisite. |
| M7 | P04b integration design contradicts — claims to exercise configConstructor but P03b doesn't change it | **Already addressed** | `plan/04b-vertical-slice-integration-tdd.md` explicitly states: "P04b integration test exercises `activateSettingsRuntimeContext` directly (the adapter bridge path), NOT `configConstructor → registerSettingsService`. P03b does NOT modify `configConstructor.ts`." The Sequencing Clarification section and P04b design section remove any configConstructor claim. The test targets the adapter bridge path (`activateSettingsRuntimeContext` → `registerSettingsService`), not the configConstructor path. |
| M8 | P07 provider sentinel mechanism underspecified | **Already addressed** | `plan/07-consumer-migration-tdd.md` P07 Provider Vertical-Slice Integration Test section specifies: (1) registers sentinel `SettingsService` using `registerSettingsService()` from settings package only; (2) no `ProviderRuntimeContext` setup — verifies settings-only consumption; (3) asserts provider behavior reads sentinel value through settings package path; (4) post-P08 static import verification confirming `BaseProvider` imports `getSettingsService` from `@vybestack/llxprt-code-settings`. |
| M9 | P07 CLI vertical-slice too broad/ambiguous | **Already addressed** | `plan/07-consumer-migration-tdd.md` specifies preferred in-package temp-filesystem profile load test through actual CLI-owned import path with `os.tmpdir()`, OR documented limitation with static import guards for both `ProfileManager` and `Storage`. The `node scripts/start.js --profile-load synthetic` smoke test is explicitly designated P10 end-to-end validation only, not a P07 integration guard. |
| M10 | Settings boundary check missing config/package files | **Already addressed** | `plan/03-decoupling-stub.md` §8a defines unified boundary check covering: (1) `packages/settings/**/*.ts(x)` source files; (2) `packages/settings/**/*.ts(x)` all TypeScript including tests; (3) `package.json` dependencies AND devDependencies via Node JSON parsing; (4) `tsconfig.json`; (5) `vitest.config.ts`; (6) export map style verification (Node JSON parsing); (7) `settingsSchema` import guard. Referenced from `analysis/phase-verification-matrix.md`. |
| M11 | P03 must inline concrete export map | **Already addressed** | `plan/03-decoupling-stub.md` §1 includes complete inlined JSON export map with all subpaths using `{types, import}` objects, plus verification commands that each declared export source file exists at `src/` path and each built file exists at `dist/src/` path after build. |
| M12 | modelParams/profile type symbol-by-symbol move map underspecified | **Already addressed** | `analysis/settings-move-map.md` Symbol-by-Symbol Move Map section lists every symbol in `modelParams.ts` with source file, destination file, and destination export path. P09 verification section includes deletion check (`test ! -f packages/core/src/types/modelParams.ts`) and no-core-re-exports check. |
| M13 | predocs:settings not concretely handled | **Already addressed** | `plan/03-decoupling-stub.md` §6a documents two explicit options: (1) replace with chained build command, or (2) replace with full build. Required verification command `npm run predocs:settings` included. `plan/03b-minimal-adapter-wiring.md` has `predocs:settings` handling section. `analysis/package-metadata-constraints.md` documents the same with concrete commands. |
| M14 | P03 empty vitest suite may fail | **Already addressed** | `plan/03-decoupling-stub.md` §8 explicitly requires `passWithNoTests: true` with explanation: "vitest run returns a nonzero exit code when zero test files are discovered." Execution tracker confirms. |
| M15 | Internal storage seam/boundary for future extraction | **Already addressed** | `plan/00-overview.md` Internal Storage Seam section documents: (1) `Storage` class with clear public API surface; (2) `LLXPRT_DIR` constant as self-contained value; (3) no imports from settings service/registry inside Storage. `analysis/settings-move-map.md` and `analysis/final-architecture.md` document the seam with `@storage-seam` marker. |

## Pedantic Improvements → Current Plan Assessment

| # | Pedantic Finding | Status | Evidence |
|---|-----------------|--------|----------|
| P1 | Phase IDs P03ba/P04ba confusing | **Fixed** | Changed `P03ba` → `P03c` in `analysis/phase-verification-matrix.md`. Changed completion marker from `P04ba.md` → `P04c.md` in `plan/04b-vertical-slice-integration-tdd-verification.md`. Simplified execution-tracker phrasing. All plan files now use consistent P03c/P04c notation. |
| P2 | Rename P06 file "stub" → "implementation" | **Not renamed** (cosmetic) | `plan/06-core-integration-stub.md` has a prominent header note: "Note on phase naming: This phase is a full core adapter implementation (not a stub). The title 'Core Runtime/Config Implementation' reflects the actual scope." Renaming the file would require updating references across 30+ plan, analysis, review, and specification files for cosmetic benefit. The substantive concern (clarity about implementation vs. stub) is addressed by the header note and execution-tracker description. |
| P3 | Remove duplicate npm run build in P05a | **Already absent** | `plan/05a-settings-package-impl-verification.md` has a single `npm run build` followed by ESM verification. No duplicate exists in the current file. |
| P4 | Prefer one reusable boundary verification script | **Already addressed** | `plan/03-decoupling-stub.md` §8a defines a unified boundary check script covering all required dimensions. `analysis/phase-verification-matrix.md` references it. |
| P5 | Clarify plan-marker requirements for copied legacy code | **Already addressed** | `plan/00-overview.md` Temporary Duplicate Policy section: "add `@plan PLAN-20260608-ISSUE1588.P05` markers only at the file level (e.g., top of file), not to every method. Do NOT add `@plan` markers to unchanged methods within copied files." |
| P6 | Package name collision/regex risk | **Already addressed** | All forbidden dependency checks use Node.js `require()` for exact JSON field parsing, not regex-only. Documented in `analysis/dependency-audit.md` ("Package metadata checks use Node.js JSON parsing"), `analysis/phase-verification-matrix.md`, P03 §8a, and execution-tracker. |
| P7 | Compression strategy preflight values | **Already addressed** | `plan/00a-preflight-verification.md` requires recording exact `COMPRESSION_STRATEGIES` values from `packages/core/src/core/compression/types.ts`. `analysis/dependency-audit.md` documents the command and expected literal values. Execution-tracker confirms. |
| P8 | Static guard: settings does not import CLI settingsSchema | **Already addressed** | `plan/03-decoupling-stub.md` §8a unified boundary check step 7: `rg -n "settingsSchema" packages/settings/src`. `plan/00-overview.md` Key Behavioral Contracts section documents the guard. |
| P9 | Verify a2a-server with actual scan | **Already addressed** | `analysis/dependency-audit.md` a2a-server section has verification commands. `plan/00a-preflight-verification.md` requires actual scan output. `plan/08-consumer-migration-impl.md` includes a2a-server scan. |
| P10 | Node JSON parsing for package metadata | **Already addressed** | All package metadata checks throughout the plan use `node -e "const p=require('./packages/...")"` for exact JSON parsing. `analysis/dependency-audit.md` documents this requirement. |

## Actual Edits Made in This Revision Cycle

Three minor corrections were made:

1. **`analysis/phase-verification-matrix.md`** (line 156): Changed `P03ba` → `P03c` in the phase-specific checks table to match the standardized phase ID convention.

2. **`plan/04b-vertical-slice-integration-tdd-verification.md`** (completion marker): Changed from `P04ba.md` → `P04c.md` to match the corrected phase ID specified in the file header (`PLAN-20260608-ISSUE1588.P04c`).

3. **`execution-tracker.md`** (line 65): Simplified the P05 prerequisite checklist item from "P05 prerequisite typo fixed: 'Phase 04b verified' not 'Phase 04ba verified'" to "P05 prerequisite: 'Phase 04b verified' (P04b not P04ba)" for clarity.

## Decision on P06 Filename

Review finding P2 requests renaming `plan/06-core-integration-stub.md` to reflect that it is a full implementation phase, not a stub phase. The header note adequately addresses the substantive concern. Renaming the file would require updating references in `plan/00-overview.md`, `specification.md`, `execution-tracker.md`, `analysis/phase-verification-matrix.md`, `analysis/final-architecture.md`, `analysis/dependency-audit.md`, `analysis/call-site-migration-matrix.md`, all review files, and all revision files (30+ reference points total). This level of cosmetic churn across historical review/revision files creates more confusion than it resolves. The header note and execution-tracker description ("Core runtime/config implementation (full implementation, not stub)") are sufficient for clarity. **Not renamed.**

## Files Inspected

All plan files (00-overview through 10a), all analysis files (anti-shim-policy, behavioral-regression-matrix, call-site-migration-matrix, consumer-import-matrix, dependency-audit, final-architecture, integration-contract, package-metadata-constraints, phase-verification-matrix, settings-move-map, pseudocode/*), execution-tracker, specification, and all review/revision files.