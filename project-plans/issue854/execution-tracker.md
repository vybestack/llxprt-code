# Execution Tracker — Issue 854 File-Driven Scrollback

Design: `issue-854-design.md` · Phase 1 plan: `phase-1-plan.md`

| Phase | ID | Status | Started | Completed | Verified | Semantic? | Notes |
|-------|-----|--------|---------|-----------|----------|-----------|-------|
| 0.5 | P0.5 | ✅ | 2026-09-17 | 2026-09-17 | 2026-09-17 | N/A | Preflight: tables filled in phase-1-plan.md; REQ-854-003 live stamping flagged blocked (see plan) |
| 01 | P01 | [OK] | 2026-09-17 | 2026-09-18 | 2026-09-18 | [OK] | Foundation: journal + index writer, seq stamps, getContextRange + event, settings flag — marker `.completed/P01.md`; fix session: journal creation made lazy (recording path null at render → journal never created; smoke proved it) + end-to-end sidecar proof; full-suite 26,673 pass with 5 cli-args profile failures proven pre-existing on origin/main (wt-main-test.log), 4 runner-fixture flakes 34/34 isolated (wsc-retest.log); lint exit 0; PR #3727 CI fully green 2026-09-18 (41 pass/0 fail) after two CI-only catches: prettier rewrap of late fix files, and `schema:settings`+`docs:settings` regeneration for the new setting (the scripts meta-shard checks these; local `npm run test` does not cover `scripts/tests/` — regenerate after any schema-ui.ts edit) |
| 01b | P01b | [OK] | 2026-09-18 | e4c053c42 | - | [OK] | Rework per Andrew's three rulings (2026-09-18): (1) no duplicate file, (2) NO UI state persisted at all, (3) no giant in-memory map. DELETED ScrollbackJournal + scrollbackIndex + journalWiring + scrollbackRecords + scrollbackTestHelpers + ui.scrollbackJournalEnabled + useAppBootstrap wiring (schema/docs regenerated, 0 hits); KEPT chronology stamps + contextRange API + contextRangeChanged. Verified: src grep 0 dangling refs (p01b-grep-proof.log), core history 441 pass / 39 files, iContentToHistoryItems 40 pass, lint 18/18 clean, smoke haiku emitted (p01b-smoke5.log), INVERSE PROOF: 6 fresh smoke chats dirs, ZERO sb-* files (p01b-inverse-sidecar-proof.log) |
| 02 | P02a | [OK] | 2026-09-19 | c9ffacffe | 2026-09-19 | [OK] | JournalCursor (core recording pkg, journalCursor.ts/.test.ts): chunked reverse/forward (chunkBytes injectable), MAX_RECORD_BYTES 16 MiB assembly + valid-oversized skip-with-diagnostic, UTF-8/CRLF/BOM, head continuation vs torn tail, group-by-offset, envelope skip w/ offsets. 12/12 + 3 stability runs; WeakRef allocation probe (tmp/verify854/p02a/); eslint 0; fix session found+fixed multi-chunk torn-tail scan bug (computeLineEndLimit walks back chunk-by-chunk). Core recording suite 823 pass/0 fail; both-sides protocol run |
| 02 | P02b | [OK] | 2026-09-19 | 8f62b23c4 | 2026-09-19 | [OK] | RowIdentity = (journal offset \| legacy index, discriminator {text,toolGroup,summaryRow}) + pending prefix; rowIdentityKey collision-free; resolvePendingRowIdentity single swap at commit boundary; iContentToHistoryItems optional ProjectionSources.envelopeOffsets (legacy fallback for all current callers, non-breaking); historyLedger.findIndexByIdentity resident-bounded; ui/types rowIdentity (memory-only). 24/24 new + existing iContentToHistoryItems 42/0, turnStore 37/0, adjacent lanes green; seq-restart + density-interior adversarial tests; envelope-offset supply deferred to P02c/P02e wiring by design |
| 02 | P02c | [OK] | 2026-09-19 | 2b2611c52 | 2026-09-19 | [OK] | ScrollbackPager store (scrollbackPager.ts 683 lines): generation-guarded async paging over real JournalCursor, boot=last page, visibility floor = last clear boundary/file start (compressed/rewind scroll through), loadingOlder lifecycle, stale-discard + close-race safety, file-growth repage merge, purge: sub-context first → far end → byteFloorBytes with oversized-visible-row + pending guarantees, resident-bounded maps. 18/18 + stores dir 163/0; JournalCursor barrel-exported from core recording index; lint clean (5 jest/prefer-strict-equal conformance renames in test). Built across 5 subagent sessions (2 red-phase timeouts → part1/part2 split; see tmp/verify854/p02c/) |
| 02 | P02d | [OK] | 2026-09-19 | 029fa7649 | 2026-09-19 | [OK] | ScrollbackViewport.tsx (+ addendum): bottom-anchored pageOffset window = identity anchoring (prepend/evict never shifts visible slice), poll-tick state observation, reporter publish gated on visibleKeys signature + unconditional on resize, PageUp/PageDown/Home/End + arrow-wheel path, top-edge precedence loading→floor-notice→spacer, bottom clamp, error row, expand-height anchor remap, pending-tail slots. 16/16 viewport ×3 runs + 18/18 store (rerun after fix). FINISHER CAUGHT REAL P02c RACE: concurrent pageBack() double-absorbed same cursor page (duplicate keys, resident 16≠12) → per-store pageQueue + call-time generation capture (stale-generation test stays green). End-test premise provably contradicted store contract (atFileEnd true on static file) → rewritten as concurrent-appender scenario, intent preserved. eslint 0 (ViewportFrame extraction for 80-fn law; ESC constants for no-control-regex; SemanticColors.status.error); tsc noemit 0; real-Ink lane per dev-docs/bun.md (test-utils/real-ink + render.js waitFor). act() poll warnings documented as noise |
| 02 | P02e | [OK] | 2026-09-19 | 533774dde | 2026-09-19 | [OK] | Settings ui.scrollbackPagerEnabled (default false until end-of-P02 flip) + scrollbackMarginViewports (2) + scrollbackByteFloorKiB (256) in schema-ui; schema:settings + docs:settings regenerated. useScrollbackBootstrap: startup-read once, flag-on constructs pager store bound to session journal, missing/failed journal → non-pager fallback + one-line notice, live toggle → restart prompt (no hot-swap), KiB→bytes mapping; DefaultAppLayout minimal-diff condition; scrollbackLimits constants. Tests: 11/11 hook suite + scrollback lanes 34/0 (viewport 16 + store 18) post-prettier rerun 45/0 combined; layout lanes 34 pass with 2 footer-trust fails PROVEN pre-existing (stashed baseline log identical); eslint 0/0 on all touched; tsc noemit 0; flag-off live smoke haiku exit 0 (p02e/smoke-off.log). Session timed out pre-commit; gates re-verified + committed by driver |
| 03 | P03 | [OK] | 2026-09-20 | fadb0cf2f (core) + a3a861e04 (cli) | 2026-09-20 | [OK] | CORE: contextRange v2 — removedInterior spans (compressed/density-replaced/density-removed/rewound/cleared; inclusive, sorted, strictly disjoint, same-reason coalescing, cumulative) + approximate flag (unmarked legacy); compressed spans derived from chronologyReplaced; HistoryServiceCore emits on empty→first-add (contract fix) + rewound/cleared/density spans; clear→rebuild resets span state (documented); getContextRange() parity with last event; batch contracts extracted to historyBatchContracts.ts (800-line law). CLI: classifyContextState (approximate/empty → n/a; in-span → purged; in-bounds → in-context); HistoryItemDisplay contextState prop ('purged' chip + DimComment); HistoryBoundaryRow controlled expander (loader only on expand, dropped on collapse, re-read on re-expand); ScrollbackViewport rows via HistoryItemDisplay + boundary rows (P05-placeholder loader); expansion state component-local, rides eviction unmount (#3428 invariant respected); core barrel exports ContextRange types. Tests: v2 10/10, history 451/0, recording 823/0, cli lane 67/0; eslint 0/0, tsc 0. Approximate pre-P05 membership documented, resolved P05b1. Finisher fixed 8 lint errors (useBoundaryExpansion/usePagingInput extraction, DimComment, readonly arrays) |
| 04 | P04a | [OK] | 2026-09-20 | 347373246 | 2026-09-20 | [OK] | staticPrintLedger (src/ui/print/): append-only ledger (spread-replace so handed arrays never mutate; ever-appended-key throw guard; forgetEvicted drops refs only; clear = epoch bump + notice row + residents, resets key sets) + StaticPrintProtocol (real Ink Static, children-function contract, keyed remount on epoch/generation, PrintBoundary error boundary with CommittedPass child owning the ack effect — failed pass acks nothing; recovery remounts Static with items.slice(printedThrough) so pending suffix prints exactly once; useSyncExternalStore re-render; Ink bounded archive counted, no per-row retention on our side). Public interface compile-pins absence of trim/splice surfaces. Harness: real-ink.ts re-exports Colors. Tests: contract 11/11 (print-byte occurrences exactly-once incl. equal-length-after-evict, no rotation 36 rows/8 batches, remount-after-clear, interrupted + recovery), regression 34/34, tsc 0, eslint 0/0. Notes: module is .tsx (JSX); act() stderr noise pre-existing in real-Ink lanes |
| 04 | P04b | [OK] | 2026-09-20 | b8cd2287a | 2026-09-20 | [OK] | store.resumeFromJournal(): Promise<ScrollbackResumeSummary{seededRows,floorOffset,tailOffset,headSeq,tailSeq,atClearBoundary}> — reuses ensureCursor + collectBack (display rows, stop at rewind boundary, drop boundary+older) + absorbBack under the pageQueue generation guard; scalars from cursor windowStart/windowEnd (torn tail excluded); state after = last page only, atFileEnd/atVisibilityFloor correct. Bootstrap wires await resumeFromJournal() post-construction; failure → existing non-pager fallback + notice (resolveBootOutcome discriminated outcome; handleResumeFailure teardown). Fixture fact documented: pager floor is coarser than replay cutSeq (post-boundary tail comparison). Tests: resume 12/12 (equivalence vs replaySession AND eager full-parse incl. rewind journal; bounded 50-rows/pageRows-10; page-on-demand identity-keyed; offsets/watermarks; header-only/zero-byte edges), store 18/18, bootstrap 12/12, viewport 16/16, staticPrintLedger 11/11 (69/0 combined); full-package tsc 0 (fresh tsbuildinfo), eslint 0/0 (nesting flattened via scanResumeFloorPage extraction), prettier clean |
| 05 | P05a | [OK]* | 2026-09-20 | 59e7bd8a5 | 2026-09-20 | [OK] | journalResolver.ts: JournalResolver.open/resolve()/stats()/close() — events-only prepass (length-based skip of non-mutating content lines; cheap chronology probe only), survivor interval list + interval-first offsets (sorted/disjoint/strictly-increasing; rowCount==seq-span except purge expansion rows), rewind = suffix-from-cut over survivors (cutSeq exact-match chronology, count fallback over dense space), compressed = whole-history replacement attributed to compressed seq, purge = payload rows attributed to purge seq w/ rowIndex expansion; malformed mid-file skip counted, torn tail silent, version ∉{1,2} rejects 'Unsupported recording version v' — ALL engine-parity via ReplayEngine oracle on identical generated journals incl. adversarial A,B,C→rewind→D,E→rewind→F = intervals [A],[D],[F]. Memory: generator frame-slot nulling (decodeSpan try/finally) + retry-loop WeakRef probes (10-cycle poll; release proven, GC-timing de-flaked, stats() alive post-GC: interval bookkeeping only). I/O: injectable ResolverIo, counting wrapper: open==1, reads ≤ 2*ceil(size/chunk)+2 (two chunked passes). Injectable chunkBytes 64KiB default. Tests 15/15 (incl. equivalence vs replaySession toStrictEqual, over-removal rewind, duplicate callIds, I/O bound); cursor 12/12; recording 838/0 (52 files); history 451/0; tsc 0; eslint 0/0 (6 sonarjs fixed via nextSpanUnits/decodeSpan extraction, behavior-preserving, re-verified). *ReplayEngine refactor onto resolver NOT yet done — follow-up before or during P05b |
| 05 | P05b1 | [OK] | 2026-09-20 | ba04305eb | 2026-09-20 | [OK] | WRITE: SessionRecordingService.recordDensityChange/recordSyntheticInsert/recordCompressionDetail (sync enqueue, SessionRecordLine\|null, null when inactive) + density_mutation/synthetic_insert/compression_detail kinds + payload types in recording/types.ts (v:1; NOT in MATERIALIZING_EVENT_TYPES). READ: JournalResolver folds — density removals compact out (replacement content pinned to original envelope slot, cleared at yield; retention O(events+pending replacements)); synthetic inserts splice after anchor in FOLD order w/ own-envelope attribution; compression_detail = membership pin (row no-op); count rewind folds post-mutation survivors; intervals re-sorted for out-of-envelope-order folds. Membership approximation from P03 RESOLVED (expectLiveSpansResolved: removed→non-survivor, density-replaced→survivor envelopes). Legacy journals: documented divergence unchanged. Resolver limits documented in-code: density targeting purge-internal chrons / mid-purge insert anchors → skip+count (needs row-level purge splitting). Test-7 fixture self-contradiction corrected (afterSeq=2 semantics, disclosed; bytes unchanged). Tests: durableMutations 16/16 (write envelopes + resolver folds + live HistoryService/RecordingIntegration equivalence incl. combined density+insert+cutSeq-rewind script), resolver+cursor 27/27, recording 854/0 (one pre-existing integration.basic ENOENT flake, 3 clean reruns), history 451/0, tsc 0, eslint 0/0. FOLLOW-UPS: replaySession parity for new kinds (still unknown-event skip); RecordingIntegration auto-journaling of density/synthetic mutations (tests call recorders directly) |
| 05 | P05b2 | [OK] | 2026-09-21 | ebc63b933 | 2026-09-21 | [OK] | commit(type,payload)/waitForCommit(line): Promise<CommitWatermark{seq,byteOffset(exclusive end)}>; acks resolve in seq order (CommitAckRegistry w/ drain gate + base offset; chained resolution); DEFAULT_MAX_QUEUE_BYTES finite default replaces MAX_SAFE_INTEGER, Infinity = only explicit opt-out (constructor admits it); overflow = backpressure (await drain room; no sync throw, no discard — 4 legacy bounds tests reconciled to gated-io pins); RecordingWriterIo injectable write seam (config.io, mirrors ResolverIo); write failures reject pending acks w/ ORIGINAL error (.code preserved ENOSPC/EACCES/ENOENT), poison service (commit paths reject loudly; legacy record* keeps enqueue-null); failed batch advances no byte accounting (append-only preserved); read-your-write: ack only after appendFile resolved (fresh JournalResolver sees rows at offset+length==watermark); post-commit observer failures ride flushAtTurnBoundary, never rollback. Extraction for 800-line law: ChatsDirWatcher.ts (watchChatsDir + diagnoseMissingPath, onWatcherError callback preserves null-on-error re-arm) + CommitAckRegistry.ts. Driver completed crashed-session repair inline (comment-opener restore, double-deref ack.chained, watcher delegation swap, no-overlap cast) after 3 subagent timeouts — mechanical only, disclosed. Tests: commitProtocol 14/14 (watermark durability, monotonicity, backpressure both modes, 3 errno codes, append-only prefix, slow-disk ordering, observer-failure isolation, 8-way concurrency, resolver read-after-commit, DEFAULT bound export), bounds 10/10, durableMutations+resolver+cursor 43/43, recording 869/0, history 451/0, tsc 0, eslint 0/0 |
| 05 | P05b3 | [OK] | 2026-09-21 | 02f79cdf8 + 953990185 | 2026-09-21 | [OK] | G1 facade: `history: IContent[]` DELETED from HistoryServiceCore; HistoryService = facade over journal (HistoryJournalStore 880: planHistoryMutation/planDensityMutation, parseFoldEvents, foldDurable, materialize; historySpanWindow + historyMutationFifo; bare-construct own mkdtemp journal, attachJournal late-attach = real CLI path; recordedIdentities Set dies → watermark dedupe; legacy prepareContentBatch rollback dies w/ array). G2 contracts: RuntimeGenerateChatOptions.contents + providers GenerateChatOptions.contents = AsyncIterable<IContent> (providers flip was agents-cascade root cause); getCuratedForProviderStream added (sync stays); collectContents/replayableContents (new providers exports entry); agents cascade migrated (DMP/StreamProcessor/compression prompt/LoadBalancing narrowing + ~20 tests). structuralAudit.bun.test.ts (array-deletion whitelist + negative controls; JournalResolver.units documented O(mutation-events)). Disclosures: 3 subagent timeouts recovered via tree inspection; driver inline repairs (2 `.at(-1)` law fixes, 8 object-shorthand, DMP replayableContents swap for 800-line law, 4 hidden sync-op-on-stream fixes in typecheck-EXCLUDED tests); providers full suite hangs + agents wide-scope stalls PRE-EXISTING on clean HEAD (auth-flow spec; byte-identical stall point proven via stash baseline). Gates: tsc 0 ×4; core 479/0 + recording 869/0 + facade 13/13; providers touched 191/12 = base exactly; agents touched 717/40 → 2 new fixed → 23/0; CLI sweep 96 names == baseline; eslint 0; prettier |
| 05 | P05b4 | ⬜ | - | - | - | ⬜ | b4 provider contracts/transports (request-scoped body build, release-after-call, byte-level equivalence; criteria 1-3 harnesses live here) |
| 05 | P05c | ⬜ | - | - | - | ⬜ | Subagent journals: fs-safe id pre-runtime, session_start kind/parentSessionId, all-path discovery filters, lifecycle cleanup; staged BEFORE final flip. Done = lifecycle matrix + child criteria |
| 05 | P05d | ⬜ | - | - | - | ⬜ | No-materialization resume end-to-end (discovery, resumeSession, checkpoints). Done = criterion 5, peak-row instrumentation incl. discovery |

Open decisions from design section 9 (PDF §9): NONE. OQ1-10 resolved;
OQ7 refined after plan review (stop = clear boundary/file start; purged
rows scroll). P05 UN-deferred 2026-09-18 per Andrew: "all done in your
PR" + facade/subagent rulings. Plan review: deepthinker round 1
(19 findings) folded into implementation-plan.md rev 2 + design rev 2;
round 2 = findings-verification only.
