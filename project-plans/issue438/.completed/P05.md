Completed PLAN-20250212-LSP.P05.

Evidence:
- Updated packages/lsp/src/service/diagnostics.ts with required plan marker and pseudocode marker block.
- Exported 8 typed stub functions:
  1) escapeXml(text: string): string
  2) mapSeverity(lspSeverity: number): string
  3) normalizeLspDiagnostic(raw: RawLspDiagnostic, file: string, workspaceRoot: string): Diagnostic
  4) deduplicateDiagnostics(diagnostics: readonly Diagnostic[]): Diagnostic[]
  5) filterBySeverity(diagnostics: readonly Diagnostic[], severities: readonly string[]): Diagnostic[]
  6) formatDiagnosticLine(diagnostic: Diagnostic): string
  7) formatSingleFileDiagnostics(file: string, diagnostics: readonly Diagnostic[], config: LspConfig): string
  8) formatMultiFileDiagnostics(writtenFile: string, allDiagnostics: Readonly<Record<string, readonly Diagnostic[]>>, config: LspConfig): string
- Stubs are minimal (empty returns / NotYetImplemented throw), no TODO/FIXME/HACK comments.
- File length constrained to <= 100 lines.
