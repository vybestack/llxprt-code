# Issue #2628 — Revision-pinned residual census

Revision: `5bedbd2385b8c4767e97c43d52b563d11e3b03db` (main, 2026-09-16, after PR #3689)
plus the working-tree changes of this branch (branch `issue2628`).
Scanner states at this revision (executed):

- `bun scripts/check-genai-enclave.ts` → **PASSED** (5122 files; permitted SDK
  declaration set empty; required manifests `.` and `packages/providers`
  readable).
- `npm run gate:agents-neutral` → **PASSED** (exit 0; zero @google/genai
  imports and zero Google-shaped fixtures in 455 agents test files).
- `@google/genai`: no declarations in any manifest; no imports repo-wide
  (PR #3380 landed; not restored).

## A. Manifests and imports

| Finding | Owner | Disposition |
|---|---|---|
| `@google/genai` declared nowhere; root/provider manifests readable | #2628 (done state) | Preserve; new gate (after #2763) re-asserts with plugin endpoint. Evidence: `bun scripts/check-genai-enclave.ts` → PASSED |
| Stale two-manifest-bridge comment in `scripts/genai-enclave/config.ts` | #2628 | DONE. Header comment now states the permitted set is EMPTY since PR #3380 and `REQUIRED_MANIFEST_WORKSPACE_DIRS` ('.', 'packages/providers') exists only so the guard can READ those manifests to prove absence; matching correction in the section-2 doc block. Comment-only; logic untouched. Evidence: guard re-run PASSED after edit |

## B. Production naming/shape residuals (from `providerAgnosticNamingAllowlist.ts` + source survey)

| # | Site | Owner | Disposition | Evidence |
|---|---|---|---|---|
| B1 | `core/src/config/models.ts:20,27` `isGemini2Model`/`isGemini3Model` | #2628 | DONE. Moved to `providers/src/gemini/modelClassification.ts`; core exports deleted; direct owner imports at `geminiReasoningTranslation.ts:7-10` and `GeminiMessageConverter.ts:14`; unit test relocated to `providers/src/gemini/modelClassification.test.ts` | `bun test packages/providers/src/gemini/modelClassification.test.ts` pass (in 43-pass batch); `bun test packages/core/test/models/profiles.test.ts packages/core/test/models/transformer.test.ts` → 90 pass; fixture `mock-data.ts` `geminiModel` renamed; no `isGemini2Model`/`isGemini3Model` refs left under packages/core |
| B2 | `core/src/llm-types/geminiContent.ts` + barrel `llm-types/index.ts:39`; `ContentConverters` Gemini-direction | #2628 | DONE. `toGeminiContent`/`toGeminiContents`/`blockToPart` deleted from `ContentConverters.ts` (-232 lines); `geminiContent.ts` reduced 6 interfaces → 2 (`GeminiContent`, `GeminiContentPart`; FunctionCall/FunctionResponse/InlineData member shapes folded inline); neutral parse direction `toIContent`/`toIContents` preserved; barrel keeps `export *` (only 2 symbols remain; explicit named re-export would trip the genai-enclave export guard for the barrel) | `bun test ContentConverters.test.ts ContentConverters.toolFailure.test.ts` → 98 pass; `geminiResponseMapper.test.ts` pass; zero repo refs (excl. node_modules/dist) to `toGeminiContents` in packages/ outside gate-scanner matcher strings; `bun scripts/check-genai-enclave.ts` PASSED with the trimmed 2-entry `GEMINI_NAME_EXPLICIT_ALLOWLIST` |
| B3 | `core/src/utils/messageInspectors.ts` | #2628 | DONE. Deleted (39 lines) | `find packages/core/src -name "*messageInspector*"` → no files (no orphaned test); typecheck green |
| B4 | `core/src/core/contentGenerator.ts` `geminiApiKey` | #2628 | DONE. Renamed `envProviderApiKey`; `GEMINI_API_KEY` env literal byte-identical; `GOOGLE_API_KEY`/`GOOGLE_CLOUD_PROJECT` untouched | `bun test packages/core/src/core/contentGenerator.test.ts` → pass (in 131-pass batch); grep shows `GEMINI_API_KEY`/`GOOGLE_API_KEY` literals unchanged |
| B5 | `core/src/utils/filesearch/ignore.ts` `geminiignorePath` | #2628 | DONE. Renamed `legacyIgnoreFilePath`; `.geminiignore` filename literal unchanged | `bun test packages/core/src/utils/filesearch/ignore.test.ts` → pass (90-pass batch) |
| B6 | `cli/src/ui/components/ContextSummaryDisplay.tsx` `geminiMdFileCount` (+ callers) | #2628 | DONE. Deprecated `geminiMdFileCount` prop removed; fallback now `llxprtMdFileCount ?? 0`; zero callers pass the old prop | repo-wide grep `geminiMdFileCount` → 0 hits; `bun test packages/cli/src/ui/components/SettingsDialog.interactions.test.tsx` → pass incl. 8 snapshots |
| B7 | `cli/src/config/providerModelResolver.ts` + `cli/src/config/config.ts` `envGeminiModel` | #2628 | DONE. Renamed `envProviderModel` in both; `GEMINI_MODEL` env literal and `'gemini'` provider-id scoping preserved | `bun test packages/cli/src/config/__tests__/providerModelResolver.behavior.test.ts` → 30 pass (incl. "resolves GEMINI_MODEL env when gemini provider is explicit" and envProviderModel leak guards); `bun test packages/cli/src/cli.test.tsx` → 15 pass |
| B8 | `cli/src/ui/privacy/GeminiPrivacyNotice.tsx` + `PrivacyNotice.tsx` | #2628 | DONE. File deleted; consent content folded into `MultiProviderPrivacyNotice.tsx` `PROVIDER_INFO.gemini` (displayName 'Gemini'; tosUrl https://developers.google.com/terms; privacyUrl https://ai.google.dev/gemini-api/terms; key points carry the Google APIs ToS + Gemini API Additional Terms message and URLs https://ai.google.dev/docs/gemini_api_overview, https://aistudio.google.com/) — all 4 URLs and the agreement message from the deleted component are preserved; `'gemini'` id literal preserved | `bun test MultiProviderPrivacyNotice.test.tsx PrivacyNotice.test.tsx` → pass (in 46-pass batch, incl. "renders ONLY the MultiProvider notice for non-gemini active provider") |
| B9 | `providers/src/loadBalancing/backendMetrics.ts` `extractGeminiTokens`/`geminiTokens` | #2628 | DONE (characterization CORRECTED in completion pass). First-pass claim "dead" was WRONG: the Gemini wire usage shape (`usageMetadata.promptTokenCount`/`candidatesTokenCount`) is a live part of `extractTokenCount`'s chunk contract — locked by `LoadBalancingProvider.tpm.test.ts` (7 tests failed when the branch was deleted: TPM recorded 0) and the metrics test's "should handle Gemini response format" case. Disposition per plan: behavior restored in place with neutral naming (`extractUsageMetadataTokens`); the Gemini key names inside are wire-compat data (same class as B17), not naming residuals. The subagent's weakening edits to `LoadBalancingProvider.metrics.test.ts` (Gemini-format case rewritten to expect 0 tokens; three fixtures switched from `usageMetadata` to OpenAI `usage` shapes) were REVERTED to HEAD — "port, don't weaken" | `bun test LoadBalancingProvider.tpm.test.ts metrics.test.ts tokenAccounting.test.ts` → 42 pass, 0 fail (post-restore; tpm was 7-fail pre-restore). `npm run gate:agents-neutral` scans `packages/agents/src/api` only, so providers-side wire-key reads are outside its scope; naming scanner: neutral name needs no allowlist entry (old `extractGeminiTokens`/`geminiTokens` entries stay removed) |
| B10 | `providers/src/utils/providerRequestConversion.ts` + `providers/src/index.ts:193` barrel export | #2628 | DONE. `buildGeminiDumpContents`/`isGeminiCompatibleProvider` moved to `providers/src/gemini/geminiDumpConversion.ts` (owner tree); neutral module imports directly from it (dispatch by provider-id only); barrel export removed; no external consumers existed | `bun test packages/providers/src/gemini/GeminiProvider.separateDump.test.ts` → pass; repo grep `buildGeminiDumpContents` → only the provider-tree module, its import in `providerRequestConversion.ts`, and the allowlist import-tuple; allowlist `ALLOWED_EXPORT_TUPLES` barrel entry removed; naming tests green |
| B11 | `agents/src/core/DirectMessageProcessor.ts` `_extractDirectGeminiOverrides`/`geminiDirectOverrides` | #2628 | DONE. DTO key characterized as a wire value: `geminiRequestBuilding.ts:92-93` reads `options.metadata.geminiDirectOverrides` by exact name (covered by `GeminiProvider.test.ts` "respects metadata geminiDirectOverrides when building request config"). Key PRESERVED as compat data; only the private method renamed (`_extractDirectProviderOverrides`) with a doc comment recording the wire key | `bun test directMessage.characterization.test.ts directMessageAfcSanitization.test.ts` → 18 pass; `GeminiProvider.test.ts` → pass (57-pass batch); allowlist extended entry `DirectMessageProcessor.ts::geminiDirectOverrides` retained |
| B12 | `providers/src/ProviderManager.ts::geminiProvider` allowlist entry | #2628 | DONE. Stale entry removed (site no longer exists) | grep `geminiProvider` in `ProviderManager.ts` → 0 hits; naming tests green after removal |
| B13 | alias factory / builtin contributions / composition index / `GeminiProvider` re-export | #2762/#2763 | OUT (reassigned 2026-07-27); untouched this PR | allowlist entries for these retained; gate + naming tests green |
| B14 | `modelEnvelope.ts`/`streamRequestHelpers.ts`/`hookTranslator.ts` residual refs | #2624 (landed) | Remaining refs are comments (prose, non-binding); no action | verified by grep 2026-09-16 |
| B15 | `partUtils`/`toolDeclaration` `Legacy*` bridges | #3694 | OUT; untouched | allowlist entries retained |
| B16 | `core/src/llm-types/finishReasons.ts` | permitted (neutral surface) | No action | verified by grep 2026-09-16 |
| B17 | Env/filename/id compat forms (`GEMINI_API_KEY`, `GEMINI_MODEL`, `GEMINI_SYSTEM_MD`, sandbox passthrough keys, `gemini` provider id, `GEMINI.md`, `.geminiignore`, `gemini_content`, `MessageType.AI='gemini'`, `llxprt:gemini`, `gemini-<n>` model data) | permanent compat | PRESERVED | grep 2026-09-16: all literals byte-identical; `sandbox-containers.ts::GEMINI_API_KEY`/`GEMINI_MODEL` allowlist entries retained; `AuthDialog.test.tsx` still uses `GEMINI_API_KEY` env var (member access, not a declared identifier) |
| B18 | `packages/core/src/integration/compression-duplicate-ids.test.ts` still referenced the deleted `ContentConverters.toGeminiContents` | #2628 (completion-pass discovery) | DONE. Ported: a local `encodeForProvider` mirror produces the same wire shapes the provider outbound converter emits (role user/model; text/functionCall/functionResponse parts), so the compression round-trip still exercises parse-side curation (`toIContent`) with identical assertions. Type-safety addendum (completion pass): `ToolCallBlock.parameters` is `unknown`, so the mirror narrows via guarded `argsAsRecord` helper (empty-object fallback) instead of the blind `as Record` the deleted encoder used | `bun test packages/core/src/integration/compression-duplicate-ids.test.ts` → 2 pass, 10 expects (both duplicate-ID invariants asserted); `tsc --noEmit -p tsconfig.noemit.json` (core) → 0 errors after fix; full `npm run typecheck` → exit 0 |
| B19 | `packages/cli/src/cli.test.tsx` rename collision: `originalEnvGeminiSandbox`→`originalEnvSandbox` collided with an existing `originalEnvSandbox` binding (duplicate `let` declaration; suite errored) | #2628 (completion-pass discovery) | DONE. Distinct neutral names: `originalEnvLlxprtSandbox` (holds `LLXPRT_SANDBOX`) and `originalEnvSandbox` (holds `SANDBOX`); save/restore semantics per variable identical to HEAD | `bun test packages/cli/src/cli.test.tsx` → 15 pass, 0 fail |
| B20 | `packages/providers/src/openai/OpenAIStreamProcessor.ts` max-lines lint failure (801 code lines vs 800 max, skipBlankLines/skipComments) — PRE-EXISTING on main @ `5bedbd238`: file is byte-identical to HEAD and unmodified by this branch; `npx eslint` on the HEAD content fails identically. Not a #2628 regression; recommend a separate issue for the underlying overage (landed via #3689/#3658) | discovered in #2628 completion pass; fix is unblock-only | Minimal behavior-preserving unblock applied so the required `npm run lint` gate passes on this branch: collapsed `const parsingText = sanitizeProviderText(workingText); let cleanedText = parsingText;` into `let cleanedText = sanitizeProviderText(workingText);` and pass `cleanedText` to the parse call (same evaluation order; `cleanedText` is only reassigned after the parse). −1 code line | `npx prettier --check` + `npx eslint` on the file → clean; `npm run lint` → exit 0 |

## C. Test residuals (from allowlist; disposition per 2026-07-27 reassignment)

| Group | Owner | Disposition |
|---|---|---|
| Provider-suite relocations (`providers/src/auth/*` gemini oauth specs, oauth-buckets/provider-switching/multi-provider-logging/oauth-submission/cli-args profile-flag suites, `LoadBalancingProvider` mocks, `providerManagerInstance` mocks, provider-alias-defaults, `ProviderManager.gemini-switch`, `provider-gemini-switching`, `aliasProviderFactory.authOnly`) | #2762/#2763 | OUT of this PR — allowlist entries retained, untouched |
| Incidental non-provider test bindings: `memoryDiscovery.test.ts` `*GeminiFile` (6), `cli.test.tsx::originalEnvGeminiSandbox`→`originalEnvLlxprtSandbox` (see B19), `App.*.test.tsx::getAllGeminiMdFilenames` (5 entries — stale, identifier no longer exists repo-wide; entries removed, files untouched), settings `corruptCanonicalProfileNonGeminiModel` (2), `ignorePatterns.test.ts::getCurrentGeminiMdFilename`, `providerModelResolver.behavior.test.ts::envGeminiModel`→`envProviderModel` | #2628 | DONE, all renamed or verified stale. Evidence: `bun test memoryDiscovery.test.ts ignorePatterns.test.ts` → 98-pass batch; `canonicalProfileRepair.test.ts` → 46-pass batch; `cli.test.tsx` → 15 pass; naming scanner tests green after entry removal |
| `ContentConverters.test.ts` gemini-direction cases | #2628 | DONE. Deleted with the deleted behavior; neutral-direction (parse) assertions retained and pass: ContentConverters.test.ts + toolFailure.test.ts in 98-pass batch |
| `core/src/config/__tests__/models.test.ts` predicate cases; `core/test/models/__fixtures__/mock-data.ts` `geminiModel` | #2628 | DONE. Predicate test relocated to `providers/src/gemini/modelClassification.test.ts` (pass); core models.test.ts deleted; fixture binding renamed; profiles/transformer tests → 90 pass |
| `respectGeminiIgnore` bindings across cli config parity tests (11 files) + `SettingsDialog.interactions.test.tsx` | #2628 | DONE. Characterized: NOT a persisted settings key — zero non-test references at HEAD; production key is `respectLlxprtIgnore` (core `config/constants.ts` DEFAULT_FILE_FILTERING_OPTIONS / configTypes.ts, tools glob/ls/ripGrep). The parity-test mocks were stale (mocked a nonexistent key); renamed to the real production key so the mocks now shadow the actual surface | `bun test` on all 7 parity suites + config.part2/3/4 + config.test.ts → 215 pass in batch (post-fix cli.test.tsx run: 15 pass); `SettingsDialog.interactions.test.tsx` → pass |
| `'gemini'` id-literal data (ProfileCreateWizard/constants, WelcomeOnboarding/*, provider-integration.ts, providerCapabilitiesService.ts, IContent.providerMetadata.test, models/provider-integration, oauth-settings-adapter.spec, file-oauth-settings.test, provider-registry.spec) | characterize | Verified `'gemini'` provider-id string literals/data (permitted); entries retained. One entry REMOVED: `AuthDialog.test.tsx::gemini` — file has no declared gemini identifier (only `process.env.GEMINI_API_KEY` member accesses, which the AST scanner does not count); removal safe. One entry ADDED: `MultiProviderPrivacyNotice.tsx::gemini` (PROVIDER_INFO record key carrying the folded-in consent content, provider-id data) | `bun test providerAgnosticNaming.test.ts geminiIdentifierScanner.test.ts geminiIdentifierScanner.extended.test.ts` → 194 pass |

## D. Gate and apparatus (blocked)

| Item | Owner | Status |
|---|---|---|
| `scripts/check-gemini-containment.ts` (structural gate, plugin endpoint) | #2628 | BLOCKED: must follow #2763 (plugins/google-gemini must exist; Layer 1 = zero SDK decls in base/root/providers + exactly one in plugins/google-gemini) |
| Retirement of `check-genai-enclave.ts`, `genai-enclave/`, `agents-neutral-gate*`, `genai-import-inventory.ts`, naming apparatus, baseline docs | #2628 | BLOCKED: requires replacement passing on main + differential parity + #3421 policy artifact (none exists yet). Note: `agents-neutral-gate-config.ts::GEMINI_CONTENT_BARREL_TYPES` still lists 'GeminiFunctionCall' — that is a scanner detection signature (flags imports of the name if any appear), not a reference to the deleted interface; left in place so the gate does not weaken (apparatus untouched per plan) |
| Old guards keep running on every PR until then | — | ACTIVE: this branch keeps them green (`check-genai-enclave` PASSED, `gate:agents-neutral` exit 0, naming tests 194 pass) |

## Review record (compliance review round 1 — 2026-09-16)

Reviewer: `reviewer`-class agent (gpt56solhigh window unavailable; deepthinker/astra quota-exhausted; review executed by codeanalyzer/glm with full guard+test reruns). Verdict: **scope PASS**, zero Blocker/High/Medium findings.

- In-scope-Fix (applied): compression-duplicate-ids.test.ts header comment overstated the local wire mirror as "exactly" the provider output; reworded to "the subset ... these fixtures use". Re-verified: 2 tests / 10 expects green.
- Reject (no change): condensed Gemini consent prose under MultiProviderPrivacyNotice — authorized by issue ("reconcile consent presentation through the existing provider identity"); all 4 URLs + legal substance preserved (reviewer confirmed).
- Reject (no change): reviewer note re `backendMetrics.test.ts` filename (brief error); real coverage = LoadBalancingProvider.{tpm,metrics,tokenAccounting}.test.ts, 42 pass.
- Note: stale `buildGeminiDumpContents` in packages/providers/dist/ is gitignored build output, not in the diff.

Reviewer-rerun verification: enclave guard exit 0; gate:agents-neutral exit 0; naming suites 194 pass; ContentConverters+compression+modelClassification 44 pass; LB suites 42 pass; DirectMessageProcessor suites 18 pass; typecheck exit 0.

## CI fix record (2026-09-17)

- PR #3702 scripts shard failed `publish-integrity.test.ts` S6 "#2352: ships every exported subpath entry of internal workspace dependencies": B3's deletion of `core/src/utils/messageInspectors.ts` left the `./utils/messageInspectors.js` exports entry dangling in `packages/core/package.json` (the entry ships `src/utils/messageInspectors.ts` under the bun condition). Removed the dead subpath entry (4 lines) — the packaging surface of the deleted path dies with it, per the same-PR deletion rule. `bun test scripts/tests/publish-integrity.test.ts` → 19 pass / 0 fail after the fix.
