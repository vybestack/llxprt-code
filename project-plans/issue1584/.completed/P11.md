# P11 Provider Move Implementation Completion

Plan ID: PLAN-20260603-ISSUE1584
Phase: P11
Status: PASS

## Scope

- Moved provider implementation tree from packages/core/src/providers to packages/providers/src.
- Removed production core dependency/path mapping/re-exports/imports for @vybestack/llxprt-code-providers.
- Expanded core-owned runtime structural contracts for the provider-facing surfaces core still consumes.
- Updated provider package exports and post-move provider package behavioral tests.
- Updated CLI/package metadata for providers workspace dependency needed by consumer wiring.

## Verification

- npm run typecheck --workspace @vybestack/llxprt-code-core: PASS
- npm run typecheck --workspace @vybestack/llxprt-code-providers: PASS
- npm run build --workspace @vybestack/llxprt-code-core: PASS
- npm run build --workspace @vybestack/llxprt-code-providers: PASS
- npm run test --workspace @vybestack/llxprt-code-providers -- src/provider-public-api.behavior.test.ts src/import-boundary-expectations.test.ts src/package-boundary.test.ts src/provider-manager-behavior.test.ts src/provider-content-generator-behavior.test.ts src/tokenizer-behavior.test.ts: PASS, 6 files / 91 tests

## Boundary Evidence

- packages/core/package.json has no @vybestack/llxprt-code-providers dependency.
- packages/core/tsconfig.json has no providers package path mapping.
- packages/core/src/providers directory is absent after the move; no compatibility shim directory remains.
- Production core source has no provider-package import; package-name mentions are limited to explanatory comments/test logs and have been removed from active production comments where practical.
- Provider package depends on core; core does not depend on providers; CLI depends on core and providers.

## Semantic Assessment

- Provider public API exports ProviderManager, concrete providers, ProviderContentGenerator, tokenizers, provider errors, config keys, and provider utilities from @vybestack/llxprt-code-providers.
- Core retains structural runtime contracts only and does not re-export provider APIs from its package index.
- HistoryService no longer constructs provider-owned tokenizers directly; token counting is contract/factory based with local fallback estimation until consumer injection is completed in later phases.
- Core contentGenerator no longer imports ProviderContentGenerator directly; runtime provider content generation requires an injected factory, to be wired through consumer migration phases.

## .llxprt Provenance

- .llxprt/LLXPRT.md remains modified from preflight state documented in P00a; P11 did not modify .llxprt/.

## Notes

- P11 subagent execution was attempted multiple times but failed before returning useful output due ProviderCacheError auth-token unavailability; coordinator repaired the already-partial P11 work to unblock the strict phase sequence.
- Full repository verification remains for P16 after consumer migration and cleanup phases.
