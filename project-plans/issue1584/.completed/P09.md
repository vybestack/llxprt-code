# Phase 09 Completion: Provider Move Import-Map Stubs

**Phase ID**: PLAN-20260603-ISSUE1584.P09
**Requirement**: REQ-PKG-001 — All provider implementations currently in packages/core/src/providers MUST live under packages/providers/src after migration.
**Completed**: 2026-06-03

## Files Changed

### Created
- `project-plans/issue1584/analysis/provider-move-map-detailed.md` — Deterministic per-file move map covering all 251 provider files with source→destination paths, classification rules, and import rewrite categories (A-L). Every inventory file has a matching entry.
- `packages/providers/src/move-map-validation.test.ts` — 18 behavioral tests that validate move-map completeness, inventory coverage, no-files-moved invariant, anti-shim guards, package boundary constraints, and import rewrite category coverage. Includes `@plan:PLAN-20260603-ISSUE1584.P09` and `@requirement:REQ-PKG-001` markers.

### Modified
- `project-plans/issue1584/analysis/provider-move-map-detailed.md` — Expanded condensed openai-responses __tests__ range entry into individual per-file entries (entries 189-198), and corrected `providerManager.context.test.ts` case to match actual filesystem case.

## What P09 Changed

P09 created two key migration-planning artifacts:

1. **`provider-move-map-detailed.md`**: A complete deterministic move map for every file in `packages/core/src/providers/`. Each of the 251 files has a source path (`packages/core/src/providers/...`), destination path (`packages/providers/src/...`), classification rule, and import rewrite category (A-L). The document also includes:
   - Directory-level move rules table
   - Per-file numbered table with all 251 entries
   - Import rewrite categories A-L with descriptions, affected core files, and remediation strategies
   - A per-consumer import remediation table listing all 28 core production files and 3 CLI files that import from providers
   - P11 execution order guidance

2. **`move-map-validation.test.ts`**: 18 behavioral tests in the providers package that serve as guards for migration readiness:
   - Inventory file existence and count verification (251 entries)
   - Core providers file count matches inventory
   - Actual files appear in inventory
   - Move map covers every inventory file
   - Move map destinations are all under `packages/providers/src/`
   - No provider implementation files moved yet (pre-migration invariant)
   - All core provider files still exist (none deleted)
   - Key provider interfaces and implementations still in core
   - Key provider subdirectories still in core
   - No V2/Compat/New/Copy suffixed files
   - No compat/shim files (except known non-shim exceptions)
   - Core production code has no `@vybestack/llxprt-code-providers` imports
   - Providers package does not depend on CLI
   - Core package does not depend on providers package
   - All import rewrite categories A-L are documented
   - ToolIdNormalization explicit exception is documented
   - Core import remediation document exists and covers all categories

## Verification Outputs

### Provider Typecheck
```
> @vybestack/llxprt-code-providers@0.10.0 typecheck
> tsc --noEmit
(exit 0)
```

### Provider Build
```
> @vybestack/llxprt-code-providers@0.10.0 build
> node ../../scripts/build_package.js
Successfully copied files.
(exit 0)
```

### Provider Lint
```
> @vybestack/llxprt-code-providers@0.10.0 lint
> eslint . --ext .ts,.tsx
(exit 0, no errors)
```

### Provider Test (47 passing: 18 move-map-validation + 29 package-boundary)
```
> @vybestack/llxprt-code-providers@0.10.0 test
> vitest run

 [OK] src/move-map-validation.test.ts (18 tests) 44ms
 [OK] src/package-boundary.test.ts (29 tests) 47ms

 Test Files  2 passed (2)
      Tests  47 passed (47)
   Duration  195ms
```

### Core Typecheck (regression)
```
> @vybestack/llxprt-code-core@0.10.0 typecheck
> tsc --noEmit
(exit 0)
```

### Move Map File Count Verification
- Core providers file count: **251**
- Move map numbered entries: **251**
- Inventory file count: **251**
- All three counts match — complete coverage confirmed.

### No Files Moved Confirmation
- `packages/providers/src/` contains only: `index.ts`, `move-map-validation.test.ts`, `package-boundary.test.ts`
- No provider implementation subdirectories exist in providers.
- No provider files deleted from core.

### Runtime Import Check
```
providers import ok, keys: []
```
(Scaffold still exports empty object — correct for P09.)

### Package Metadata Constraint Checks
- [OK] `packages/providers` in root `workspaces` array
- [OK] `@vybestack/llxprt-code-core: "file:../core"` in providers `dependencies`
- [OK] No `@vybestack/llxprt-code-providers` dependency in core
- [OK] No `@vybestack/llxprt-code` CLI dependency in providers

### Anti-Shim Scans
- [OK] Zero `@vybestack/llxprt-code-providers` imports in core production code
- [OK] Zero `@vybestack/llxprt-code-providers` imports in CLI production code
- [OK] Zero V2/Compat/New/Copy suffixed provider files in any package
- [OK] Core package has no providers dependency (no cycle risk)

### Core Import Sites from Providers (Pre-Migration Baseline)
- 70 production import sites across 28 core files importing from `providers/`
- 3 CLI production deep-import sites from `@vybestack/llxprt-code-core/providers/`
- All catalogued in move-map detailed document with per-consumer remediation plans

## Semantic Assessment

P09 creates deterministic migration-planning artifacts that are essential prerequisites for P11 (provider move implementation). No files were moved, deleted, or modified in `packages/core/src/providers/`. The providers package still contains only its P06-P08 scaffold plus the new test file.

**Why this matters**: The move map and validation tests establish a verifiable contract that every provider file has a planned destination, every import rewrite category is documented, and no migration step is left unspecified. The 18 behavioral tests serve as guards that will fail-fast if files are moved without updating the map (or vice versa).

**No behavioral changes**: No provider implementations were moved. No CLI imports were updated. Core re-exports remain unchanged. The providers package still exports an empty scaffold.

**P07/P08 tests still pass**: All 29 package-boundary tests continue to pass because no package metadata was changed. The 18 new move-map-validation tests are additive and validate the planning artifacts rather than production code behavior.

## No-Shim Assessment

- No compatibility wrapper files were introduced
- No `V2`/`Compat`/`New`/`Copy` suffixed types exist (verified by scan)
- No core re-export shims from providers package (core still re-exports from its own `./providers/` directory — pre-migration state; P15 will remove these)
- The providers package exports only an empty scaffold `src/index.ts` with phase markers from P06/P07
- The `move-map-validation.test.ts` file is a migration-readiness guard, not a shim
- Core's existing provider files remain untouched in `packages/core/src/providers/`
- No core → providers production dependency created

## P09 Code Markers

- `packages/providers/src/move-map-validation.test.ts` — `@plan:PLAN-20260603-ISSUE1584.P09`, `@requirement:REQ-PKG-001` (18 test markers)

## .llxprt Provenance

- Phase executed by LLxprt Code (glm-5.1) in accordance with `project-plans/issue1584/plan/09-provider-move-stub.md`
- Analysis documents referenced: `analysis/provider-move-map-detailed.md`, `analysis/provider-move-map.md`, `analysis/provider-file-inventory.txt`, `analysis/provider-file-classification-complete.md`, `analysis/core-import-remediation.md`, `analysis/anti-shim-policy.md`, `analysis/phase-verification-matrix.md`, `analysis/core-deep-import-policy.md`, `analysis/package-metadata-constraints.md`, `analysis/provider-external-dependencies.md`
- No `.llxprt/` directory was modified or deleted
- Generated build/test artifacts (coverage/, junit.xml, .tsbuildinfo, dist/) were left in place per project conventions