# Phase 03 Completion Marker

Plan ID: PLAN-20260603-ISSUE1584.P03
Phase: 03 — Core-owned contract stubs

## Provenance: .llxprt/LLXPRT.md

`.llxprt/LLXPRT.md` was already in a modified state **before** any implementation phases began, as documented in P00a preflight results (`analysis/preflight-results.md`):

> ```
> M .llxprt/LLXPRT.md
> ?? project-plans/issue1584/
> ```
>
> **Verdict:** Repository has only `.llxprt/LLXPRT.md` modified and `project-plans/issue1584/` untracked. No stray production-code changes. [OK]

Phase 03 did **not** modify `.llxprt/LLXPRT.md` or any other `.llxprt/` content. No files under `.llxprt/` were created, edited, or deleted by P03.

## Files Changed

### New Files Created

| File | Action | Description |
|------|--------|-------------|
| `packages/core/src/runtime/contracts/RuntimeProvider.ts` | Created | Core-owned structural provider contract (RuntimeProvider, RuntimeToolDeclaration, RuntimeToolset) |
| `packages/core/src/runtime/contracts/RuntimeProviderManager.ts` | Created | Core-owned structural provider manager contract (RuntimeProviderManager) |
| `packages/core/src/runtime/contracts/RuntimeModel.ts` | Created | Core-owned structural model contract (RuntimeModel) |
| `packages/core/src/runtime/contracts/RuntimeTokenizer.ts` | Created | Core-owned structural tokenizer contract (RuntimeTokenizer) |
| `packages/core/src/runtime/contracts/RuntimeTokenizerFactory.ts` | Created | Core-owned structural tokenizer factory contract (RuntimeTokenizerFactory) |
| `packages/core/src/runtime/contracts/RuntimeContentGeneratorFactory.ts` | Created | Core-owned structural content generator factory contract (RuntimeContentGeneratorFactory) |
| `packages/core/src/runtime/contracts/TelemetryContext.ts` | Created | Core-owned structural telemetry context contract (TelemetryContext) |
| `packages/core/src/runtime/contracts/BucketFailureReason.ts` | Created | Core-owned BucketFailureReason type |
| `packages/core/src/runtime/contracts/ReasoningOutput.ts` | Created | Core-owned structural reasoning output contract (ReasoningOutput) |
| `packages/core/src/runtime/contracts/MediaBlockContracts.ts` | Created | Core-owned structural media block contracts (MediaBlockType, ClassifiedMediaBlock) |
| `packages/core/src/runtime/contracts/index.ts` | Created | Barrel export for all runtime contracts |
| `packages/core/src/runtime/errors/MissingRuntimeProviderError.ts` | Created | Core-owned missing provider runtime error class |
| `packages/core/src/runtime/errors/index.ts` | Created | Barrel export for runtime errors |
| `packages/core/src/tools/toolIdNormalization.ts` | Created | Core-owned shared utility (moved from providers/utils/) |
| `packages/core/src/tools/toolIdNormalization.test.ts` | Created | Core-owned test (copied from providers/utils/) |

### Modified Files

| File | Action | Description |
|------|--------|-------------|
| `packages/core/src/runtime/index.ts` | Modified | Added exports for contracts/index.js and errors/index.js |

### Files NOT Changed (Intentional)

- `packages/core/src/providers/` — No provider files were moved, removed, or modified per P03 scope.
- `packages/core/src/index.ts` — No re-export changes; that is P09/P15 scope.
- `packages/cli/` — No CLI import changes; that is P14 scope.
- No `packages/providers/` directory created; that is P08 scope.

## Contract-to-Blocker Mapping

| P01 Blocker | Contract Created | Core Location | Pseudocode Ref |
|-------------|-----------------|---------------|----------------|
| Blocker 1 (HistoryService tokenizers) | RuntimeTokenizer, RuntimeTokenizerFactory | runtime/contracts/ | C-CB-01, lines 10-15 |
| Blocker 2 (ToolIdStrategy normalization) | Core-owned toolIdNormalization.ts | tools/toolIdNormalization.ts | C-CB-02, lines 20-24 |
| Blocker 3 (ProviderContentGenerator) | RuntimeContentGeneratorFactory | runtime/contracts/ | C-CB-03, lines 30-34 |
| Blocker 4 (Runtime provider errors) | MissingRuntimeProviderError | runtime/errors/ | C-CB-04, lines 40-44 |
| Blocker 5 (Config types) | RuntimeProviderManager, BucketFailureReason | runtime/contracts/ | C-CB-05, lines 50-54 |
| Blocker 6 (Model hydration) | RuntimeModel | runtime/contracts/ | C-CB-06, lines 60-63 |
| Blocker 7 (Telemetry types) | TelemetryContext | runtime/contracts/ | C-CB-07, lines 70-73 |
| Blocker 8 (Reasoning utilities) | ReasoningOutput | runtime/contracts/ | C-CB-08, lines 80-85 |
| Blocker 9 (Media utilities) | MediaBlockType, ClassifiedMediaBlock | runtime/contracts/ | C-CB-09, lines 80-85 |

## Verification Evidence

### Typecheck

```
npm run typecheck --workspace @vybestack/llxprt-code-core
> @vybestack/llxprt-code-core@0.10.0 typecheck
> tsc --noEmit
Exit Code: 0 (success)
```

### Plan Markers

```
rg -n "@plan:PLAN-20260603-ISSUE1584.P03" packages/core/src/runtime/contracts packages/core/src/runtime/errors packages/core/src/tools/toolIdNormalization.ts
30 matches across 14 files (every contract file has markers)
```

### Requirement Markers

```
rg -n "@requirement:REQ-DEP-001" packages/core/src/runtime/contracts packages/core/src/runtime/errors packages/core/src/tools/toolIdNormalization.ts
29 matches across 14 files
```

### No Provider Imports in Contract Files

```
rg -n "@vybestack/llxprt-code-providers|from ['\"].*/providers/" packages/core/src/runtime/contracts packages/core/src/runtime/errors --glob '*.ts'
Zero matches (exit code 1 — correct: no provider imports in core contracts)
```

### toolIdNormalization Test Passes

```
npx vitest run packages/core/src/tools/toolIdNormalization.test.ts
[OK] packages/core/src/tools/toolIdNormalization.test.ts (19 tests) 5ms
Test Files  1 passed (1)
Tests  19 passed (19)
```

### Forbidden Names Check

```
find packages/core/src -type f | rg '/(IProvider|IProviderManager|ProviderManager|ProviderContentGenerator)(V2|Compat|New|Copy)?\.ts$'
Only existing provider files (not V2/Compat/New/Copy variants) — zero forbidden compatibility-named files created
```

### No packages/providers/ Created

No `packages/providers/` directory was created. That is P08 scope.

## Semantic Assessment

### What Was Done

- Created **11 contract files** in `packages/core/src/runtime/contracts/` covering all 9 P01 blockers that require structural contracts.
- Created **1 error class** in `packages/core/src/runtime/errors/` for the core-owned missing-provider error (Blocker 4).
- Created **1 shared utility** at `packages/core/src/tools/toolIdNormalization.ts` for the core-owned tool ID normalization (Blocker 2).
- All new types are **type-only exports** (interfaces and type aliases) except `MissingRuntimeProviderError` which is a concrete error class needed for runtime throw/catch behavior.
- All contract files include `@plan:PLAN-20260603-ISSUE1584.P03` and `@requirement:REQ-DEP-001` markers.
- No contract file imports from `providers/` — verified by exhaustive ripgrep scan.
- Core `runtime/index.ts` re-exports all contracts and errors through barrel files.

### What Was NOT Done

- No consumer files (HistoryService, contentGenerator, configTypes, etc.) were updated to use the new contracts. That is P05/P07 scope (consumer migration).
- No `packages/providers/` package was created. That is P08 scope.
- No CLI imports were updated. That is P14 scope.
- No core `index.ts` re-export removal. That is P09/P15 scope.
- No provider files were moved from `packages/core/src/providers/`. That is P09–P11 scope.
- The existing `packages/core/src/providers/utils/toolIdNormalization.ts` was NOT deleted (consumers still import from it). Provider files will redirect to the core utility in P05/P07 when consumers are migrated.

### Consistency Check

- All 9 P01 blockers (Blockers 1–9) that require structural contracts have corresponding core-owned contract files.
- Blocker 10 (index.ts re-exports) and Blocker 11 (test-utils) do not require contract stubs — they require removal and isolation respectively, which are P09/P14/P15 scope.
- Contract names follow the anti-shim policy: no `V2`, `Compat`, `New`, `Copy` suffixes. Names use `Runtime` prefix for structural contracts owned by core.
- All contracts are under `packages/core/src/runtime/contracts/` or `packages/core/src/runtime/errors/` — not under `providers/` path.
- `toolIdNormalization.ts` is under `packages/core/src/tools/` — a non-provider core path per IC-05.

## Next Phase Gate

P04 (contract tests) may proceed when ready. Expected P04 tasks:
- Add behavioral/compile tests proving core contracts are usable without provider imports.
- Add compile tests for each contract verifying structural typing with provider shapes.