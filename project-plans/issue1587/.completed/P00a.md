# Phase 00a: Preflight Verification — Completed

## Checks Performed

### Dependency Check
- `@modelcontextprotocol/sdk@1.26.0` — used in core, cli, lsp, vscode-ide-companion
- `google-auth-library@9.15.1` — used in core (MCP auth providers)
- `packages/auth` and `packages/tools` do NOT exist (confirmed)

### Source Files Confirmed
All domain-model.md files exist:
- `packages/core/src/mcp/auth-provider.ts`
- `packages/core/src/mcp/file-token-store.ts` (+ test)
- `packages/core/src/mcp/google-auth-provider.ts` (+ test)
- `packages/core/src/mcp/oauth-provider.ts` (+ test)
- `packages/core/src/mcp/oauth-provider-utils.ts`
- `packages/core/src/mcp/oauth-token-storage.ts` (+ test)
- `packages/core/src/mcp/oauth-utils.ts` (+ test)
- `packages/core/src/mcp/sa-impersonation-provider.ts` (+ test)
- `packages/core/src/mcp/token-store.ts` (+ test)
- `packages/core/src/mcp/token-storage/` (6 files: base, file, hybrid, keychain + tests + types)
- `packages/core/src/tools/mcp-client.ts` (+ test)
- `packages/core/src/tools/mcp-client-manager.ts` (+ test)
- `packages/core/src/tools/mcp-tool.ts` (+ test)

### Consumer Touchpoints Confirmed
- `packages/core/src/config/config.ts` — imports McpClientManager
- `packages/core/src/config/configTypes.ts` — imports MCPOAuthConfig type
- `packages/core/src/config/configBaseCore.ts` — type import McpClientManager
- `packages/core/src/index.ts` — re-exports MCP symbols
- `packages/core/src/telemetry/types.ts` — imports DiscoveredMCPTool
- `packages/core/src/telemetry/loggers.test.ts` — imports DiscoveredMCPTool
- `packages/core/src/code_assist/oauth-credential-storage.ts` — imports from mcp/token-storage
- `packages/cli/src/commands/mcp/list.ts` — MCP status display
- `packages/cli/src/ui/commands/mcpCommand.ts` — MCP OAuth + restart
- `packages/cli/src/ui/commands/diagnosticsCommand.ts` — MCP token diagnostics
- `packages/a2a-server/src/agent/task.ts` — imports getAllMCPServerStatuses, MCPServerStatus
- `packages/a2a-server/src/types.ts` — imports MCPServerStatus type

### Package Conventions
- Providers package follows: tsconfig extends root, vitest.config.ts with source aliases, build via build_package.js
- Core package follows same pattern
- Workspace packages referenced as `file:../core`, etc.

### Plan Adjustments
- `packages/auth` and `packages/tools` do NOT exist. MCP package must import small types/utilities directly from core (no circular deps).
- `code_assist/oauth-credential-storage.ts` imports from `../mcp/token-storage/` — this is a core-internal consumer that must be updated when moving token-storage.
- The circular dependency risk is real: `mcp-client.ts` imports `../mcp/*` and `mcp/*` imports `../config/config.js`. The mcp package must avoid importing from config directly; instead, types needed by MCP should be defined locally in the mcp package or passed in.