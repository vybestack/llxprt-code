# Phase 07 Completion Marker: StreamProcessor neutralization

@plan:PLAN-20260707-AGENTNEUTRAL.P07
@requirement:REQ-002.1, REQ-002.6, REQ-005.3

## Status: COMPLETE

## Changes verified
- `_convertIContentStream`: uses `toModelStreamChunk(iContent)`, yields `AsyncGenerator<ModelStreamChunk>`
- `_processAfterModelHook`: uses `filterHookRestrictedBlocks`, `iContentFromBlocks`, neutral MODIFY branch via `afterModelModifiedToChunk`
- `processStreamResponse`: accepts/yields `ModelStreamChunk`, uses `accumulateModelStreamChunk`, `emptyModelOutput`
- `_finalizeStreamProcessing`: operates on `ModelOutput`, neutral block extraction
- BLOCK branch (C3): retains `GenerateContentResponse` transport until P13
- Before-model hook path (C3): untouched, still uses `_patchMissingFinishReason`
- `hookWireAdapter.ts` created with `afterModelModifiedToChunk`
- `filterHookRestrictedBlocks` exists in `hookToolRestrictions.ts`

## Verification
- P06 characterization tests: 6/6 PASS
- typecheck: PASS
- No `{candidates:}` literal in StreamProcessor
- `convertIContentToResponse` only in BLOCK branch (C3, expected)
- `iContentFromBlocks` used on after-model event path
