P20: Characterization tests for client contract surface — 8 tests all pass. History round-trip (clone + idle-wait), direct-message observable output, sendMessageStream event sequence. Observer helper abstracts Gemini-shape reads so P21 only updates the helper. 25% property-based.
