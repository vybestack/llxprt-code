P15 Complete: Structural-access sites migrated to ContentBlock[]. ConversationManager consolidation/thought on blocks, clientLlmUtilities next_speaker on TextBlock, streamResponseHelpers accumulation on blocks, MessageStreamOrchestrator pending-tool-call via ToolCallBlock, clientHelpers compress-split on blocks, client stripThoughts on blocks. isValidResponse DELETED. getHistory return-type stays Content[] until P21. All 30 P14 tests pass. typecheck+build green. AST gate count: 21 (down from 36).
