# P12: Direct-message characterization TDD — COMPLETED

## Summary
Created behavioral characterization tests for DirectMessageProcessor that pin OBSERVABLE current behavior (blocking BeforeModel + normal completion + after-model hook filtering). Tests pass against current code (returns GenerateContentResponse). Uses phase-local observer helper to avoid leaking Google internals into test files.

## Files Created
1. `packages/agents/src/core/__tests__/helpers/directMessageObservers.ts` — Observer helper with visibleText/committedHistory/usageCounts/eventSequence. Only neutral observables exposed.
2. `packages/agents/src/core/__tests__/directMessage.characterization.test.ts` — 10 tests (7 passing, 3 property-based):
   - Blocking BeforeModel: visible text equals block reason, committed history reflects request, property test for arbitrary reasons
   - Normal completion: visible text + usage populated, aggregates text across chunks, property test for arbitrary model text, property test for usage round-trip
   - After-model hook: modified text reflected, preserves text when no modification, property test for arbitrary filtered text

## Verification
- typecheck: PASS (0 errors)
- 10/10 tests pass
- No direct Google-internal indexing in test file (only in comments)
- ≥30% property-based tests (3/10 = 30%)