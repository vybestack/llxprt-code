# Phase 01 Completion Marker: Core mutation tooling provisioning

@plan:PLAN-20260707-AGENTNEUTRAL.P01
@requirement:REQ-012.4

## Summary
Provisioned Stryker mutation tooling for `packages/core` (tooling-only; NO production `src/**` changes).
Mirrors the structure of `packages/agents/stryker.conf.json`, scoped to the `llm-types` slice files.

## Verification Command Output

### Config + scripts present
```
=== Config + scripts present ===
core stryker config present
480:    "test:mutation": "stryker run stryker.conf.json",
582:    "@stryker-mutator/core": "^9.6.1",
583:    "@stryker-mutator/vitest-runner": "^9.6.1",
```

### Lockfile updated by `npm install` (from repo root)
```
$ git status --porcelain package-lock.json
 M package-lock.json
```

### Lockfile diff confined to stryker family (Additional Risk 2 — drift review)
The lockfile diff is exactly 2 added lines — the two `@stryker-mutator/*` devDeps in the
`packages/core` workspace `devDependencies` block. No other packages changed.

```
=== Non-stryker node_modules paths added ===
OK: lockfile changes confined to the stryker family

=== Lint/type/test toolchain drift check ===
OK: no lint/type/test toolchain drift
```

Full lockfile diff (only changed lines):
```
diff --git a/package-lock.json b/package-lock.json
--- a/package-lock.json
+++ b/package-lock.json
@@ -24048,6 +24048,8 @@
         "zod": "^3.25.76"
       },
       "devDependencies": {
+        "@stryker-mutator/core": "^9.6.1",
+        "@stryker-mutator/vitest-runner": "^9.6.1",
         "@types/debug": "^4.1.12",
```

### Dependency resolves
```
$ npm ls @stryker-mutator/core --prefix packages/core 2>/dev/null | head
@vybestack/llxprt-code-core@0.10.0 /Users/acoliver/projects/llxprt/branch-2/llxprt-code/packages/core
└── (empty)

$ npm ls @stryker-mutator/vitest-runner --prefix packages/core 2>/dev/null | head
@vybestack/llxprt-code-core@0.10.0 /Users/acoliver/projects/llxprt/branch-2/llxprt-code/packages/core
└── (empty)
```
Note: `npm ls` shows `(empty)` due to npm workspaces hoisting. The stryker packages resolve to the
root `node_modules/@stryker-mutator/` (verified: `@stryker-mutator/{api,core,instrumenter,util,vitest-runner}`
present at root). `npx stryker --version` from `packages/core` returns `9.6.1`, confirming resolution.

### End-to-end mutation run (`src/llm-types/modelEnvelope.ts`)
```
$ npm --prefix packages/core run test:mutation -- --mutate "src/llm-types/modelEnvelope.ts"

[Survived] LogicalOperator
src/llm-types/modelEnvelope.ts:99:7
-     if (acc.content.metadata || chunk.content.metadata) {
+     if (acc.content.metadata && chunk.content.metadata) {
Tests ran:
    accumulateModelStreamChunk concatenates blocks in order across 3 chunks
    accumulateModelStreamChunk usage on middle chunk then final chunk → final wins
    accumulateModelStreamChunk finishReason only on terminal chunk → present in result
  and 13 more tests!

[Survived] LogicalOperator
src/llm-types/modelEnvelope.ts:142:7
-     if (acc.providerMetadata || chunk.providerMetadata) {
+     if (acc.providerMetadata && chunk.providerMetadata) {

Ran 2.09 tests per mutant on average.
------------------|------------------|----------|-----------|------------|----------|----------|
                  | % Mutation score |          |           |            |          |          |
File              |  total | covered | # killed | # timeout | # survived | # no cov | # errors |
------------------|--------|---------|----------|-----------|------------|----------|----------|
All files         |  97.65 |   97.65 |       83 |         0 |        2 |        0 |        0 |
 modelEnvelope.ts |  97.65 |   97.65 |       83 |         0 |        2 |        0 |        0 |
------------------|--------|---------|----------|-----------|------------|----------|----------|
Final mutation score of 97.65 is greater than or equal to break threshold 80
JsonReporter Your report can be found at: .../packages/core/reports/mutation/mutation.json
Done in 1 minute and 0 seconds.
Exit Code: 0
```

### mutation.json emitted
```
$ test -f packages/core/reports/mutation/mutation.json && echo "mutation.json emitted"
mutation.json emitted
-rw-r--r--@ 1 acoliver  staff  809417 Jul  8 00:51 packages/core/reports/mutation/mutation.json
```

### No production src changes (Deferred Implementation Detection)
```
$ git status --porcelain packages/core/src | grep -vE '^\?\?'
no src changes (expected)
```

## Files Changed
- **Created:** `packages/core/stryker.conf.json` — Stryker config mirroring `packages/agents/stryker.conf.json`,
  scoped to `src/llm-types/{agentMessageInput.ts,modelEnvelope.ts}` (the P05 slice files; the sane-floor
  default that P05 overrides with explicit `--mutate`).
- **Modified:** `packages/core/package.json` — added `@stryker-mutator/core@^9.6.1`,
  `@stryker-mutator/vitest-runner@^9.6.1` devDeps and `"test:mutation": "stryker run stryker.conf.json"` script.
- **Modified:** `package-lock.json` (repo root) — 2 added lines (stryker devDeps in core workspace block).

## Success Criteria Checklist
- [x] `packages/core/stryker.conf.json` exists mirroring the agents config structure, scoped to `llm-types`.
- [x] `packages/core/package.json` has `@stryker-mutator/core` + `@stryker-mutator/vitest-runner` devDeps (pinned `^9.6.1`) and `test:mutation` script.
- [x] Root `package-lock.json` updated by `npm install` (from repo root) and is part of the diff.
- [x] Lockfile diff confined to `@stryker-mutator/*` family — no eslint/typescript/typescript-eslint/vitest/prettier drift.
- [x] `npm --prefix packages/core run test:mutation -- --mutate "src/llm-types/modelEnvelope.ts"` runs to completion and writes `reports/mutation/mutation.json` (97.65% score, exit 0).
- [x] NO production `packages/core/src/**` code modified (tooling-only).
- [x] No lint/complexity loosening; no suppression directives.

## Constraints Honored
- NO changes to `packages/core/src/**`.
- NO lint/complexity rule loosening, NO suppression directives.
- Root `package-lock.json` is part of the diff.
- Used `npm install` (NOT bun) per project conventions.
