P17 Complete: Runtime Type enum → JSON-schema string literals, FinishReason → CanonicalFinishReason, ApiError → isProviderApiError. googlePartHelpers.ts RENAMED to contentBlockHelpers.ts with zero @google/genai imports. All import specifiers updated. All 40 P16 tests pass. Build+typecheck green.
