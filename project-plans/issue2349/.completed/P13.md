# Phase 13 Complete — DirectMessageProcessor neutralization

## Phase ID
`PLAN-20260707-AGENTNEUTRAL.P13`

## Completion Summary

- `sendMessage`/`generateDirectMessage`/`generateContent` return `Promise<ModelOutput>` (not `GenerateContentResponse`)
- `AgentExecutionBlockedError.syntheticResponse` → `blockedOutput?: ModelOutput` (chatSession.ts)
- `MessageConverter.convertIContentToResponse` + `applyResponseMetadata` + `applyFinishReasonMapping` DELETED (synthetic fabricator chain)
- `providerStopReason` WRITER (`setProviderStopReason`) removed; FILE survives until P25 (reader in streamChunkWrapper.ts)
- `beforeModelBlockingCompat.ts` DELETED
- `beforeModelHookDecision.ts` fully neutralized (no `GenerateContentResponse`/`FinishReason`/`PatchFinishReasonFn`)
- `streamRequestHelpers.patchMissingFinishReason` DELETED
- `StreamProcessor._patchMissingFinishReason` DELETED
- `DirectMessageProcessor` fully rewritten: zero `@google/genai` imports, returns `ModelOutput`, block-based text extraction, neutral hook filtering
- `hookWireAdapter.ts` extended with P13 exports: `afterModelModifiedToModelOutput`, `beforeModelBlockingToModelOutput`, `afterModelBlockingToModelOutput`
- All three BLOCK writers (streaming AfterModel, before-model, direct) pass neutral `blockedOutput` payload
- `TurnProcessor` reader consumes `error.blockedOutput` (not `error.syntheticResponse`)
- `isValidResponse` KEPT (survives until P15, last caller `streamResponseHelpers.ts:109`)
- `streamChunkWrapper.ts` + `providerStopReason.ts` FILES survive (P25 delete — C2)
- `turnLogging.ts` retyped to accept `IContent[]` + `UsageStats`
- `summarizer.ts` uses `getResponseTextFromBlocks` instead of `getResponseText`
- CLI `autoPromptGenerator.ts` + `usePromptCompletion.ts` updated for `ModelOutput` shape
- AST gate baseline: 36 (P11) → 24 (P13)
- Type-level API shape test: `turnProcessor.sendMessage.apiShape.test.ts`

## Verification Results

- `npm run typecheck`: CLEAN (0 errors)
- `npm run test --workspace packages/agents`: 2729/2729 PASS (250 files)
- `npm run test --workspace packages/core`: 5022/5022 PASS (274 files)
- `npm run test --workspace packages/cli`: 5130/5130 PASS (434 files)
- `npm run build`: PASS
- `npm run format`: PASS
- Smoke haiku: pre-existing 404 on clean HEAD (not caused by our changes)
- AST gate `--count`: 24 (down from 36)
- Grep checks: all deleted functions confirmed absent from production