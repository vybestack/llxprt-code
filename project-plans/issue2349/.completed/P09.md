# P09: MessageConverter neutralization — COMPLETED

## Changes Made

### MessageConverter.ts
- Removed `createUserContent` (value) import from `@google/genai`
- Added `iContentFromLegacyInput` import from core `llm-types/index.js`
- Retyped `createUserContentWithFunctionResponseFix`: now returns `IContent` (was `Content`)
  - Builds `IContent{speaker:'human', blocks}` via `iContentFromLegacyInput` instead of `{role:'user',parts}`
  - Handles LegacyConversionResult properly (ok → merge blocks, err → fallback text)
- Retyped `normalizeToolInteractionInput`: now returns `IContent` (was `Content | Content[]`)
  - Routes all input shapes through `createUserContentWithFunctionResponseFix`
- Removed dead `pushMixedContentParts` function (no longer called)
- Fabricator chain (`convertIContentToResponse`, `applyResponseMetadata`, `applyFinishReasonMapping`, `isValidResponse`) KEPT intact (quarantined, deleted at P13)

### TurnProcessor.ts
- `_normalizeUserContent` return type: `Content | Content[]` → `IContent`
- `_convertToIContents` param type: `Content | Content[]` → `IContent`
  - Stamps neutral IContent directly (no Gemini→neutral conversion needed)
- `_prepareSendMessage` return type: `Content | Content[]` → `IContent`
- `_createStreamGenerator` / `_runStreamAttempt` / `_executeSendWithRetry` param: `Content | Content[]` → `IContent`
- `_commitSendResult` param: `Content | Content[]` → `IContent`

### DirectMessageProcessor.ts
- `_convertUserInput` simplifies: normalizeToolInteractionInput returns IContent directly, stamp with id
- `makePositionMatcher` renamed to `_makePositionMatcher` (now unused, will be cleaned in P15)

### StreamProcessor.ts
- `makeApiCallAndProcessStream` / `_createCancellableStream` / `_buildAndSendStreamRequest` / `processStreamResponse` / `_buildRequestContents` / `_validateStreamCompletion` param: `Content | Content[]` → `IContent`
- `_validateStreamCompletion`: tool-continuation detection now uses `userInput.blocks.some(b => b.type === 'tool_response')` instead of `.parts?.some(p => p.functionResponse)`
- `recordHistory` call passes neutral IContent directly (ConversationManager handles conversion internally)

### streamRequestHelpers.ts
- `buildRequestContentsResult` param: `Content | Content[]` → `IContent`
  - No longer needs `makePositionMatcher` or `ContentConverters.toIContent` (input is already IContent)

### ConversationManager.ts
- `recordHistory` accepts `IContent | Content | Content[]` (bridge)
  - Detects neutral IContent via `'speaker' in userInput && 'blocks' in userInput`
  - Converts to Gemini Content for legacy internal paths (will be fully retyped at P15)

## Verification
- `npm run typecheck`: PASS (zero errors)
- 6/6 characterization tests PASS
- AST gate count: 37 → 38 (added one `G-call-toGeminiContent` at StreamProcessor:1004 for the `recordHistory` bridge — this is expected and will be eliminated at P15 when ConversationManager is fully retyped)
- `createUserContent` value import: GONE from MessageConverter.ts
- `normalizeToolInteractionInput` now returns neutral IContent

## Build-order invariant (C3) preserved
- `convertIContentToResponse` chain KEPT (callers in DirectMessageProcessor/chatSession/client/streamResponseHelpers still exist until P13/P15)
- `FinishReason` value import KEPT (needed by `applyFinishReasonMapping` until P13)
- `providerStopReason` writer KEPT (inside `applyFinishReasonMapping`, deleted at P13)
