# P08: TurnProcessor wrap + Turn consume ContentBlock[] — COMPLETED

## Changes Made

### turn.ts (MAJOR REWRITE)
- **Removed all `@google/genai` imports** (`PartListUnion`, `FunctionCall`)
- Removed `getFunctionCallsFromParts`, `chunkToParts`, hook restriction helper imports
- Added `TurnRequest` type (`string | object | readonly unknown[]`) to replace `PartListUnion`
- `processStreamChunk` now operates on `ContentBlock[]`/`ToolCallBlock[]` directly
- `handlePendingFunctionCall` takes `ToolCallBlock` instead of `FunctionCall`
- `createSyntheticFunctionCallId` takes `ToolCallBlock` instead of `FunctionCall`
- `EmitFinishReasonOptions.functionCalls` is now `ToolCallBlock[]`
- `openResponseStreamIterator` uses minimal bridge cast at chatSession boundary (vanishes at P21)
- `fnCall.parameters` handled with runtime type guard (no unguarded `as`)

### TurnProcessor.ts
- `wrapChunk` now takes `ModelStreamChunk` directly (was `GenerateContentResponse`)
- Removed `responseToModelStreamChunk(resp)` call for normal streaming path
- Blocking error path (C3): `error.syntheticResponse` → `responseToModelStreamChunk()` → `wrapChunk()` (deferred to P13)
- `ModelStreamChunk` imported from core `llm-types/index.js`

### Build-order constraints preserved
- C2: `streamChunkWrapper.ts` NOT deleted (has P23/P25 consumers)
- C3: `sendMessage` return type stays `GenerateContentResponse` until P13
- C3: `AgentExecutionBlockedError.syntheticResponse` stays `GenerateContentResponse` until P13

## Verification
- `npm run typecheck`: PASS (zero errors)
- 6/6 characterization tests PASS
- AST gate count: 38 → 37 (decreased)
- `--enforce-imports --files turn.ts`: PASS (exit 0)
- No `@google/genai` imports in `turn.ts`
- No `responseToModelStreamChunk` usage in normal streaming path (only C3 blocking path)
