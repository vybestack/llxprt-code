# P11: Side-channel retirement — COMPLETED

## Summary
Neutralized `hookToolRestrictions.ts` to use neutral `ModelStreamChunk`/`ContentBlock[]`/`HookRestrictions` types. Created `hookRestrictionsLegacyCompat.ts` as a temporary bridge for not-yet-migrated consumers (deleted in P25). Created `beforeModelBlockingCompat.ts` for the pre-P13 BeforeModel blocking path (deleted in P13).

## Changes
1. **hookToolRestrictions.ts** — FULLY NEUTRALIZED. Zero `@google/genai` imports, zero WeakMaps, zero Symbols, zero GenerateContentResponse. Uses `ModelStreamChunk`/`HookRestrictions`/`ContentBlock[]`/`ToolCallBlock`.
2. **hookRestrictionsLegacyCompat.ts** — NEW. Temporary bridge holding old WeakMap/Part[]/FunctionCall helpers for files not yet migrated (streamChunkWrapper, streamResponseHelpers, subagent*, executor*). Deleted in P25.
3. **beforeModelBlockingCompat.ts** — NEW. Temporary BeforeModel blocking compat. Deleted in P13.
4. **All consumer imports re-pointed**: StreamProcessor, TurnProcessor, streamChunkWrapper, streamResponseHelpers, DirectMessageProcessor, subagent*, executor*, test files → either neutral hookToolRestrictions or hookRestrictionsLegacyCompat.
5. **AST gate allow-list** — Added entries for beforeModelBlockingCompat.ts and hookRestrictionsLegacyCompat.ts.
6. **StreamProcessor.yieldAsYouGo.test.ts** — Updated to use neutral ModelStreamChunk types instead of GenerateContentResponse.

## Verification
- typecheck: PASS (0 errors)
- 63 tests pass (stream-pipeline, sideChannel, turn.tool-restrictions, subagentToolProcessing, yieldAsYouGo)
- AST gate count: 36 (down from 37)
- hookToolRestrictions.ts: zero @google/genai/WeakMap/Symbol/GenerateContentResponse (only in comments)