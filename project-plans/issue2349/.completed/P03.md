# Phase 03: Neutral gap types — STUB — COMPLETE

## Phase ID
`PLAN-20260707-AGENTNEUTRAL.P03`

## Files Created
- `packages/core/src/llm-types/agentMessageInput.ts` (98 lines incl. license header)
  - `type AgentMessageInput` (string | ContentBlock[] | IContent | IContent[]) — REQ-001.1
  - `iContentFromAgentMessageInput(input, speaker?)` → `{ speaker, blocks: [] }` non-throwing stub — REQ-001.1
  - `iContentFromLegacyInput(input)` → `{ ok: false, error: 'stub' }` non-throwing stub — REQ-001.1
  - `iContentFromBlocks(blocks, speaker?)` → `{ speaker, blocks: [] }` non-throwing stub — REQ-001.2 (C4)
  - `sendParamsToRequest(message, settings?)` → `{ contents: [], settings }` non-throwing stub — REQ-001.3
  - Re-exports `ModelGenerationRequest` / `ModelGenerationSettings` from `modelRequest.ts`

## Files Modified
- `packages/core/src/llm-types/modelEnvelope.ts`
  - Added `afcHistory?: IContent[]` to `ModelOutput` interface (REQ-001.4, pseudocode line 50)
  - Doc comment is provider-agnostic (no @google/genai, Gemini, or Google-specific terms)
  - `toModelStreamChunk` left unchanged (extended in P05)
- `packages/core/src/llm-types/index.ts`
  - Added `export * from './agentMessageInput.js'`

## Verification Results
- `npm run typecheck` — PASS (all 15 workspaces, exit 0)
- `grep -r "@plan:PLAN-20260707-AGENTNEUTRAL.P03" packages/core/src/llm-types | wc -l` → 7 (>0) ✓
- No `NotYetImplemented` throws in source ✓
- No `NotYetImplemented` assertions in test files (no reverse tests) ✓
- No TODO comments ✓
- All stubs return non-throwing empty correctly-typed values ✓
