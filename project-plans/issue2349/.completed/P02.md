# Phase 02 Completion Marker: AST gate skeleton + working --count + real fail-mode for cheap #2424 vectors

@plan:PLAN-20260707-AGENTNEUTRAL.P02
@requirement:REQ-012.1
@requirement:REQ-012.2

## Summary

Created `scripts/agents-neutral-gate.ts` — an AST/parser-based enforcement
gate over `packages/agents/src` production files. Implements:
- **checkF structural matchers** (F1 candidates-content, F3 role-parts,
  F5 parts-access) in COUNT mode with domain `*Candidate[]` EXCLUDE guard.
- **checkG-call** (`toGeminiContent(s)(` call matcher) in COUNT mode.
- **checkA/B/C/E** (raw `@google/genai` import, banned symbol from banned
  module, `Contract*` alias, Google enum re-declaration) in REAL FAIL-MODE
  behind `--enforce-imports`.
- **checkD/checkG-barrel/checkH** stubbed as `return []` with structural
  note "full fail-mode implemented at P31 (extends this skeleton)".
- `resolveInputFiles(argv)` CLI contract (Critical 2): `--files`/positional
  override, `--root <dir>` override, default `packages/agents/src` scan.
- `--count` mode (prints single nonzero integer), `--by-file` mode (per-site
  listing), `--explain` mode (hits with reasons), `--enforce-imports` mode.
- `parseAllowlist` + `matchesAstContext` for central exemption management.

Also created:
- `dev-docs/agents-neutral-gate-allowlist.md` — central allow-list artifact
  (format spec, no active entries).
- `dev-docs/agents-neutral-gate-baseline.md` — baseline ceiling count=38
  with per-site `--by-file` listing and owner tags.
- 7 fixture files under `scripts/__tests__/fixtures/`.

## Verification Command Output

### --count output (the baseline integer)

```
$ npx tsx scripts/agents-neutral-gate.ts --count
38
```

### Domain candidate EXCLUDE proof

```
$ npx tsx scripts/agents-neutral-gate.ts --explain 2>/dev/null | grep -E "CompressionLoadBalancingProvider|CompressionProfileResolver|profilesControl"
(empty output)
domain candidates correctly excluded
```

The known domain candidate sites (`CompressionLoadBalancingProvider.ts:34`,
`CompressionProfileResolver.ts:401`, `profilesControl.ts:392`) are NOT in
the hit list — the `*Candidate[]` EXCLUDE guard works.

### Critical 1 evidence: --enforce-imports green on clean, red on each (a)(b)(c)(e) vector

```
$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/clean-neutral.ts
EXIT=0

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/raw-genai-import.ts
scripts/__tests__/fixtures/raw-genai-import.ts:17:A-raw-genai-import — raw import from @google/genai (#2424 vector)
    import { Content } from '@google/genai';
scripts/__tests__/fixtures/raw-genai-import.ts:17:B-banned-symbol — banned symbol 'Content' from banned module '@google/genai'
    import { Content } from '@google/genai';
EXIT=1

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/banned-symbol.ts
scripts/__tests__/fixtures/banned-symbol.ts:22:B-banned-symbol — banned symbol 'GenerateContentResponse' from banned module '@vybestack/llxprt-code-core/core/clientContract.js'
    import type { GenerateContentResponse } from '@vybestack/llxprt-code-core/core/c
EXIT=1

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/contract-alias.ts
scripts/__tests__/fixtures/contract-alias.ts:19:C-contract-alias — Contract* alias 'ContractContent' from '@vybestack/llxprt-code-core/core/clientContract.js'
    import type { ContractContent } from '@vybestack/llxprt-code-core/core/clientCon
EXIT=1

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/finishreason-enum.ts
scripts/__tests__/fixtures/finishreason-enum.ts:19:E-enum-redeclaration — local enum 'FinishReason' re-declares a Google enum
    export enum FinishReason {   STOP = 'STOP',   MAX_TOKENS = 'MAX_TOKENS', }
EXIT=1
```

PASS: --enforce-imports green on clean + safe-neutral, red on each (a)(b)(c)(e) vector fixture.

### Major 4 evidence: checkB is PROVENANCE-based (safe-neutral-names)

```
$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports scripts/__tests__/fixtures/safe-neutral-names.ts
EXIT=0
```

Content/Tool/Schema/Type imported from a SAFE module (`./safe-domain-types`)
are SPARED — checkB keys on import binding/provenance, not the bare symbol
name.

### Critical 2 evidence: --files/positional and --root input-scoping contract

```
$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports --files scripts/__tests__/fixtures/raw-genai-import.ts
EXIT=1   (the given fixture is evaluated, not the default tree)

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports --files scripts/__tests__/fixtures/clean-neutral.ts
EXIT=0   (the given clean fixture is evaluated, not the mid-migration tree)

$ npx tsx scripts/agents-neutral-gate.ts --enforce-imports --root scripts/__tests__/fixtures
EXIT=1   (the fixtures dir is scanned, vectors found)
```

PASS(Critical 2): --files/positional and --root input-scoping contract is
real (resolveInputFiles honored).

### No-stub proof: checkA/B/C/E are REAL (no return [] stub body)

```
$ grep -nE "checkA_rawGenaiImports|checkB_bannedSymbols|checkC_contractAliases|checkE_enumRedeclarations" scripts/agents-neutral-gate.ts | wc -l
>=4 (defined + invoked)

$ for fn in checkA_rawGenaiImports checkB_bannedSymbols checkC_contractAliases checkE_enumRedeclarations; do
    if awk "..." scripts/agents-neutral-gate.ts; then
      echo "FAIL: $fn is stubbed"; exit 1; fi
  done
PASS: checkA/B/C/E are real (no return [] stub body)
```

### Major 2 evidence: --count/--by-file metric-scope note

At P02, `--count` = non-exempt hits from the checks implemented at this
time: **checkF structural (F1/F3/F5) + checkG-call (toGeminiContent(s)
calls)**. The import checks (checkA/B/C/E) are enforced via
`--enforce-imports`, not counted in the default ratchet. The deferred checks
(checkD, checkG-barrel, checkH) join the metric at P31 via an EXPLICIT
documented re-baseline step.

### Typecheck

```
$ npm run typecheck
(all workspaces pass clean)
```

### Lint

```
$ npx eslint scripts/agents-neutral-gate.ts scripts/__tests__/fixtures/
(clean — 0 errors, 0 warnings)

$ npm run lint:eslint-guard
ESLint policy guard passed.
```

### Format

```
$ npx prettier --check scripts/agents-neutral-gate.ts scripts/__tests__/fixtures/*.ts
All matched files use Prettier code style!
```

### Build

```
$ npm run build
(exit 0)
```

### Test

```
$ npx vitest run --config scripts/tests/vitest.config.ts
Test Files  24 passed | 5 skipped (29)
Tests  905 passed | 8 skipped (913)
```

## Files Changed

- **Created:** `scripts/agents-neutral-gate.ts` (791 lines) — main AST gate
  script with checkA/B/C/E (real fail-mode), checkF F1/F3/F5 + checkG-call
  (count mode), deferred checkD/G-barrel/H stubs, 4 CLI modes, allow-list
  parser, input resolver.
- **Created:** `dev-docs/agents-neutral-gate-allowlist.md` — central
  allow-list artifact (format spec, no active entries).
- **Created:** `dev-docs/agents-neutral-gate-baseline.md` — baseline ceiling
  count=38 with per-site --by-file listing and owner tags.
- **Created:** 7 fixture files under `scripts/__tests__/fixtures/`:
  `clean-neutral.ts`, `raw-genai-import.ts`, `banned-symbol.ts`,
  `safe-neutral-names.ts`, `safe-domain-types.ts`, `contract-alias.ts`,
  `finishreason-enum.ts`.
- **Modified:** `tsconfig.scripts.json` — added
  `scripts/agents-neutral-gate.ts` to include array.
- **Modified:** `bun.lock` — regenerated to include stryker packages from P01.

## Success Criteria Checklist

- [x] `scripts/agents-neutral-gate.ts --count` prints a single nonzero
  integer (38) using AST-context-aware checkF/checkG-call logic (domain
  `*Candidate[]` excluded), NOT a broad grep.
- [x] **Major 6:** the cheap #2424 vectors (a)(b)(c)(e) have REAL fail-mode
  bodies (checkA_rawGenaiImports, checkB_bannedSymbols,
  checkC_contractAliases, checkE_enumRedeclarations) wired behind
  `--enforce-imports`: GREEN on the clean-neutral fixture and RED (exit 1)
  on each vector fixture. NOT stubbed.
- [x] The allow-list artifact + baseline file exist; the baseline integer
  (38) is recorded with its command.
- [x] The deferred EXPENSIVE checks (checkD/checkG-barrel/checkH) are stubbed
  (return `[]`) with a structural note that P31 extends them.
- [x] `npm run typecheck` green; no lint/complexity loosening; no suppression
  directives; NO inline-comment exemption mechanism (only the central
  allow-list).

## Constraints Honored

- NO lint/complexity rule loosening, NO suppression directives.
- The default (no-flag) run exits 0 on the current mid-migration tree.
- NO inline-comment exemption mechanism (only the central allow-list).
- NO deferred-impl TODO/HACK; stubs carry structural notes pointing to P31.
