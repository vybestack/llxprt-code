# P27 — Remaining Group Retypes (ZERO prod @google/genai imports)

## Status: COMPLETE

## Summary
Migrated ALL remaining ~30 production files in `packages/agents/src/` from Google
Gemini types (@google/genai Content/Part/PartListUnion/FunctionCall/FunctionDeclaration/
GenerateContentConfig/Tool/SendMessageParameters/GenerateContentResponse) to neutral
types (IContent/ContentBlock/ToolCallBlock/ToolResponseBlock/AgentMessageInput/
ToolDeclaration/ChatSessionConfig/ModelOutput/ModelStreamChunk).

## Key Changes
- Removed `@google/genai` imports from ~30 production files
- `CompressionHandler.ts`, `compressionBudgeting.ts`, `providerContentEnforcement.ts`:
  `GenerateContentConfig` → `ChatSessionConfig`
- `AgenticLoop.ts`, `loopHelpers.ts`, `agenticLoop/types.ts`: `PartListUnion` →
  `AgentMessageInput`, `{role:'user',parts}` literals → `iContentFromBlocks`
- `api/agent.ts`, `agentBootstrap.ts`, `control/sessionControl.ts`: `Content` → `IContent`
- `ChatSessionFactory.ts`, `chatSession.ts`, `client.ts`: all Google types removed
- `clientHelpers.ts`, `clientLlmUtilities.ts`, `clientToolGovernance.ts`: neutral types
- `ConversationManager.ts`, `MessageStreamOrchestrator.ts`: `Content` → `IContent`
- `StreamProcessor.ts`, `TurnProcessor.ts`, `DirectMessageProcessor.ts`: remaining types
- `MessageConverter.ts`: removed `FinishReason`, `createUserContent`, `Part` imports
- `baseLlmClient.ts`, `streamCleanup.ts`, `streamResponseHelpers.ts`: neutral
- `TodoContinuationService.ts`, `turnAbortHelpers.ts`, `MessageStreamTerminalHandler.ts`: neutral
- `subagentNonInteractive.ts`, `subagentRuntimeSetup.ts`: neutral

## Fixes During P27
- `streamRequestHelpers.ts`: `PrepareRequestPayloadParams.tools` → `unknown` type to
  accommodate both `ToolDeclaration[]` and `ToolGroupArray`
- `clientHelpers.ts`: Added legacy Part fallback in `charLengthForBlock` for `{text:...}`
  shapes without a `type` field
- `MessageConverter.ts`: `convertPartListUnionToIContent` now sets `speaker: 'tool'`
  when all blocks are tool responses
- `sessionControl.ts`: Removed double `ContentConverters.toIContents` conversion in
  `restoreCheckpoint` (history is already `IContent[]` post-P21)
- `session.spec.ts`: `textMessage` now produces neutral `IContent` shape

## Verification
- Typecheck: 0 errors (excluding a2a-server)
- Tests: 263 files, 2921 tests, ALL PASS
- Lint: clean on all modified production files
- AST Gate count: 1 (only G3 bounded hook-wire adapter at streamRequestHelpers.ts:196)
- Build: passes

## Gate Baseline
count=1 owner=P27

@plan:PLAN-20260707-AGENTNEUTRAL.P27
@requirement:REQ-005.5c
