# Phase Completion: P04A Consumer Migration Verification

Plan: PLAN-20260610-ISSUE1592
Phase: P04A
Completed: 2026-06-11
Verdict: PASS

## Full Battery + Smoke

```text
npm run format
PASS — Prettier completed across the workspace.

git diff --check
PASS — no whitespace errors.

npm run typecheck
PASS — all workspaces typechecked.

npm run build
PASS — all workspaces built, including core, agents, CLI, and a2a-server.

npm run test
PASS — full workspace test suite passed.

npm run lint
PASS — exit 0 with existing warnings only (1228 warnings, 0 errors).

node scripts/check-lockfile.js
PASS — Lockfile check passed.

npm run bundle
PASS — bundle completed.

node bundle/llxprt.js --version
PASS — printed 0.10.0.

node scripts/start.js --profile-load ollamaglm51 "write me a haiku and nothing else"
PASS — exit 0 with haiku output.

node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
PASS — exit 0; synthetic profile printed the existing OpenAI API error report line but did not fail the command.
```

## Consumer Audit Spot Check

P04 generated a 441-hit inventory over CLI/a2a source and tests. P04A spot-checked these files against the audit dispositions:

| File | Disposition verified |
|---|---|
| `packages/cli/src/config/configBuilder.ts` | imports `AgentClient`, `CoreToolScheduler`, and `createTaskToolRegistration` from agents and injects all three into core config construction. |
| `packages/cli/src/runtime/runtimeContextFactory.ts` | imports concrete agents runtime factories and TaskTool registration for runtime context config creation. |
| `packages/cli/src/ui/utils/autoPromptGenerator.ts` | imports concrete `AgentClient` from agents for detached prompt generation. |
| `packages/cli/src/nonInteractiveCliSupport.ts` | imports concrete `executeToolCall` from agents. |
| `packages/cli/src/ui/hooks/useReactToolScheduler.ts` | uses agents-owned concrete `CoreToolScheduler` type for UI scheduler instance handling. |
| `packages/cli/src/integration-tests/test-utils.ts` | wires agents-owned `AgentClient`, `CoreToolScheduler`, and `createTaskToolRegistration` into integration test configs. |
| `packages/cli/src/ui/hooks/geminiStream/__tests__/toolCompletionHandler.test.ts` | structural `AgentClientContract` import is from explicit core contract subpath, not core root barrel. |
| `packages/cli/src/ui/commands/compressCommand.test.ts` | structural contract and turn compression enums are explicit core subpath imports. |
| `packages/a2a-server/src/config/config.ts` | imports agents-owned concrete factories and TaskTool registration. |
| `packages/a2a-server/src/agent/task.ts` | imports concrete `AgentClient` from agents for task execution. |

## Workspace-Leakage Gate

Generated inventory command:

```bash
grep -rhoE "(from|import\(|require\(|vi\.mock\()\s*['\"][^.'\"][^'\"]*['\"]" packages/agents --include="*.ts" --include="*.tsx" | grep -oE "['\"][^'\"]+['\"]" | sort -u
```

Results show workspace package usage limited to:

- `@vybestack/llxprt-code-core` subpaths — direct production dependency.
- `@vybestack/llxprt-code-auth` — direct production dependency.
- `@vybestack/llxprt-code-settings` — direct production dependency.
- `@vybestack/llxprt-code-test-utils` — dev/test-only alias and dependency.

No `@vybestack/llxprt-code-providers` or root CLI package import is present in `packages/agents`. `packages/agents/package.json` declares no providers/CLI dependency in dependencies, devDependencies, peerDependencies, or optionalDependencies.

Root-core specifier note: the inventory includes `@vybestack/llxprt-code-core` because tsconfig/vitest aliases need to resolve the package root for tooling. A source search finds no `packages/agents/src` root-barrel import or `@vybestack/llxprt-code-core/index.js`; runtime source imports use explicit core subpaths.

## No Test Weakening

```text
git diff main -- '**/*.test.ts' '**/*.spec.ts' | grep -E "^-.*expect"

Output only removed import-list lines containing the word expect:
-import { describe, it, expect, beforeEach } from 'vitest';
-  expect,
```

No assertion bodies were weakened or deleted by P04/P04A.

## Behavioral Regression Checklist

| Invariant | Passing coverage in new layout |
|---|---|
| #897 subagent provider/runtime isolation | agents subagent runtime tests, including `subagentRuntimeSetup.*.test.ts` and `subagentExecution.test.ts`, run under `@vybestack/llxprt-code-agents`. |
| #898/#905 duplicate-output prevention | `packages/agents/src/core/subagentToolProcessing.test.ts` and subagent execution tests run under agents. |
| #1060 single shared scheduler | `packages/agents/src/core/coreToolScheduler.duplication.test.ts` and P01/P03 config inversion tests verify shared injected scheduler behavior. |
| #1373 subagent self-identification | agents subagent execution/orchestration tests remain with the moved implementation. |
| Compression behavior | agents compression strategy tests plus core continuation helper import coverage passed in full test suite. |
| Retry/failover behavior | moved stream/failover tests, including `StreamProcessor.*failover*.test.ts`, passed in agents full workspace test run. |

## TaskTool Wiring Proof

```text
grep -rn "taskToolRegistration" packages/cli/src packages/a2a-server/src --include="*.ts" | grep -v test
packages/cli/src/config/configBuilder.ts:335:    taskToolRegistration: createTaskToolRegistration(),
packages/cli/src/runtime/runtimeContextFactory.ts:246:      taskToolRegistration: createTaskToolRegistration(),
packages/a2a-server/src/config/config.ts:111:    taskToolRegistration: createTaskToolRegistration(),
```

REQ-INV-003.3 coverage is in `packages/core/src/config/config.agentInversion.test.ts`: registered TaskTool record parity, missing manager/registration diagnostic, coreTools allow-list, and excludeTools behavior.

## Holistic Assessment

P04A confirms the consumer side of the extraction is integrated rather than isolated. CLI and a2a composition roots own the concrete runtime package imports and pass concrete factories into core. Core callers and tests use structural contracts from explicit core subpaths. The bundle resolves the agents workspace, boundary scripts scan agents source, and no stale moved implementation import or mock path remains in CLI/a2a tests.

## Verdict

PASS. Consumer migration is verified and ready for final cleanup/review phases.
