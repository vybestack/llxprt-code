# Phase Completion: P02 Package Scaffold

Plan: PLAN-20260610-ISSUE1592
Phase: P02
Completed: 2026-06-11
Verdict: PASS

## Files Created

- `packages/agents/package.json` — new workspace package metadata for `@vybestack/llxprt-code-agents` version `0.10.0`.
- `packages/agents/index.ts` — package root entrypoint exporting `./src/index.js`.
- `packages/agents/src/index.ts` — intentionally empty placeholder (`export {};`) until the atomic P03 move.
- `packages/agents/tsconfig.json` — TypeScript config using the root package entry and local/core/auth/settings/test-utils path aliases.
- `packages/agents/vitest.config.ts` — Vitest config with `passWithNoTests: true` for the P02 scaffold phase.

## Files Modified

- `package.json` — added `packages/agents` workspace after `packages/providers` and before `packages/cli`.
- `package-lock.json` — regenerated with the agents workspace entry using `npm install --package-lock-only`.
- `packages/core/package.json` — added core-owned subpath exports needed by future moved agents files for staying shared modules and contracts.
- `.github/workflows/release.yml` — added agents package publish and sandbox tarball preparation.
- `.github/workflows/build-sandbox.yml` — added agents package tarball packing.
- `scripts/build_sandbox.js` — packs and chmods the agents tarball.
- `scripts/version.js` — added agents package to release workspace list.
- `Dockerfile` — copies and globally installs the agents tarball with the other local package tarballs.
- `scripts/tests/release-process.test.js` — updated release/sandbox/Docker/release-dependency assertions to include agents.

## Package Decisions and Audits

- `packages/cli/package.json` and `packages/a2a-server/package.json` intentionally do NOT depend on agents in P02; P03 adds those dependencies atomically with the first imports.
- `packages/providers` intentionally does NOT depend on agents in P02, despite the provisional moved-test inventory showing a provider deep import. P03 owns rewriting/relocating that test without creating a providers↔agents dependency.
- `scripts/prepare-package.js` was audited. It only handles core/CLI README/LICENSE/.npmrc copying; providers also has no package-specific entry, so no agents-specific change was required in P02.
- `esbuild.config.js` was audited. It wires CLI/a2a bundling and unrelated externals, with no providers package-specific wiring to mirror for agents in P02.
- `.npmrc` / publish config was audited. Root registry/auth configuration covers the scoped agents package; no package-specific `.npmrc` is required.
- `scripts/check-settings-boundary.js` was audited. It hard-codes scan paths including current core agent/runtime paths and must be updated after the P03/P04 moves when `packages/agents/src` contains production code. No P02 change was made because the scaffold contains no moved source yet.
- `packages/agents/package.json` intentionally contains only the root `.` export in P02. Deep exports are added in P03 when real moved files establish public subpaths, matching the no-fake-API-skeleton rule.
- `packages/agents/tsconfig.json` intentionally does not include sibling package source trees. Path aliases are available for later source resolution, but the P02 build artifact contains only agents package files.

## Verification Commands

### Targeted package scaffold

```text
npm run typecheck -w @vybestack/llxprt-code-agents
## Provisional Dependency Inventory

P02 scaffold dependencies are intentionally provisional and will be reconciled against the post-move import inventory in P03/P03A.

| Package | Section | Reason | Boundary Status |
| --- | --- | --- | --- |
| `@google/genai` | dependencies | Moved chat/runtime code currently imports Google GenAI content/types. | Allowed external dependency. |
| `@vybestack/llxprt-code-auth` | dependencies | Moved stream/failover paths use auth runtime helpers. | Allowed sibling dependency. |
| `@vybestack/llxprt-code-core` | dependencies | Agents will consume core-owned structural contracts and staying shared modules by deep subpath. | Required dependency direction (`agents -> core`). |
| `@vybestack/llxprt-code-settings` | dependencies | Moved subagent/task orchestration reads profile/settings contracts. | Allowed sibling dependency. |
| `diff` | dependencies | Moved scheduler/tool formatting code uses existing diff dependency. | Allowed external dependency. |
| `fast-levenshtein` | dependencies | Moved scheduler/tool matching code uses existing Levenshtein dependency. | Allowed external dependency. |
| `undici` | dependencies | Moved request/streaming paths use existing fetch-related dependency. | Allowed external dependency. |
| `zod` | dependencies | Moved validation/schema paths use existing Zod dependency. | Allowed external dependency. |
| `zod-to-json-schema` | dependencies | Moved tool/schema conversion paths use existing dependency. | Allowed external dependency. |
| `@types/node` | devDependencies | TypeScript node typings. | Dev-only. |
| `@vybestack/llxprt-code-test-utils` | devDependencies | Future moved tests use shared test utilities. | Dev-only allowed sibling dependency. |
| `fast-check` | devDependencies | Future moved property tests use existing property-testing dependency. | Dev-only external dependency. |
| `typescript` | devDependencies | Package-local typecheck script. | Dev-only external dependency. |
| `vitest` | devDependencies | Package-local test script. | Dev-only external dependency. |

Forbidden dependencies (`@vybestack/llxprt-code-providers`, `@vybestack/llxprt-code`, `packages/cli`) are absent.


PASS

npm run build -w @vybestack/llxprt-code-agents
PASS — Successfully copied files.

npm test -w @vybestack/llxprt-code-agents
PASS — No test files found, exiting with code 0.
```

### Lockfile and scripts

```text
node scripts/check-lockfile.js
PASS — Lockfile check passed.

npm run test:scripts
FAIL — pre-existing local image harness test tried to run missing packages/ui/scripts/image-harness.ts.

LLXPRT_SKIP_IMAGE_HARNESS=1 npm run test:scripts
PASS — 8 passed, 4 skipped; release-process.test.js passed.
```

### Full verification battery

```text
npm run format
PASS

npm run typecheck
PASS — all workspaces typechecked, including agents.

npm run build
PASS — all workspaces built, including agents.

npm run test
PASS — all workspace tests passed. Summary highlights:
- auth: 34 files passed
- settings: 5 files passed
- telemetry: 5 files passed
- mcp: 15 files passed
- core: 397 files passed, 1 skipped
- providers: 152 files passed, 1 skipped
- agents: no tests found, passWithNoTests true
- cli: 509 files passed
- a2a-server: 11 files passed
- vscode companion: 4 files passed
- lsp: 11 files passed

npm run lint
PASS — exit 0 with existing warnings only (1231 warnings, 0 errors).
```

### Package and smoke tests

```text
npm run build -w @vybestack/llxprt-code-agents && npm pack --dry-run -w @vybestack/llxprt-code-agents
PASS — dry-run tarball included dist/.last_build, dist/index.*, dist/src/index.*, package.json.

node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
PASS exit 0 — printed an upstream OpenAI API error report path, same synthetic-profile behavior observed before this phase.

node scripts/start.js --profile-load ollamaglm51 "write me a haiku and nothing else"
PASS exit 0 — returned a haiku.
```

### P02 isolation guards

```text
git grep -n "@vybestack/llxprt-code-agents" -- ':!package-lock.json' ':!packages/agents'
PASS — hits are limited to release/sandbox wiring, P01 comments/contracts, and project plan docs; no production consumer imports.

grep -n "llxprt-code-agents" packages/cli/package.json packages/a2a-server/package.json
PASS — no matches; consumer dependencies are deferred to P03.

git diff --check
PASS
```

## Notes

Generated `packages/agents/dist`, `packages/agents/coverage`, and `packages/agents/junit.xml` artifacts were removed before committing; the package remains source-only like other workspace packages.
