# Phase Completion: P05 Cleanup and Final Hardening

Plan: PLAN-20260610-ISSUE1592
Phase: P05
Completed: 2026-06-11
Verdict: PASS

## Cleanup Checks

```text
core/agents package export resolution check
PASS — every core/agents package.json export target resolves to source or built output.

core stale moved export scan
PASS — no stale core exports for moved implementations remain. `core/geminiRequest.js` remains intentionally because `packages/core/src/core/geminiRequest.ts` still exists as a small request type/stringification utility.

agents public API scan
PASS — agents exports the concrete consumer surface required by CLI/a2a: AgentClient, ChatSession, CoreToolScheduler, executeToolCall, SubagentOrchestrator, TaskTool, and createTaskToolRegistration.

new TODO/FIXME/HACK/STUB/XXX/for now/placeholder additions under packages/agents/src and packages/core/src
PASS — none found in production additions vs main.

NotYetImplemented in changed files
Only plan/review documentation references the term; no production source files contain new NotYetImplemented entries.

README/docs search
packages/providers/README.md does not exist; packages/agents/README.md was deliberately not added for parity. Search of docs/dev-docs/README.md found no package-list references to packages/providers requiring an agents twin.

CODEOWNERS
.github/CODEOWNERS contains only `* @acoliver`; no package path update required.
```

## Cycle Check

```text
npx madge --circular --extensions ts packages/agents/src packages/core/src
Exit status: 1 — reports existing core cycles plus moved chat-loop cycles.

npx madge --circular --extensions ts packages/agents/src
Exit status: 1 — reports 5 cycles:
1) auth dist declaration cycle: auth-precedence-resolver.d.ts > precedence.d.ts
2) core/chatSession.ts > core/StreamProcessor.ts
3) core/chatSession.ts > core/TurnProcessor.ts
4) core/chatSession.ts > core/turn.ts
5) core/MessageStreamOrchestrator.ts > core/MessageStreamTerminalHandler.ts
```

Assessment: the repo-wide madge command is not a clean gate in the current repository because core already reports numerous cycles. The agents-only cycles are moved chat-loop relationships preserved from the extracted runtime architecture plus an auth declaration-file cycle from generated dist types. Typecheck/build/test/lint all pass, and no package dependency cycle is introduced (`core` has no agents dependency; agents depends on core/auth/settings only). These cycles are documented for final review rather than silently ignored.

## Final Verification Commands

```text
npm run format
PASS — Prettier completed across the workspace.

git diff --check
PASS — no whitespace errors.

npm run typecheck
PASS — all workspaces typechecked.

npm run build
PASS — all workspaces built, including core, agents, CLI, and a2a-server.

npm run test
PASS — full workspace test suite passed.

npm run lint
PASS — exit 0 with existing warnings only (1228 warnings, 0 errors).

node scripts/check-lockfile.js
PASS — Lockfile check passed.

npm run bundle
PASS — bundle completed.

node bundle/llxprt.js --version
PASS — printed 0.10.0.

node scripts/start.js --profile-load ollamaglm51 "write me a haiku and nothing else"
PASS — exit 0 with haiku output.

node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
PASS — exit 0; synthetic profile printed the existing OpenAI API error report line but did not fail the command.
```

## Dependency and Anti-Shim Summary

- `packages/core/src` contains no `llxprt-code-agents` imports.
- `packages/agents` contains no providers or CLI package dependency/import.
- CLI/a2a concrete runtime construction imports from `@vybestack/llxprt-code-agents` at composition roots.
- Core root barrel no longer re-exports moved concrete runtime implementations.
- Core keeps only structural contracts and shared type/utility modules required by core/providers/agents consumers.

## Holistic Assessment

P05 leaves the extraction in a clean final state for review: package exports resolve, source scans find no deferred implementation markers, docs/CODEOWNERS require no package-list update, and all full verification commands pass. The only non-green diagnostic is madge circular dependency reporting, which reflects existing/moved architectural cycles rather than a newly introduced package-level dependency cycle.

## Verdict

PASS. Cleanup and final hardening are complete; proceed to P05A semantic review.
