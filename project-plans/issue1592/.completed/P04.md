# Phase Completion: P04 Consumer Audit + Integration Hardening

Plan: PLAN-20260610-ISSUE1592
Phase: P04
Completed: 2026-06-11
Verdict: PASS

## Verification Commands

```text
npm run format
PASS — Prettier completed across the workspace.

git diff --check
PASS — no whitespace errors.

npm run typecheck
PASS — all workspaces typechecked.

npm run build
PASS — all workspaces built, including core, agents, CLI, and a2a-server.

npm run test
PASS — full workspace test suite passed.

npm run lint
PASS — exit 0 with existing warnings only (1228 warnings, 0 errors).

node scripts/check-lockfile.js
PASS — Lockfile check passed.

npm run bundle
PASS — esbuild resolved the agents workspace and copied bundle assets.

node scripts/check-settings-boundary.js
PASS — all requested settings-boundary checks passed after adding packages/agents/src to the relevant scans.

node scripts/start.js --profile-load ollamaglm51 "write me a haiku and nothing else"
PASS — exit 0 with haiku output.

node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
PASS — exit 0; synthetic profile printed the existing OpenAI API error report line but did not fail the command.
```

## Consumer Audit Summary

Generated inventory command:

```bash
grep -rnE "(AgentClient|CoreToolScheduler|ChatSession|SubAgentScope|SubagentOrchestrator|SubagentScheduler|TaskTool|executeToolCall|nonInteractiveToolExecutor)" packages/cli/src packages/a2a-server/src --include="*.ts" --include="*.tsx" | grep -vE "AgentClientContract|ToolSchedulerContract"
```

The inventory produced 441 text hits. Disposition groups:

| Consumer area | Symbols | Final source/disposition |
|---|---|---|
| `packages/cli/src/config/configBuilder.ts` | `AgentClient`, `CoreToolScheduler`, `createTaskToolRegistration` | Concrete runtime imported from `@vybestack/llxprt-code-agents`; injected into core `ConfigParameters`. |
| `packages/cli/src/runtime/runtimeContextFactory.ts` | `AgentClient`, `CoreToolScheduler`, `createTaskToolRegistration` | Concrete runtime imported from `@vybestack/llxprt-code-agents`; injected into runtime config construction. |
| `packages/cli/src/ui/utils/autoPromptGenerator.ts` | `AgentClient` | Concrete detached client imported from `@vybestack/llxprt-code-agents`. |
| `packages/cli/src/nonInteractiveCliSupport.ts` and non-interactive tests | `executeToolCall` | Concrete helper imported/mocked from `@vybestack/llxprt-code-agents`. |
| CLI React scheduler hooks/tests | `CoreToolScheduler` | Concrete type imported from `@vybestack/llxprt-code-agents` where the actual scheduler instance is manipulated. |
| CLI command/hook tests with `AgentClientContract` | `AgentClient` aliases | Structural fakes retargeted to explicit `@vybestack/llxprt-code-core/core/clientContract.js` instead of the core root barrel. |
| `packages/cli/src/integration-tests/test-utils.ts` | `AgentClient`, `CoreToolScheduler`, `createTaskToolRegistration` | Concrete agents runtime factories injected into test configs. |
| `packages/a2a-server/src/config/config.ts` | `AgentClient`, `CoreToolScheduler`, `createTaskToolRegistration` | Concrete agents runtime factories and TaskTool registration injected into a2a config. |
| `packages/a2a-server/src/agent/task.ts` | `AgentClient` | Concrete class imported from `@vybestack/llxprt-code-agents` and constructed for task execution. |
| `packages/a2a-server` tests/utils | `CoreToolScheduler`, `AgentClient` mocks | Concrete type imports from agents or structural fakes; no stale moved core module mocks remain. |
| Miscellaneous `getAgentClient` references | n/a | Existing calls to core `Config.getAgentClient()` remain correct because `Config` exposes the structural contract while composition roots inject concrete agents-owned clients. |
| Auth test strings containing `SubagentOrchestrator` | string only | Source labels in test data, not imports. |

## Boundary Scan Results

```text
core importing agents:
(empty)

CLI root-core imports needing audit:
(empty after retargeting three test-only AgentClientContract imports to explicit core subpaths)

stale moved vi.mock/import paths:
Only explicit @vybestack/llxprt-code-core/core/clientContract.js contract imports remain in three CLI tests; no stale moved implementation import or mock path remains.
```

## Settings Boundary Script Update

`scripts/check-settings-boundary.js` now includes `packages/agents/src` in the relevant model params, relative settings import, and adapter single-owner scans. The obsolete `packages/core/src/agents` path was removed from the first scan list. The reduced `packages/core/src/core`/`packages/core/src` paths remain because core still owns structural contracts and shared types/utilities.

## Bundle Assessment

`npm run bundle` passed after the agents package move. `scripts/start.js` uses the built CLI workspace entry (`./packages/cli`) rather than a standalone bundle file; the normal runtime smoke path was therefore covered by both profile smokes after the bundle command.

## Holistic Assessment

The consumer boundary is now hardened around clear ownership: CLI and a2a-server composition roots import concrete agents package classes/helpers, while core remains free of agents imports and exposes only structural contracts. Test surfaces were included in the audit so stale `vi.mock()` literals or root-barrel contract imports could not silently mask moved implementation paths. The settings-boundary script now scans the extracted agents source so later settings-boundary regressions are not hidden by the package move.

## Verdict

PASS. P04 confirms consumer imports, mocks, bundle resolution, and boundary scripts are aligned with the extracted agents package.
