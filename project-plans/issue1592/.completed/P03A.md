# Phase Completion: P03A Code Move Verification

Plan: PLAN-20260610-ISSUE1592
Phase: P03A
Completed: 2026-06-11
Verdict: PASS

## Verification Commands

```text
npm run format
PASS — Prettier completed across the workspace.

git diff --check
PASS — no whitespace errors.

npm run typecheck
PASS — all workspaces typechecked.

npm run build
PASS — all workspaces built, including core and agents.

npm run test
PASS — full workspace test suite passed.

npm run lint
PASS — exit 0 with existing warnings only (1228 warnings, 0 errors).

node scripts/check-lockfile.js
PASS — Lockfile check passed.

node scripts/start.js --profile-load ollamaglm51 "write me a haiku and nothing else"
PASS — exit 0 with haiku output.

node scripts/start.js --profile-load synthetic "write me a haiku and nothing else"
PASS — exit 0; synthetic profile printed the existing OpenAI API error report line but did not fail the command.

npm pack --dry-run -w @vybestack/llxprt-code-core
PASS — core tarball generated successfully.

rm -rf packages/agents/dist && npm run build -w @vybestack/llxprt-code-agents && npm pack --dry-run -w @vybestack/llxprt-code-agents
PASS — agents tarball generated successfully; clean pack output contains no dist/src/test-utils entries.
```

## Boundary Scan Results

```text
core tests stale moved paths:
(empty)

agents core root imports after cleanup:
(empty)

agents providers/cli imports after cleanup:
packages/agents/src/core/__tests__/providerAgnosticNaming.test.ts:466: test description string only; no import.

duplicate continuation directive in agents:
Only imports/usages of @vybestack/llxprt-code-core/core/compression/continuationDirective.js remain. agents no longer defines buildContinuationDirective.
```

## Move-Map Compliance

Spot checks for mandatory moved implementations passed:

```text
OK absent packages/core/src/core/client.ts
OK absent packages/core/src/core/coreToolScheduler.ts
OK absent packages/core/src/core/subagent.ts
OK absent packages/core/src/core/chatSession.ts
OK absent packages/core/src/core/StreamProcessor.ts
OK absent packages/core/src/tools/task.ts
OK absent packages/core/src/core/compression/OneShotStrategy.ts

OK present packages/agents/src/core/client.ts
OK present packages/agents/src/core/coreToolScheduler.ts
OK present packages/agents/src/core/subagent.ts
OK present packages/agents/src/core/chatSession.ts
OK present packages/agents/src/core/StreamProcessor.ts
OK present packages/agents/src/tools/task.ts
OK present packages/agents/src/compression/OneShotStrategy.ts
```

The P03 code review also independently verified the move-map and boundary claims. Final review artifact: `project-plans/issue1592/reviews/p03-code-move-review-final.md`, verdict APPROVE.

## Dependency Inventory Reconciliation

Generated package specifier inventory under `packages/agents` shows direct workspace imports only for:

- `@vybestack/llxprt-code-core` subpaths — production dependency.
- `@vybestack/llxprt-code-auth` — production dependency used by stream/auth runtime code.
- `@vybestack/llxprt-code-settings` — production dependency used by task/subagent orchestration.
- `@vybestack/llxprt-code-test-utils` — dev-only test dependency.

`packages/agents/package.json` declares exactly those workspace dependencies and no providers/CLI dependency. `tsconfig`/Vitest aliases mirror those dependencies for source/test resolution.

## Test Accounting

Moved runtime tests now run in `@vybestack/llxprt-code-agents`. Full workspace `npm run test` passed after the move and after the P03A root-barrel cleanup. The provider stop-reason test formerly under providers was relocated as `packages/agents/src/core/MessageConverter.stopReason.test.ts`, and provider move-map validation records that intentional agent-owned override.

## ToolRecord Parity

`packages/core/src/config/config.agentInversion.test.ts` covers injected TaskTool registration metadata and the missing-registration diagnostic path. P03 implementation preserves TaskTool `ToolRecord` identity (`toolName: TaskTool`, `displayName: task`) without importing the agents-owned concrete class into core. When a composition root omits registration while managers exist, core emits a disabled diagnostic record instead of silently omitting TaskTool.

## Holistic Assessment

A normal chat turn now crosses the package boundary at composition-root construction time. CLI/A2A import `AgentClient` and `CoreToolScheduler` from `@vybestack/llxprt-code-agents`, inject factories into core `Config`, and core only stores/uses the structural `AgentClientContract` and `ToolSchedulerContract`. When a user message is sent, the concrete agents-owned `AgentClient` creates/uses `ChatSession`, `Turn`, `StreamProcessor`, and scheduler code from the agents package while continuing to consume core-owned contracts, tools, config, telemetry, policy, history, and utility subpaths. Core no longer constructs or imports the concrete runtime.

A subagent task execution now follows the same boundary. CLI/A2A provide `createTaskToolRegistration()` from agents, core `toolRegistryFactory` records/registers the agents-owned `TaskTool` through the injected descriptor, and `TaskTool` delegates to agents-owned subagent orchestration/scheduler/runtime code. If registration is missing, core emits an explicit disabled diagnostic record without depending on agents.

The continuation directive now has a single owner in core (`core/compression/continuationDirective.ts`), and agents compression strategies import it through the exported core subpath. This avoids divergent compression continuation behavior while keeping concrete compression strategy implementations in agents.

## Verdict

PASS. P03A verification confirms the P03 extraction preserves the intended dependency direction and behavior boundaries, with full typecheck/build/test/lint/smoke verification green.
