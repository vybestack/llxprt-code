Phase: P04
Completed: 2025-12-16
Commit: cff2dac00586d0869183f45a0238e12b3dad05b2

What changed:
- Switched Ink dependency to match upstream gemini-cli: `npm:@jrichman/ink@6.4.6` (root + `packages/cli`) and updated `package-lock.json`.
- Added `ui.useAlternateBuffer` + `ui.incrementalRendering` settings to `packages/cli/src/config/settingsSchema.ts`.
- Added `packages/cli/src/ui/inkRenderOptions.ts` (and tests) to centralize Ink `render()` options.
- Wired `packages/cli/src/gemini.tsx` `render()` call to `inkRenderOptions(...)` (validated by a new included Vitest file `packages/cli/src/gemini.renderOptions.test.tsx`).

Commands / tests:
- `npm install`
- `npm run test --workspace @vybestack/llxprt-code -- src/ui/inkRenderOptions.test.ts` → PASS
- `npm run test --workspace @vybestack/llxprt-code -- src/gemini.renderOptions.test.tsx` → PASS

Baseline scripts:
- (No change expected yet; structural scroll refactor not started in this phase.)
