# Reimplement Plan: Extension Documentation Parity

**Upstream SHA:** `19d4384f1694d970e32045120105e8fcaf0833b9`
**Upstream subject:** docs: Update uninstall command to reflect multiple extension support (#13582)

## Context

The upstream commit is a tiny 3-line doc change, but our extension docs (`docs/extension.md`, 95 lines) are missing entire sections that upstream has (277 lines). The goal is to bring our docs to parity with upstream's extension management CLI documentation, adapted for LLxprt's branding and process.

## What We Have

- `docs/extension.md` — 95 lines covering: how it works, extension.json, extension commands, conflict resolution, custom transports, variables
- All extension CLI commands are implemented: `install`, `uninstall`, `disable`, `enable`, `update`, `new`, `link`, `list`, `validate`

## What We're Missing (vs Upstream)

1. **Extension Management CLI section** — install, uninstall (with multiple extension support), disable, enable, update, new, link commands with usage examples
2. **Settings section** — extension-defined settings that users provide on install
3. Better organization with separate "Extension management" and "How it works" sections

## Implementation Steps

1. Read the current `docs/extension.md`
2. Read upstream's `docs/extensions/index.md` for reference (run `git show 19d4384f:docs/extensions/index.md`)
3. Rewrite `docs/extension.md` to include:
   - **Extension Management** section with all CLI commands:
     - `llxprt extensions install <source> [--ref <ref>] [--auto-update] [--pre-release] [--consent]`
     - `llxprt extensions uninstall <name...>` (note: supports multiple names)
     - `llxprt extensions disable <name> [--scope <scope>]`
     - `llxprt extensions enable <name> [--scope <scope>]`
     - `llxprt extensions update <name> | --all`
     - `llxprt extensions new <path> [template]`
     - `llxprt extensions link <path>`
     - `llxprt extensions list`
     - `llxprt extensions validate <path>`
   - Keep existing sections: How it works, llxprt-extension.json, Extension Commands, Conflict Resolution, Custom Transports, Variables
4. Branding substitutions:
   - `gemini` → `llxprt`
   - `gemini-extension.json` → `llxprt-extension.json`
   - `GEMINI.md` → `LLXPRT.md`
   - `@google/gemini-cli-core` → `@vybestack/llxprt-code-core`
   - `.gemini/extensions` → `.llxprt/extensions`
5. Run `npm run format` on the docs file

## Verification

- File exists and is well-formed markdown
- All LLxprt extension commands are documented
- No upstream branding remains
- `npm run format` passes
