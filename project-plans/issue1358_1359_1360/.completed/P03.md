# Phase 03: Framing Protocol Stub — COMPLETED

**Plan:** PLAN-20250214-CREDPROXY.P03
**Completed:** 2025-02-16

## Summary

Created two stub files for the framing protocol layer:

1. **`packages/core/src/auth/proxy/framing.ts`** (38 lines)
   - Exports `MAX_FRAME_SIZE` (65536) and `PARTIAL_FRAME_TIMEOUT_MS` (5000) constants
   - Exports `encodeFrame()` function stub (throws NotYetImplemented)
   - Exports `FrameDecoder` class with `feed()` and `reset()` method stubs

2. **`packages/core/src/auth/proxy/proxy-socket-client.ts`** (47 lines)
   - Exports `REQUEST_TIMEOUT_MS`, `IDLE_TIMEOUT_MS`, `PROTOCOL_VERSION` constants
   - Exports `ProxyResponse` type
   - Exports `ProxySocketClient` class with `ensureConnected()`, `request()`, `close()`, and `gracefulClose()` stubs

## Verification

- `npm run typecheck` — passes [OK]
- Both files exist [OK]
- Both files contain `@plan PLAN-20250214-CREDPROXY.P03` [OK]
- All methods throw `new Error('NotYetImplemented')` [OK]
