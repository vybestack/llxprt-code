# REIMPLEMENT Plan: Hook Input/Output Contracts

**Upstream SHA:** `b25915340325fbb72366fce3e9db82580136c3a4`  
**Subject:** feat(hooks): Hook Input/Output Contracts (#9080)

## Overview

This commit adds hook I/O type definitions - continuing the hooks system series.

## Files Changed (Upstream)

- `packages/core/src/config/config.test.ts` (+15 lines modified)
- `packages/core/src/config/config.ts` (+45 lines modified)
- `packages/core/src/hooks/types.test.ts` (+38 lines - new)
- `packages/core/src/hooks/types.ts` (+602 lines - new)
- `packages/core/src/index.ts` (+3 exports)

## LLxprt Considerations

1. **New Types File** - 602 lines of new types, mostly clean
2. **Config Changes** - May conflict with LLxprt's config structure
3. **Index Exports** - Check for naming conflicts

## Implementation Steps

1. Cherry-pick the commit
2. Resolve config.ts conflicts (LLxprt's multi-provider additions)
3. Verify new types don't conflict with existing LLxprt types
4. Add exports to index.ts

## Verification

```bash
npm run lint && npm run typecheck
npm run test --workspace @vybestack/llxprt-code-core -- hooks
```

## Decision

- [ ] Cherry-pick with conflicts in config.ts
- [ ] New files should be clean

---

*Plan to be executed during Batch 18*
