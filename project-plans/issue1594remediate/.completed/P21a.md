Phase: P21a
Completed: 2026-06-22 10:18
Files Created: none (verification-only gate)
Files Modified:
  - project-plans/issue1594remediate/plan/21a-boundary-and-nonbreaking-verification.md (+16/-3) — CCF-10 GATE-BUG fix (defect in the GATE, not the worker): the additive-only (REQ-006) check did a per-file `git diff origin/main -- index.ts internals.ts api/index.ts | grep "^-export"`. But on origin/main (the #1594 baseline) ONLY `index.ts` existed; `internals.ts` + `api/index.ts` are NET-NEW. The remediation RELOCATED the low-level export lines OUT of `index.ts` INTO the new `internals.ts` (then re-surfaced via `export * from './internals.js'`). The per-file grep MIS-reads all 21 relocated lines as "removals" though each reappears verbatim in another barrel and the PUBLIC surface lost nothing → guaranteed FALSE-FAIL. REPLACED with the correct UNION-SUPERSET invariant: every baseline export LINE must still appear SOMEWHERE in the union of the 3 current barrels (comm -23 baseline current_union must be empty).
Tests Added: 0 (gate verifies P21's 10 tests)
Verification: |
  Coordinator independently reproduced ALL checks (ground-truth, not trusting the subagent):
    build core=0, build agents=0, full `npm run build`=0
    additive-only UNION-SUPERSET: baseline=20 export lines, current union (index+internals+api/index)=35; comm -23 MISSING set = EMPTY -> additive-only CONFIRMED (current union is a strict superset of the #1594 baseline; the 21 "moved" lines all re-present in internals.ts)
    semantic checklist (all file:line ground-truthed):
      (a) createAgent NET-NEW, signature `createAgent(rawConfig: AgentConfig): Promise<Agent>` @ api/createAgent.ts:71 (no prior baseline signature to break)
      (b) fromConfig SEPARATE export @ api/index.ts:16 (not an overload of createAgent)
      (c) AgentClientContract TYPE-ONLY @ api/index.ts:20 (`export type`), reachable from root via `export * from './api/index.js'`; runtime root has NO AgentClientContract value
      (d) concrete AgentClient class exported ONLY from internals.ts:38; runtime probe: api_has_AgentClient=false/undefined, internals_has_AgentClient=true/function
    whole-__tests__ deep-import scan -> clean (no /src/, core/src, providers/src)
    CRIT-6 Path-A internals scan -> internals confined to the 2 permitted meta files (nonBreaking.exports.test.ts + publicSurface.nonbreaking.test.ts); 0 Path-A offenders; 21 public-root importers
    behavioral quality (both files read in full) -> genuinely behavioral, no mock theater / no reverse tests / no weakened assertions
    property adequacy -> 3 property-labeled it-blocks / 10 total = 30%; 4 distinct fc.property cases (>=MIN-2)
    vitest 2-file -> 10/10 passed; FULL agents __tests__ -> 38 files / 488 tests passed; typecheck TC=0; agents lint=0

  Independent subagent verifier (typescriptreviewer, clean history-free brief): VERDICT=PASS on every dimension; sole caveat was an explicit "observational note only — command-wrapper/pipe-shell artifacts, not source or test failures" (the known set -e/pipefail wrapper behavior). The spec's additive block uses the safe `if [ -n "$MISSING" ]; then exit 1; fi` form (the test's nonzero is consumed by `if`, so set -e does not trip) — coordinator's own reproduction of that exact form returned exit 0.
Semantic Assessment: PASS — Remediation is provably ADDITIVE-ONLY (union of 3 barrels is a superset of the #1594 baseline; zero baseline export lines vanished — the apparent index.ts "removals" are cross-barrel relocations into the new internals.ts) and the #1595 CLI can reach EVERY needed symbol via public imports only (createAgent/fromConfig/AgentClientContract type + the boundary test proves no /src/ leak and internals confined to test-only meta files). One plan edit (CCF-10) fixed a gate that would have false-failed on the legitimate index.ts→internals.ts relocation; no worker work-around accepted. 488/488, TC0/BUILD0/LINT0.
