Phase: P02
Completed: 2026-06-22 21:01
Files Created:
  - project-plans/issue1594remediate/analysis/pseudocode/config-injection-seam.md (220 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/settings-surface.md (113 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/get-current-sequence-model.md (87 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/client-contract-promotion.md (131 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/provider-runtime-seam.md (92 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/cli-integration-adapter.md (181 lines)
  - project-plans/issue1594remediate/analysis/pseudocode/provider-manager-seam.md (241 lines, the 7th CRIT-1 seam file driving P03-P05)
Files Modified:
  - project-plans/issue1594remediate/analysis/pseudocode/provider-manager-seam.md (+3 net lines, prose-only revision; no .ts files touched). All 7 pseudocode files are currently UNTRACKED in git (the analysis/pseudocode/ dir shows as `??`), so there is no git diff stat against HEAD; the revision is reported as a net line-count delta on the untracked draft. Three edits corrected a materially inaccurate grounding claim: the pseudocode stated (in 3 places) that runtimeContextFactory.ts ALREADY imports RuntimeProviderManager "at ~line 24" — verified FALSE (grep for RuntimeProviderManager in that file is empty; it imports Config/MessageBus/etc. from @vybestack/llxprt-code-core at line 22 but NOT the type). Corrected all 3 spots to state the import must be ADDED to the existing core import block in P03 (mirroring the form sibling files like runtimeLifecycle.ts:29 already use). The CRIT-1 type-safety decision itself (type the option as the structural RuntimeProviderManager interface) is unchanged and remains valid.
Tests Added: 0
Verification:
  Phase verification block (verbatim from plan/02-pseudocode.md), run twice (pre- and post-revision), both ending OK:
    $ cd project-plans/issue1594remediate/analysis/pseudocode
    $ for f in config-injection-seam settings-surface get-current-sequence-model client-contract-promotion provider-runtime-seam cli-integration-adapter; do
        test -f "$f.md" || { echo "MISSING $f.md"; exit 1; }
        grep -q "Interface Contracts" "$f.md" || { echo "$f MISSING Interface Contracts"; exit 1; }
        grep -q "Integration Points" "$f.md" || { echo "$f MISSING Integration Points"; exit 1; }
        grep -q "Anti-Pattern Warnings" "$f.md" || { echo "$f MISSING Anti-Pattern Warnings"; exit 1; }
        grep -qE "^[0-9]+:" "$f.md" || { echo "$f MISSING numbered lines"; exit 1; }
        grep -q "@plan:PLAN-20260621-COREAPIREMED.P02" "$f.md" || { echo "$f MISSING plan marker"; exit 1; }
      done ; echo "OK"
    OK
  7th file (provider-manager-seam.md) mandatory-section/marker checks (post-revision):
    Interface Contracts OK / Integration Points OK / Anti-Pattern Warnings OK / numbered lines OK / plan marker OK
Semantic Assessment:
  PASS — All 7 pseudocode files are contract-first (each has Interface Contracts + Integration Points + Anti-Pattern Warnings + numbered pseudocode + the P02 plan marker) and contain NO executable TypeScript (anti-TypeScript self-check returned CLEAN for all 7: zero non-fence const/let/function/class/import/return/await/throw statements outside the ```typescript contract fences). Real-symbol spot-checks CONFIRMED every cited anchor against actual source: finalizeAgent createAgent.ts:210 (exact), assembleFacade createAgent.ts:327 (exact), resolveClient createAgent.ts:257 (exact: `const resolveClient = () => config.getAgentClient();`), configBase ephemeral methods configBase.ts:173/191/265 (exact), getProviderManager configBaseCore.ts:265 (exact), clientContract getCurrentSequenceModel clientContract.ts:118 (exact), AgentClientContract clientContract.ts:67 (exact), messageBus?/config? runtimeContextFactory.ts:187/199 + new ProviderManager at :502 (exact), AgenticLoop object-form ctor AgenticLoop.ts:182 (exact), internals.ts:38 AgentClient export (exact), index.ts barrel re-exports (exact), useAgenticLoop.ts:254 `new AgenticLoop` (exact), agentImpl.ts:668 getCurrentSequenceModel stub returning null (exact), RuntimeProviderManager.ts:50 interface (exact), ProviderManager.ts:80 `implements IProviderManager` (exact), registerCliProviderInfrastructure param typed RuntimeProviderManager at runtimeLifecycle.ts:92 (exact), configureProviderRuntimeFactories param typed RuntimeProviderManager at providerManagerInstance.ts:175 (exact). Dependencies are injected (FromConfigOptions.messageBus, IsolatedRuntimeContextOptions.providerManager?, this.deps.config/runtimeId/resolveClient), errors propagate (no try/catch swallowing in setEphemeralSetting), and no responses are hardcoded. One material grounding defect in provider-manager-seam.md (the false "RuntimeProviderManager already imported in runtimeContextFactory.ts:24" claim) was REVISED with prose-only edits and re-verified. The pseudocode is ready to drive the P03-P05 implementation.

---
## Remediation (post-P02a FAIL #1)
Date: 2026-06-22
P02a found a residual internal contradiction in provider-manager-seam.md line 88: it still said
RuntimeProviderManager was "already imported in this file at ~line 24", contradicting the corrected
line 107/108 (which says P03 must ADD a type-only import). Ground-truthed: `grep RuntimeProviderManager
packages/providers/src/runtime/runtimeContextFactory.ts` returns NOTHING — the type is NOT imported.
FIX: rewrote line 86-88 P03-stub bullet to instruct ADDING `import type { RuntimeProviderManager }`
to the existing core import block (~line 22), mirroring sibling files (runtimeLifecycle.ts:29,
runtimeAccessors.ts:29, runtimeRegistry.ts:24). All three import-instruction spots now consistent.
Line 41's `runtimeContextFactory.ts:24` citation is accurate (evidences the core import block at
lines 20-29; NOT a RuntimeProviderManager claim). Files Modified: provider-manager-seam.md (+2/-2).
Re-verification: P02 structural loop re-run → all 7 files OK.

---
## Remediation (post-P02a FAIL #2)
Date: 2026-06-22
P02a re-review found two more defects in provider-manager-seam.md (both VALID, ground-truthed):
1. Header line 5 said "thread adoption flag to cleanup" — contradicted the body (lines 135-138,
   163-166, 190-193) which correctly say NO providerManagerAdopted flag is needed (no manager
   disposal to gate). Verified factory cleanup (~400-447) does NO ProviderManager disposal. FIX:
   rewrote the header Component-target clause to match the body (adopt via `?? new ProviderManager`,
   explicitly NO cleanup flag, same instance passed through to onCleanup).
2. Lines 241-245 were a malformed duplicated tail (stray ``` fences + "adopt path. The default"
   fragment repeating "construction site remains exactly one new ProviderManager("). FIX: deleted
   the corrupt fragment. Code fences now balanced (14, even); Verification Hooks block closes cleanly.
Files Modified: provider-manager-seam.md (+4/-2 header; -5 tail). Re-verification: structural loop →
all 7 files OK; fence balance EVEN; remaining providerManagerAdopted mentions are the correct
"no flag needed" body statements (now consistent with header).

---
## Remediation (post-P02a FAIL #3)
Date: 2026-06-22
P02a fresh whole-file scan found a source-faithfulness fabrication in provider-runtime-seam.md
(VALID, ground-truthed): it claimed agent.getProvider()/getModel()/getProviderStatus() "resolve via
getCliRuntimeServices()" from the active runtime. GROUND TRUTH: getCliRuntimeServices does NOT exist
in agentImpl at all (only referenced in CLI discovery.ts docs). Real mechanism: those methods return
this.providerState.* (agentImpl.ts:594 getProvider, 621 getProviderStatus, 648 getModel) — a per-agent
snapshot seeded in the constructor from deps.runtimeState (agentImpl.ts:267-275); the switch pipeline
(switchActiveProvider/setActiveModel) operates on this.deps.config + this.deps.providerManager and
mutates providerState. FIX: corrected all 4 inaccurate spots (Dependencies comment, the prose
paragraph, Numbered Pseudocode lines 20-24, Integration Points) to describe the real providerState
mechanism and explicitly note NO getCliRuntimeServices call exists in agentImpl. Adopted-runtime intent
preserved (runtimeState/config/providerManager derive from the adopted context → no second manager,
REQ-001.2). Files Modified: provider-runtime-seam.md (4 edits). Re-verification: only remaining
getCliRuntimeServices mentions are explicit "NO such call" disclaimers; fences EVEN (10); structural OK.
