Phase: P07
Completed: 2026-06-22 23:40
Files Created:
  - packages/agents/src/api/__tests__/helpers/buildCliStyleConfig.ts (331 lines)
  - packages/agents/src/api/__tests__/fixtures/parity-toolcall.jsonl (2 lines)
  - packages/agents/src/api/__tests__/cli-turn-parity.early.spec.ts (214 lines)
Files Modified: none (test-authoring phase only)
Tests Added: 3 (EP1 adopt, EP2 parity, EP3 property)
Verification:
  Guards (all PASS):
  - All 3 files exist.
  - No mock theater (mockResolvedValue/toHaveBeenCalled) in spec.
  - No deep imports (/src/|core/src|providers/src) in spec or helper.
  - No reverse tests (toThrow('NotYetImplemented')/not.toThrow()).
  - Object-form AgenticLoop: `new AgenticLoop({` confirmed; no positional form.
  - agentClient: present in reference options.
  - property-based CASES: 1 / 3 = 33% (it.prop/test.prop=0, classic-blocks=1) — ≥30% PASS.
  - Deferred-impl detection: PASS (no TODO/FIXME/HACK/WIP/placeholder; no skip/xit/xdescribe).

  RED-state (vitest run src/api/__tests__/cli-turn-parity.early.spec.ts from packages/agents/):
  ```
  ❯ src/api/__tests__/cli-turn-parity.early.spec.ts (3 tests | 3 failed) 54ms
    × EP1 fromConfig adopts the external Config and drives a terminal done (REQ-INT-001) 44ms
      → NotYetImplemented
    × EP2 Path A (fromConfig stream) projects identically to Path B ... (REQ-INT-002) 2ms
      → NotYetImplemented
    × EP3 property: Path A vs Path B projection equivalence ... (REQ-INT-002) 7ms
      → Property failed after 1 tests
      Counterexample: ["read_file"," "]
      Caused by: Error: NotYetImplemented
        ❯ fromConfig src/api/fromConfig.ts:10:9

   Test Files  1 failed (1)
        Tests  3 failed (3)
  ```
  EXIT_CODE=1 (RED). No "Cannot find module|SyntaxError|ReferenceError|Failed to resolve import" in the log.
  RED confirmed for a behavioral reason (public fromConfig path not yet adequate).

  NOTE on spec verification command (→ now systematized as CCF-3 in execution-tracker.md): the original
  spec form `npm test --workspace @vybestack/llxprt-code-agents -- run "$SPEC"` (where $SPEC is repo-relative)
  passes vitest filters ["run", "packages/agents/src/..."] which match 17 unrelated *runtime* files containing
  "run" in their path (OR semantics) but NOT this spec, AND runs with cwd=package so the repo-relative path
  matches nothing — a FALSE EXIT 0. The spec's RED state is confirmed via the corrected canonical form
  `npx vitest run packages/agents/src/api/__tests__/cli-turn-parity.early.spec.ts` (run from repo root; vitest
  auto-discovers the package vitest.config.ts), which produces the expected 3-failure behavioral RED above
  (EXIT 1, Test Files 1 failed (1)). All plan specs have been migrated to this `npx vitest run` form (CCF-3).

  typecheck: PASS (npm run typecheck — all workspaces exit 0).
  prettier: PASS (all 3 new TS files use Prettier code style; .jsonl has no parser — expected).
  lint: PASS (npm run lint --workspace @vybestack/llxprt-code-agents — no errors).

  Frozen hashes recorded to project-plans/issue1594remediate/.completed/P07-frozen-hashes.txt.

  BOUNDARY-FIX amendment (coordinator, post-authoring): the consumer-facing spec originally imported
  `Config` and `MessageBus` as `import type` directly from `@vybestack/llxprt-code-core/config/config.js`
  and `/confirmation-bus/message-bus.js`. The pre-existing governance test `boundary.spec.ts`
  (PLAN-20260617-COREAPI.P09, T17) scans consumer-facing `.spec.ts` files for ANY import whose specifier
  starts with a forbidden deep-package prefix (`@vybestack/llxprt-code-core/`, …) — it does NOT exempt
  `import type` — so those two lines were real violations (boundary.spec.ts → 3 failed, naming exactly
  these 2 specifiers). FIX (mirrors the helper's existing `export type { AgentEvent, DoneReason }`
  precedent): the scan-EXEMPT helper `helpers/buildCliStyleConfig.ts` now adds `export type { Config,
  MessageBus };` (it already imports both as values), and the spec imports the two types FROM the helper
  (`type Config, type MessageBus` on the existing `./helpers/buildCliStyleConfig.js` import). Result:
  boundary.spec.ts → 5 passed (5) EXIT 0; the early-parity slice remains behavioral-RED (EXIT 1, Test
  Files 1 failed (1), 3 failed); `npm run typecheck` exit 0 all workspaces; prettier clean on both files.
  Spec + helper sha256 in P07-frozen-hashes.txt were REGENERATED to match (fixture hash unchanged).
Semantic Assessment: Early integration-first RED slice authored — 3 behavioral test cases drive the not-yet-implemented fromConfig public path; all 3 are RED for the correct behavioral reason (fromConfig P06 stub throws NotYetImplemented), the slice compiles and runs, and property ratio is 33% (≥30%). The canonical shared helper (buildCliStyleConfig + projectToComparable) is clean and general for P19 reuse.
