Phase: P08
Completed: 2026-06-23 01:12 (initial); REMEDIATED 2026-06-23 01:31
Files Created:
  - packages/agents/src/api/__tests__/fromConfig.behavior.test.ts (455 lines)
Files Modified:
  - packages/agents/src/api/__tests__/fromConfig.behavior.test.ts
    (P08 remediation: 3 verifier-flagged defects fixed; suite stays BEHAVIORAL-RED)
Tests Added: 22 (15 behavioral + 7 property-based)

REMEDIATION — 3 defects fixed (TDD RED phase; suite STAYS RED for the
behavioral reason: the P06 fromConfig stub throws NotYetImplemented before any
agent is returned):

1. DEFECT 1 (scoped-scan trip) — the top-of-file docblock contained the prose
   phrase "reverse test", which the phase's MANDATORY scoped deferred/reverse-
   test scan matches (case-insensitive) and exits 1 on. Rewrote the docblock to
   convey the same meaning without any banned substrings
   ("reverse test", "toThrow(...NotYetImplemented...)", "should (not) be
   implemented", "negative test (expected)", "expect(...).not.toBeDefined").
   New docblock: "Every test fails at RED because the P06 stub raises
   NotYetImplemented before any agent is returned, so identity/turn/ownership
   assertions are never reached. These are genuine forward behavioral
   assertions (identities and values), not assertions about the stub error
   itself, and contain no mock theater." The scoped scan now prints PASS / 0.

2. DEFECT 2 (T6b mislabeled + vacuous) — T6b's NAME claimed to test "a Config
   whose getProviderManager() returns undefined", but its BODY used the helper
   Config (whose manager IS defined), asserted `expect(callerManager).toBeDefined()`
   filler, then re-asserted the SAME identity as T6 (a duplicate wearing a false
   label). Re-scoped T6b to the REAL invariant it can truthfully reach and
   renamed the title to match the body. T6b now asserts the SINGLE-MANAGER /
   no-second-construction invariant in a way T6 does NOT: it builds the agent
   via fromConfig, asserts the adopted runtime manager identity ===
   config.getProviderManager() (the one supplied), DRIVES a stream turn, asserts
   exactly one `done` event, AND re-asserts the adopted manager identity is
   unchanged after the turn — proving one manager governed the turn (no second
   construction). Removed the false "returns undefined" claim and the misleading
   `expect(callerManager).toBeDefined()` filler. At RED it fails with
   NotYetImplemented (fromConfig never returns an agent) — correct.

3. DEFECT 3 (T7c vacuous) — T7c's body, after agent.dispose(), only asserted
   `expect(callerBus).toBeDefined()` on a local const (can NEVER be undefined;
   proved nothing about ownership). Re-scope: T7c now asserts REAL post-dispose
   USABILITY of the caller-owned bus via the MessageBus public surface
   (subscribe / listenerCount / removeAllListeners), probed through the SAME
   documented structural-narrowing idiom already used by captureProviderManager
   (no deep imports of MessageBusType, no new any/as). It records a caller
   subscription BEFORE the agent exists, builds + disposes the agent, then
   asserts: (a) listenerCount(eventType) after dispose === before (the caller
   subscription survived dispose — the bus was not torn down), (b) a NEW
   subscribe increases the observable count (bus still accepts subscriptions),
   (c) removeAllListeners() runs without throwing and collapses the count to 0
   (bus is alive). Each assertion would FAIL if agent.dispose() had
   disposed/cleared the caller bus. At RED it fails with NotYetImplemented
   (fromConfig never returns an agent) — correct.

Verification (post-remediation, run from repo ROOT):
  Property-ratio counter:
  ```
  T=packages/agents/src/api/__tests__/fromConfig.behavior.test.ts
  TOTAL=22 PROP=7
  PROP*100/TOTAL = 31%
  ```
  (≥30% PASS; total test count 22 preserved — 15 behavioral + 7 property)

  Scoped deferred/reverse-test scan (Defect 1 gate) — PASS / exit 0:
  ```
  PASS: no deferred-implementation markers / reverse tests in the new spec/helper file(s).
  ```

  RED-state (npx vitest run packages/agents/src/api/__tests__/fromConfig.behavior.test.ts):
  ```
   Test Files  1 failed (1)
        Tests  21 failed | 1 passed (22)
  ```
  EXIT=1 (RED). All failures `Caused by: Error: NotYetImplemented` thrown by
  the P06 fromConfig stub (packages/agents/src/api/fromConfig.ts:10) before any
  agent is returned. The 1 passing test (T7b) uses createAgent (which IS
  implemented) as a contrast anchor. ZERO banned errors
  (Cannot find module|SyntaxError|Failed to resolve import|ReferenceError) in
  the log. T6b and T7c both appear in the failure list with their new titles
  and fail with NotYetImplemented — RED confirmed for the behavioral reason.

  typecheck: PASS (npm run typecheck — all 15 workspaces exit 0).
  prettier: PASS (npx prettier --check — All matched files use Prettier code style!).
  boundary: PASS (npx vitest run packages/agents/src/api/__tests__/boundary.spec.ts
  — 5 passed (5), EXIT 0; no new deep imports).

Semantic Assessment: The suite is a genuine behavioral RED driver for fromConfig
identity/adoption/ownership — 22 tests (15 behavioral + 7 property at 31%),
reusing the canonical buildCliStyleConfig helper + disposalProbe + agentHarness
(no duplication), importing ONLY from the bare root @vybestack/llxprt-code-agents
and the allowed @vybestack/llxprt-code-tools, with zero mock theater, zero
reverse tests, zero skipped tests, zero deferred-impl markers, and zero deep
core/providers imports. The three verifier-flagged defects are fixed: the
docblock no longer trips the scoped scan; T6b honestly asserts the single-
adopted-manager / no-second-construction invariant via a real turn drive
(distinct from T6's pure identity assertion); T7c honestly asserts post-dispose
caller-bus usability via the MessageBus public surface (subscribe/listenerCount/
removeAllListeners). The suite STAYS BEHAVIORAL-RED for the correct reason
(P06 stub throws NotYetImplemented before any agent is returned); fromConfig was
NOT implemented (deferred to P09).
