Phase: P14a
Completed: 2026-06-23 06:50
Files Created: P14a.md (141 lines)
Files Modified: project-plans/issue1594remediate/.completed/P14a.md
Tests Added: 0 (verification phase)

Verification:
```text
# BUILD core + agents

> @vybestack/llxprt-code-core@0.10.0 build
> node ../../scripts/build_package.js

Successfully copied files.

> @vybestack/llxprt-code-agents@0.10.0 build
> node ../../scripts/build_package.js

Successfully copied files.
# 1) the implementation method:
693-   * @plan:PLAN-20260621-COREAPIREMED.P14
694-   * @requirement:REQ-003
695-   * @pseudocode lines 10-15
696-   * Resolves the bound client FRESH on every call (R-CLIENT invariant — never
697-   * cache), null-guards a missing client, and delegates to the client's
698-   * current sequence model. Returns null when there is no active client or no
699-   * active load-balancer sequence model.
700-   */
701:  getCurrentSequenceModel(): string | null {
702-    // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition -- resolveClient is typed non-nullable but may return undefined before client initialization (T9c null-guard contract).
703-    return this.deps.resolveClient()?.getCurrentSequenceModel() ?? null;
704-  }
705-
# 2) stub must be gone (no bare return null; as the whole body):
stub-gone OK
# 3) delegation present:
delegation OK
# 4) pseudocode citation present:
cite OK
# 5) NO caching of the resolved client in a field (R-CLIENT):
no-cache OK
# 6) getConfig still declared/impl exactly once (regression guard):
getConfig impl count: 1
# 7) the Phase-13 behavioral suite must now be GREEN:

 RUN  v3.2.4 /Users/acoliver/projects/llxprt/branch-3/llxprt-code

 ✓ packages/agents/src/api/__tests__/agent.sequenceModel.behavior.test.ts (7 tests) 81ms

 Test Files  1 passed (1)
      Tests  7 passed (7)
   Start at  06:49:43
   Duration  2.58s (transform 1.13s, setup 0ms, collect 2.22s, tests 81ms, environment 0ms, prepare 30ms)

# 8) changed-line deferred-marker scan (must be empty):
deferred-scan CLEAN
# 9) typecheck:

> @vybestack/llxprt-code@0.10.0 typecheck
> npm run typecheck --workspaces --if-present


> @vybestack/llxprt-code-tools@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-storage@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-auth@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-settings@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-telemetry@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-ide-integration@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-policy@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-mcp@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-core@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-lsp@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-providers@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-agents@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-a2a-server@0.10.0 typecheck
> tsc --noEmit


> @vybestack/llxprt-code-test-utils@0.10.0 typecheck
> tsc --noEmit
```

Additional source evidence:
- Pseudocode lines 33-39 require: line 35 `SET client = this.deps.resolveClient()`, lines 36-37 return null for undefined/null client, line 38 `RETURN client.getCurrentSequenceModel()`.
- Implementation at packages/agents/src/api/agentImpl.ts:701-704 is:
  ```typescript
  getCurrentSequenceModel(): string | null {
    // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition -- resolveClient is typed non-nullable but may return undefined before client initialization (T9c null-guard contract).
    return this.deps.resolveClient()?.getCurrentSequenceModel() ?? null;
  }
  ```
- Git-change view for packages/agents/src/api/agentImpl.ts:692-704 shows the P14 citation/comment and the method replacement only inside this method body. The broader current `git diff HEAD` hunk also contains adjacent pre-existing phase additions after line 704 (`getConfig`, ephemeral-setting methods), but they are outside this method body; the P14 production method itself is scoped to the stub replacement and citation.
- Behavioral suite evidence is real: packages/agents/src/api/__tests__/agent.sequenceModel.behavior.test.ts includes T9c at lines 153-158 injecting `() => undefined` and asserting `.toBeNull()`, and T9d at lines 164-174 mutating the captured `current` client between calls on the same agent, proving no caching. It also includes positive return-value assertions at lines 121-128 and property round-trip assertions at lines 180-189. There are no spy-call assertions in these inspected tests.

Semantic Assessment: PASS — packages/agents/src/api/agentImpl.ts:701-704 resolves through `this.deps.resolveClient()` at call time, uses `?.`/`?? null` to null-guard missing clients, and delegates to `getCurrentSequenceModel()`; mandatory build, targeted vitest (7/7), and full typecheck all exited 0.

Holistic Functionality Assessment:
1. Pseudocode fidelity: PASS. Pseudocode lines 34-39 say to implement `getCurrentSequenceModel`, resolve the bound client, return null if absent, otherwise return `client.getCurrentSequenceModel()`. The implementation quote above is semantically equivalent in one expression: `this.deps.resolveClient()?.getCurrentSequenceModel() ?? null`.
2. R-CLIENT/no-cache invariant: PASS. The method body calls `this.deps.resolveClient()` directly inside the return expression on every invocation. The grep no-cache guard found no assignment of `this.<client> = this.deps.resolveClient`, and T9d passed by rebinding the closure-returned client from `gpt-4o` to `claude-x` on the same agent.
3. Eslint-disable legitimacy: PASS. The disable is narrow, line-local, and has an honest rationale: `resolveClient` is statically non-nullable in AgentDeps but the runtime/pseudocode contract explicitly includes the pre-ready/mid-rebind no-client case. The T9c behavioral test injects `undefined` and proves the public contract is to return null rather than throw. This matches the repository's documented runtime-vs-static-type pattern rather than masking a defect.
4. Final verdict: PASS. Stub is gone, delegation and pseudocode citation are present, no client caching was found, deferred-marker scan is clean, getConfig count remains 1, the Phase-13 behavioral suite is GREEN (7 passed), and full `npm run typecheck` exits 0.
