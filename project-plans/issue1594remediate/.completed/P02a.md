Phase: P02a
Completed: 2026-06-22 21:29
Files Created: []
Files Modified: [project-plans/issue1594remediate/.completed/P02a.md overwritten with this holistic re-verification assessment]
Tests Added: 0
Verification:
- Scope read in full:
  - project-plans/issue1594remediate/plan/02a-pseudocode-verification.md
  - project-plans/issue1594remediate/analysis/pseudocode/config-injection-seam.md
  - project-plans/issue1594remediate/analysis/pseudocode/settings-surface.md
  - project-plans/issue1594remediate/analysis/pseudocode/get-current-sequence-model.md
  - project-plans/issue1594remediate/analysis/pseudocode/client-contract-promotion.md
  - project-plans/issue1594remediate/analysis/pseudocode/provider-runtime-seam.md
  - project-plans/issue1594remediate/analysis/pseudocode/cli-integration-adapter.md
  - project-plans/issue1594remediate/analysis/pseudocode/provider-manager-seam.md
- Structural checks over all 7 pseudocode files:
  - cli-integration-adapter.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 8 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - client-contract-promotion.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 14 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - config-injection-seam.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 8 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - get-current-sequence-model.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 8 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - provider-manager-seam.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 14 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - provider-runtime-seam.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 10 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - settings-surface.md: Interface Contracts present; Integration Points present; Anti-Pattern Warnings present; numbered NN/NNx lines present; fence count 8 (balanced); @plan marker present as PLAN-20260621-COREAPIREMED.P02.
  - Note: the user requested an @plan:PLAN-20260621-COREAPIREMED.P02 marker; all seven files contain that marker. They do not contain the P02a marker because these are P02 artifacts being verified by P02a, matching the phase file's own wording.
- Source citation and behavioral-claim audit by file:
  - config-injection-seam.md: PASS.
    - createAgent.ts:57 is generateRuntimeId in the import list; source line 57 is `generateRuntimeId,`.
    - createAgent.ts:210 is `async function finalizeAgent(` and createAgent.ts:327 is `async function assembleFacade(deps: AssembleFacadeDeps): Promise<Agent> {`.
    - createAgent.ts:71-183 covers createAgent validation, runtimeId generation, Config construction, isolated runtime context construction, activation, provider/model/auth application, initialize, and refreshAuth.
    - createAgent.ts:438 starts registerProvidersOntoManager; lines 452-463 create a registered source manager and register providers onto the isolated manager; lines 467-470 mirror the active provider.
    - runtimeContextFactory.ts:187 is the existing `config?: Config;` option; line 199 is the existing `messageBus?: MessageBus;`; lines 482-484 implement `options.messageBus ?? new MessageBus(...)`.
    - configBaseCore.ts:265 declares `getProviderManager(): RuntimeProviderManager | undefined`; configBaseCore.ts:523 declares `getAgentClient(): AgentClientContract` returning `this.agentClient` at line 524.
    - config.ts:192 has private `getAgentClientIfReady()` and line 197 checks `client.isInitialized()`; grep of core config found no public `getMessageBus(` or `Config.isInitialized(` accessor.
    - Behavioral claims hold: fromConfig is specified to adopt the supplied Config, reuse extracted finalizeAgent, delegate settingsService from Config, accept caller messageBus because Config cannot return one, pass Config.getProviderManager() directly into the future structural providerManager seam, skip double-init via a public client-readiness signal, and preserve createAgent's existing construction path.
  - settings-surface.md: PASS.
    - agentImpl.ts already has `deps.config` threaded and uses it in setModel at line 662 (`this.deps.config.initializeContentGeneratorConfig()`).
    - configBase.ts:173 implements `getEphemeralSetting(key)`, normalizing streaming and context-limit on read; configBase.ts:191 implements `setEphemeralSetting(key, value)`, normalizing streaming/context-limit, throwing for invalid streaming at lines 201-208, propagating task-max-async at lines 213-221, and clearing provider caches for auth/base-url/socket/streaming keys at lines 224-262; configBase.ts:265 implements `getEphemeralSettings()` returning the full settings map.
    - Behavioral claims hold: the surface delegates to Config, keeps Config as the single source of truth, does not introduce a parallel store, does not re-normalize in Agent, and does not swallow Config errors.
  - get-current-sequence-model.md: PASS.
    - agent.ts:330 declares `getCurrentSequenceModel(): string | null;` and agentImpl.ts:668-670 is the current stub returning null.
    - createAgent.ts:257 defines `const resolveClient = () => config.getAgentClient();`; createAgent.ts:282 threads it into assembleFacade; createAgent.ts:348 threads it into buildAgent.
    - clientContract.ts:118 declares `getCurrentSequenceModel(): string | null;`; concrete client.ts:653-654 returns `this.currentSequenceModel`.
    - Behavioral claims hold: the pseudocode resolves the client fresh each call through deps.resolveClient, returns null if no client exists, delegates to the bound client's getCurrentSequenceModel, and explicitly forbids caching.
  - client-contract-promotion.md: PASS.
    - clientContract.ts:67 defines `export interface AgentClientContract`.
    - agents core agenticLoop types.ts:27 imports `AgentClientContract` from `@vybestack/llxprt-code-core/core/clientContract.js`.
    - internals.ts:38 exports `{ AgentClient, PostTurnAction }` from `./core/client.js`.
    - index.ts:26-27 exports both `./internals.js` and `./api/index.js`, so the package root already transitively exposes internals and the AgentClient class today.
    - api/index.ts lines 6-14 do not export AgentClientContract today.
    - Behavioral claims hold: the promotion is type-only on the curated API barrel, re-exports the core-owned structural contract, leaves the concrete AgentClient class on internals, and does not rely on the current root class exposure as stable surface.
  - provider-runtime-seam.md: PASS.
    - createAgent.ts:240 is the sessionId property in createAgentRuntimeState, with runtimeId passed into createAgentRuntimeState at line 235 after being threaded into finalizeAgent.
    - agentImpl.ts:267-275 seeds `this.providerState` from `deps.runtimeState` in the constructor.
    - agentImpl.ts:594 returns `this.providerState.provider`; agentImpl.ts:621 assigns `const s = this.providerState` for status and lines 632-639 return the status from that snapshot; agentImpl.ts:648 returns `this.providerState.model`.
    - The prior false getCliRuntimeServices claim is not present. The file now consistently says direct provider/model/status methods read the per-agent providerState snapshot seeded from deps.runtimeState, and explicitly says there is no getCliRuntimeServices call in agentImpl.
    - Behavioral claims hold: adding getRuntimeId as `this.deps.runtimeId` is source-faithful to the threaded runtimeId, and adoption of provider/model/status is described through fromConfig/finalize seeding of runtimeState/config/providerManager rather than through a nonexistent helper call.
  - cli-integration-adapter.md: PASS.
    - AgenticLoop.ts:182 is `constructor(options: AgenticLoopOptions) {`, confirming object-form construction.
    - useAgenticLoop.ts:254-261 constructs `new AgenticLoop({ agentClient: args.agentClient, config: args.config, messageBus: args.messageBus ?? new MessageBus(), interactiveMode: args.interactiveMode ?? false, approvalHandler: args.approvalHandler, displayCallbacks })`.
    - useAppInput.ts:331 passes `config.getAgentClient()` into useGeminiStream.
    - Behavioral claims hold: the parity path uses a real Config and FakeProvider fixture, compares projected public events rather than internal prompt_id/traceId, models Path A as curated-public-surface-only, permits Path B reference AgenticLoop/internals only for the comparison drive, and forbids deep /src paths.
  - provider-manager-seam.md: PASS with one corrected citation-path note.
    - configBaseCore.ts:262 declares `setProviderManager(providerManager: RuntimeProviderManager)` and configBaseCore.ts:265 declares `getProviderManager(): RuntimeProviderManager | undefined`.
    - RuntimeProviderManager.ts:50-67 defines the structural RuntimeProviderManager interface and line 64 is `setConfig(config: Config): void;`.
    - ProviderManager.ts:80 is `export class ProviderManager implements IProviderManager {`, not RuntimeProviderManager, matching the type-safety rationale.
    - runtimeContextFactory.ts:24 imports ProviderRuntimeContext from core today and runtimeContextFactory.ts does not currently import RuntimeProviderManager; P03 must add the type-only import.
    - runtimeLifecycle.ts:29 imports RuntimeProviderManager and line 92 types registerCliProviderInfrastructure's manager parameter as RuntimeProviderManager; line 104 calls `registerProviderManagerSingleton(manager as never, oauthManager)`.
    - runtimeAccessors.ts:29 and runtimeRegistry.ts:24 import RuntimeProviderManager.
    - providerManagerInstance.ts is actually at packages/providers/src/composition/providerManagerInstance.ts, not under src/runtime/composition. At that real path, line 19 imports RuntimeProviderManager, lines 173-176 type configureProviderRuntimeFactories(config, manager: RuntimeProviderManager), line 177 calls config.setProviderManager(manager), and lines 612-614 define registerProviderManagerSingleton(manager: ProviderManager, oauthManager: OAuthManager). The pseudocode's bare `composition/providerManagerInstance.ts:19,175` wording is source-faithful when resolved under packages/providers/src; any `runtime/composition/providerManagerInstance.ts` wording would be a path prefix mistake, but the cited file content exists at the real source path and supports the claim.
    - runtimeContextFactory.ts:135-153 currently types RuntimeActivationBindings registerInfrastructure/linkProviderManager with ProviderManager; the pseudocode explicitly requires widening these internal types to RuntimeProviderManager as part of the seam.
    - runtimeContextFactory.ts:183-205 shows IsolatedRuntimeContextOptions has config/messageBus/prepare and no providerManager option today; lines 482-484 adopt messageBus; lines 502-506 are the single unconditional `new ProviderManager({ runtime, settingsService, config })` construction site today.
    - runtimeContextFactory.ts:315-390 buildActivateClosure casts providerManager to an ad-hoc runtime shape, calls options.prepare with providerManager at lines 369-377, then calls bindings.registerInfrastructure at 380-383 and bindings.linkProviderManager at 385-387.
    - runtimeContextFactory.ts:400-447 buildCleanupClosure resets infrastructure, clears settings runtime context, flushes runtime auth scope, invokes onCleanup with providerManager at line 432, and optionally calls bindings.disposeRuntime at lines 438-440. There is no ProviderManager disposal in cleanup.
    - Behavioral claims hold: the seam needs a structural RuntimeProviderManager option so Config.getProviderManager() can flow in with no assertion, P03 declares/imports the option while P05 implements `options.providerManager ?? new ProviderManager(...)`, cleanup needs no adoption flag because no manager disposal exists today, and the default path still has a single construction site.
- P02a checklist invariants:
  - Shared finalize reuse: PASS. config-injection-seam requires fromConfig to call the extracted shared finalizeAgent, and createAgent source already has a single finalizeAgent/assembleFacade path.
  - Settings delegate to Config with no parallel store: PASS. settings-surface delegates all settings methods to this.deps.config and source Config owns normalization and side effects.
  - No sequence-model caching: PASS. get-current-sequence-model resolves deps.resolveClient fresh each call.
  - AgentClientContract promotion is TYPE-ONLY and class stays internal/power-user: PASS. client-contract-promotion uses export type on api/index.ts and leaves internals.ts:38 unchanged.
  - Real-FakeProvider parity / no mock theater / public-surface-only imports for Path A: PASS. cli-integration-adapter uses a real FakeProvider JSONL fixture, compares projected events, and partitions Path A curated public root from Path B reference drive.
  - CRIT-1 provider seam typed structurally and P03 adds the import: PASS. provider-manager-seam specifies `providerManager?: RuntimeProviderManager`, says runtimeContextFactory.ts does not import the type today, and directs P03 to add a type-only core import.
- Internal consistency / corruption audit:
  - No duplicated corrupt tails found in any of the seven files.
  - Header/body/pseudocode/integration/anti-patterns are internally consistent in all seven files.
  - The three previously reported defects are not re-raised: provider-manager-seam no longer claims RuntimeProviderManager is already imported, no longer contradicts itself about cleanup adoption flags, and provider-runtime-seam no longer claims agentImpl methods resolve through getCliRuntimeServices.
Semantic Assessment: PASS — all seven pseudocode artifacts are source-faithful after holistic audit. Every checked citation matches the real source or is accurately described as future seam work; behavioral claims match the implementation; code fences are balanced; mandatory sections and numbered lines are present; no corrupt/duplicated text remains; and all P02a invariants hold. The only noted path nuance is that providerManagerInstance.ts lives at packages/providers/src/composition/providerManagerInstance.ts; the pseudocode's cited line claims are correct at that real path and do not create a substantive defect.
