Phase: P16a (Contract Promotion Implementation Verification)
Completed: 2026-06-23 08:06
Verifier: typescriptreviewer (independent, clean brief)
Verdict: PASS
Coordinator independent reproduction: PASS (every check re-run with concrete output before AND after the gate)

## Gate Result

Independent verification of the Phase 16 type-only contract promotion PASSED. The verifier's verdict
was corroborated by the coordinator's own independent reproduction of every check (not accepted on
say-so).

## The Change (single additive, type-only)

`packages/agents/src/api/index.ts` line 20:
    export type { AgentClientContract } from '@vybestack/llxprt-code-core/core/clientContract.js';
plus phase markers @plan:PLAN-20260621-COREAPIREMED.P16 / @requirement:REQ-004 / @pseudocode lines 10-15.

## Checks (all GREEN, ground-truthed by coordinator)

1. GREEN flip: `npm run typecheck` EXIT=0, 0 total TS errors. The Phase-15 RED error
   ("has no exported member 'AgentClientContract'") count went 1 → 0. No TS6196 in
   contractPromotion.types.ts (the exported `_AssertContractMembers` alias resolves clean against the
   now-promoted type). Pre-state was exactly 1 TS error, post-state 0 — a precise, measured flip.
2. `export type` (not value), pointing at the CORE module specifier
   `@vybestack/llxprt-code-core/core/clientContract.js` (the same specifier agents already uses in
   10+ files); agents does NOT redefine `interface AgentClientContract` (grep empty).
3. TYPE-ONLY at runtime (REQ-004.2) — proven at the DIST level: a node probe importing the built
   `dist/index.js` (root) and `dist/src/api/index.js` (curated) shows `Object.keys(...)` includes
   'AgentClientContract' = FALSE on BOTH; yet the TYPE IS present in the emitted
   `dist/src/api/index.d.ts:19`. So consumers (#1595) can type against it with ZERO runtime footprint.
4. NO class leak (CRIT-4): the curated `api/index.ts` does not export the concrete `AgentClient`
   class (grep empty). The package ROOT exposes AgentClient only transitively via `src/index.ts:26
   export * from './internals.js'` — intentional/non-breaking, out of scope for the curated barrel.
5. Non-breaking guards GREEN: `nonBreaking.exports.test.ts` 3/3 (Test C still asserts no runtime
   AgentClientContract value — holds precisely because it is `export type`).
6. UNCHANGED files: `git diff HEAD` for `internals.ts` and `src/index.ts` is EMPTY; internals.ts:38
   `export { AgentClient, PostTurnAction } from './core/client.js';` intact. Scope limited to
   api/index.ts (+ the P16.md marker).
7. Marker `.completed/P16.md` exists (74 lines) with real verification output (typecheck EXIT=0 tail
   showing the error gone + vitest 3-pass).

## Outcome

REQ-004 / REQ-004.1 / REQ-004.2 / REQ-006 satisfied: a stable, type-only `AgentClientContract` is now
on the curated public API boundary that #1595 imports from; the concrete `AgentClient` class stays on
`./internals.js` (non-breaking power-user access); nothing else changed. The Phase 15 contract-promotion
TDD is now fully GREEN.
