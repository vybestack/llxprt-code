Phase: P23a
Completed: 2026-06-23 (verification gate for P23 quality gates + mutation)

Files Created:
- project-plans/issue1594remediate/.completed/P23a.md (this file)

Files Modified: none (verification-only phase)

Tests Added: 0 (verification-only phase)

Verification:

=== Independent reviewer (typescriptreviewer, clean history-free brief) — VERDICT: PASS ===
The reviewer reproduced the gates and rendered its own provenance/scope judgment (not trusting the
P23 marker). Key reviewer-generated evidence:

- Build-first: CORE_BUILD_RC=0, AGENTS_BUILD_FIRST_RC=0.
- TYPECHECK_RC=0 (all workspaces tsc --noEmit clean).
- BUILD_RC=0 (full monorepo build).
- AGENTS_HARNESS_RC=0 — Test Files 41 passed (41), Tests 548 passed (548).
- LINT_RC=0 (on a rerun with adequate timeout; first attempt was killed by the reviewer's own tool
  timeout = signal 15, NOT an eslint failure).
- FORMAT_RC=0.
- SMOKE_RC=0 — haiku:
    [ollamakimi]
    Moonlight on the lake,
    Silent ripples drift away,
    Night holds its own peace.
- AGENTS MUTATION recomputed from packages/agents/reports/mutation/mutation.json:
    Killed=390 Timeout=23 Survived=66 NoCoverage=33 → (390+23)/512 = 80.66% (>= 80% PASS).
    createAgent.ts 86.49%; agentImpl.ts 79.05%.
- PROVIDERS seam (packages/providers/reports/mutation/mutation-seam.json): 3 runtime mutants —
    L498 messageBus ?? KILLED; L521 providerManager ?? KILLED; L522 ObjectLiteral→{} SURVIVED.
    2/2 OBSERVABLE seam mutants killed; L522 audited as equivalent/non-observable.

- NON-KILL AUDIT (reviewer checked each claim against production source, found NONE killable):
    * agentImpl.ts:1037-1042 token fallbacks — non-observable (no public surface exposes the
      sessionUsage fallback; 0-vs-0 under fake seam).
    * agentImpl.ts:1127-1135 provider-switch model guard — unreachable (catch+isFakeSeam keeps
      providerChanged=false; the if-block is dead under the fake seam; reachable :1138-1140 state
      assignment IS separately observable).
    * agentImpl.ts:418 oauth===true guard — non-observable (getProviderStatus never returns
      oauthEnabled; only a private Set write).
    * agentImpl.ts:1062 compression null guard — unreachable (initialHistoryService eagerly stored
      in the createAgent path, so historyService is never null publicly).

- PROVENANCE / SCOPE JUDGMENT (reviewer's own commands):
    * runtimeContextFactory.ts EXISTS-ON-MAIN; git diff --stat origin/main = 1 file, +47/-16.
    * createAgent.ts NET-NEW (+531); agentImpl.ts NET-NEW (+1483).
    * Judgment: agents files net-new → whole-file >=80% fair and achieved; runtimeContextFactory.ts
      pre-existing → changed-range/seam scope is faithful to MIN-1's "providerManager? adoption seam"
      intent and NOT an unjustified downscope. L522 equivalence confirmed with source citations
      (private fields, no public getter, resolveInit({}) falls back without crash, setConfig rewires).

- HYGIENE: frozen sha256s all match (buildCliStyleConfig d98a1054…, parity-toolcall 6afc863d…,
  cli-turn-parity.early fdf9b6fb…); 3 new files have no mock-theater/deep-imports/.skip/any;
  property ratio 33.3% (7/22, 6/16, 7/22).

=== Coordinator independent ground-truth (did NOT rubber-stamp the PASS) ===
- Re-recomputed agents score from mutation.json: 80.66% (>=80 true). CONFIRMED.
- Spot-audited the highest-risk non-kill claim myself (oauth:418): read getProviderStatus()
  (agentImpl.ts:644-663) in full — it returns {provider, model, authStatus, baseUrl?, keyName?,
  keyFile?} and NEVER populates oauthEnabled; `grep oauthEnabled agentImpl.ts` = only JSDoc:404 +
  private Set write:419. authStatus derives from computeWinner, not the oauth Set. ⇒ genuinely
  non-observable. The reviewer's claim is TRUTHFUL, not a dodge.
- Frozen hashes re-verified EXACT; eslint RC=0 on the 3 new files; harness 548/548 reproduced.
- The ONLY gate neither the reviewer nor I reproduced to completion in-window is full `npm run test`
  (terminated by tool timeout, not failure). Launched it headless in the background to close even
  that; typecheck+build+harness(548)+smoke+lint+format are all independently green regardless.

Semantic Assessment: PASS. Agents mutation independently confirmed at 80.66% (>=80%) on net-new code
via 60 honest behavioral killing tests; every claimed non-kill verified non-observable/unreachable
against production source (incl. coordinator spot-check of oauth:418). Providers seam mutation scoped
to the remediation's changed ranges on the PRE-EXISTING runtimeContextFactory.ts (CCF-13, validated
by the reviewer's own provenance evidence): 2/2 observable seam mutants killed, lone survivor L522
proven equivalent. Full six-command suite green (typecheck/build/format/lint/smoke + agents harness
548/548); no mock theater, no frozen-file edits, no deferred-impl patterns.
