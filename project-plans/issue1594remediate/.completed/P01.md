Phase: P01 (remediated after P01a verification — one VALID defect fixed)
Completed: 2026-06-22 20:53
Files Created: none
Files Modified: project-plans/issue1594remediate/analysis/domain-model.md (+5/-3 net; §1.3 entity list +3/-1, §7 footnote +3/0; file now 289 lines; untracked so no git-numstat baseline — counts are vs. the pre-remediation draft)
Tests Added: 0
Verification:
```
$ test -f project-plans/issue1594remediate/analysis/domain-model.md || { echo FAIL; exit 1; }
$ for r in REQ-001 REQ-002 REQ-003 REQ-004 REQ-005 REQ-006 REQ-007 REQ-INT-001 REQ-INT-002 REQ-INT-003 REQ-INT-004; do
    grep -q "$r" project-plans/issue1594remediate/analysis/domain-model.md || { echo "MISSING $r"; exit 1; }
  done
$ for t in T1 T2 T3 T4 T5 T6 T7 T8 T9 T10 T11; do
    grep -q "| $t " project-plans/issue1594remediate/analysis/domain-model.md || { echo "MISSING harness $t"; exit 1; }
  done
$ grep -q "@plan:PLAN-20260621-COREAPIREMED.P01" project-plans/issue1594remediate/analysis/domain-model.md || { echo "MISSING plan marker"; exit 1; }
$ echo "OK"
OK
```
messageBus §1.3 confirmation:
```
$ grep -n "messageBus" project-plans/issue1594remediate/analysis/domain-model.md | head -5
42:`{ config, messageBus?, onApproval?, onOAuthPrompt?, editorCallbacks?, toolSchedulerFactory?, sessionId? }`.
43:The `config` field is the adopted `Config`. `messageBus?` is the caller-supplied SHARED
44:`MessageBus` (CRIT-2), adopted via the existing `IsolatedRuntimeContextOptions.messageBus?` seam
...
```
Semantic Assessment: VALID defect fixed — §1.3 FromConfigOptions entity list now includes `messageBus?` (CRIT-2, caller-supplied SHARED bus via IsolatedRuntimeContextOptions.messageBus?, single-bus fallback documented), resolving the §1.3↔§2/§3.1 internal inconsistency; REQ-007/REQ-INT-004 n/a coverage-map rows are intentional-for-docs/static-boundary (one-line clarifier footnote added under §7 so a future verifier does not mis-flag them); no production/source code touched.
