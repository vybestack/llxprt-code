Phase: P11
Completed: 2026-06-22 20:37
Files Created: [packages/agents/src/api/__tests__/agent.settings.behavior.test.ts (302 lines)]
Files Modified: [none]
Tests Added: 16
Verification:
```
# Static gates
behavioral asserts present (toBe/toEqual/toThrow): OK
mock theater (toHaveBeenCalled): NONE
reverse (toThrow('NotYetImplemented') | not.toThrow()): NONE
deferred/scan scoped: PASS — no TODO/FIXME/HACK/XXX/TEMPORARY/WIP/placeholder/"for now"/"in a real"/"coming soon"
reverse/weakened test patterns: NONE
skipped/disabled (it.skip/xit/xdescribe): NONE
any/as casts: NONE (grep " as [A-Z]|: any\b|<any>" → no matches)

# Property-based ratio (plan algorithm)
TOTAL it/test = 16
PROP_CASE_FORMS (it.prop/test.prop) = 0
CLASSIC_PROP_BLOCKS (it/test blocks containing fc.assert|fc.property) = 5
property-based CASES: 5 / 16 = 31% (>= 30% PASS)

# Builds
npm run build --workspace @vybestack/llxprt-code-core → CORE BUILD OK
npm run build --workspace @vybestack/llxprt-code-agents → AGENTS BUILD OK

# RED enforcement (vitest run, expect non-zero exit, behavioral failure)
npx vitest run packages/agents/src/api/__tests__/agent.settings.behavior.test.ts
STATUS=1
 Test Files  1 failed (1)
      Tests  15 failed | 1 passed (16)
   Duration  2.78s
Caused by: Error: NotYetImplemented
 ❯ AgentImpl.setEphemeralSetting packages/agents/src/api/agentImpl.ts:708:11
 ❯ AgentImpl.getEphemeralSetting packages/agents/src/api/agentImpl.ts:703:11
 (T3 getConfig identity is the 1 GREEN — expected, declared P06 / implemented P09, coverage assertion not the RED driver)
module/compile check (Cannot find module|SyntaxError|Failed to resolve import|ReferenceError): NONE
→ RED confirmed behavioral (ephemeral stubs reach the assertion)

# typecheck
npm run typecheck → TYPECHECK STATUS=0 (all packages: tsc --noEmit clean)
```
Semantic Assessment: A 16-test behavioral RED suite pins the Agent settings/config surface (REQ-002/REQ-INT-003) against a REAL Config wrapped by fromConfig — asserting identity (getConfig === bound Config), get/set round-trip normalization parity with the bound Config (context-limit numeric + numeric-string, streaming='disabled', custom keys), full getEphemeralSettings deep-equality, propagated Config error on setEphemeralSetting('streaming', <non-resolvable>) matching "must resolve", and bidirectional delegation proof (Config→agent and agent→Config, no parallel store). Five fast-check property cases (31% of tests) generalize the round-trip, settings-map, context-limit, streaming-mode, and no-parallel-store invariants. The suite is RED overall (15/16 fail) for the correct behavioral reason — the P10 ephemeral stubs throw NotYetImplemented at agentImpl.ts:703/708, reaching the assertions — and contains no mock theater, no reverse tests, no banned literals, no skips, and zero any/as casts. typecheck is exit 0.
