Phase: P16
Completed: 2026-06-22 20:02

Files Created: none

Files Modified:
- packages/agents/src/api/index.ts (+5/-0 attributable to P16: 4 JSDoc marker lines +
  1 `export type { AgentClientContract }` re-export line. Full working-tree diff vs HEAD
  shows +6/-0 because the working tree also carried pre-existing uncommitted P06 work
  — the `@plan:PLAN-20260621-COREAPIREMED.P06` marker and `export { fromConfig }` line —
  which are NOT part of this phase.)

Tests Added: 0 (Phase 16 is implementation-only; the Phase 15 test files
contractPromotion.types.ts and nonBreaking.exports.test.ts were already authored and
are turned GREEN by this change — they were NOT modified.)

Verification:

1) GREEN flip — typecheck (the authoritative P16 gate; captured without a pipeline so
   $? is tsc's real code):

   $ set +e; npm run typecheck > /tmp/p16-tc.log 2>&1; TC=$?; set -e; tail -20 /tmp/p16-tc.log; echo "EXIT=$TC"
   > @vybestack/llxprt-code-providers@0.10.0 typecheck
   > tsc --noEmit
   > @vybestack/llxprt-code-agents@0.10.0 typecheck
   > tsc --noEmit
   > @vybestack/llxprt-code@0.10.0 typecheck
   > tsc --noEmit
   > @vybestack/llxprt-code-a2a-server@0.10.0 typecheck
   > tsc --noEmit
   > @vybestack/llxprt-code-test-utils@0.10.0 typecheck
   > tsc --noEmit
   EXIT=0

   Previously-RED error is GONE: grep for
   "has no exported member 'AgentClientContract'" / TS6196 / any AgentClientContract
   missing-member line in /tmp/p16-tc.log returns NO matches (grep rc=1). The full log
   (64 lines) is every workspace's `tsc --noEmit` banner with zero diagnostic output.
   Pre-state was exactly 1 TS error (the AgentClientContract TS2305 in
   contractPromotion.types.ts); post-state is 0.

2) Runtime guards — vitest:

   $ npx vitest run packages/agents/src/api/__tests__/nonBreaking.exports.test.ts
   ✓ packages/agents/src/api/__tests__/nonBreaking.exports.test.ts (3 tests) 1ms
    Test Files  1 passed (1)
         Tests  3 passed (3)
   All 3 pass. Test C (REQ-004.2) still asserts NO runtime AgentClientContract value
   on the curated barrel — holds because the promotion is `export type` (erasable).

3) Type-only proof — no runtime value named AgentClientContract on the built root:

   $ node -e "const r=require('./packages/agents/dist/src/index.js'); console.log(Object.keys(r).includes('AgentClientContract'))"
   false
   Root barrel has 125 runtime keys; AgentClientContract is absent; AgentClient class
   is still present (typeof === 'function', sourced from internals — unchanged).

4) No class leak — `grep -nE "export \{[^}]*\bAgentClient\b[^C]" packages/agents/src/api/index.ts`
   → EMPTY (grep rc=1). The curated barrel does not export the AgentClient class.

5) Re-export (not redefine) — `grep -n "interface AgentClientContract" packages/agents/src/api/index.ts`
   → EMPTY (grep rc=1). The contract is re-exported from core, not redefined.

6) internals unchanged — `grep -q "export { AgentClient, PostTurnAction } from './core/client.js'" packages/agents/src/internals.ts`
   → MATCH. Root index.ts still `export * from './api/index.js'` → MATCH.

7) Deferred-impl scan — `git diff HEAD -- packages/agents/src/api/index.ts | grep -E "^\+" | grep -nE "(TODO|FIXME|HACK|STUB|XXX|TEMPORARY|WIP)"`
   → EMPTY (grep rc=1). No deferred-implementation markers in changed lines.

Build prerequisites (agents resolves through gitignored dist/):
- npm run build --workspace @vybestack/llxprt-code-core  → Exit 0
- npm run build --workspace @vybestack/llxprt-code-agents → Exit 0

Semantic Assessment: Added a single type-only `export type { AgentClientContract } from '@vybestack/llxprt-code-core/core/clientContract.js'` re-export to the curated agents API barrel (packages/agents/src/api/index.ts), plus the @plan/@requirement/@pseudocode markers. This promotes the core-owned stable client contract TYPE onto the #1595-facing public surface as an erasable, type-only export (REQ-004 / REQ-004.2), flips the Phase 15 RED (TS2305 missing-member) to GREEN (typecheck EXIT=0), and is fully additive/non-breaking (REQ-006): the concrete AgentClient class stays on internals.js, no runtime value named AgentClientContract is introduced, and all 3 nonBreaking.exports runtime guards plus the 125-key root snapshot remain GREEN.
