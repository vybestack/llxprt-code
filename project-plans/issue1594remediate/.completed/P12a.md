# P12a — Settings Surface IMPL Verification (pseudocode-compliance gate)

**Phase:** P12a (independent verifier gate for P12 settings-surface implementation)
**Plan:** PLAN-20260621-COREAPIREMED
**Verifier:** typescriptreviewer (gpt55high), clean/no-history brief
**Verdict:** **PASS**
**Date:** 2026-06-23

## Compliance table (pseudocode `analysis/pseudocode/settings-surface.md`)

| Pseudocode lines | Implemented at (file:line) | Matches? |
|---|---|---|
| 10-12 getConfig identity (PRECONDITION — exists exactly once, not re-added) | packages/agents/src/api/agentImpl.ts:697 | YES |
| 20-22 getEphemeralSetting delegate | packages/agents/src/api/agentImpl.ts:702 | YES |
| 30-33 setEphemeralSetting delegate (no catch) | packages/agents/src/api/agentImpl.ts:707 | YES |
| 40-42 getEphemeralSettings delegate | packages/agents/src/api/agentImpl.ts:712 | YES |

## Verification command results (build-first)

- `npm run build --workspace @vybestack/llxprt-code-core` → exit 0
- `npm run build --workspace @vybestack/llxprt-code-agents` → exit 0
- `npx vitest run packages/agents/src/api/__tests__/agent.settings.behavior.test.ts` → **Test Files 1 passed (1), Tests 16 passed (16)**, exit 0
- `npx vitest run packages/agents/src/api/__tests__/` → **Test Files 30 passed (30), Tests 448 passed (448)**, exit 0
- `npm run typecheck` → exit 0 (all workspace tsc --noEmit clean)
- `npm run lint` → exit 0 (eslint . + eslint integration-tests clean)
- `grep -q "@pseudocode" agentImpl.ts` → present (exit 0)
- getConfig impl count → **1**
- parallel-store guard → no matches
- deferred-marker changed-line scan → no matches

## Semantic checklist

- All settings tests pass; full agents api suite green — **YES** (16/16; 448/448)
- Pure delegation; no parallel store; no re-normalization; errors propagate — **YES**
- lint clean (exit 0) and typecheck clean (exit 0) — **YES**
- No deferred-implementation patterns in changed lines — **YES**

## Deviations

**NONE.** The three ephemeral settings methods are pure one-line delegates to `this.deps.config`; `setEphemeralSetting` has no try/catch and lets Config errors propagate; no parallel settings store; no re-normalization; `getConfig` exists exactly once returning `this.deps.config`.

## Notes — verifier loop (FAIL#1 → root-cause fix → PASS)

The first P12a verification reported FAIL, but every substantive compliance check passed; the sole basis was two full-suite **timeouts** (`core-tools.spec.ts` T2p and `fromConfig.behavior.test.ts` PROP6) observed at the built-in 5000ms default.

Root-cause (ground-truthed, NOT dismissed as "flaky"): both are heavy `fc.asyncProperty` tests that drive a full `drain(agent.stream(...))` turn per generated case. In isolation they pass comfortably (T2p ~1.9s, PROP6 ~1.6s), but under full-directory parallel load — and when the run resolves Vitest's built-in 5000ms instead of the agents package `testTimeout: 30000` — the heavy properties can exceed the budget.

Fix (matches the established EP-slice precedent `}, 30000)` in `cli-turn-parity.early.spec.ts:177/223/270`): added explicit per-test timeouts to the three heavy turn-driving property tests, making them robust regardless of which vitest config loads:
- `fromConfig.behavior.test.ts` PROP6 → `}, 30000)` (line 465)
- `fromConfig.behavior.test.ts` PROP7 → `}, 30000)` (line 490)
- `core-tools.spec.ts` T2p → `}, 30000)` (line 387) — NOT frozen; belongs to PLAN-20260617-COREAPI but a safe, non-behavioral robustness hardening

These are timeout-only changes; no assertion, logic, or generator was altered. Post-fix: full agents api suite **448/448 across 3 consecutive runs**, typecheck exit 0, full repo lint exit 0. Clean re-review returned PASS.
