Phase: P17a (Provider-Runtime Reachability Seam — Behavioral TDD Verification Gate)
Completed: 2026-06-23 08:24
Files Created: (none — verification gate; the verified deliverable is
  packages/agents/src/api/__tests__/runtimeSeam.behavior.test.ts, 160 lines, from P17)
Files Modified: (none — gate phase)
Tests Added: 0 (verifies P17's 5 tests: T6a, T6b, T6c, PROP-A, PROP-B)

Verification: Independent verification reproduced every gate check (build-first core+agents, then the
phase spec's Verification Commands block and Deferred Implementation Detection block, plus the 17a
Semantic Verification Checklist). All substantive checks PASS:

  - RED-for-the-right-reason: `npx vitest run …/runtimeSeam.behavior.test.ts` → EXIT non-zero,
    "Tests 2 failed | 3 passed (5)". The 2 failures are T6a and PROP-A, both failing with
    `TypeError: asWithRuntimeId(...).getRuntimeId is not a function` (the absent public method,
    added in P18). No Cannot find module / SyntaxError / ReferenceError setup error; no unrelated
    "is not a function". The 3 passes are T6b (adopted provider/model), T6c + PROP-B (no-2nd-manager
    identity) — forward guards that already hold post-P09.
  - no-2nd-manager identity: T6c (lines 113-115) and PROP-B (lines 154-155) assert
    `captureProviderManager(agent)` IS the SAME instance as `config.getProviderManager()` /
    `built.config.getProviderManager()` (the real public Config accessor, configBaseCore.ts:265,
    same plain idiom as sibling fromConfig.behavior T6). captureProviderManager (lines 51-57)
    structurally probes the real agent `providerManager`/`manager` field — NOT a spy/mock.
  - providers reflect adopted runtime: T6b (lines 97-98) asserts
    `agent.getProvider() === 'fake'` and `agent.getModel() === 'fake-model'`.
  - property-based CASES: 2 / 5 = 40% (it.prop/test.prop=0, classic fc.assert blocks=2)
    [>=30% PASS, MIN-2 PASS]. PROP-A (line 123) and PROP-B (line 143) both use
    `fc.assert(fc.asyncProperty(fc.string({ minLength: 1, maxLength: 40 }), …))`.
  - Fraud scan: no `toHaveBeenCalled`; corrected root-manager guard (CCF-7) finds no agent-root
    accessor and no non-config `.getProviderManager` receiver (only `config.getProviderManager()`
    is used, which is REQUIRED); Deferred Implementation Detection → PASS (no TODO/FIXME/HACK/
    placeholder/"for now"/"in a real"; no `.not.toBeDefined`; no `toThrow(NotYetImplemented)`;
    no it.skip/xit/xdescribe).
  - Boundary: the ONLY new (untracked) file for this phase is runtimeSeam.behavior.test.ts;
    `getRuntimeId` is ABSENT from both packages/agents/src/api/agent.ts and agentImpl.ts (grep → none);
    the working-tree diff of those two files contains ZERO added `getRuntimeId` lines (the `M` status
    on them is pre-existing uncommitted P09/P12/P14 work — getConfig/getEphemeralSetting(s)/
    providerManager+runtimeId fields/getCurrentSequenceModel — NOT part of P17).
  - Marker: .completed/P17.md exists with real verification output.

Semantic Assessment: PASS — runtimeSeam.behavior.test.ts is a genuine behavioral RED suite for the
not-yet-existent public Agent.getRuntimeId() (REQ-005.1) and the single-ProviderManager adoption seam
(REQ-001/REQ-005). T6a/PROP-A fail for the correct behavioral reason (method absent until P18);
T6b/T6c/PROP-B already pass as forward adopt-path guards over the real provider seam; >=30%
property-based (40%, 2 cases); identity asserted against the real Config manager via the plain
config.getProviderManager() idiom (not the banned agent-root accessor, not a mock); no reverse tests;
production boundary clean for a RED phase. Verified independently across two clean-brief verifier runs
(agent IDs typescriptreviewer-s35o4n and -3sohzg); the only non-substantive FAIL signals from those
runs were (1) a coordinator brief that mis-specified the boundary as `git diff HEAD` EMPTY — invalid
on this multi-phase uncommitted branch, re-confirmed PASS under the correct (a)/(b)/(c) criteria —
and (2) the absence of THIS P17a marker, which is the post-gate artifact created here. Gate: PASS.
