Phase: P19a
Completed: 2026-06-22 09:52
Files Created: (none — verification gate)
Files Modified:
  - project-plans/issue1594remediate/plan/19a-parity-harness-tdd-verification.md (+9/-4) — CCF-8 gate-bug fix (two defects in the GATE itself, not the verified-correct P19 specs):
    (1) internal-field grep made comment-aware (was blindly matching an explanatory comment that NAMES the projected-away fields — the blessed frozen early slice carries the identical comment at cli-turn-parity.early.spec.ts:204; intent is "must not COMPARE internal fields" in CODE, not prose);
    (2) property-ratio numerator (PROP_CASE_FORMS + CLASSIC_PROP_BLOCKS) realigned to scan ALL THREE specs (matching TOTAL and the P19 authoring gate) — it previously excluded config-injection.spec.ts from the numerator while counting it in TOTAL, understating 5/15=33% as 3/15=20%.
Tests Added: 0 (verification-only phase; P19 added 15)
Verification: |
  Independent reviewer (typescriptreviewer-pb6ye3) returned FAIL on 3 defects; coordinator ground-truthed EACH against source:

  DEFECT #1 (prompt_id|traceId at cli-turn-parity.spec.ts:176) — GATE BUG, not a spec defect.
    - The hit is an EXPLANATORY COMMENT ("// ... Internal fields (prompt_id, traceId) are [projected away]"), NOT a field comparison.
    - The blessed FROZEN P07 early slice carries the IDENTICAL comment at cli-turn-parity.early.spec.ts:204.
    - The real projection logic lives in the frozen projectToComparable helper (extracts public fields only).
    - This is the CCF-7 pattern (a blunt gate grep catching legitimate prose). Rewording the spec to dodge it = obfuscation (must reject). FIX = make the gate grep comment-aware. Re-run: PASS (only comments mention the names).

  DEFECT #2 (property ratio 20% < 30%) — GATE BUG (numerator/denominator spec-list mismatch).
    - Smoking gun: P19a numerator scanned only {cli-turn-parity, settings-surface}; TOTAL scanned all three. config-injection.spec.ts has 2 GENUINE fast-check properties (verified: it()=6, fc-blocks=2), counted in TOTAL but not the numerator -> 3/15=20%.
    - The P19 authoring gate (plan/19 line 124 SPECS) correctly scans all three -> 5/15=33%.
    - FIX = align numerator to all three specs. Re-run: 5/15 = 33% PASS.

  DEFECT #3 (early slice frozen-integrity unprovable from P19-frozen-hashes.txt) — INVALID.
    - The early slice is frozen by P07, NOT P19. P07-frozen-hashes.txt records: fdf9b6fbff4658ba3ca2b0cdf9d951bf8e3116422c5539bff213e5b1c26b063b  cli-turn-parity.early.spec.ts. Live file sha256 = fdf9b6fb... — EXACT MATCH. Properly accounted for and untouched. ("untracked" = known all-uncommitted-branch state.) Dismissed (my brief mis-pointed the verifier at the wrong snapshot file).

  Full corrected P19a Verification Commands block re-run verbatim:
    [A] internal-field (comment-aware): PASS (only comments mention names; no code compare)
    [B] property-based CASES: 5 / 15 = 33% (it.prop/test.prop=0, classic-blocks=5) -> PASS >=30%
    [C] no deep imports; no mock theater; object-form AgenticLoop; NOT positional; agentClient field present -> all PASS
    [D] P19-frozen-hashes.txt present; recorded harness_result=pass (consistent with re-run)
    [E] build core=0, build agents=0, vitest: Test Files 3 passed (3) | Tests 15 passed (15)

  Frozen integrity (coordinator-recomputed sha256):
    buildCliStyleConfig.ts = d98a1054e2d3451a5174b3dea7405283b02df817018cf2b165eaaaa292eb13c2 (matches P19-frozen-hashes.txt) — UNCHANGED
    parity-toolcall.jsonl  = 6afc863dc4f1fb58b4882c1c730bfca5f357316590c3809c6c5e52ef45a8516a (matches) — UNCHANGED
    cli-turn-parity.early.spec.ts = fdf9b6fb... (matches P07-frozen-hashes.txt) — UNCHANGED

  Pseudocode line-by-line compliance (cli-integration-adapter.md), reviewer-confirmed:
    10-16 adopt + one done           -> config-injection.spec.ts:53-74   MATCH
    20-25 runtime reuse (identity)   -> config-injection.spec.ts:80-92   MATCH
    30-34 ownership contrast         -> config-injection.spec.ts:98-125  MATCH (half-A supplied-Config-usable; half-B createAgent owns+disposes via real pre/post probe)
    40-51 turn-drive parity (A vs B) -> cli-turn-parity.spec.ts:61-80,152-190  MATCH (object-form AgenticLoop, independent configs, toStrictEqual, read_file + single-done anchors)
    60-68 settings normalization     -> settings-surface.spec.ts:40-108  MATCH (numeric 1000, streaming enum, invalid throws /must resolve/, deep-equal)
    70-77 property round-trip        -> all three (5 real fc properties)  MATCH
    80-84 boundary scan as a test    -> cli-turn-parity.spec.ts:238-279  MATCH (executed test; forbids core/src,providers/src,/internals.js; requires >=1 public-root import)
Semantic Assessment: PASS — Holistic adequacy (reviewer-confirmed): this harness CANNOT have been written without the new public seams (depends on fromConfig adoption of a caller-supplied CLI-style Config, public agent.stream over it, getConfig/getRuntimeId/getProvider/getModel, ephemeral settings get/set/get-all, public AgenticLoop+mapLoopStream, and Config.getAgentClient()/AgentClientContract for the reference drive). It drives REAL FakeProvider-backed turns end-to-end through the PUBLIC API only (no mocks; real JSONL fixtures). All 15 tests genuinely behavioral (T10 toStrictEqual parity + read_file/single-done anchors; T6 provider/model parity; T7 real dispose probe; T8 mirrors P12; 5 real properties). The reviewer's FAIL was rooted entirely in two bugs in the P19a GATE itself (CCF-8) plus one invalid finding (wrong frozen-snapshot baseline); the verified-correct P19 specs are UNCHANGED and the corrected gate passes end-to-end. harness_result=pass -> P20 is a no-op verification.
