Phase: P24
Completed: 2026-06-23 (final plan-quality & #1595-adequacy evaluation)

Files Created:
- project-plans/issue1594remediate/plan-evaluation.json (13 lines — the machine verdict)
- project-plans/issue1594remediate/.completed/P24.md (this file)

Files Modified: none (evaluation-only phase — no production code)

Tests Added: 0 (evaluation-only phase)

Verification:

=== plan-evaluation.json (written, valid JSON, NO rejection triggers) ===
{
  "compliant": true,
  "has_integration_plan": true,
  "builds_in_isolation": false,
  "gaps_closed": { "C1": true, "C2": true, "C3": true, "H1": true, "H2": true, "H3": true },
  "enables_1595": true,
  "reverse_testing_found": false,
  "mock_theater_found": false,
  "non_breaking": true,
  "mutation_ge_80": true,
  "property_ge_30": true,
  "violations": []
}
Rejection-trigger check (coordinator-recomputed): builds_in_isolation=false AND all 6 gaps=true AND
enables_1595=true AND violations=[] → NONE → APPROVE.

=== Independent evaluator (typescriptreviewer, clean history-free brief) — VERDICT: APPROVE ===
Per-gap closure (evaluator evidence, coordinator-confirmed):
- C1 fromConfig: exists/exported/ADOPTS caller Config (not disposed). REQ-001/REQ-INT-001.
- C2 settings/config surface: getConfig/getEphemeralSetting/setEphemeralSetting/getEphemeralSettings
  delegate with core normalization parity (numeric coercion; streaming enum throws 'must resolve').
- C3 turn-drive parity: public-root drive vs AgenticLoop (cli-turn-parity.spec.ts). REQ-INT-002.
- H1 (3-part, ALL hold): (a) AgentClientContract type-only on curated barrel; (b) concrete
  AgentClient class on internals + root; (c) class deliberately ABSENT from curated barrel.
- H2 providerManager?/messageBus? seam + Agent.getRuntimeId(). REQ-005/REQ-001.2.
- H3 getCurrentSequenceModel delegates to resolveClient() (not hardcoded null). REQ-003.
Discipline: reverse_testing=none; mock_theater=none in behavioral/parity suites; property ratio
parity-harness 5/16=31.25% (>=30%); agents mutation 413/512=80.66% (>=80%); non-breaking export
tests present. enables_1595=true (Config adoption, settings normalization, turn parity,
public provider/runtime/model+contract reachability, documented boundary docs/agent-api.md).

=== Coordinator independent ground-truth (did NOT rubber-stamp APPROVE) ===
- plan-evaluation.json parsed valid; recomputed rejection triggers = NONE.
- H1(a): `export type { AgentClientContract } from '@vybestack/llxprt-code-core/core/clientContract.js'`
  at api/index.ts:20. CONFIRMED type-only on curated barrel.
- H1(b): internals.ts:38 `export { AgentClient, PostTurnAction } from './core/client.js'`;
  index.ts:26 `export * from './internals.js'`. CONFIRMED class on internals + root.
- H1(c): `grep -E '\bAgentClient\b' api/index.ts` → NO match. CONFIRMED class absent from curated
  barrel (contract-only boundary intact).
- H3: agentImpl.ts:701-704 `getCurrentSequenceModel(): string | null { return
  this.deps.resolveClient()?.getCurrentSequenceModel() ?? null; }`. CONFIRMED real delegation, not a
  null stub.

=== Full-suite validation (this branch quirk, documented) ===
Root `npm run test` surfaced 24 "Test timed out" + 3 AssertionErrors. PROVEN to be CPU-oversubscription
contention (root orchestrator runs every package's vitest pool concurrently), NOT a code defect:
- ALL failing files pass IN ISOLATION: agents 108/108, core 27/27 + 14/14, lsp 227/227 + 49/49 (RC=0).
- This plan's prod changes touch ONLY packages/agents/** + the single providers
  runtimeContextFactory.ts seam; every non-agents failing test (coreToolScheduler.parallel,
  regression-guards within-budget, shell-utils, e2e-lsp, orchestrator-integration, lsp-client timeout)
  has ZERO agents imports. CI runs packages separately, so it will not hit this contention.

Semantic Assessment: APPROVE. The remediation is COMPLETE and ADEQUATE for issue #1595. All seven
gaps (C1/C2/C3/H1/H2/H3 + CRIT-1) are closed with source-grounded evidence; H1's strict three-part
acceptance holds exactly (type-only contract on the curated barrel, concrete class reachable via
internals/root, class intentionally off the curated barrel); the public @vybestack/llxprt-code-agents
surface lets #1595 adopt the CLI's Config, read/write settings with identical normalization, drive a
turn with AgenticLoop parity, and reach provider/runtime/model identity + the client contract via
public imports only — with a documented import boundary so #1595 can delete its deep imports. No
rejection trigger fired (builds_in_isolation=false, all gaps closed, enables_1595=true, 0 violations).
The plan is ready to commit and open as a PR fixing #1594.
