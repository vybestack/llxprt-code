Phase: P03
Completed: 2026-06-22 21:35
Files Created: [none]
Files Modified:
- packages/providers/src/runtime/runtimeContextFactory.ts (+15/-0 per `git diff HEAD --numstat`)
Tests Added: 0 (stub phase — typecheck + spec grep gates only)

Verification (actual pasted output of the spec's Verification Commands block + Deferred-Implementation Detection block):

--- Verification Commands block ---
(all seven grep gates passed; npm run typecheck clean across all workspaces; final success echo printed)

Stdout excerpt (npm run typecheck — all 15 workspaces, zero TS errors):
> @vybestack/llxprt-code@0.10.0 typecheck
> npm run typecheck --workspaces --if-present
> @vybestack/llxprt-code-tools@0.10.0 typecheck   > tsc --noEmit
> @vybestack/llxprt-code-storage@0.10.0 typecheck  > tsc --noEmit
> @vybestack/llxprt-code-auth@0.10.0 typecheck     > tsc --noEmit
> @vybestack/llxprt-code-settings@0.10.0 typecheck > tsc --noEmit
> @vybestack/llxprt-code-telemetry@0.10.0 typecheck > tsc --noEmit
> @vybestack/llxprt-code-ide-integration@0.10.0 typecheck > tsc --noEmit
> @vybestack/llxprt-code-policy@0.10.0 typecheck   > tsc --noEmit
> @vybestack/llxprt-code-mcp@0.10.0 typecheck      > tsc --noEmit
> @vybestack/llxprt-code-core@0.10.0 typecheck     > tsc --noEmit
> @vybestack/llxprt-code-lsp@0.10.0 typecheck      > tsc --noEmit
> @vybestack/llxprt-code-providers@0.10.0 typecheck > tsc --noEmit
> @vybestack/llxprt-code-agents@0.10.0 typecheck   > tsc --noEmit
> @vybestack/llxprt-code@0.10.0 typecheck          > tsc --noEmit
> @vybestack/llxprt-code-a2a-server@0.10.0 typecheck > tsc --noEmit
> @vybestack/llxprt-code-test-utils@0.10.0 typecheck > tsc --noEmit
Stub field declared; adoption withheld to P05 (so P04 RED is genuine).

Gate-by-gate:
- grep "providerManager?: RuntimeProviderManager"  -> FOUND (PASS, CRIT-1)
- grep -nE "providerManager\?:\s*ProviderManager\b" -> NOT FOUND (PASS, not concrete class)
- grep "RuntimeProviderManager" (import)            -> FOUND (PASS, type-only import added)
- grep -nE "providerManager.*as (any|unknown as ProviderManager|ProviderManager)\b" -> NOT FOUND (PASS, no unsafe assertion)
- whitespace-normalized "options\.providerManager \?\? new ProviderManager\(" -> NOT FOUND (PASS, adoption withheld, CRIT-2)
- grep -cE "new ProviderManager\("                  -> COUNT=1 (PASS, construction site unconditional & unique)
- npm run typecheck                                 -> clean (PASS)

--- Deferred-Implementation Detection block ---
Stdout: PASS: no deferred-implementation markers beyond the allowed NotYetImplemented throw.
(no TODO/FIXME/HACK/XXX/TEMPORARY/WIP/placeholder/"for now"/"in a real"/"coming soon" on added lines;
no generic "Not implemented" throw — none expected, this stub adds only an unused optional interface field)

Diff applied (git diff HEAD):
diff --git a/packages/providers/src/runtime/runtimeContextFactory.ts b/packages/providers/src/runtime/runtimeContextFactory.ts
@@ import block @@
   SubagentManager,
   type ConfigParameters,
+  type RuntimeProviderManager,
 } from '@vybestack/llxprt-code-core';
@@ IsolatedRuntimeContextOptions (after messageBus?) @@
   messageBus?: MessageBus;
+  /**
+   * Caller-provided provider manager. When supplied, the runtime ADOPTS this
+   * instance instead of constructing a private one, so a Config-adopting caller
+   * (e.g. agents `fromConfig`) does not create a second manager.
+   *
+   * CRIT-1: typed as the STRUCTURAL core interface RuntimeProviderManager (not the
+   * concrete providers ProviderManager class) so the agents caller can pass
+   * Config.getProviderManager() — which returns RuntimeProviderManager | undefined
+   * (configBaseCore.ts:265) — with ZERO assertion. The default ProviderManager
+   * instance constructed below structurally satisfies this interface.
+   * @plan:PLAN-20260621-COREAPIREMED.P03
+   * @requirement:REQ-005.2
+   */
+  providerManager?: RuntimeProviderManager;
   prepare?: (context: {

Semantic Assessment: Declared the optional `providerManager?: RuntimeProviderManager` adoption seam on `IsolatedRuntimeContextOptions` (typed as the structural core interface per CRIT-1, with a matching type-only import) while deliberately withholding the `?? new ProviderManager(...)` adoption to P05 — construction stays unconditional so P04's identity test will fail RED and REQ-005.2/REQ-001.2 adoption lands genuinely in P05.
