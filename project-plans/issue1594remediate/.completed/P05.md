# P05 — Provider-Manager Adoption Seam (CRIT-1 Cross-Package Typecheck Resolution)

- **Phase:** P05
- **Completed:** 2026-06-23T01:30:22Z

## Files Created
- [none]

## Files Modified
From `git diff --numstat HEAD` (added/deleted):

- `packages/providers/src/runtime/runtimeContextFactory.ts` — +32/-12 (P05 CRIT-1 handle-field widening; pre-existing in this branch, untouched by this step)
- `packages/agents/src/api/createAgent.ts` — +3/-3 (import swap + 2 type-site widenings: `finalizeAgent` param line 222, `AssembleFacadeDeps.manager` line 301)
- `packages/agents/src/api/agentImpl.ts` — +2/-2 (import swap + 1 type-site widening: `AgentDeps.providerManager` line 120)

Full `git diff --stat HEAD`:
```
.llxprt/LLXPRT.md                                  |  10 +++--
package-lock.json                                  | Bin 907576 -> 908068 bytes
packages/agents/src/api/agentImpl.ts               |   4 +-
packages/agents/src/api/createAgent.ts             |   6 +--
packages/providers/package.json                    |   1 +
.../providers/src/runtime/runtimeContextFactory.ts |  44 +++++++++++++++------
6 files changed, 44 insertions(+), 21 deletions(-)
```

## Tests Added
- 0 — implementation phase (type-only widening, no behavior change)

## Verification

### 1. `npm run typecheck` — CLEAN (zero errors)
Ran across all 15 workspaces (`@vybestack/llxprt-code-tools`, `storage`, `auth`, `settings`,
`telemetry`, `ide-integration`, `policy`, `mcp`, `core`, `lsp`, `providers`, `agents`, root,
`a2a-server`, `test-utils`). Exit code 0, stderr empty. No TS2345 / TS-errors of any kind.
The CRIT-1 blocker —
`packages/agents/src/api/createAgent.ts(191,5): error TS2345: Argument of type
'RuntimeProviderManager' is not assignable to parameter of type 'ProviderManager'.` — is RESOLVED.

### 2. P04 suite — `providerManagerAdoption.behavior.test.ts` — 10 GREEN
```
✓ src/runtime/__tests__/providerManagerAdoption.behavior.test.ts (10 tests) 46ms
```

### 3. Full providers runtime suite — 341 GREEN (31 files)
```
Test Files  31 passed (31)
     Tests  341 passed (341)
  Duration  5.52s
```
(Includes the P04 adoption suite, profileApplication, runtimeLifecycle, runtimeAccessors,
providerSwitch, messageBus wiring, and all other runtime specs.)

### 4. CRIT-1 gate on `runtimeContextFactory.ts` — PASS (unchanged)
- Construction count = 1: `new ProviderManager(` appears exactly once (line 522). ✓
- `providerManager` typed `RuntimeProviderManager` on the adoption option (line 214),
  the handle interface fields (lines 218, 226), and the context return type (line 243). ✓
- No unsafe cast ADDED by P05: line 373 (`as unknown as { runtime?: ProviderRuntimeContext }`)
  and line 92 (`as unknown as Pick<...>`) are pre-existing access/config shims, unrelated to the
  manager typing and not introduced by this widening. No `as ProviderManager`, `as any`, or
  `as unknown` cast was added. ✓

### 5. Agents package build
Covered by `npm run typecheck` (`@vybestack/llxprt-code-agents` workspace: `tsc --noEmit` clean).
No `packages/agents/src/api/**/*.test.ts` unit-test files exist, so there is no narrow
createAgent/agentImpl suite to run; typecheck-green is the primary gate per plan.

### 6. Lint — CLEAN
`npx eslint packages/agents/src/api/createAgent.ts packages/agents/src/api/agentImpl.ts`
→ exit 0, no output. The now-dead `import type { ProviderManager } from
'@vybestack/llxprt-code-providers'` lines were removed in both files (replaced by the
`RuntimeProviderManager` type-only import); `createProviderManager` (value import) was kept
as it is still used at createAgent.ts:452.

## Semantic Assessment
Adoption seam complete; CRIT-1 structural widening lands across factory + agents consumer chain
with zero casts; default path unchanged. The `RuntimeProviderManager` supertype (exported from
core root barrel, index.ts:603) now flows structurally from `runtimeContextFactory` →
`createAgent` (`finalizeAgent` param + `AssembleFacadeDeps`) → `agentImpl` (`AgentDeps`),
matching `Config.getProviderManager(): RuntimeProviderManager | undefined`. The sole runtime
consumer, `this.deps.providerManager.listProviders()` (agentImpl.ts:910), is a
`RuntimeProviderManager` member — provably safe. No `any`/`as` introduced; purely 3 type-site
relaxations + 2 type-only import swaps (the dead `ProviderManager` imports were removed to
satisfy lint, as they had no remaining type references after the widening).
