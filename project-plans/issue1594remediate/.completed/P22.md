Phase: P22
Completed: 2026-06-22 10:34
Files Created: none
Files Modified:
  - docs/agent-api.md (+206/-0; 526 → 732 lines) — ADDITIVE-ONLY: 6 new sections inserted at former line 427 (before "## Runtime vs App-Service"). Zero existing lines deleted/rewritten.
Tests Added: 0 (docs phase)
Verification: |
  Coordinator independently reproduced the spec's Verification Commands:
    test -f docs/agent-api.md -> exists
    symbol-presence loop (all 8 required): fromConfig OK, getEphemeralSetting OK, setEphemeralSetting OK, getEphemeralSettings OK, getConfig OK, getCurrentSequenceModel OK, AgentClientContract OK, getRuntimeId OK
    grep "@vybestack/llxprt-code-agents" -> OK (package name present)
    deep-import scan grep -nE "from '[^']*(/src/|core/src|providers/src)" docs/agent-api.md -> NO matches (PASS: no deep-import examples)
    grep "@plan:PLAN-20260621-COREAPIREMED.P22" -> present, count=6 (one marker per new section)
    additive check: 732 lines (was 526, +206); original headers KEPT (Quick Start, createAgent and AgentConfig, The AgentEvent Union, Recorded Decisions, Power-User Subpath internals.js); top-of-file P28 marker intact.

  Accuracy ground-truth (coordinator READ all 6 new sections in full — non-vacuous, every claim matches the harness/source):
    - "Adopting an existing Config (fromConfig)": `await fromConfig({ config })`; `agent.getConfig() === config` (matches config-injection.spec.ts T1:65); ownership contrast createAgent-owns/disposes vs fromConfig caller-owned-NOT-disposed (matches T7 half-A: config.getEphemeralSettings() still works post-dispose); optional onApproval.
    - "Settings & Config Projection": getConfig() identity; setEphemeralSetting('context-limit','1000')→number 1000; streaming 'enabled' enum; setEphemeralSetting('streaming',123) throws "must resolve"; getEphemeralSettings() deep-equals config snapshot (all match settings-surface.spec.ts T8a-T8d).
    - "Current Sequence Model": getCurrentSequenceModel(): string|null, nullable, reflects rebinds (matches agent.ts:332).
    - "Public Client Contract": import type { AgentClientContract } from '@vybestack/llxprt-code-agents'; concrete AgentClient class stays on /internals.js (matches api/index.ts:20 type-only + internals.ts:38).
    - "Runtime Identity": getRuntimeId(): string read-only (matches agent.ts:338).
    - "Import Boundary for #1595": enumerates the 3 permitted specifiers (root + /app-service.js + /internals.js); the "never deep-import" guidance is DESCRIPTIVE PROSE (not literal `from '...'` lines) so it conveys the forbidden /src/ paths WITHOUT tripping the deep-import grep — correct resolution, not gate-gaming.

  Import provenance verified: the doc's `import { ToolConfirmationOutcome } from '@vybestack/llxprt-code-tools'` MIRRORS the passing harness EXACTLY (config-injection.spec.ts:40/61) — a sibling PUBLIC package, NOT a deep import. All other code-fence imports use only the public root.
Semantic Assessment: PASS — REQ-007 satisfied. docs/agent-api.md now accurately documents every remediation addition (fromConfig + ownership semantics, getConfig/getEphemeralSetting/setEphemeralSetting/getEphemeralSettings projection, getCurrentSequenceModel, public type-only AgentClientContract, read-only getRuntimeId, and the #1595 import boundary) with copy-pasteable PUBLIC-ONLY examples drawn from the passing P19 harness. Additive-only (526→732, all original content + headers preserved); no deep-import examples; 6 plan markers. Every documented symbol exists in code and is exercised by a passing test.
