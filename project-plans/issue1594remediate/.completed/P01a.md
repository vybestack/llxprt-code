Phase: P01a
Completed: 2026-06-22 20:56
Files Created: none
Files Modified: project-plans/issue1594remediate/.completed/P01a.md (overwritten verification marker)
Tests Added: 0
Verification:
```text
Command: for r in REQ-001 REQ-002 REQ-003 REQ-004 REQ-005 REQ-006 REQ-007 REQ-INT-001 REQ-INT-002 REQ-INT-003 REQ-INT-004; do
  grep -q "$r" project-plans/issue1594remediate/analysis/domain-model.md || echo "MISSING $r"
done
grep -cE "R-ADOPT|R-SHAREDFINALIZE|R-CONFIGOWNER|R-DELEGATE|R-IDENTITY|R-SEQMODEL|R-CONTRACT|R-NONBREAK|R-PARITY|R-NODEEP" project-plans/issue1594remediate/analysis/domain-model.md
Stdout: 20
Stderr: (empty)
Exit Code: 0
```
Semantic Assessment: PASS — all 11 requirement identifiers are present, invariant grep count is 20, all 10 named invariants are present and mapped to harness rows, FromConfigOptions now includes messageBus? with the CRIT-2 adoption/fallback semantics required by specification.md:473-477 and 515, and the coverage map includes an intentional-n/a clarifier for documentation/static-boundary rows.

## Evidence Checked

### FromConfigOptions messageBus? fix

```text
41:### 1.3 `FromConfigOptions` (NEW value object — config-injection input)
42-`{ config, messageBus?, onApproval?, onOAuthPrompt?, editorCallbacks?, toolSchedulerFactory?, sessionId? }`.
43-The `config` field is the adopted `Config`. `messageBus?` is the caller-supplied SHARED
44-`MessageBus` (CRIT-2), adopted via the existing `IsolatedRuntimeContextOptions.messageBus?` seam
45-(specification.md:473-477, 515); when omitted, `fromConfig` builds exactly ONE bus from
46-`config.getPolicyEngine()` (Config has no `getMessageBus()` accessor — see §2). Callback fields
```

This is consistent with specification.md:473-477, which defines `readonly messageBus?: MessageBus` as the caller-supplied shared bus forwarded into the runtime context messageBus seam with a fallback bus from `config.getPolicyEngine()`, and specification.md:515, which states the shared bus is supplied via `FromConfigOptions.messageBus`, never read off `Config`.

### Intentional n/a clarifier

```text
266-| REQ-INT-004 | imports | n/a | R-NODEEP | T11 | P07/P19/P21 |
267-
268:> **n/a rows are intentional.** REQ-007 (Documentation) and REQ-INT-004's transition are marked
269-> n/a because they are documentation / static-import-boundary requirements with no runtime entity
270-> state transition or invariant; no synthetic transition or invariant should be inferred.
```

## Semantic Verification Checklist

- All 11 requirements covered with entities + transitions + invariants + harness rows: PASS. Every required identifier appears in the domain model and the coverage map has a row for each of REQ-001 through REQ-007 and REQ-INT-001 through REQ-INT-004. Behavioral requirements have applicable entities, transitions, invariants, and harness rows. REQ-007 is a documentation requirement and its docs/(doc)/P22 coverage is appropriate. REQ-INT-004 is a static import-boundary requirement; its n/a transition is appropriate and its invariant/harness coverage is R-NODEEP/T11. These n/a entries satisfy the checklist because no runtime transition or synthetic invariant applies.
- All 10 named invariants present, each mapped to >=1 harness row: PASS. R-ADOPT, R-SHAREDFINALIZE, R-CONFIGOWNER, R-DELEGATE, R-IDENTITY, R-SEQMODEL, R-CONTRACT, R-NONBREAK, R-PARITY, and R-NODEEP are present in Section 4, and each includes a Testable T-row reference. The verification grep returned count 20, reflecting definitions plus coverage-map references.
- Ownership distinction (adopted vs constructed Config) is unambiguous: PASS. Section 1.2 distinguishes Config constructed by createAgent as Agent-owned/disposed from Config supplied to fromConfig as caller-owned/not disposed; R-CONFIGOWNER and §3.1 preserve that distinction for Config and caller-supplied bus/manager ownership.
- No implementation code leaked into analysis: PASS. The document contains entity definitions, relationships, state-transition prose, API names, and pseudocode-style transition sketches, but no production implementation body.
- Coverage-map phase references match plan/00-overview.md (contiguous; P03-P16): PASS. The domain model's coverage map references phases that exist in plan/00-overview.md and align with the authoritative REQ-to-phase mapping. The implementation-relevant worker coverage includes the contiguous P03-P16 range, and later documentation/parity/boundary phases P17-P22 are valid overview-mapped continuations rather than mismatches.

## Holistic Assessment

The domain model describes the remediation as an additive public Agent API model built around existing core and providers runtime pieces. It identifies the public Agent facade, adopted Config, FromConfigOptions, promoted AgentClientContract, isolated runtime context handle/options, and existing engine collaborators. It then connects those entities through the two bootstrap paths, createAgent and fromConfig, showing how both converge on the same finalize/facade path while differing only in whether Config/runtime dependencies are constructed or adopted. It also models settings delegation, getConfig identity, current-sequence-model delegation, runtime reachability, public contract promotion, CLI turn parity, ownership, edge cases, error scenarios, requirement coverage, and harness rows T1 through T11.

The model captures the six gaps. C1 is covered by fromConfig adopting an external Config, accepting/adopting a caller-supplied shared MessageBus when provided, deriving one fallback bus when omitted, preserving getConfig identity, sharing finalize with createAgent, and preserving caller ownership. C2 is covered by getEphemeralSetting, setEphemeralSetting, getEphemeralSettings, and getConfig delegating to the bound Config without a parallel store. C3 is covered by R-PARITY/T10 and the modeled equivalence between agent.stream() and the reference AgenticLoop drive over the same FakeProvider script. H1 is covered by type-only promotion of AgentClientContract at the public root/curated API while leaving the AgentClient class on internals. H2 is covered by provider runtime adoption through runtime context seams, ProviderManager reachability/adoption, and getRuntimeId/provider/model visibility. H3 is covered by getCurrentSequenceModel delegating through resolveClient to the current bound client, including null passthrough and rebind behavior.

I found no substantive gap between the revised model and the cited specification requirements. The prior valid defect is fixed: FromConfigOptions now explicitly includes `messageBus?` and states the CRIT-2 shared-bus adoption and single-bus fallback semantics required by specification.md:473-477 and 515. The previously disputed REQ-007/REQ-INT-004 n/a concern is also clarified in the coverage map: documentation-only and static import-boundary requirements do not have runtime state transitions, and fabricating one would be incorrect. The coverage map still provides applicable harness/phase coverage for those requirements.

Verdict: PASS. Command-level verification passed with no missing requirement output and invariant count 20; semantic review confirms all five checklist items pass, the messageBus? remediation landed correctly, the intentional-n/a footnote exists, and no new substantive defects were identified.

Final Semantic Assessment: PASS — evidence: no missing REQ output, invariant grep count 20, FromConfigOptions lines 41-46 include `messageBus?` with CRIT-2 adoption/fallback semantics, coverage-map lines 268-270 clarify intentional n/a rows, and all five semantic checklist items pass.
