Phase: P07a
Completed: 2026-06-23 00:05
Files Created: [none — verification only]
Files Modified: [none — verification only]
Tests Added: [0 — this is a verifier]
Verification: |
  Independent verifier = typescriptreviewer (gpt55high). VERDICT: PASS. All gates executed from repo
  root with the canonical `npx vitest run <root-relative>` form (CCF-3).

  - File existence: spec + helper + fixture all present.
  - Mock-theater guard: PASS (no toHaveBeenCalled / mockResolvedValue / mockReturnValue in spec).
  - Reverse-test guard: PASS (no toThrow('NotYetImplemented') / not.toThrow()).
  - Deep-import guard: PASS (grep -nE "from '[^']*(/src/|core/src|providers/src)" empty on spec AND
    helper); spec imports the public root '@vybestack/llxprt-code-agents' (line 37).
  - Object-form reference drive: PASS (cli-turn-parity.early.spec.ts:62 `new AgenticLoop({`, :63
    `agentClient: config.getAgentClient()`; no positional-constructor violation).
  - Property ratio: 1/3 = 33% (it.prop/test.prop=0, classic-blocks=1) ≥ 30%.
  - RED-before-green: `npx vitest run packages/agents/src/api/__tests__/cli-turn-parity.early.spec.ts`
    EXIT 1, Test Files 1 failed (1), Tests 3 failed (3); behavioral RED (no Cannot find
    module|SyntaxError|ReferenceError|Failed to resolve import).
  - Boundary governance: `npx vitest run packages/agents/src/api/__tests__/boundary.spec.ts` EXIT 0,
    5 passed (5) — the P07 boundary-fix (deep core type-imports rerouted through the scan-exempt helper)
    is real; spec no longer violates the no-deep-import rule.
  - typecheck: `npm run typecheck` EXIT 0 across all 15 workspaces (helper's `export type { Config,
    MessageBus }` cross-package re-export compiles).

  COORDINATOR POST-VERIFICATION FINDING (CCF-4 — stale dist / bare-specifier export gap): the verifier
  (correctly, per PLAN.md:733-737) accepted the RED even though its proximate reason was
  `TypeError: (0, fromConfig) is not a function` rather than the P06 stub's `NotYetImplemented`. The
  coordinator ran that down because "export missing" (vs "method missing") would mean P09 could NOT turn
  the slice GREEN. Root cause: a STALE pre-P06 `packages/agents/dist/` (built before fromConfig existed)
  combined with the agents vitest bare-specifier alias (which rewrites ONLY the bare root entry to
  `index.ts`, while the entry's own `export * from './src/index.js'` re-export graph is resolved by Vite
  through package.json `exports['.'] -> ./dist/index.js`). createAgent (pre-existing in dist) survived on
  the bare root; fromConfig (new in P06) was `undefined`. PROVEN via layered probes: relative imports of
  src/index.ts AND the top index.ts both expose `fromConfig = function`, but the bare specifier exposed
  `undefined`; stale dist/src/api/index.js had `fromConfig` count 0. FIX: `npm run build --workspace
  @vybestack/llxprt-code-agents` (dist is GITIGNORED → zero source/git footprint). POST-BUILD re-verify:
  bare-root `typeof fromConfig === 'function'`, and the slice is STILL RED (EXIT 1, 3 failed) but now for
  the STRONGER/correct reason `Error: NotYetImplemented`. This confirms P09 (implementing the body) will
  turn it GREEN. Recorded as CCF-4; downstream cross-package phases must `npm run build` before any
  bare-specifier resolution / directory characterization gate.
Semantic Assessment: PASS — early integration-first slice is a genuine RED driver against the public
fromConfig path (33% property, object-form AgenticLoop reference, real FakeProvider fixture, public-only
imports, boundary GREEN, typecheck clean). Post-build the RED is the canonical NotYetImplemented; the
slice can only go GREEN when fromConfig genuinely adopts the external Config and reaches turn-drive parity.
