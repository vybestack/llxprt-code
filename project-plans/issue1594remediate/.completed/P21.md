Phase: P21
Completed: 2026-06-22 10:02
Files Created:
  - packages/agents/src/api/__tests__/publicSurface.nonbreaking.test.ts (127 lines) — @plan:PLAN-20260621-COREAPIREMED.P21 @requirement:REQ-006
  - packages/agents/src/api/__tests__/boundary.adequacy.test.ts (173 lines) — @plan:PLAN-20260621-COREAPIREMED.P21 @requirement:REQ-INT-004
Files Modified:
  - project-plans/issue1594remediate/plan/21-boundary-and-nonbreaking.md (+11/-4) — CCF-9 GATE-BUG fix (defect in the GATE, not the worker): the CRIT-6 Path-A internals scan greps the WHOLE __tests__ dir for internals.js importers excluding only *.reference-drive.*, which FALSE-POSITIVED on the pre-existing P16a-blessed nonBreaking.exports.test.ts (imports internals.js at line 15 to assert REQ-006's "every ./internals.js export keeps working" — irreducibly requires importing the subpath). Added a SECOND exemption category (*nonbreaking*/*nonBreaking*) for export-surface characterization tests (neither Path A consumer nor Path B drive). The in-test boundary.adequacy.test.ts encodes the SAME two-category exemption so it agrees with the shell scan.
Tests Added: 10 (5 per file; 3 property-based via fast-check, 7 deterministic)
Verification: |
  Build-first: build core=0, build agents=0.

  P21 Verification Commands (coordinator-reproduced verbatim, set -e + set -o pipefail):
    test -f publicSurface.nonbreaking.test.ts        -> OK
    test -f boundary.adequacy.test.ts                -> OK
    whole-__tests__ deep-import scan (/src/,core/src,providers/src) -> PASS (no deep imports)
    CRIT-6 Path-A internals scan (exempts *.reference-drive.* + *nonbreaking*) -> PASS (internals confined to nonBreaking.exports.test.ts + publicSurface.nonbreaking.test.ts; no Path-A leak)
    deferred-impl scan (TODO/FIXME/HACK/STUB/XXX/placeholder/"for now"/"in a real") -> PASS (none)
    npx vitest run (2 new files)                     -> Test Files 2 passed (2) | Tests 10 passed (10)
    npx vitest run packages/agents/src/api/__tests__/ -> Test Files 38 passed (38) | Tests 488 passed (488) (478 prior + 10 new, zero regressions)
    npm run typecheck                                -> TC=0
    npm run lint --workspace @vybestack/llxprt-code-agents -> LINT=0

  Behavioral (coordinator read BOTH files in full — non-vacuous):
    publicSurface.nonbreaking.test.ts: dynamic Object.keys(root) enumeration; typeof==='function' on 10 load-bearing symbols (createAgent/fromConfig/listProviders/listTools/mapLoopStream/mapStreamEvent/toConfigParameters/createTaskToolRegistration/AdapterError/AgenticLoop); TYPE-LEVEL compile anchor `const _: (cfg:AgentConfig)=>Promise<Agent> = root.createAgent` (signature change breaks typecheck); createAgent.length===1 arity; fromConfig!==createAgent identity (proves no overload mutation); internals.AgentClient typeof function + PostTurnAction !== undefined + Object.keys(internals) superset; 2 real fc properties (root-key presence + value-callable).
    boundary.adequacy.test.ts: REAL disk scan (readdirSync recursive) + plain-string extractFromSpecifiers (NO regex, mirrors cli-turn-parity idiom); Test A deep-import offenders toStrictEqual([]); Test B CRIT-6 Path-A internals (skips permitted *.reference-drive.*/*nonbreaking* consumers) toStrictEqual([]); Test C >=1 public-root importer; 2 fc properties incl. a CONTRAPOSITIVE lock (every internals-importer must be a justified exemption — keeps the exemption set honest). It is itself Path-A (reads files as strings, imports NO internals). No mock theater anywhere.

  No existing test/helper/fixture/production file modified (git: both new files untracked; nothing else touched). No genuine production deep-import leak found.
Semantic Assessment: PASS — REQ-006 (non-breaking) + REQ-INT-004 (no-deep-import boundary) are now locked by executable characterization. The non-breaking test dynamically proves every #1594-era public symbol (root + internals) is still present with compatible shape (incl. createAgent's single-arg signature and fromConfig as a separate non-mutating export); the boundary test statically proves no file deep-imports /src/ and that ./internals.js is confined to the two permitted TEST-ONLY meta categories (reference-drive + export-surface characterization) — Path A imports only the curated public root. 488/488 agents tests, TC=0, lint=0. The lone plan edit (CCF-9) corrected a gate false-positive on a prior-phase file; no worker work-around/obfuscation was accepted.
