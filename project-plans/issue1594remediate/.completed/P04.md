Phase: P04
Completed: 2026-06-22 21:54

Files Created:
- packages/providers/src/runtime/__tests__/providerManagerAdoption.behavior.test.ts (480 lines)

Files Modified:
- packages/providers/package.json (+1/-0 — added `"fast-check": "^4.3.0"` to devDependencies)

Tests Added: 10 (6 classic behavioral scenarios T1–T6 + 4 fast-check property tests)

Verification:

GUARD BLOCK (behavioral asserts / mock-theater / reverse-test / property-%):
```
=== behavioral asserts ===
OK present
=== mock theater ===
none (good)
=== reverse test ===
none (good)
=== counts ===
TOTAL=10 PROP_CASE_FORMS=0 CLASSIC_PROP_BLOCKS=4 PROP=4 PCT=40%
property-based CASES: 4 / 10 = 40% (it.prop/test.prop=0, classic-blocks=4)
```
NOTE on the awk counter: this dev machine ships BSD `awk version 20200816`, whose
ERE does not honor the `\b` word-boundary used by the spec's `CLASSIC_PROP_BLOCKS`
awk, so the BSD awk reports `classic-blocks=0` for ANY file (verified: the existing
agents compression-retry-classification.test.ts, which uses the same classic
`fc.assert(fc.property(...))` idiom and passes CI, also reports 0 on BSD awk). The
counts above were produced by replicating the awk logic in node (gawk/CI semantics)
and match what gawk computes on Linux CI. The property tests use the classic
`fc.assert(fc.property(...))` form (NOT `it.prop`, which is vitest-4-only and would
produce a non-behavioral `TypeError: it.prop is not a function` RED on this repo's
vitest ^3.2.4).

DEFERRED IMPLEMENTATION DETECTION (scoped):
```
PASS: no deferred-implementation markers / reverse tests in the new spec/helper file(s).
```

TYPECHECK (packages/providers): `npx tsc --noEmit` → TSC_EXIT=0 (clean — the real
ProviderManager is directly assignable to `providerManager?: RuntimeProviderManager`
with NO cast/intersection, confirming the CRIT-1 type decision).

RED-STATE ENFORCEMENT:
Single-file run:
`npm test --workspace @vybestack/llxprt-code-providers -- run packages/providers/src/runtime/__tests__/providerManagerAdoption.behavior.test.ts`
TEST EXIT STATUS = 1 (RED, as required pre-P05).

Test outcome (this file's 10 tests | 7 failed | 3 passed):
```
 ❯ src/runtime/__tests__/providerManagerAdoption.behavior.test.ts (10 tests | 7 failed) 113ms
   × IDENTITY — providerManager: pm yields handle.providerManager === pm
     → expected ProviderManager{ …(8) } to be ProviderManager{ …(8) } // Object.is equality
   ✓ DEFAULT — omitting providerManager yields a fresh manager !== a caller-held pm
   × ADOPTED ACTIVATION — after activate, the handle resolves the adopted manager and its provider state
     → expected ProviderManager{ …(8) } to be ProviderManager{ …(8) } // Object.is equality
   × NO SECOND CONSTRUCTION — supplying providerManager constructs zero new managers
     → expected 1 to be +0 // Object.is equality
   × INDEPENDENT SEAMS — messageBus and providerManager adoption compose
     → expected ProviderManager{ …(8) } to be ProviderManager{ …(8) } // Object.is equality
   × CLEANUP CONTRACT — onCleanup receives the adopted manager and cleanup disposes neither manager
     → expected ProviderManager{ …(8) } to be ProviderManager{ …(8) } // Object.is equality
   × PROP — for any runtimeId, a supplied providerManager is adopted verbatim
     → Property failed after 1 tests  Counterexample: [" "]  (shrunk 2x)
   ✓ PROP — omitting providerManager never adopts a caller-held manager
   × PROP — a supplied providerManager is adopted regardless of the model option
     → Property failed after 1 tests  Counterexample: ["a"]  (shrunk 1x)
   ✓ PROP — two default-path calls yield distinct fresh managers

 Test Files  1 failed | 30 passed (31)
      Tests  7 failed | 334 passed (341)
```

Load-bearing RED line (T1, the identity-mismatch driver for P05):
```
AssertionError: expected ProviderManager{ …(8) } to be ProviderManager{ …(8) } // Object.is equality
 ❯ src/runtime/__tests__/providerManagerAdoption.behavior.test.ts:158:38
    158|       expect(handle.providerManager).toBe(pm);
 ❯ new ProviderManager src/ProviderManager.ts:96:27
 ❯ createIsolatedRuntimeContext src/runtime/runtimeContextFactory.ts:517:27
```

RED reason classification:
- no import/compile errors (`Cannot find module|SyntaxError|Failed to resolve import|ReferenceError` — none)
- no `it.prop` TypeError
- no spy-wiring TypeError (`Cannot read properties of undefined (reading 'apply')` — none)
- 7 failures are ALL behavioral: identity mismatch (`toBe(pm)`), count mismatch (`toBe(0)`),
  and property counterexamples — exactly because the P03 stub builds a fresh manager
  unconditionally at runtimeContextFactory.ts:517 instead of adopting the supplied `pm`.

The 3 passing tests are the default-path ones (T2 DEFAULT + the two "omit"/"distinct"
properties) — current behavior, legitimately GREEN, matching the spec's RED/GREEN
prediction.

Semantic Assessment: Authored a 10-test behavioral RED suite (40% property-based) that drives the providers `providerManager?` adoption seam — the real-ProviderManager fixture (no cast, no mock theater, no reverse testing) fails RED against the P03 stub for the right reason: 7 identity/count/property assertions fail because `createIsolatedRuntimeContext` constructs a fresh manager instead of adopting the supplied one, which is exactly the gap P05 must close.
