# Playbook: Unify shell security policy and remove legacy logic

**Upstream SHA:** `d3c206c6770d`
**Upstream Subject:** Unify shell security policy and remove legacy logic
**Upstream Stats:** 14 files, 771 insertions

## What Upstream Does

Major refactoring that removes legacy shell permission checking (`isCommandAllowed`, `isShellInvocationAllowlisted`) and consolidates all shell security into the Policy Engine. Adds comprehensive shell command decomposition logic to PolicyEngine: validates compound commands recursively, blocks redirections in non-interactive mode (unless explicitly allowed), and upgrades ASK_USER to ALLOW when all sub-commands are allowed. Creates `policy/utils.ts` with `escapeRegex` and `buildArgsPatterns` helpers. Updates CLI config to merge exclude/allowed tools into policy settings before creating PolicyEngine. Adds extensive Policy Engine tests for shell command validation.

## LLxprt Adaptation Strategy

Complex integration - requires careful mapping of shell security logic. LLxprt has:
- Similar shell-utils but may have implementation differences
- PolicyEngine with similar structure but possibly different recursion logic
- Need to preserve LLxprt-specific shell permission patterns while adopting upstream's unified approach

Key adaptation points:
- Extract shell permission logic from tools to policy layer
- Add utils.ts for policy helper functions
- Update PolicyEngine.check() to handle shell command decomposition
- Modify CLI config to pass effective settings to createPolicyEngineConfig
- Remove legacy shell permission checks from ShellTool

## Files to Create/Modify

**Create:**
- `packages/core/src/policy/utils.ts` - escapeRegex and buildArgsPatterns helpers
- `packages/core/src/policy/utils.test.ts` - Tests for policy utilities

**Modify:**
- `packages/core/src/policy/policy-engine.ts` - Add shell command decomposition logic to check()
- `packages/core/src/policy/policy-engine.test.ts` - Add extensive shell validation tests
- `packages/core/src/policy/config.ts` - Update createPolicyEngineConfig to handle legacy ShellTool(args) format
- `packages/core/src/policy/config.test.ts` - Add test for ShellTool alias normalization
- `packages/core/src/policy/toml-loader.ts` - Update to use buildArgsPatterns, add allowRedirection support
- `packages/core/src/policy/toml-loader.test.ts` - Test allowRedirection parsing
- `packages/core/src/policy/types.ts` - Add allowRedirection to PolicyRule interface (if not present)
- `packages/core/src/tools/shell.ts` - Remove isCommandAllowed checks, rely on PolicyEngine
- `packages/core/src/tools/shell.test.ts` - Update tests to use PolicyEngine, remove legacy permission tests
- `packages/core/src/test-utils/mock-message-bus.ts` - Add defaultToolDecision support
- `packages/cli/src/config/config.ts` - Create effective settings before PolicyEngine creation
- `packages/cli/src/config/config.test.ts` - Add PolicyEngine integration tests
- Remove `packages/core/src/utils/shell-permissions.ts` if it exists

## Implementation Steps

1. **Create policy/utils.ts**:
   - Create new file with `escapeRegex(text: string)` - escapes regex special chars
   - Add `buildArgsPatterns(argsPattern?, commandPrefix?, commandRegex?)` that:
     - Handles array or string commandPrefix
     - Escapes and wraps with word boundaries: `"command":"${escaped}(?:[\\s"]|$)`
     - Returns array of patterns
   - Copy implementation from upstream diff

2. **Create policy/utils.test.ts**:
   - Test escapeRegex with special characters
   - Test buildArgsPatterns with various inputs (string prefix, array prefix, regex, etc.)
   - Test quote escaping in command prefixes

3. **Update PolicyRule interface** (in types.ts):
   - Add `allowRedirection?: boolean` field to PolicyRule
   - This allows TOML policies to override redirection blocking

4. **Update toml-loader.ts**:
   - Import `buildArgsPatterns` from utils
   - Update PolicyRuleSchema to include `allow_redirection: z.boolean().optional()`
   - In rule transformation, replace manual commandPrefix/commandRegex handling with `buildArgsPatterns()` call
   - Pass `allowRedirection: rule.allow_redirection` to created PolicyRule
   - Repeat for SafetyCheckerRule transformation

5. **Update policy-engine.ts check() method**:
   - Add comprehensive shell command decomposition logic after initial rule matching
   - For shell tools with ALLOW decision:
     - Get command from args
     - Call splitCommands to get sub-commands
     - If parse fails (empty array for non-empty command), return ASK_USER or DENY
     - If multiple sub-commands, recurse into check() for each
     - Aggregate decisions: DENY overrides all, ASK_USER downgrades ALLOW
     - Check for redirections in allowed commands, downgrade to ASK_USER unless allowRedirection
   - Preserve original args (including dir_path) during recursion
   - Add infinite recursion protection (check if subCmd === original command)
   - Apply nonInteractive mode conversion at end

6. **Add shell validation tests to policy-engine.test.ts**:
   - Mock shell-utils to return predictable results
   - Test compound command validation (&&, ||)
   - Test DENY propagation from any sub-command
   - Test upgrade from ASK_USER to ALLOW when all parts allowed
   - Test explicit DENY for compound commands
   - Test redirection blocking in non-interactive mode
   - Test allowRedirection override
   - Test dir_path preservation in recursive checks
   - Test regex matching with quotes in commandPrefix
   - Test atomic command fallback to rule decision (not auto-upgrade)

7. **Update config.ts createPolicyEngineConfig**:
   - In settings-based overload, handle legacy `ShellTool(args)` format in allowed tools
   - Parse with regex: `/^([a-zA-Z0-9_-]+)\((.*)\)$/`
   - For shell tools, extract command prefix and use buildArgsPatterns
   - Normalize ShellTool alias to run_shell_command
   - Add rules with argsPattern for prefix-based allows
   - Import SHELL_TOOL_NAMES and SHELL_TOOL_NAME from tool-names/shell-utils

8. **Update CLI config.ts**:
   - In loadCliConfig, create effectiveSettings object that merges:
     - Base settings
     - tools.allowed from argv
     - tools.exclude from argv and extraExcludes
     - mcp.allowed from argv
   - Pass effectiveSettings to createPolicyEngineConfig
   - Set policyEngineConfig.nonInteractive based on interactive mode detection

9. **Add CLI config tests**:
   - Test nonInteractive wiring (one-shot vs interactive mode)
   - Test merged allowed tools passed to createPolicyEngineConfig
   - Test merged exclude tools include non-interactive defaults

10. **Update ShellTool**:
    - Remove imports of `isCommandAllowed` and `isShellInvocationAllowlisted`
    - Remove `isInvocationAllowlisted()` private method
    - In `shouldConfirmExecute()`, remove non-interactive rejection logic
    - Rely entirely on PolicyEngine for all permission checks
    - Add fallback for rootCommands display if parser returns empty array
    - In `validate()`, remove `isCommandAllowed` check
    - Remove `getCommandRoots` length check

11. **Update shell.test.ts**:
    - Remove `isCommandAllowed` tests
    - Remove `isInvocationAllowlisted` tests
    - Add PolicyEngine-based test: command needs confirmation, then "Always" allowlists it
    - Mock MessageBus with defaultToolDecision control
    - Test both confirmation and allowlisting flow

12. **Update mock-message-bus.ts**:
    - Add `defaultToolDecision: 'allow' | 'deny' | 'ask_user'` property
    - In publish handler, respond to TOOL_CONFIRMATION_REQUEST based on defaultToolDecision
    - Allow tests to control policy decisions via mock

13. **Update persistence.test.ts**:
    - Fix regex expectation: `"command":"git\\ status(?:[\\s"]|$)` (note escaped space)

14. **Remove legacy shell permission files**:
    - Check if `packages/core/src/utils/shell-permissions.ts` exists and remove it
    - Remove related imports from shell.ts

## Execution Notes

- **Batch group:** Policy (execute after dcd2449)
- **Dependencies:** 37be162, dcd2449
- **Verification:** `npm run typecheck && npm run test -- packages/core/src/policy/ packages/core/src/tools/shell.test.ts packages/cli/src/config/config.test.ts`
- **Note:** Major refactor - test thoroughly. Windows CI failures in shell parsing should be handled by mocking shell-utils in tests.
- **Windows caveat:** Shell parser may behave differently on Windows. Ensure tests mock shell-utils properly.
