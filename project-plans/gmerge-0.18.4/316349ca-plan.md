# REIMPLEMENT: 316349ca — Alternate buffer default to false

## Upstream Summary

Changes the `useAlternateScreen` setting default from `true` to `false`, and adds an explicit `=== true` check. Also updates `settingsSchema.ts`, `settings.schema.json`, test helpers, and introduces `useAlternateBuffer.ts` hook.

## Why REIMPLEMENT

LLxprt already has alternate buffer support but implemented differently:
- In `AppContainer.tsx` (~line 764+) and `inkRenderOptions.ts`
- Not a separate `useAlternateBuffer.ts` hook
- The default and check pattern need adapting to LLxprt's structure

## What to Take

1. **Default change**: `useAlternateScreen` defaults to `false` (currently `true` in LLxprt)
2. **Strict check**: Use `=== true` instead of truthy check
3. **Schema update**: Update `settings.schema.json` with corrected default
4. **Settings schema**: Update `settingsSchema.ts` with corrected default

## What NOT to Take

1. Do NOT create a separate `useAlternateBuffer.ts` hook — LLxprt's inline implementation in AppContainer.tsx is fine
2. Do NOT change the integration test helper unless needed for test compatibility

## Implementation Steps

1. **Read** `packages/cli/src/config/settingsSchema.ts` — find `useAlternateScreen` default
2. **Read** `schemas/settings.schema.json` — find `useAlternateScreen` default
3. **Read** `packages/cli/src/ui/AppContainer.tsx` — find where `useAlternateScreen` is checked
4. **Read** `packages/cli/src/ui/inkRenderOptions.ts` — find alternate buffer usage
5. **Change** default in `settingsSchema.ts` from `true` to `false`
6. **Change** default in `settings.schema.json` from `true` to `false`
7. **Change** check in `AppContainer.tsx` to use `=== true` (explicit boolean comparison)
8. **Change** check in `inkRenderOptions.ts` to use `=== true` if applicable
9. **Update** any tests that depend on the default being `true`

## Branding: N/A

## Verification

```bash
npm run lint && npm run typecheck
```
