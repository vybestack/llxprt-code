# Phase P14 Completion: Release Process And Trusted Publish Updates

Completed: 2026-06-08
Plan ID: `PLAN-20260608-ISSUE1585.P14`

## Files Modified / Created

- `.github/workflows/release.yml`
  - Added `Publish @vybestack/llxprt-code-tools` before core publish.
  - Added tools sandbox tarball packing before core/providers/cli.
- `.github/workflows/build-sandbox.yml`
  - Added tools/core/providers/cli pack order.
- `scripts/build_sandbox.js`
  - Added tools package dir, tools pack step, tools chmod, and canonical sandbox order.
- `Dockerfile`
  - Added tools tarball COPY before core/providers/cli.
  - Added tools tarball first in the single npm install transaction.
- `scripts/prepare-package.js`
  - Added `copyFiles('tools', { README.md, LICENSE, .npmrc })`.
- `scripts/version.js`
  - Added `@vybestack/llxprt-code-tools` in canonical publish order.
- `package.json`
  - Reordered workspaces to canonical publish order: tools, core, lsp, providers, cli, then non-publishable workspaces.
- `package-lock.json`
  - Updated via `npm install` after tools dependency/package metadata changes.
- `packages/tools/package.json`
  - Added missing publish/runtime dependencies identified by tarball import smoke testing: AST language packages, ripgrep, and `mime-types`; added `@types/mime-types` dev dependency.
- `scripts/tests/release-process.test.js`
  - Added/updated coverage for tools publish order, sandbox pack order, Dockerfile order, bind-release-deps order, and tools dependency rewriting.
- `scripts/tests/ui-image-harness.test.js`
  - Skips the optional UI image harness when the harness source path is absent, matching the current workspace shape.
- `project-plans/issue1585/manual-trusted-publishing.md`
  - Added manual npm trusted publishing checklist for `@vybestack/llxprt-code-tools`.

## Release Order Verified

Publishable order is now:

1. `@vybestack/llxprt-code-tools`
2. `@vybestack/llxprt-code-core`
3. `@vybestack/llxprt-code-lsp`
4. `@vybestack/llxprt-code-providers`
5. `@vybestack/llxprt-code`

Sandbox order is now:

1. tools
2. core
3. providers
4. cli

## Verification

```text
npm run test:scripts
PASS: 8 files passed, 4 skipped; 105 tests passed, 5 skipped.

node scripts/bind-release-deps.js --dry-run
PASS: NPM release packages: @vybestack/llxprt-code-tools, @vybestack/llxprt-code-core, @vybestack/llxprt-code-lsp, @vybestack/llxprt-code-providers, @vybestack/llxprt-code
PASS: Verification passed: no workspace file: deps in NPM release packages.

Tools package production dependency audit
PASS: all production external imports are present in packages/tools/package.json dependencies/devDependencies.

Workspace order guard
PASS: root workspace order starts tools, core, lsp, providers, cli.

package-lock guards
PASS: packages/tools exists; core/providers declare tools; CLI has no direct tools dependency.

npm run build --workspace @vybestack/llxprt-code-tools
PASS.

Tools pack + ESM/subpath smoke
PASS: root ESM import OK with 166 exports.
PASS: subpath imports OK for IToolFormatter.js, ToolFormatter.js, ToolIdStrategy.js, toolIdNormalization.js, doubleEscapeUtils.js, toolNameUtils.js, utils/fetch.js.
```

## Semantic Assessment

The new tools package is now wired into release as a first-class publishable package. Release publishing, local sandbox packing, CI sandbox packing, Dockerfile copy/install order, version processing, package preparation, package-lock, and release dependency binding all include tools before downstream packages that depend on it. The manual trusted publishing checklist captures the npm-side setup that cannot be completed purely in repository source.
