# P16a — Final Semantic Review: COMPLETED

Plan ID: PLAN-20260608-ISSUE1585.P16a

## Assessment: PASS — no remaining blockers.

Issue #1585 (extract a standalone `@vybestack/llxprt-code-tools` package) is
fully satisfied. The full verification suite passes (see P16.md) and all
semantic review items below are confirmed by direct evidence.

## Issue Intent

- [x] Issue #1585 intent (extract tools package) fully satisfied.
- [x] Tool contracts, registry, formatters, and implementations live in `packages/tools`.
- [x] No user-visible behavior changes (smoke test produces a haiku; full suite green).

## Behavior Preservation

- [x] CLI startup reaches the same tool registry state (cli suite 5742 pass).
- [x] Built-in tools discoverable/executable through scheduler (core/cli suites green).
- [x] Provider tool formatting and ID normalization identical (providers 1923 pass).
- [x] Shell tool execution works through adapter (CoreShellToolHostAdapter present; tests green).
- [x] Todo/memory/MCP tool behavior preserved through adapters.
- [x] Key storage masking and resolution preserved (tool-key-storage tests green).
- [x] Memory path (LLXPRT dir) resolution preserved.

## Package Boundaries

- [x] `packages/tools` does not import core/cli/providers (forbidden-import scan clean; forbidden-imports.test.ts 4/4).
- [x] Core adapters implement tools-owned interfaces only.
- [x] No core-local interfaces consumed by tools.
- [x] Providers import tools formatter/ID utilities from `@vybestack/llxprt-code-tools`.
- [x] No re-export shims in `packages/core/src/tools` (scan clean); allowed re-exports in `core/src/index.ts` (25 lines) verified separately.

## Release Readiness

- [x] `release.yml` publishes tools (line 320/322) before dependents; packs tools (line 358).
- [x] `release-process.test.js` asserts toolsIndex < core/providers/cli (lines 73-84).
- [x] `build_sandbox.js` packs tools tarball (lines 92-150).
- [x] `Dockerfile` COPYs (lines 53-56) and installs (lines 62-64) tools first.
- [x] `manual-trusted-publishing.md` exists.
- [x] Release order reconciled between tests and workflow.

## MCP Ownership

- [x] `mcp-client.ts` and `mcp-client-manager.ts` remain in `packages/core/src/tools`.
- [x] `mcp-tool.ts` moved to `packages/tools/src/tools/mcp-tool.ts`.
- [x] No MCP OAuth/auth code leaked into tools package.

## Tool Key Storage

- [x] IToolKeyStorage interface defined in tools.
- [x] Key utility functions (maskKeyForDisplay, getSupportedToolNames, isValidToolKeyName) moved to tools (`tool-key-storage-types.ts` / `tool-key-storage-facade.ts`).
- [x] `ToolKeyStorage` class remains in core (imports SecureStore — `packages/core/src/tools/tool-key-storage.ts`).
- [x] `CoreToolKeyStorageAdapter` owns ToolKeyStorage/SecureStore lifecycle.
- [x] Tests cover masking, key storage, storage boundary (observable behavior).

## Scripts Coverage

- [x] `scripts/version.js` includes `@vybestack/llxprt-code-tools` (line 44).
- [x] `scripts/prepare-package.js` has copyFiles for tools (lines 38-39).
- [x] `scripts/build.js` uses workspaces build.

## Dockerfile Ordering

- [x] COPY tools tarball before core/providers/cli.
- [x] npm install places tools first.
- [x] Sandbox build packs tools before core/providers/cli (toolsPackageDir=packages/tools).
- [x] Tools tarball chmod consistent with existing 0o755 usage.

## Package Metadata

- [x] `packages/tools/package.json` has no core/providers/cli dependencies.
- [x] `@vybestack/llxprt-code-test-utils` is devDependency-only.
- [x] IToolFormatter export maps to `dist/src/formatters/`.
- [x] tsconfig references create no cycles.

## Documentation

- [x] Move map / dependency-relocation analysis documents file classifications (analysis/).
- [x] `manual-trusted-publishing.md` documents npm trusted publisher setup.
- [x] Integration-contract analysis documents interface/adapter mappings.
- [x] Preflight results record approved decisions (P00a.md).

## Note on Smoke Profile

The plan's canonical smoke command references `--profile-load waferglm5`. That
endpoint (pass.wafer.ai) is no longer available (HTTP 401, invalid key). The
smoke test was run with the equivalent OpenAI-compatible profile
`--profile-load ollamaglm51`, which exercises the identical non-interactive CLI
→ provider path and produced a haiku with exit 0. This is an
environment/credential substitution only and does not affect the package
extraction or any reviewed behavior.

## Conclusion

All review checklist items pass. Final verification passes. No remaining
blockers. Issue #1585 is ready to ship.
