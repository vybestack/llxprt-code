# Phase P12 Completion: Core Adapters and Registry Integration

Phase: P12
Completed: 2026-06-08
Plan ID: PLAN-20260608-ISSUE1585.P12

## Files Created

- `packages/core/src/tools-adapters/index.ts` — adapter barrel exporting current core adapters.

## Files Modified During P12 Remediation

- `packages/tools/src/__tests__/boundary-scan.test.ts` — tightened forbidden-import scan to real import statements and updated moved-module canonical paths.
- `packages/tools/src/__tests__/edit-ast-tools.test.ts` — updated AST grep and edit behavioral assertions for moved declarative tools and current `ToolResult` wording.
- `packages/tools/src/tools/direct-web-fetch.test.ts` — moved behavioral DirectWebFetch coverage into tools package so `node-fetch` mocks exercise the moved implementation.
- `packages/core/src/tools/direct-web-fetch.test.ts` — reduced stale core behavioral duplicate to moved-tool/core-adapter import smoke coverage.
- `packages/core/src/config/config.test.ts` — mocks/inspects `ToolRegistry` from `@vybestack/llxprt-code-tools` and removes stale absolute `/llxprt-code-tools` mock.
- `packages/core/src/tools/grep.test.ts`, `packages/core/src/tools/grep.timeout.test.ts` — partial `child_process` mocks preserve actual exports while overriding `spawn`.
- `packages/core/src/tools/modifiable-tool.test.ts` — mocks moved tools editor module and updates cleanup assertion to moved implementation behavior.
- `packages/core/src/lsp/__tests__/system-integration.test.ts`, `packages/core/src/lsp/__tests__/e2e-lsp.test.ts`, `packages/core/src/config/config-lsp-integration.test.ts` — tools-package mocks updated so moved `ToolRegistry`/MCP exports are used correctly.
- Core production/test imports for `ToolRegistry` were rewritten from local `../tools/tool-registry.js` paths to `@vybestack/llxprt-code-tools`.
- `packages/core/src/index.ts` now re-exports moved registry symbols from `@vybestack/llxprt-code-tools` rather than the local core tools shim.

## Adapter Verification

- Existing adapters implement narrow tools-owned interfaces: `CoreAsyncTaskServiceAdapter`, `CoreIdeServiceAdapter`, `CoreLspServiceAdapter`, `CoreMcpToolServiceAdapter`, `CoreMessageBusAdapter`, `CorePromptRegistryServiceAdapter`, `CoreSettingsServiceAdapter`, `CoreShellToolHostAdapter`, `CoreSkillServiceAdapter`, `CoreStorageServiceAdapter`, `CoreSubagentServiceAdapter`, `CoreTodoServiceAdapter`, `CoreToolHostAdapter`, `CoreToolKeyStorageAdapter`, `CoreToolRegistryHostAdapter`, `CoreWebSearchServiceAdapter`.
- `packages/core/src/tools-adapters/index.ts` exports required adapters including `CoreToolRegistryHostAdapter`, `CoreMessageBusAdapter`, `CoreSubagentServiceAdapter`, and `CoreMcpToolServiceAdapter`.
- Production `ToolRegistry` construction is adapter-backed:
  - `packages/core/src/config/toolRegistryFactory.ts:363-365` uses `new CoreToolRegistryHostAdapter(config)` and `new CoreMessageBusAdapter(messageBus)`.
  - `packages/core/src/agents/executor.ts:106-108` uses `new CoreToolRegistryHostAdapter(runtimeContext)` and `new CoreMessageBusAdapter(messageBus)`.

## Verification Commands Run

```bash
npm run typecheck --workspace=@vybestack/llxprt-code-tools
npm run typecheck --workspace=@vybestack/llxprt-code-core
npm run test --workspace=@vybestack/llxprt-code-tools
npm run test --workspace=@vybestack/llxprt-code-core
```

Results:

- tools typecheck: PASS
- core typecheck: PASS
- tools tests: PASS — 23 files, 250 tests
- core tests: PASS — 429 files passed, 1 skipped; 7871 tests passed, 30 skipped

## Scan Evidence

```text
P12 scan: tools forbidden production imports
(no matches)

P12 scan: ToolContext service bag
(no matches)

P12 scan: old core moved tool-registry imports
(no matches)

P12 scan: production ToolRegistry construction locations
packages/core/src/agents/executor.ts:106:    const agentToolRegistry = new ToolRegistry(
packages/core/src/config/toolRegistryFactory.ts:363:  const registry = new ToolRegistry(

P12 scan: adapter-backed constructors
packages/core/src/config/toolRegistryFactory.ts:198:  const messageBusAdapter = new CoreMessageBusAdapter(messageBus);
packages/core/src/config/toolRegistryFactory.ts:364:    new CoreToolRegistryHostAdapter(config),
packages/core/src/config/toolRegistryFactory.ts:365:    new CoreMessageBusAdapter(messageBus),
packages/core/src/agents/executor.ts:107:      new CoreToolRegistryHostAdapter(runtimeContext),
packages/core/src/agents/executor.ts:108:      new CoreMessageBusAdapter(messageBus),

P12 scan: adapters barrel
PASS: adapter barrel exists
packages/core/src/tools-adapters/index.ts exports CoreMcpToolServiceAdapter, CoreMessageBusAdapter, CoreSubagentServiceAdapter, and CoreToolRegistryHostAdapter.
```

## Semantic Assessment

P12 integration is complete. Core constructs the moved `ToolRegistry` through narrow adapters instead of raw core dependencies, moved tools remain free of core/provider/CLI production imports, `ToolContext` remains a narrow context instead of a service bag, and full tools/core package test suites pass after remediating stale core test assumptions around local tool shims and mocks.
