# Phase P15 Completion: Cleanup And No Shims

Completed: 2026-06-08
Plan ID: `PLAN-20260608-ISSUE1585.P15`

## Cleanup Applied

Removed moved tool implementation/test artifacts from `packages/core/src/tools/**` after P14 release wiring completed.

## Retained Core Tools Files

The final retained core tools directory was saved to `project-plans/issue1585/analysis/core-tools-final-files.txt` and contains exactly:

```text
packages/core/src/tools/mcp-client-manager.test.ts
packages/core/src/tools/mcp-client-manager.ts
packages/core/src/tools/mcp-client.test.ts
packages/core/src/tools/mcp-client.ts
packages/core/src/tools/tool-key-storage.test.ts
packages/core/src/tools/tool-key-storage.ts
```

Rationale:

- `mcp-client.ts` and `mcp-client-manager.ts` remain core infrastructure because they own MCP auth/transport/lifecycle integration.
- Their tests remain with the retained core infrastructure.
- `tool-key-storage.ts` remains core infrastructure because `ToolKeyStorage` owns `SecureStore`/keyring storage integration.
- Pure tool-key helpers live in `@vybestack/llxprt-code-tools`; core top-level re-exports are still allowed for CLI compatibility.

## Import/Ownership Remediation

- Removed the `packages/core/src/tools/tool-registry.ts` re-export shim.
- Updated retained MCP files/tests to import the moved `ToolRegistry` type from `@vybestack/llxprt-code-tools` instead of a local core shim.
- Removed pure-helper re-exports from `packages/core/src/tools/tool-key-storage.ts`; the retained file now only imports helpers it uses internally.
- Updated `CoreToolKeyStorageAdapter` so pure helper methods come directly from `@vybestack/llxprt-code-tools`, while persisted storage still delegates to retained core `getToolKeyStorage()`.
- Deleted empty subdirectories under `packages/core/src/tools` after removing moved tests/snapshots.

## Verification

```text
find packages/core/src/tools -type f | sort
PASS: retained files match the approved six-file allowlist exactly.

find packages/core/src/tools -name '*V2*' -o -name '*New*' -o -name '*Wrapper*' -o -name '*Shim*'
PASS: no matches.

rg no-shim scan for re-export forwarding from @vybestack/llxprt-code-tools in packages/core/src/tools
PASS: no matches.

rg stale local tool-registry imports
PASS: no matches.

node duplicate implementation scan comparing tools production basenames against core/src/tools
PASS: no duplicate core/tools implementations outside retained MCP and ToolKeyStorage infrastructure.

node core package exports scan
PASS: no moved `./tools/*` deep exports remain in packages/core/package.json.

rg allowed top-level re-exports in packages/core/src/index.ts
PASS: explicit public API re-exports from @vybestack/llxprt-code-tools are present and intentionally allowed for CLI compatibility.

npm run typecheck --workspace @vybestack/llxprt-code-core
PASS.

npm run test --workspace @vybestack/llxprt-code-core -- src/tools/mcp-client.test.ts src/tools/mcp-client-manager.test.ts src/tools/tool-key-storage.test.ts src/config/config.test.ts
PASS: 4 files, 211 tests.

npm run typecheck --workspace @vybestack/llxprt-code-tools
PASS.

npm run test --workspace @vybestack/llxprt-code-tools
PASS: 23 files, 250 tests.

npm run test --workspace @vybestack/llxprt-code-core
PASS: 357 files passed, 1 skipped; 6772 tests passed, 29 skipped.

npm run build --workspace @vybestack/llxprt-code-core
PASS.

npm run build --workspace @vybestack/llxprt-code-tools
PASS.
```

## Semantic Assessment

The cleanup removes the old core tool implementation/test surface rather than leaving compatibility shims or duplicate implementations. The remaining core tools files are only retained infrastructure with explicit ownership rationale. Core consumers now use moved tool APIs from `@vybestack/llxprt-code-tools` or top-level core compatibility exports; the `packages/core/src/tools/**` directory no longer hosts moved tool wrappers, snapshots, or legacy test files.
