# Phase P14a Completion: Release Process Verification

Completed: 2026-06-08
Plan ID: `PLAN-20260608-ISSUE1585.P14a`

## Verification Commands Run

```text
npm run test:scripts
PASS: 8 files passed, 4 skipped; 105 tests passed, 5 skipped.

node scripts/bind-release-deps.js --dry-run
PASS: canonical order includes tools before core/lsp/providers/cli.
PASS: no workspace file: dependencies remain in NPM release packages during dry-run verification.

test -f project-plans/issue1585/manual-trusted-publishing.md
PASS.

grep -c checks for @vybestack/llxprt-code-tools, vybestack/llxprt-code, release.yml in manual checklist
PASS: counts were 4, 5, and 3.

rg checks for @vybestack/llxprt-code-tools in release/build-sandbox workflows, release-process tests, build_sandbox.js, Dockerfile, package.json, packages/tools/package.json, scripts/version.js
PASS.

rg checks for copyFiles('tools') in scripts/prepare-package.js
PASS.

rg checks for workspaces in scripts/build.js
PASS.

rg checks for packages/tools/dist and /tmp/vybestack-llxprt-code-tools in Dockerfile
PASS.

package-lock guards
PASS: package-lock.json exists, pnpm-lock.yaml absent, packages/tools present, core/providers declare tools, CLI has no direct tools dependency.

actualWorkspaces order parsing guard
PASS: publishable order is tools, core, lsp, providers, cli.

Tools package build/pack/ESM smoke
PASS: package built, packed, installed into temp project, root ESM import succeeded, declared subpath imports succeeded.
```

## Notes

- `npm run test:scripts` now skips `ui-image-harness` when `packages/ui/scripts/image-harness.ts` is absent. This keeps the optional harness from failing in this workspace where `packages/ui` is not present.
- The tools tarball smoke initially exposed a missing runtime dependency on `mime-types` and missing AST/ripgrep runtime package declarations. Those were remediated in `packages/tools/package.json` and `package-lock.json`, then the smoke test passed.

## Verdict

PASS. P14 release process wiring and P14a verification are complete.
